# 当前任务

## 2026-09-22 当前任务：G6-P0完成，旁路训练合同待制定审批

| 字段 | 内容 |
|---|---|
| 当前研究线 | main-e2e：同时输出CH3频带/源数与CH4位置的统一模型 |
| 本轮完成 | 分块EVD修复与9项测试通过，同一128条正式重跑完成、审计PASS；数值与资源可行，物理图在部分重叠场景提供配对信息，见实验结果记录E2E-G6-P0-RERUN-20260922 |
| 加载器结论 | 按样本读取与单batch预取通过同batch更新等价及短程速度验证，已接入batch4/共享2 GiB缓存；不能把短测当长期吞吐保证，事实见实验结果记录E2E-G5-PREFETCH-BATCH-20260919 |
| 当前模型基础 | 固定位置方案保住定位；独立身份匹配未能替代原绑定。G6物理图提供新配对信号，但接入后的系统收益与额外E2E反馈收益仍待训练验证 |
| 已批准范围 | 用户批准按实现文档第25章执行G6-P0，由Codex实现、检查、运行与回填；不启动后续训练 |
| 已接受工程代价 | 历史整片加载未达原mmap相对速度目标的事实保留，不追改为PASS；当前新工程配置按独立预取筛选与接入证据登记 |
| 执行方案 | G6-P0：128条开发样本，缓存/直接IQ等价、标签/预测频带身份信息、网格影响与成本；详见实现文档25.3 |
| 审批边界 | 仅G6-P0；1小时计算预算，不训练网络、不读取val_compare/test、不自动进入三轨训练 |
| 读取异常新规则 | 保留首次异常字节与日志，最多重读一次；重读匹配登记身份则继续并标记恢复事件，再次失败则停止。不能静默忽略或无限重试 |
| 风险 | 逐源频带绑定仍有缺口；本批无100 m内近源，不能确认30 m排斥的近源适用性。原G5预算未解析，开发集结果不能替代test；历史读取异常根因仍未知 |
| 禁止范围 | 不写原outputs、不读取test、不临场调loss、不增加空间计数头、不全解冻、不将失效报告用于科研结论 |
| 当前执行 | `COMPLETED_G6_P0`；本轮授权工作已完成。下一步按实现文档25.4制定C0/C1/C2旁路训练合同，另行审批，不自动训练 |
| 结果回读 | `outputs_e2e/unified/e2e_g6_p0/20260922_165854/`的report.json、samples.jsonl、numerical_audit.json、gradient_audit.json、final_audit.json；两个历史资源失败目录保留 |
