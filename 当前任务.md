# 当前任务

## 2026-09-06 当前任务：E2E-G4完成，等待制定E2E-G5正式证据方案

| 字段 | 内容 |
|---|---|
| 当前研究线 | `main-e2e`工作树：研究将第三章与第四章统一为真正端到端模型的方法 |
| 任务状态 | `E2E-G4`已完成，正式判定`G4_PARTIAL_UNFREEZE_SELECTED`；下一步为E2E-G5方案待制定，尚未获批执行 |
| 已确认基础 | G3已用三个training seed确认短反馈路径的总体定位收益；Hard频带轻微下降仍为黄色监控项，不是当前阻断项 |
| 历史RMSE补评 | G2-R2及G3三组E2E matched RMSE点估计均优于SG，但仅一组区间排除0；P90约1 km，后续继续联合GOSPA、Recall与coverage解释 |
| G4数据 | 从冻结母集抽取`train/val_select/val_compare=1024/256/512`，各K均衡，train严格包含原256条；不重新仿真IQ，test为0 |
| G4筛选 | seed `20260907`的16 epoch筛选中仅A0/A2满足全部非劣约束；A2相对A0 GOSPA改善`2.0593 m`，超过`0.5 m`复杂度边界，故A2进入第二seed |
| G4最终定位 | seed `20260907` A2−A0 GOSPA=`-1.7717 m`、RMSE=`-25.5384 m`；seed `20260908`分别为`-1.2828/-7.8158 m`。两seed方向一致，Recall@100m与coverage均改善；GOSPA区间仍跨0，属于稳定方向与护栏通过，不是每个seed均显著 |
| G4结论 | 选择A2：在A0基础上解冻D8 `decoder.c1/up0`与CH3 `cross_attn`；其余CH3 CNN/global encoder及D8 backbone/PAN/更深decoder继续冻结。空间计数头仍不加入 |
| 完整性处置 | 大型HDF5和磁盘特征缓存出现读取漂移，所有依赖磁盘特征的训练均作废保留。最终`training_v4`在单进程内从登记粗DPD与1792个SHA匹配的逐样本细DPD重建RAM特征并训练，不读取磁盘特征缓存；两次RAM重建摘要完全一致，27个最终小型证据文件连续三次哈希稳定 |
| 有效证据 | `outputs_e2e/unified/e2e_g4/20260905_164500`；精确配置、指标、资源和哈希见`实验结果记录.md`的`E2E-G4-20260905` |
| 获批完整性复核 | 三路径小范围只读诊断已完成，历史异常分片当前匹配登记身份，判定未复现；根因仍未知，RAM路径及旧训练作废决定维持，事实见`实验结果记录.md`的`E2E-CACHE-READ-20260906` |
| 获批训练环境复核 | 真实G4 A2训练操作的两种懒加载配对诊断已完成，输入与参数更新一致且异常未复现；分片LRU内存有界，但高换入换出使速度下降，作为后续候选，尚未替换正式Gate加载器。PID级文件跟踪因权限不足未取得，事实见`实验结果记录.md`的`E2E-CACHE-TRAIN-20260906` |
| 下一步 | 以A2为候选制定E2E-G5正式规模、多seed、最终基线/消融、一次冻结test及论文证据封存方案；同时预注册A2一条seed在epoch 32触顶的预算处理；获批前不启动训练或读取test |
| 禁止范围 | 不写原`outputs`，不把`training/`、`training_v2/`、`training_v3/`或失效报告作为证据，不因G4结果临场调loss、上梯度手术、增加空间计数头或继续全解冻 |
