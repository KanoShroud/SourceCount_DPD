# 当前任务

## 2026-09-02 当前任务：G1-R1已完成，等待次要路线决策

| 字段 | 内容 |
|---|---|
| 当前研究线 | `main-e2e`工作树：研究将第三章与第四章统一为真正端到端模型的方法 |
| 审批状态 | 已执行并回填；判定`G1_R1_NO_GO`，G2继续锁定，是否切换次要路线待用户确认 |
| 目标 | 以`max-logit槽位存在＋直接槽位BCE＋Hard最终K`修复计数语义，并重新检验CH4定位梯度是否给CH3带来可归因收益 |
| 冻结前置证据 | 原主线已完成至`S2-G5-R6-B0`；R6-A=`R6A_MIXED`，R6-B0=`PAPER_CONDITION_MIXED` |
| 参考输入 | `F:\SourceCount_DPD\outputs`，仅通过显式参考根读取并核对产物身份 |
| 新输出 | `F:\SourceCount_DPD_E2E\outputs_e2e`，保存本研究线新数据、缓存、权重和结果 |
| 当前完成 | P0/P1全部通过；两轨各完成4 epoch和256步，三轨在新512条比较集完成评价及2000次bootstrap；未读取test |
| 禁止范围 | 不写入原`outputs`；不读取冻结test；不增加CH4空间计数头；不扩大训练规模、不解冻更多模块、不改Soft细DPD桥、优化器、loss权重或精度合同 |
| 当前边界 | max-logit槽位存在、直接槽位BCE和Hard最终K已消除绝对计数崩溃；但SG/E2E均选择epoch 0，定位反馈没有可选择收益，不能进入G2 |
| 下一步 | 用户确认是否按停止规则结束当前首要实现并为“双编码器＋逐源中期融合”制定新Gate；不自动调loss、扩seed、解冻或读取test |
| 验证 | 有效目录`outputs_e2e/unified/e2e_g1_r1/20260902_153159`；`final_report.json`判定`G1_R1_NO_GO`、`g2_unlocked=false`；精确事实见`E2E-G1-R1-20260902` |
