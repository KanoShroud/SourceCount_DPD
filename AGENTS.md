# AGENTS.md

本文件记录本项目稳定适用的仓库协作规则，不记录当前任务、实验结果、临时状态或研究路线。

## 环境与编码

- 当前环境为 Windows 11，默认 shell 为 PowerShell 7；命令使用 PowerShell 语法，不默认使用 Bash 语义。
- 项目 Python 解释器为 `D:\Software\anaconda3\envs\PyTorch\python.exe`。
- 缺少依赖时，先说明缺失项并提供 Conda 或 pip 安装命令；未经用户批准，不主动安装或修改环境。
- 读取中文路径、中文文档或中文注释时，PowerShell 使用 `Get-Content -Encoding UTF8 -LiteralPath ...` 等 UTF-8 安全方式。若仍乱码，应先确认实际编码，不得依据乱码内容修改文件。
- 新建或已确认采用 UTF-8 的文本文件，Python 读写时显式指定 `encoding="utf-8"`；外部文件保持原编码。
- Python 中处理路径优先使用 `pathlib.Path`；确需字符串路径时避免不安全的转义。

### Windows 子进程与日志

- Python 在 Windows 上调用 `subprocess.run(..., text=True)` 时必须显式指定实际编码和错误策略，不得依赖系统默认代码页。项目内 Python、Git、`nvidia-smi` 等 UTF-8/ASCII 输出统一使用 `encoding="utf-8", errors="replace"`；已确认输出为其他代码页的外部程序应显式声明对应编码，并将保存日志统一写为 UTF-8。
- 启动项目内 Python 子进程时，应显式设置 `PYTHONUTF8=1` 和 `PYTHONIOENCODING=utf-8:replace`。长任务通过管道转发日志时优先按字节读取、显式解码；控制台转发还需使用安全替换，避免单个不可编码字符使读取线程退出并造成管道回压或假超时。
- 后台日志线程的异常必须传回主线程并写入阶段报告，禁止静默退出；读取 `stdout`、`stderr` 等可空字段前必须处理 `None`，避免原始异常被二次异常覆盖。
- 新建涉及中文路径、中文日志或子进程捕获的执行入口，在长任务前至少完成一次非 ASCII 输出捕获与报告写入检查。乱码只能作为编码待确认信号，不能据此修改源码、数据或实验结论。

## 审批与修改范围

- 用户要求“先出方案”“只检查”或“只分析”时，不修改文件。
- 实质性代码或文档修改，应先说明问题、影响范围和修改方案，经用户批准后执行。
- 修改应保持小范围、可解释，并沿用现有代码和文档风格；不修改任务范围外文件。
- 保留用户和既有工作的未提交修改，不擅自回滚或覆盖。
- 避免无关格式化、批量替换、编码转换或整文件重写，尤其是包含中文注释的文件。

### 高置信工程小错的自动修复

- 执行已批准任务时，若失败根因明确且属于编码、日志转发、路径拼接、参数传递、导入遗漏、报告字段或同类低风险工程错误，Codex可直接实施最小修复并继续，无需再次请求审批。
- 自动继续必须同时满足：不改变算法、数据内容、数据划分、随机种子、模型结构、loss、优化配置、资源门槛、评价指标或科学判定；失败输出不被复用为通过证据；原失败目录和日志保留；修复后通过针对性静态/轻量验证；受影响阶段从全新目录或全新输出文件重新运行。
- 自动修复和重跑应在下一次进度更新及最终交付中说明根因、修改和新旧证据目录。若根因不唯一、修复可能影响科研语义、需要覆盖/删除证据、同一问题重复出现，或无法通过轻量验证建立高置信度，仍须停止并请求用户决策。
- 启动训练或关键评估前，必须由文件系统重新解析输入路径并检查存在性；已有权重或数据若有已知 SHA256，还应先核对哈希，不得仅凭对话记忆手写历史时间戳。输出目标必须为空或不存在。
- 总门禁应区分有效 PASS 证据与保留的失败尝试；失败目录不得删除或伪装为通过，汇总逻辑也不得因为保留失败证据而要求清理历史目录。

## 代码修改与验证

- 手工修改源码、配置和文档时优先使用 `apply_patch`。
- 修改 Python 文件后，使用项目解释器对本轮涉及文件执行语法编译；Ruff 可用时执行 `python -m ruff check <文件列表> --select F,E9`。
- 修改 MATLAB 文件后，使用 MATLAB Code Analyzer 或等价静态检查检查本轮涉及文件。
- 修改数据处理、模型、损失函数、定位、评估或结果保存逻辑时，还应核对数据形状、坐标和索引约定、单位、训练/验证/评估目标及输出字段的一致性。
- 影响科研结论的修改必须通过相应正式实验重新验证；未运行正式实验时，应明确说明验证边界，不复用旧结论。
- 正式训练、完整评估和长耗时仿真默认由用户在 PyCharm 或 MATLAB 中运行，除非用户明确要求 Codex 执行。

## 结果、图表与表述

- 不编造实验指标、模型权重、日志、运行目录或研究结论；不确定内容标记为“待确认”。
- 明确区分静态检查、轻量冒烟测试和正式实验；冒烟测试只证明运行链路，不作为性能结论。
- 生成或修改图表时，应核对数据来源、结果字段、图例、坐标轴、单位和指标定义；图表应能追溯到结果文件，不得仅凭手工硬编码数值宣称完成复现。
- 回答、注释和文档优先使用准确自然的中文；代码标识符、配置键、文件名和通行英文术语保持原样。

## Git 与交付

- 默认只执行必要的只读 Git 检查。
- 未经用户明确要求，不执行 commit、push、merge、rebase、checkout、创建分支或回滚。
- 发现已有未提交修改时，默认其属于用户或前序工作，不擅自处理。
- 完成修改后，最终回复应说明修改文件、验证命令及结果、未执行的正式验证和需要用户后续运行的入口。

## 文档与结果记录

根目录维护以下长期文档。文档按事件更新，不要求每轮全部更新；用户明确要求“不修改文档”时，不更新任何长期文档。

### 文档职责与更新规则

- `AGENTS.md`：记录项目级稳定协作规则。
  - 仅在用户明确要求或批准规则变更时更新。
  - 不记录当前任务、实验结果、临时状态或下一步研究计划。

- `对话上下文.md`：记录当前研究阶段、论文与代码的对应关系、核心实现、已核验结论、主要风险和下一步方向。
  - 上述状态发生实质变化时更新。
  - 实验指标只保留综合结论，详细事实指向 `实验结果记录.md`。
  - 仅提出方案、任务自然流转或未改变研究认识的轻量验证不更新。

- `当前任务.md`：记录当前唯一的主线任务及其审批、执行和验证状态。
  - 用户确认新的主线任务，或任务进入“方案待审批、已批准待执行、修改完成待运行、结果待分析、已完成”等阶段时更新。
  - 临时文档整理、格式修正、规则小修和纯讨论不更新。

- `研究总结.md`：总结研究定位、第三章和第四章的创新边界、方法关系及阶段性结论。
  - 对论文或代码的理解发生实质变化、创新边界被修正、路线形成新结论或研究主线切换时更新。
  - 不记录逐次修改过程和完整实验表。

- `修改记录.md`：记录已经完成且具有项目意义的代码或文档修改。
  - 同一修改事件的修改原因、涉及文件和验证结果合并为一条记录。
  - 纯讨论、未执行方案、任务自然流转、单独追加实验结果、轻微排版和错别字修正不单独记录。
  - 更新本文件本身不触发新的修改记录。

- `实验结果记录.md`：记录正式训练、完整评估、重要只读诊断、结果审查及具有独立研究意义的验证事实。
  - 单独执行语法编译、静态检查或代码结构审查不作为实验结果。
  - 仅验证运行链路、结果字段或工程不变量的冒烟测试通常写入 `修改记录.md`，不单独作为性能证据。
  - 正式运行、重要结果回读或影响研究结论的分析完成后必须更新；若主线状态同时变化，再同步更新 `对话上下文.md` 和 `当前任务.md`。

- `运行基础说明.md`：记录稳定适用的环境、路径、设备、安全模式和可复用运行命令。
  - 不记录某次运行的指标、成功或失败经过、历史输出目录及下一步任务。
  - 某次运行的事实分别指向 `实验结果记录.md` 或 `修改记录.md`。

- `第N阶段_*.md`：记录对应研究阶段的目标、方法解读、决策逻辑、Gate 路线、阶段综合结论与退出条件。
  - 精确实验配置、指标、资源、哈希和运行目录只引用 `实验结果记录.md`，不复制完整实验表。
  - 阶段完成后标记为“已完成并冻结”；除事实纠错、失效链接修复或用户明确批准外，不再追加后续阶段内容。
  - 后续阶段另建文档，不在已冻结阶段文档中继续累积 Gate 方案。

### 单一事实源与去重规则

- 同一事实只设一个主责文档。非主责文档最多保留一句综合结论和相对路径链接，不复制完整表格、指标组或事件经过。
- 精确实验配置、指标、墙钟、内存/显存、checkpoint 哈希和运行目录以 `实验结果记录.md` 为唯一事实源。
- 当前唯一任务、审批状态和下一步入口只在 `当前任务.md` 完整维护；`对话上下文.md` 只保留恢复工作所需的当前阶段、关键决定、未决风险和链接。
- `研究总结.md` 只保留稳定研究认识、方法关系和创新边界，不记录逐次 Gate、故障修复、精确资源或任务流水。
- `修改记录.md` 只记录修改原因、涉及文件、算法边界和验证方式；实验性能与资源结果使用实验 ID 或链接指向 `实验结果记录.md`。
- 新事实先更新主责文档，再按需更新其他文档的摘要或链接。发现冲突时以主责文档和原始结果文件为准，并修正其他副本。
- 阶段切换必须经用户明确确认。阶段文档中的 Gate 使用 `S<阶段号>-G<序号>` 编号，避免与跨阶段的历史 Gate 混淆。

### 按需读取与证据规范

- 恢复项目背景或分析论文与代码关系时，优先读取 `对话上下文.md` 和 `研究总结.md`。
- 执行主线工作前读取 `当前任务.md`；追溯代码修改或实验事实时，分别检索 `修改记录.md` 和 `实验结果记录.md`。
- 没有实际运行实验、读取原始结果文件或获得明确用户反馈时，不写具体指标；用户口述结果必须标注“用户反馈/待复核”。
- 必须区分静态审查、冒烟测试、正式运行、仅重绘或导出以及用户反馈；冒烟测试只证明工程链路，不构成性能结论。
