# 第二阶段：缩减规模复现与端到端闭环

> 建立日期：2026-08-21
> 当前状态：`S2-G0`、`S2-G1`、`S2-G2` 已完成；`S2-G3` 工程执行通过，科学判定为 `REQUIRES_INTERFACE_REDESIGN`；`S2-G4` 在0.3阈值数据门停止；`S2-G4-R1` 已完成，判定为 `REPRESENTATION_LIMIT_CANDIDATE_T020`。
> 前置阶段：`第一阶段_论文与代码接管审查.md` 已完成并冻结。

## 1. 阶段目标

在不直接启动论文全量数据和控制实验的前提下，为第三章和第四章分别建立可重复的小规模 checkpoint，确认第四章训练上限，打通第三章预测到第四章定位的接口，并建立结果文件到图表的可追溯链。

## 2. 非目标

- 不在本阶段直接宣称论文指标复现或当前创新性成立。
- 不直接生成40k/5k/5k正式数据或全部4A–4D控制实验。
- 不以缩减数据结果替代正式规模、对比实验、消融和统计验证。
- 不因资源限制擅自改变物理网格、DPD定义、模型结构或研究算法。

## 3. 当前输入基线

- 第一阶段形成的论文—代码映射、完整性判断和资源边界。
- 第三章 Gate 1 数据契约与单步工程闭环。
- 第四章 Gate 3A 已审计缩减数据和 Gate 3B 冻结 oracle-K checkpoint。
- 第三章 S2-G1 已获得缩减数据上的严格重复checkpoint；精确配置、结果和路径以 `实验结果记录.md` 中 `CH3-S2G1-20260822` 为准。
- 第四章精确实验配置、结果和路径以 `实验结果记录.md` 中 `CH4-G3A-20260821`、`CH4-G3B-20260821` 为准。
- 最小封存引用固定为：代码提交 `f6a802a`、数据证据 `CH4-G3A-20260821`、权重与评估证据 `CH4-G3B-20260821`。该引用不新增快照、manifest或Git标签。

## 4. Gate 路线

| Gate | 目标 | 当前状态 | 主要退出条件 |
|---|---|---|---|
| `S2-G0` | 最小逻辑封存当前基线 | `PASS` | 已复用现有代码提交和Gate 3A/3B实验ID；历史目录未移动 |
| `S2-G1` | 第三章小规模收敛 | `PASS` | 已获得只由validation选择、可独立重载和严格重复的第三章checkpoint |
| `S2-G2` | 第四章缩减数据训练充分性诊断与接口checkpoint建立 | `PASS` | 200 epoch趋势CLOSED；新epoch 93 checkpoint按validation RMSE冻结；未运行test |
| `S2-G3` | 跨章可组合性审查与联合数据契约决策 | 工程 `PASS`；`REQUIRES_INTERFACE_REDESIGN` | 第三章冻结模型通过跨分布门，但19子带二值接口的定位RMSE退化超线；按预案停止完整四轨 |
| `S2-G4` | Coarse-Oracle D8受控适配与根因诊断 | `STOPPED_AT_DATA_GATE` | 预检和pilot通过；完整train含空Coarse-Oracle频带，按预案停止，未训练D8 |
| `S2-G4-R1` | 0.2阈值下的快速D8适配诊断 | `PASS`；`REPRESENTATION_LIMIT_CANDIDATE_T020` | 0.2支持域闭合，但scratch只恢复15.86% RMSE缺口，未达到等效门槛 |
| 后续待排期 | 4A–4D结果追溯pilot | 未开始 | 待最终定位输入和模型路线明确后再固定Gate编号；评估输出和绘图必须结构化可追溯 |

每个 Gate 只在进入时编制详细执行方案，经用户批准后实施。上一 Gate 未通过或存在未处置异常时，不自动进入下一 Gate。

## 5. 阶段完成条件

第二阶段只有在以下条件全部满足后才能关闭：

1. 第三章和第四章均有身份明确、可重复验证的小规模 checkpoint。
2. 第四章训练上限已有预先约定的判定，不再仅因best靠近epoch上限而无限延长。
3. oracle band/K与predicted band/K四条评估轨道能够区分频带、计数和定位误差；若前置表示或模型门失败，则已形成可追溯的停止判定和适配方向。
4. `K=0`、漏检和多检具有明确评价规则，不通过删除失败样本维持RMSE。
5. 评估结果可追溯到代码版本、数据manifest、命令、checkpoint和结构化输出。
6. 已形成进入第三阶段“创新核验与正式实验设计”所需的资源和方法证据。

## 6. 总停止规则

- 任一 Gate 失败后保留证据，不自动重试、扩大数据或改变模型。
- test 不参与模型、epoch、阈值或数据规模选择。
- predicted结果不理想时不得以oracle结果替代端到端结论。
- 资源越界时优先使用懒加载、字段精简和流式评估，不先改变研究算法。
- 无法追溯到原始结果的数据或图表不进入后续论文证据包。

## 7. 文档与证据分工

- 当前唯一任务和审批状态：`当前任务.md`
- 精确实验配置、指标、资源和目录：`实验结果记录.md`
- 已完成修改：`修改记录.md`
- 稳定研究认识：`研究总结.md`
- 环境与安全运行方式：`运行基础说明.md`
- 第一阶段历史方法和Gate：`第一阶段_论文与代码接管审查.md`

本文只维护第二阶段目标、Gate路线、综合判定和退出条件。具体实验事实先写入 `实验结果记录.md`，本文只保留结论和实验 ID。

## 8. S2-G1 已批准执行方案

### 8.1 目标与边界

S2-G1只检验第三章 Transformer 主模型能否在缩减数据上形成稳定学习信号，并产出只由validation选择、可独立重载和严格重复验证的checkpoint。不以本Gate复现论文最终指标，不运行Exp 1A/2B/2C，不调输出阈值，也不改变物理模型、DPD、标签、网络或loss。

### 8.2 固定配置

| 项目 | 固定值 |
|---|---|
| train/val/test | `2048/512/512`；0/1/2/3源按`20%/20%/30%/30%`分层 |
| 数据/Python seed | `20260823 / 42` |
| 模型 | Transformer，`M=10`，FP32 |
| batch/val batch/workers | `64/64/0` |
| 优化 | AdamW，lr=`1e-3`，weight decay=`5e-4`，Focal BCE `gamma=2`，梯度裁剪`1.0` |
| 训练 | 100 epoch，warmup 5，cosine至`1e-6`，patience 25 |
| checkpoint与阈值 | warmup后最低validation loss；固定阈值`0.5` |

数据只生成一次；训练A/B使用相同数据、配置和seed在独立进程运行。test在checkpoint冻结后只运行一次，不参与epoch、阈值或模型选择。

### 8.3 增量兼容实现

- `main30.m`只增加成对的`SOURCECOUNT_CH3_TRIALS_LIST`和`SOURCECOUNT_CH3_RANDOM_SEED`可选覆盖；未设置时保留原smoke/formal规模和入口行为。
- 不重构或替换师弟的`train_v26.py`；新增S2-G1专用训练入口，直接复用其`SourceDetectionDataset`、`SourceDetectionNet`和`compute_loss`。
- 新增一个S2-G1审计入口，负责数据审计、容量检查、训练比较、checkpoint引用和总报告；继续复用已验证的资源监控包装器。
- 所有结果写入唯一的`outputs/s2g1_ch3/<timestamp>/`，拒绝覆盖非空目录；失败后不自动重试。

### 8.4 执行顺序

环境与旧入口兼容性检查 → 受控数据生成 → shape/标签复算/split泄漏/SHA256审计 → batch 64单步容量检查 → 两次100 epoch确定性训练 → 科研字段和checkpoint状态逐值比较 → 冻结训练A的best引用 → 两次独立validation → 一次test → 总报告与长期文档回填。

### 8.5 通过条件

- 数据和标签审计通过，训练前后数据SHA256一致，无NaN/Inf、OOM、覆盖、设备或资源红线。
- 相对初始化，best validation loss下降至少20%；balanced count accuracy和活跃槽位band macro-F1均至少提高0.20且最终不低于0.50。
- A/B逐epoch科研字段、best epoch、模型/optimizer/scheduler/RNG状态逐值一致；冻结checkpoint可在新进程重载，两次validation一致。
- early stop，或best不在最后10个epoch，或最后10个epoch相对此前最佳改善不足1%。否则记为`NEEDS_EXTENSION_APPROVAL`，不运行test、不自动延长。

计数评估保留预测`K=0...10`，禁止把4–10源裁剪成3源；同时报告exact/balanced count accuracy、逐类准确率、count MAE、漏计/多计、0源虚警、`4×11`混淆矩阵、活跃槽位band precision/recall/F1和空槽位误激活率。

### 8.6 资源与停止规则

启动前可用RAM中位数低于14 GB不启动；运行中可用RAM低于10/8 GB、进程RSS高于6/8 GB、GPU占用高于12/14 GB分别记为预警/红线；磁盘低于120/100 GB预警/停止；本Gate输出超过2/3 GB预警/红线。MATLAB、数据审计、容量检查、单次训练、单次评估的硬超时分别为90、30、20、180、20分钟。只有硬超时允许自动终止进程树；其他失败停在当前层并保留证据。

### 8.7 首次执行状态

首次运行 `outputs/s2g1_ch3/20260821_204124` 的预检、`2048/512/512`数据生成和数据manifest均通过；MATLAB阶段墙钟537.635 s、输出1,241,624,245 bytes，未触发资源预警或红线。数据审计随后发现train第1989号2源样本的最小源间距为124.982 m，低于代码声明的300 m。原因是原 `gen_multi_source_pos_v2.m` 在5万次约束放置失败后会无约束随机补齐。S2-G1按停止规则记为数据完整性失败，未进入容量检查和训练；精确证据见 `CH3-S2G1-20260821-A`。

2026-08-24 经审批进行证据瘦身：上述三个不合格 MAT 文件已删除，运行根中的审计JSON、manifest、资源CSV和日志继续保留；本节所列体积、类别与违规样本信息是删除前完成审计的历史快照。该清理不改变失败判定，也不把失败目录转换为通过证据。

### 8.8 最小修复与第二次执行状态

经审批，仅在S2受控路径增加生成后300 m不变量检查：违规布局重新采样位置，单样本最多重试100次，达到上限立即报错；保存逐样本重试次数和split拒绝总数。共享位置生成器及普通smoke/formal行为不变，审计要求元数据自洽且全部样本零违规。

全新运行 `outputs/s2g1_ch3/20260822_120601` 的60秒资源预检因可用RAM中位数低于14 GiB启动线而停止；预检后只读快照约13.861 GiB。未启动MATLAB、未生成数据、未执行容量检查或训练，也未自动重试。精确事实见 `CH3-S2G1-20260822-B`。

### 8.9 第三次执行与Gate结论

用户释放内存后，全新运行 `outputs/s2g1_ch3/20260822_122055` 完成全部门禁。数据生成时原生成器再次返回1个违规train布局，受控后验门禁拒绝并一次重采样；最终3072条样本全部满足300 m、标签复算、split隔离和有限性要求，训练前后SHA256一致。

batch 64容量检查通过；两次100 epoch训练的逐epoch科研字段和checkpoint内部科研状态严格一致。训练A第96 epoch由validation loss冻结，两次独立validation重载一致，随后test仅运行一次。学习门禁、收敛门禁和全部资源门禁通过，总报告为 `PASS`；精确指标见 `CH3-S2G1-20260822`。该结果只证明缩减配置可学习、可重复和可重载，不是论文正式指标复现。

## 9. S2-G2 已执行方案与结论

### 9.1 目标与边界

S2-G2不再试图从缩减数据推断论文正式epoch，而是完整观察一次200 epoch余弦调度，比较Gate 3B与新训练checkpoint的统一validation表现，诊断P90/P95及极端样本，并冻结一个供S2-G3使用的接口checkpoint。200来自论文记载和代码默认上限，但只作为有限诊断预算；本Gate不复现论文正式训练量，不运行test，也不验证200 epoch训练的双重复性。

### 9.2 固定配置

只读复用Gate 3A的train/val/test=`1024/256/256`数据；D8 `dualhead_std`、FP32/no-AMP、batch/val batch=`8/8`、AdamW、lr=`1e-3`、weight decay=`5e-3`、dropout=`0.4`、Focal+offset、dice=`0`、seed=`42`和严格确定性均保持Gate 3B口径。训练从随机初始化完整运行200 epoch，CosineAnnealingLR的`T_max=200`、`eta_min=1e-6`，patience设为200以避免在低学习率阶段前提前停止；checkpoint只按epoch 6–200的validation oracle-K RMSE选择。

### 9.3 增量兼容实现

- `train_yolo.py`只新增默认关闭的`--s2g2_d8`严格配置，不改变原Gate 3A/3B和普通入口。
- `eval_ch4_checkpoint.py`只在显式开关下输出逐源误差、P90/P95和极端样本，原评估字段保持兼容。
- 新增`s2g2_ch4.py`完成预检、数据manifest、200 epoch审计、checkpoint比较、长尾分析、冻结引用和总报告；继续复用既有资源监控包装器。
- 所有结果写入唯一`outputs/s2g2_ch4/<timestamp>/`，输入数据只读，失败后不自动重试。

### 9.4 执行与判定

兼容性检查与60秒资源预检 → 数据manifest → 单次完整200 epoch训练 → 训练审计 → 使用同一详细评估器分别重评Gate 3B和新best → 按validation RMSE机械选择checkpoint → 逐源配对长尾分析 → 所选checkpoint第二次独立validation重载 → 数据SHA256复核与总报告。新checkpoint RMSE严格更低时选择新权重，否则保留Gate 3B；P90/P95不参与事后模型选择。

最后20 epoch相对此前best的改善不足1%，或best不晚于epoch 180，记为`TREND_CLOSED`；best位于181–200且改善不低于1%时记为`TREND_OPEN_AT_200`。后一状态也不自动延长训练，只保留为后续数据规模、长尾和loss分析证据。最终报告必须明确`TRAINING_REPRODUCIBILITY_NOT_RETESTED_AT_200`，不得沿用Gate 3B双重复结论。

### 9.5 资源与停止规则

启动前可用RAM中位数低于14 GiB不启动；运行中可用RAM低于10/8 GiB、进程RSS高于6/8 GiB、GPU占用高于12/14 GiB分别记为预警/红线；磁盘低于120/100 GiB预警/停止；本Gate输出超过2/4 GiB预警/红线。单次200 epoch训练提醒线60分钟、硬超时180分钟，单次评估和审计硬超时分别20和30分钟。只有硬超时自动终止进程树，其他异常停在当前层并保留证据。

### 9.6 执行结论

唯一有效运行根为`outputs/s2g2_ch4/20260822_143749`，总报告为`PASS`。D8在固定缩减数据上完成200 epoch，best位于epoch 93，最后20轮无新增收益，记为`TREND_CLOSED`；后段出现train loss继续下降而validation变差的可能过拟合模式。新checkpoint的validation oracle-K RMSE为130.726516 m，低于Gate 3B的150.791921 m，故按预设规则冻结新权重；SHA256=`5d5224d8d6478739af5042391ef1e718c08e2f81565882e133372c9fbd116dfd`，第二次独立重载逐值一致。

该改进主要来自>500 m极端错误由15降至9，median、P90和P95并未同步改善，不能表述为所有样本普遍更准。训练耗时2383.900 s，峰值GPU已用6621 MiB，4份有效监控报告无warning/red flag；训练前后数据SHA256完全一致，未运行test。一次错误历史checkpoint路径导致的失败评估被保留，修正路径后在新目录通过；精确事实见`CH4-S2G2-20260822`。

## 10. S2-G3 已执行方案与结论

### 10.1 目标与证据边界

S2-G3不再把两个异分布冻结模型直接串联后的数值解释为联合系统性能，而是审查两章是否具备直接组合的前提。依次回答：第三章19子带二值频带是否保留第四章定位所需信息；S2-G1第三章模型能否迁移到第四章共享IQ分布；仅当前两项满足最低继续门槛时，现有两个checkpoint直接组合后如何退化。本Gate不生成新IQ、不训练或微调模型、不调整阈值、不运行test，也不形成论文正式端到端性能证据。

第三章与第四章共享4站阵列、5.8 GHz载频、100 MHz采样率、4096点观测、19个重叠子带和粗DPD网格，因此可组合性是合理待验证假设；但两章在源数比例、逐源带宽、频率重叠拓扑、功率/SNR定义、距离范围和最小源间距上存在分布差异，直接复用不得被预设为成立。

### 10.2 固定输入与实现边界

- 第三章固定使用S2-G1训练A的epoch 96 Transformer、`M=10` checkpoint和阈值`0.5`；第四章固定使用S2-G2冻结的epoch 93 D8 `dualhead_std` checkpoint、FP32/no-AMP。
- 只读使用Gate 3A的256条validation共享IQ及其频带、源数和位置元数据，2源/3源各128条；使用已有S2-G2 exact-oracle validation作为回归基线。
- 新增独立的第三章粗DPD计算入口和第四章S2-G3编排/评估入口，复用现有模型、DPD和监控实现；不改变`main30.m`、`main31_loc_v3.m`、`train_v26.py`、`train_yolo.py`及原评估入口默认行为。
- 所有结果写入唯一`outputs/s2g3_composability/<timestamp>/`，拒绝覆盖非空目录；失败运行保留，不复用为通过证据。

### 10.3 阶段A：数据分布契约审计

逐项比较源数及比例、symbol rate/实际带宽、频率重叠、位置半径、源间距、源到接收站距离、功率/SNR/噪声定义、阵列与采样参数、子带范围、标签与`ignore_mask`语义以及接口shape/单位/坐标/索引。差异标记为完全一致、支持范围变化、概率分布变化、定义不一致或当前不可比较，输出结构化JSON和简明Markdown表。

### 10.4 阶段B：离散频带表示上限

在同一validation IQ和oracle K上比较两种细DPD：`Exact-Oracle Band`直接使用逐源真实连续频率范围；`Coarse-Oracle Band`按第三章标签规则将第四章真值转换为19子带二值标签，再把全部正子带映射回FFT掩模。先要求Exact-Oracle新评估与S2-G2基线精确回归，再评价Coarse-Oracle。

继续完整直连的表示门槛为：Coarse-Oracle相对Exact-Oracle的匹配RMSE增幅不超过10%，`recall@100 m`下降不超过5个百分点，且没有空频带、样本遗漏或身份错配。不满足则科研状态至少为`REQUIRES_INTERFACE_REDESIGN`，停止predicted-band细DPD生成。

### 10.5 阶段C：第三章跨分布诊断

对全部256条第四章validation IQ按第三章原算法计算`19×81×81`粗DPD，使用冻结checkpoint执行两次独立确定性推理。固定阈值`0.5`，保留`K_pred=0...10`，禁止把`K>3`裁剪为3或为空频带补oracle信息。

计数报告真实2/3源对预测0–10的混淆矩阵、exact/balanced accuracy、MAE、漏计、多计、`K=0`和`K>3`比例；频带报告槽位precision/recall/F1、合并频带IoU、真值覆盖率、预测纯度、带宽膨胀比和空频带比例。继续完整直连的模型门槛为：计数准确率95% Wilson下界高于0.5；合并频带IoU优于全频带平凡基线；真值频带覆盖率中位数不低于0.8；空频带和`K>3`比例均不超过5%；两次推理的logits、频带与K完全一致。不满足则科研状态至少为`REQUIRES_CH3_ADAPTATION`，不生成predicted-band细DPD。

### 10.6 阶段D：条件式接口轨道

仅在阶段B和C均达到继续门槛时，运行`OB-OK`（exact oracle band/oracle K）、`OB-PK`（exact oracle band/predicted K）、`PB-OK`（predicted band/oracle K）和`PB-PK`（predicted band/predicted K），并保留Coarse-Oracle/Oracle-K表示控制。`K_pred=0`输出空集合，`K_pred>3`保留多检，预测频带为空时不得回退oracle；所有漏检、多检和空输出进入集合指标。

主指标为GOSPA（`p=2, c=100 m, alpha=2`）及定位、漏检、误报分量，同时报告precision/recall/F1@10/30/50/100 m、输出基数、精确计数率、匹配RMSE/median/P90/P95/max、N=2/N=3分层和最严重10个样本。配对差值采用样本级bootstrap 10000次、固定seed，只描述不确定性，不进行事后阈值或显著性包装。

### 10.7 判定与停止规则

工程执行与科研结论分离。输入、算法复算、身份审计和决策链全部完成可记工程`PASS`；科研状态必须明确为`DIRECT_REUSE_CANDIDATE`、`REQUIRES_CH3_ADAPTATION`、`REQUIRES_INTERFACE_REDESIGN`、`REQUIRES_BOTH_ADAPTATIONS`或`ENGINEERING_BLOCKED`之一。前置门槛失败是有效审查结论，不以oracle结果替代predicted结论，也不自动改阈值、频带映射、数据或模型。

若科研状态为直接复用候选，后续再审批统一分布的新validation/test与正式联合实验；若需第三章适配，下一步设计共享IQ和重训练/微调；若需接口重设计，优先评估连续频带边界、概率频带或更细频率表示。若论文按两种独立方法组织，本Gate作为跨章兼容性证据即可，不强求端到端性能。

### 10.8 资源和验证

启动前可用RAM中位数低于14 GiB不启动；运行中可用RAM低于10/8 GiB、GPU占用高于12/14 GiB、磁盘低于120/100 GiB分别预警/停止；输出超过1/2 GiB预警/停止。粗DPD和每类细DPD硬超时各30分钟，总硬超时120分钟，只有硬超时自动终止进程树。明显且高把握的编码、路径或日志类小错误按`AGENTS.md`最小修复并在新目录继续；科研语义、阈值、映射或模型行为异常立即停止。

### 10.9 执行结论

有效运行根为 `outputs/s2g3_composability/20260824_132001`，工程状态 `PASS`。数据契约确认两章基础阵列与采样参数一致，但带宽、重叠拓扑、位置、功率和SNR定义存在显著分布差异。第三章冻结模型在256条第四章validation IQ上通过预设模型门，两次推理逐元素一致；计数准确率为0.691406，物理频带IoU中位数为0.753765，真实频带覆盖率中位数为1.0。

Exact-Oracle新评估精确回归S2-G2的640条匹配误差和130.726516 m RMSE；Coarse-Oracle使用第三章19子带真值接口后，RMSE升至164.148382 m，比例1.255662，超过1.10门槛，尽管`recall@100 m`只由0.95625降至0.95。表示门因此失败，科学状态为`REQUIRES_INTERFACE_REDESIGN`；该对照只能证明当前映射与冻结D8不能直接复用，不能单独区分接口信息损失和D8输入分布失配。按预案未生成predicted-band细DPD、未运行完整四轨，也未训练、调阈值、生成新IQ或运行test。下一步应比较连续边界、概率/更细频率表示与接口适配，精确事实见`SYS-S2G3-20260824`。

## 11. S2-G4 已批准执行方案：Coarse-Oracle D8受控适配与根因诊断

### 11.1 目标与范围

本Gate只回答：S2-G3中Coarse-Oracle细DPD造成的退化，主要来自冻结D8的输入分布失配，还是来自19子带表示本身的信息不足。主要科学证据为使用Coarse-Oracle细DPD从随机初始化训练D8；从S2-G2 Exact-D8权重开始的全网络微调只作为低成本工程对照。本Gate不修改或训练第三章，不运行predicted-band、predicted-K、test或论文正式实验，不开展4A–4D控制实验。

原计划占用`S2-G4`编号的4A–4D结果追溯pilot转为后续待排期任务，暂不固定新Gate编号，避免在接口和最终定位输入尚未确定时提前固化实验链。

### 11.2 固定输入与配置

- 只读复用Gate 3A的train/validation=`1024/256`原始IQ、精确频带定位数据及标签；不读取test指标。固定S2-G2 epoch 93 Exact-D8及其SHA256=`5d5224d8d6478739af5042391ef1e718c08e2f81565882e133372c9fbd116dfd`。
- Coarse-Oracle频带沿用S2-G3定义：按第三章标签规则使用真实逐源元数据生成19子带二值标签，合并全部正子带并映射为FFT掩模；不使用预测频带，不将ignore子带转为正带，不对空频带回退Exact信息。
- 在上述掩模内按第四章原算法重新计算`401×401`、10 m网格细DPD，执行`log(DPD+1)`并保存float16；监督标签、源数、位置、样本索引和增强语义必须与Exact数据完全一致。
- 两项训练均固定D8 `dualhead_std`、FP32/no-AMP、batch/val batch=`8/8`、dropout=`0.4`、Focal+offset、dice=`0`、weight decay=`5e-3`、seed=`42`和严格确定性；checkpoint只由Coarse validation oracle-K RMSE选择。

### 11.3 实现与数据审计

新增独立S2-G4数据构建、训练编排、交叉评估和总判定入口，复用现有DPD、D8、损失、评估和监控实现；原`main30.m`、`main31_loc_v3.m`和普通训练/评估入口默认行为不变。Coarse数据保持现有定位分片格式，只替换`fine_dpd`；train/validation分别为1024/256条。必须核验样本索引、源数、位置及监督字段逐值一致，validation Coarse-DPD与S2-G3既有结果逐张量一致，且无空频带、非有限值、split交叉或原始文件身份变化。

执行顺序为：身份与60秒资源预检；8/8条数据pilot及一次forward/loss/backward/optimizer step；完整Coarse train/validation生成；从头200 epoch训练；Exact-D8全网络微调；六项统一validation交叉评估；根因判定和证据汇总。任一科研语义门失败即停在当前层，不以test、阈值调整或oracle回退补救。

### 11.4 两项训练

从头训练使用AdamW、初始学习率`1e-3`、CosineAnnealingLR `T_max=200`/`eta_min=1e-6`、200 epoch、patience 200，并从第6轮起按Coarse validation RMSE选best。若best不在最后20轮，或最后20轮相对此前best改善不足1%，记为趋势关闭；否则记`TRAINING_TREND_OPEN`，不得据此判断表示不足。

微调从S2-G2 epoch 93模型权重初始化，不恢复优化器或调度器，全网络解冻；使用AdamW、初始学习率`1e-4`、CosineAnnealingLR `T_max=60`、60 epoch、patience 60。单一微调配置失败只能说明该低成本方案未通过，不能证明微调路线普遍无效。

### 11.5 交叉评估与判定

统一评估原Exact-D8、新Coarse-D8和微调D8在Exact/Coarse validation上的六个组合，全部标记为oracle-K。报告RMSE、mean、median、P90、P95、max、10/30/50/100 m召回、N=2/N=3、100/500/1000 m极端错误、GOSPA及最严重样本，并对同一样本的差值执行固定seed配对bootstrap。

记`R_EE`为Exact模型在Exact数据的RMSE、`R_EC`为Exact模型在Coarse数据的RMSE、`R_CC`为Coarse模型在Coarse数据的RMSE，恢复比例为`(R_EC-R_CC)/(R_EC-R_EE)`。Coarse训练趋势关闭，且`R_CC/R_EE<=1.10`、GOSPA比值不超过1.10、100 m召回下降不超过5个百分点时，判为`D8_DISTRIBUTION_SHIFT_DOMINANT`；恢复比例不低于50%但未进入等效范围，或总体恢复而长尾未恢复时，判为`MIXED_CAUSES`；趋势关闭但恢复不足50%时，仅判为`REPRESENTATION_LIMIT_CANDIDATE`；训练趋势未关闭、学习失败或证据异常时判为`INCONCLUSIVE`。

### 11.6 资源、输出与后续分支

唯一运行根为`outputs/s2g4_coarse_d8/<timestamp>/`。启动前可用RAM中位数低于14 GiB不启动；运行中低于10/8 GiB、GPU总占用高于12/14 GiB、磁盘低于120/100 GiB、输出高于6/8 GiB分别预警/停止。数据生成、从头训练、微调、评估和总Gate硬超时分别为45/90/45/40/180分钟，只有硬超时自动结束进程树。预计总墙钟80–120分钟。

若D8分布失配占主导，后续Gate比较Exact/Coarse混合训练及频带扰动增强；若表示不足，再做软19子带、连续边界和更细频率栅格的oracle pilot；只有oracle表示证据成立后才修改并训练第三章；最终再运行OB-OK、OB-PK、PB-OK、PB-PK完整级联。4A–4D追溯pilot在最终定位输入和模型路线明确后另行排期。

### 11.7 执行结果与停止判定

有效运行根为`outputs/s2g4_coarse_d8/20260824_164707`。身份/资源预检、train/validation各8条Coarse数据pilot，以及一次实际forward、loss、backward、optimizer step均通过；完整train数据构造在样本205触发“空Coarse-Oracle频带”断言，按本Gate禁止oracle回退和阈值调整的规则停止。

随后对完整train/validation元数据进行只读支持域审计。第三章规则采用10 MHz子带和0.3正标签覆盖阈值，因此信号主瓣对任一子带形成正标签至少需要3 MHz。train的2560个有效源中154个无任何正子带，恰好等于`symbolRate<3 MHz`的154个源；其中样本205的两个源均不受支持，导致联合频带为空。validation的640个源中同样有38个窄带源无正子带，但它们均被同样本其他源的正子带掩盖，因此没有整样本为空。

本次工程状态为`PARTIAL_PASS`，结构结论为`REPRESENTATION_SUPPORT_GAP_CONFIRMED`，原拟比较的“D8分布失配还是19子带信息不足”仍为`INCONCLUSIVE_FOR_D8_VS_REPRESENTATION`。完整Coarse train/validation、200 epoch从头训练、60 epoch微调、六项交叉评估和test均未执行；没有修改第三章、降低阈值或使用Exact频带兜底。总停止报告为`s2g4_stop_report.json`，精确事实见`SYS-S2G4-20260824`。

继续前必须另行审批接口定义实验。优先在相同IQ上比较当前正标签、正标签加ignore、按物理频带相交赋值、软重叠权重及Exact连续边界的支持率和定位上限；先选出能覆盖2–20 MHz第四章带宽域且语义清楚的oracle表示，再重新启动D8适配，不能直接把0.3阈值改小后沿用本Gate结论。

## 12. S2-G4-R1 已批准执行方案：0.2阈值下的快速D8适配诊断

### 12.1 目标与证据边界

本Gate在用户明确接受接口阈值变化及后续可能重训CH3/CH4的前提下，固定正标签覆盖阈值为0.2，快速判断19个10 MHz硬子带在覆盖第四章2–20 MHz symbol-rate域后，定位退化能否通过重新训练D8恢复。它只评价0.2 Coarse-Oracle、缩减train/validation和oracle-K，不代表现有0.3标签CH3已经兼容新接口，也不运行test、predicted-band/K或论文正式实验。

### 12.2 固定数据、标签和模型

- 只读复用Gate 3A的train/validation=`1024/256`原始IQ、Exact定位监督及S2-G2 epoch 93 Exact-D8；不重新生成IQ，不修改2–20 MHz symbol-rate范围。
- 保持19个10 MHz子带和5 MHz步长，仅将主瓣覆盖阈值固定为0.2。必须根据`fc_offset`、逐源`symbolRate`和子带边界独立重算标签，不读取MAT中按0.3生成的旧`band_mask`，不把ignore直接转为正带，也不使用Exact频带回退。
- 重新计算0.2联合频带内的`401×401`细DPD；数据索引、源数、位置和监督字段必须与Exact数据逐值一致。所有结果显式记录`coverage_threshold=0.2`。
- 新D8从随机初始化训练，固定`dualhead_std`、FP32/no-AMP、batch/val batch=`8/8`、AdamW、lr=`1e-3`、weight decay=`5e-3`、dropout=`0.4`、Focal+offset、dice=`0`、seed=`42`、200 epoch和完整余弦调度；checkpoint只按Coarse validation oracle-K RMSE选择。

### 12.3 最短执行链

完整元数据支持门 → train/validation各8条数据和一次训练单步pilot → 完整0.2 Coarse train/validation生成 → 原Exact-D8在Coarse validation上的`EC20`评估 → 单次200 epoch Coarse-D8 scratch → 新模型在Coarse validation上的`CC20`评估 → 与已冻结`EE`基线组成三轨判定。原0.3部分数据和运行目录不得复用，输出写入唯一`outputs/s2g4r_threshold020/<timestamp>/`。

支持门要求每个有效源至少有一个正子带、每个样本联合频带非空，且所有标签有限、互斥和不越界；若0.2仍不能覆盖全部源则立即停止，不继续降低阈值。工程pilot只证明链路，不作为性能证据。取消原S2-G4的60 epoch微调和CE/FE/FC评估，避免在回答主问题前扩大实验。

### 12.4 三轨判定

复用S2-G2的`R_EE=130.72651561958395 m`及其结构化评估；新运行得到原Exact-D8在0.2 Coarse数据上的`R_EC20`和新Coarse-D8在同一数据上的`R_CC20`。恢复比例定义为`(R_EC20-R_CC20)/(R_EC20-R_EE)`，并同步比较mean GOSPA、100 m召回、median、P90/P95、极端错误及N=2/N=3分层。

训练趋势关闭且`R_CC20/R_EE<=1.10`、GOSPA比值不超过1.10、100 m召回下降不超过5个百分点，判为`D8_DISTRIBUTION_SHIFT_DOMINANT_T020`；恢复比例不低于50%但未进入等效范围，或总体恢复而长尾未恢复，判为`MIXED_CAUSES_T020`；趋势关闭但恢复不足50%，判为`REPRESENTATION_LIMIT_CANDIDATE_T020`；训练趋势未关闭、有效学习失败或证据异常则判为`INCONCLUSIVE_T020`。

### 12.5 资源、停止与后续

沿用S2-G4的RAM、GPU、磁盘和输出红线；数据生成、200 epoch训练和评估硬超时分别为45、90和40分钟，总硬超时180分钟。预计总墙钟65–90分钟。只有硬超时自动结束进程树；科研语义、身份、支持域或数值异常立即停止，高置信低风险的编码、日志和包装器错误按`AGENTS.md`最小修复并使用新目录继续。

若0.2 Coarse-D8基本恢复，下一步才审批使用0.2标签重训/微调CH3；若只部分恢复，再决定是否补做低成本微调或Exact/Coarse混合训练；若恢复不足，则转向软子带或连续频带。无论结果如何，本Gate不自动运行test或完整级联。

### 12.6 执行结果

有效运行根为`outputs/s2g4r_threshold020/20260824_182523`，13个受监控阶段全部PASS且无warning/red flag。0.2支持域审计确认train的2560个源和validation的640个源全部至少有一个正子带，两个split均无空联合频带；8/8数据pilot和一次真实训练单步通过。完整数据为1024/256条，全部由物理元数据重算0.2标签，未读取旧0.3 `band_mask`。

三轨oracle-K结果为：`EE=130.726516 m`、`EC20=162.551301 m`、`CC20=157.505013 m`。新Coarse-D8完成200 epoch，best位于epoch 93，最后20轮无改善，训练趋势关闭且相对初始化有效学习；但只恢复Exact—Coarse RMSE缺口的15.856%，`CC20/EE=1.204844`。mean GOSPA比值为1.172947，超过1.10门槛；100 m召回由0.95625降至0.93125，下降2.5个百分点，通过5个百分点门槛。CC20的P90/P95为53.9678/224.7599 m，长尾没有随RMSE小幅恢复而改善。

工程状态为`PASS`，科学状态为`REPRESENTATION_LIMIT_CANDIDATE_T020`。这表明在当前1024/256缩减数据、D8结构、完整200 epoch调度和单seed范围内，单纯降低阈值并重新训练D8不足以恢复Exact性能；证据更支持19个10 MHz硬子带表示仍是限制候选，而非主要由D8输入分布失配造成。该结论不是信息论证明，也不外推至更大数据、其他模型或软/连续频带。

本Gate未运行test、predicted-band/K、CH3训练、微调或六格扩展评估。暂不建议直接用0.2标签重训CH3；下一步应优先审批软19子带、连续边界或更细频率栅格的oracle表示比较，只有新表示显著改善定位上限后再同步训练CH3。总报告为`s2g4r_report.json`，精确事实见`SYS-S2G4-R1-20260824`。
