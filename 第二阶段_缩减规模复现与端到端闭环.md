# 第二阶段：缩减规模复现与端到端闭环

> 建立日期：2026-08-21
>
> 当前状态：`S2-G0`至`S2-G5-R3`均已有明确结论。R2冻结8k CH3接口checkpoint；R3确认当前8k模型的级联损失主要来自源数判决，但1k→4k→8k学习曲线尚未饱和。
>
> 当前Gate：`S2-G5-R4`已完成新增8k、严格嵌套16k和容量pilot，但eager Dataset使系统可用RAM跌破6 GiB红线；正式长训练未启动，需先决定内存方案。
>
> 前置阶段：`第一阶段_论文与代码接管审查.md`已完成并冻结。

## 1. 阶段目标

本阶段使用本机可承受的缩减数据完成三件事：

1. 为第三章和第四章分别建立可学习、可重载的小规模checkpoint。
2. 找出第三章频带输出与第四章细DPD输入之间的真实障碍，并选择可行的接口表示。
3. 在接口确定后建立可解释的oracle/predicted端到端链路，为第三阶段正式实验设计提供依据。

## 2. 非目标

- 不把缩减数据结果写成论文正式性能。
- 不直接生成40k/5k/5k正式数据或全部4A–4D控制实验。
- 不用test选择模型、epoch、阈值或接口。
- 不因资源限制擅自改变物理网格、DPD定义、模型结构或研究算法。
- 不在现有接口尚未闭合时强行运行完整级联。

## 3. 当前证据基线

| 证据 | 当前可用结论 | 精确记录 |
|---|---|---|
| 最小基线封存 | 代码提交、Gate 3A数据和Gate 3B权重已有逻辑引用，不另建快照 | `S2-G0`相关修改记录 |
| 第三章checkpoint | Transformer在`2048/512/512`缩减数据上可学习、严格重复并独立重载 | `CH3-S2G1-20260822` |
| 第四章checkpoint | D8在`1024/256`缩减数据上完成200 epoch诊断，冻结epoch 93权重 | `CH4-S2G2-20260822` |
| 跨章兼容性 | CH3冻结模型能处理CH4共享IQ，但硬19子带到细DPD的定位门失败 | `SYS-S2G3-20260824` |
| 0.3支持域 | 10 MHz窗口和0.3阈值不能覆盖CH4全部2–20 MHz symbol-rate范围 | `SYS-S2G4-20260824` |
| 0.2适配诊断 | 0.2阈值闭合支持域，但重训D8只恢复15.86%的RMSE缺口 | `SYS-S2G4-R1-20260824` |
| Soft-19公平诊断 | 同一`BW_actual`下Hard-19适配优于Soft-19；Soft-19不足，物理频带定义是R1的重要混杂因素 | `SYS-S2G4-R2-20260825` |
| 跨seed候选 | 三seed下Hard-19-Actual成为稳定oracle候选，Soft收益不稳定 | `SYS-S2G4-R3-20260825` |
| 数据规模学习曲线 | Hard在4k/8k可逼近Exact；Soft仍无稳定优势 | `SYS-S2G4-R4-20260826` |
| 统一接口契约 | K=0/1/2/3统一IQ、Hard-19-Actual标签和按K分流已闭合 | `SYS-S2G5-R1-20260827` |
| CH3规模诊断 | 1k→4k→8k的计数和频带指标连续改善，8k冻结为R3接口checkpoint | `CH3-S2G5-R2-20260827` |

第一阶段的论文—代码映射、完整性判断和资源边界继续有效。精确路径、命令、SHA256、完整指标及资源数据只在`实验结果记录.md`中维护。

## 4. Gate路线与当前决策

| Gate | 只回答什么问题 | 结论 | 对后续的影响 |
|---|---|---|---|
| `S2-G0` | 如何最小成本封存现有基线 | `PASS` | 后续证据可追溯，不新增冗余快照 |
| `S2-G1` | CH3在缩减数据上能否稳定学习 | `PASS` | 获得可用于接口审查的CH3 checkpoint |
| `S2-G2` | CH4缩减配置能否充分训练 | `PASS` | 获得epoch 93 Exact-D8 checkpoint |
| `S2-G3` | 两章现成模型和接口能否直接组合 | 工程`PASS`；`REQUIRES_INTERFACE_REDESIGN` | 停止完整四轨，先拆分接口问题 |
| `S2-G4` | 原0.3硬子带能否用于D8适配诊断 | `STOPPED_AT_DATA_GATE` | 先解决窄带源无正子带问题 |
| `S2-G4-R1` | 0.2硬子带下重训D8能否恢复Exact性能 | 工程`PASS`；`REPRESENTATION_LIMIT_CANDIDATE_T020` | 不直接重训CH3，先比较更强的oracle表示 |
| `S2-G4-R2` | Soft-19能否比同定义Hard-19保留更多定位信息 | 工程`PASS`；`SOFT19_INSUFFICIENT` | 不采用Soft-19；Hard-19-Actual成为当前oracle候选 |
| `S2-G4-R3` | Hard候选和Soft/Hard方向能否跨seed重复 | 工程`PASS`；`HARD19_STABLE_CANDIDATE` | 固定Hard-19-Actual为下一接口候选；Soft不再优先 |
| `S2-G4-R4` | Hard/Soft/Exact排序是否随数据规模稳定 | 工程`PASS`；`HARD19_INTERFACE_SUPPORTED` | Hard在4k/8k逼近Exact；停止Soft，转入CH3 Actual接口设计 |
| `S2-G5-R1` | 如何统一物理分布、Actual带宽和CH3标签语义 | 已通过 | 统一IQ、Hard-19-Actual标签和K=0/1/2/3分流均已验证 |
| `S2-G5-R2` | CH3能否在统一数据上学习Hard-19-Actual，1k→4k→8k是否仍受益 | 工程`PASS`；`FREEZE_8K_FOR_R3` | 4k和8k两次扩展均带来明确配对改善；冻结8k供R3使用 |
| `S2-G5-R3` | oracle/predicted四轨能否隔离频带、计数和定位误差 | 工程`PASS`；`COUNT_PATH_PRIORITY` | 频带路径无稳定损失，完整级联退化主要由K错误造成 |
| `S2-G5-R4` | 16k规模能否自然降低K错误，剩余错误是否值得做候选K或稳健解码 | 方案已批准，执行至长训练前 | 先区分数据不足与方法不足，不提前修改K算法 |
| `S2-G5-R5` | 最终规模和冻结test能否形成端到端证据 | 候选子阶段 | 仅在R4确定规模或接口路线后设计 |

每个Gate进入前单独编制详细方案并由用户审批。失败或停止也是有效结论，不自动扩大数据、改模型或转入下一Gate。

## 5. 第二阶段完成条件

第二阶段同时满足以下条件后才能关闭：

1. CH3和CH4均有身份明确、可独立重载的缩减规模checkpoint。
2. 已选择一种有oracle证据支持的跨章频带表示，并明确需要由CH3还是CH4适配。
3. 最终接口能够生成稳定、可审计的细DPD，不遗漏`K=0`、漏检或多检样本。
4. oracle band/K与predicted band/K四轨能够区分频带、计数和定位误差；若决定两章独立成文，则应形成明确的停止理由。
5. 评估结果能追溯到代码、数据、命令、checkpoint和结构化输出。
6. 已形成第三阶段“创新核验与正式实验设计”所需的方法和资源依据。

## 6. 总停止规则

- test不参与模型、epoch、阈值、接口或数据规模选择。
- 一次实验只改变一个需要归因的核心因素；oracle表示无改善时，不直接投入CH3重训练。
- predicted结果不理想时，不用oracle结果替代端到端结论。
- 频带为空、样本遗漏、身份错配或指标定义不一致时，先停在当前层。
- 资源越界时优先采用懒加载、字段精简和流式评估，不先改变研究算法。
- 无法追溯到原始结果的数据或图表不进入论文证据包。
- 高置信、低风险的编码、路径或日志错误按`AGENTS.md`最小修复；涉及标签、DPD、模型、损失或科研判定的变化必须重新审批。

## 7. 文档与Gate写法

| 内容 | 唯一主责文档 |
|---|---|
| 当前任务和审批状态 | `当前任务.md` |
| 精确配置、路径、指标、资源和哈希 | `实验结果记录.md` |
| 已完成代码或文档修改 | `修改记录.md` |
| 稳定研究认识和创新边界 | `研究总结.md` |
| 第二阶段Gate方案、简明结论和路线 | 本文 |

本文中的每个Gate统一使用以下写法：

1. 先用一句话说明要回答的问题。
2. 用“方案卡”列出输入、本次改变、保持不变、输出和禁止范围。
3. 只有连续步骤不少于5个时才使用箭头流程；否则使用表格或短段落。
4. 先给结论，再列2–5项关键证据，最后说明证据边界和路线影响。
5. 运行目录、完整SHA、全部资源和工程流水不在本文重复，统一引用实验ID。

## 8. S2-G1：第三章小规模收敛

### 8.1 要回答的问题

CH3 Transformer能否在缩减数据上形成稳定学习信号，并产出只由validation选择、可独立重载和严格重复的checkpoint？本Gate不复现论文最终指标，不运行Exp 1A/2B/2C，也不调整输出阈值。

### 8.2 方案卡

| 项目 | 固定方案 |
|---|---|
| 输入 | 新生成train/val/test=`2048/512/512`；0/1/2/3源按`20%/20%/30%/30%`分层 |
| 本次改变 | 只增加S2受控规模、seed、审计和训练入口 |
| 保持不变 | 第三章物理模型、粗DPD、标签、Transformer、`M=10`和原loss |
| 训练 | FP32，batch=`64`，AdamW，lr=`1e-3`，100 epoch，warmup 5，patience 25，seed=`42` |
| 模型选择 | warmup后最低validation loss；固定阈值`0.5`；test只运行一次 |
| 主要输出 | 数据审计、两次严格重复训练、冻结checkpoint、独立validation和一次test |
| 资源停止 | 启动可用RAM≥14 GiB；RAM/GPU/磁盘硬线为8 GiB/14 GiB/100 GiB；单次训练硬超时180分钟 |
| 禁止范围 | 不修改师弟原`train_v26.py`默认入口，不用test调参，不自动扩大数据 |

所有S2功能通过显式参数或环境变量启用；未启用时保持原smoke/formal行为。输出写入唯一目录并拒绝覆盖。

### 8.3 执行和判定

```text
兼容性与资源预检
→ 受控数据生成
→ 数据、标签和split审计
→ batch 64容量检查
→ 两次100 epoch训练
→ checkpoint严格比较
→ 独立validation
→ 单次test
→ 总判定
```

| 判定项 | 通过要求 |
|---|---|
| 数据 | 标签复算、源间距、split隔离、有限性和训练前后SHA均通过 |
| 学习 | validation loss相对初始化下降至少20%，计数和频带指标明显提高 |
| 重复性 | A/B逐epoch科研字段和checkpoint科研状态逐值一致 |
| 收敛 | early stop、best不在最后10轮，或末10轮相对此前best改善不足1% |
| 评估完整性 | 保留`K=0...10`，不裁剪超范围预测；只由validation选择checkpoint |

### 8.4 执行结论

**结论：`PASS`。CH3在当前缩减配置下能够稳定学习、严格重复并独立重载。**

| 关键证据 | 结果 | 说明 |
|---|---:|---|
| 数据完整性 | 3072条最终样本零源间距违规、零标签复算错误、无split泄漏 | 首次发现的无约束位置回退已由S2专用后验门禁解决 |
| 严格重复 | 两次100 epoch训练的科研字段和checkpoint状态逐值一致 | 证明当前固定seed配置可重复 |
| 冻结模型 | best位于epoch 96 | 只由validation loss选择 |
| Validation | count accuracy=`0.90625`，band macro-F1=`0.93296` | 模型获得清晰学习信号 |
| 收敛 | 末10轮改善低于1% | 不需要继续延长当前缩减训练 |

首次数据运行暴露了共享位置生成器可能无约束补齐的问题；另一次重跑因启动RAM不足在训练前停止。两者均作为历史证据保留，不影响最终有效运行。精确事实见`CH3-S2G1-20260822`及其前置失败记录。

本结论只适用于固定缩减数据和单seed，不证明论文全量指标、跨seed统计稳定性或当前创新性。

## 9. S2-G2：第四章充分训练诊断与接口checkpoint

### 9.1 要回答的问题

D8在当前缩减数据上完成一次200 epoch调度后，训练趋势是否关闭，并能否获得比Gate 3B更适合作为接口基线的checkpoint？这里的200 epoch是参考论文和原代码设置的诊断预算，不是正式训练的最优epoch结论。

### 9.2 方案卡

| 项目 | 固定方案 |
|---|---|
| 输入 | 只读复用Gate 3A train/val/test=`1024/256/256`定位数据；不查看test |
| 本次改变 | 从随机初始化完整运行200 epoch并增加长尾诊断 |
| 保持不变 | D8 `dualhead_std`、FP32/no-AMP、batch=`8`、Focal+offset、AdamW和seed=`42` |
| 调度 | `T_max=200`、`eta_min=1e-6`、patience 200；第6轮后按validation oracle-K RMSE选best |
| 比较对象 | Gate 3B epoch 57与新200 epoch训练best，使用同一validation评估器 |
| 主要输出 | 趋势判定、统一checkpoint比较、长尾分析和第二次独立重载 |
| 资源停止 | 启动可用RAM≥14 GiB；RAM/GPU/磁盘硬线为8 GiB/14 GiB/100 GiB；训练硬超时180分钟 |
| 禁止范围 | 不运行test，不由P90/P95事后改选模型，不外推正式epoch |

### 9.3 执行和判定

```text
兼容性与资源预检
→ 数据manifest
→ 单次200 epoch训练
→ 趋势与长尾审计
→ 两候选统一validation
→ 机械选择checkpoint
→ 第二次独立重载
→ 数据身份复核
→ 总判定
```

最后20轮相对此前best改善不足1%，或best不晚于epoch 180，记为`TREND_CLOSED`；否则记为`TREND_OPEN_AT_200`，但不自动延长。新checkpoint只有在同一validation RMSE严格更低时才替换Gate 3B权重。

### 9.4 执行结论

**结论：`PASS`。200 epoch趋势已关闭，并冻结epoch 93 D8作为后续接口checkpoint。**

| 关键证据 | 结果 | 说明 |
|---|---:|---|
| best位置 | epoch 93；最后20轮无新增收益 | 当前缩减配置不需要继续机械增加epoch |
| 统一RMSE | Gate 3B `150.791921 m` → 新模型`130.726516 m` | 新checkpoint按预设规则胜出 |
| 长尾来源 | `>500 m`错误由15降至9，但median/P90/P95略差 | RMSE收益主要来自减少部分灾难性错误，不是普遍变准 |
| 独立重载 | 聚合指标和640条逐源误差完全一致 | checkpoint可稳定复用 |

训练后段出现train loss继续下降而validation变差的可能过拟合模式。200 epoch训练本身没有做第二次严格重复，也未运行test。精确事实见`CH4-S2G2-20260822`。

## 10. S2-G3：跨章可组合性与数据契约审查

### 10.1 要回答的问题

两章是否具备直接组合的前提？本Gate分别检查数据分布、硬19子带表示和CH3跨分布能力，不把异分布冻结模型直接串联后的数值解释成论文端到端性能。

### 10.2 方案卡

| 项目 | 固定方案 |
|---|---|
| 输入 | 256条CH4 validation共享IQ；CH3 epoch 96 Transformer；CH4 epoch 93 D8 |
| 本次改变 | 新增跨章粗DPD、频带映射和统一集合评估，不改原模型 |
| 保持不变 | 4站阵列、100 MHz采样、4096点观测、19子带；CH3阈值`0.5`；D8 FP32/no-AMP |
| 主要输出 | 分布契约、Exact/Coarse oracle表示对照、CH3跨分布指标及条件式四轨决定 |
| 评估 | GOSPA及分量、10/30/50/100 m集合指标、RMSE/median/P90/P95和N=2/N=3分层 |
| 资源停止 | 启动可用RAM≥14 GiB；RAM/GPU/磁盘硬线为8 GiB/14 GiB/100 GiB；总硬超时120分钟 |
| 禁止范围 | 不生成新IQ，不训练或微调，不调阈值，不运行test，不裁剪`K>3` |

两章基础阵列和采样参数一致，但源数比例、逐源带宽、频率重叠、位置、功率和SNR定义不同，因此直接复用只能作为待验证假设。

### 10.3 四阶段设计

| 阶段 | 做什么 | 继续条件 |
|---|---|---|
| A：分布契约 | 比较源数、带宽、频率拓扑、位置、功率、SNR、标签及shape/单位 | 差异能够被明确分类和追溯 |
| B：表示上限 | 同一IQ、oracle K下比较Exact-Oracle与19子带Coarse-Oracle细DPD | RMSE增幅≤10%，100 m召回下降≤5个百分点，无空频带或样本遗漏 |
| C：CH3跨分布 | 在CH4 IQ上运行冻结CH3两次，检查计数、频带和确定性 | 计数Wilson下界、频带IoU/覆盖率、空输出和重复性全部过门 |
| D：条件四轨 | 只有B和C均通过才运行OB-OK、OB-PK、PB-OK、PB-PK | 所有漏检、多检和空输出进入GOSPA及集合指标 |

阶段B或C失败即形成有效停止结论，不用oracle兜底，也不自动调整频带映射、阈值或模型。

### 10.4 执行结论

**结论：工程`PASS`，科学状态为`REQUIRES_INTERFACE_REDESIGN`。CH3本身通过跨分布门，但硬19子带到细DPD的接口未通过。**

| 关键证据 | 结果 | 说明 |
|---|---:|---|
| CH3跨分布计数 | accuracy=`0.691406`，Wilson下界=`0.632342` | 达到预设继续门槛，但N=2明显弱于N=3 |
| CH3跨分布频带 | 合并IoU中位数=`0.753765`，真实频带覆盖率中位数=`1.0` | 冻结CH3并非当前首要阻碍 |
| Exact-Oracle | RMSE=`130.726516 m` | 精确回归S2-G2基线 |
| Coarse-Oracle | RMSE=`164.148382 m`，为Exact的`1.255662`倍 | 超过1.10表示门槛，主要问题表现为长尾放大 |
| 100 m召回 | `0.95625` → `0.95` | 典型样本接近，但不能掩盖RMSE长尾退化 |

阶段B失败后按计划没有生成predicted-band细DPD，也没有运行完整四轨。该实验只能证明当前硬19子带映射与冻结D8不能直接复用，尚不能区分表示信息损失和D8输入分布失配。精确事实见`SYS-S2G3-20260824`。

## 11. S2-G4：原0.3硬子带的D8适配诊断

### 11.1 要回答的问题

S2-G3中的退化主要来自冻结D8不适应Coarse细DPD，还是来自硬19子带表示本身？原计划用Coarse-D8从头训练作为主证据，并把Exact-D8低学习率微调作为低成本对照。

### 11.2 方案卡

| 项目 | 固定方案 |
|---|---|
| 输入 | Gate 3A train/validation=`1024/256` IQ、Exact监督和epoch 93 Exact-D8 |
| 本次改变 | 按CH3原0.3标签把真值频带映射成19子带并重算细DPD |
| 保持不变 | D8结构、FP32/no-AMP、batch=`8`、Focal+offset、seed=`42`和oracle K |
| 两项训练 | Coarse-D8从头200 epoch；Exact-D8全网络微调60 epoch |
| 主要输出 | Exact/Coarse六项交叉评估和“D8失配/混合原因/表示候选”判定 |
| 资源停止 | 启动可用RAM≥14 GiB；RAM/GPU/磁盘硬线为8 GiB/14 GiB/100 GiB；Gate总硬超时180分钟 |
| 禁止范围 | 不修改CH3，不运行predicted band/K或test，不为空频带补Exact信息 |

### 11.3 原执行流程和停止条件

```text
身份与资源预检
→ 8/8数据及训练单步pilot
→ 完整Coarse数据生成
→ Coarse-D8从头训练
→ Exact-D8微调
→ 六项统一validation评估
→ 根因判定
```

数据层必须保证每个有效源有可表达频带、每个样本联合频带非空，并与Exact数据的索引、位置和监督逐值一致。任一数据语义门失败即停止，不以阈值调整、ignore转正或Exact回退补救。

### 11.4 执行结论

**结论：`STOPPED_AT_DATA_GATE`。原0.3标签不覆盖CH4全部窄带源，因此尚未进入D8适配比较。**

| 关键证据 | 结果 | 说明 |
|---|---:|---|
| 预检和pilot | 通过 | DPD、D8单步和保存重载工程链可用 |
| train支持域 | 2560个源中154个无正子带；1个样本联合频带为空 | 154个源全部满足`symbolRate<3 MHz` |
| validation支持域 | 640个源中38个无正子带 | 其他源的正带掩盖了逐源缺失，整样本未表现为空 |
| 结构结论 | `REPRESENTATION_SUPPORT_GAP_CONFIRMED` | 10 MHz窗口和0.3覆盖阈值不能覆盖2–20 MHz完整范围 |

没有运行完整Coarse数据、D8训练、微调、六项评估或test，因此不能评价D8适配是否有效。该停止后来促成单独审批的S2-G4-R1；精确事实见`SYS-S2G4-20260824`。

## 12. S2-G4-R1：0.2硬子带快速D8适配诊断

### 12.1 要回答的问题

把正标签覆盖阈值固定为0.2并闭合2 MHz下限后，重新训练D8能否基本恢复Exact定位性能？本Gate只评价缩减train/validation、0.2 Coarse-Oracle和oracle K。

### 12.2 方案卡

| 项目 | 固定方案 |
|---|---|
| 输入 | Gate 3A train/validation=`1024/256` IQ与Exact监督；epoch 93 Exact-D8 |
| 本次改变 | 只把主瓣覆盖阈值由0.3改为0.2，并从物理元数据重算标签 |
| 保持不变 | 19个10 MHz窗口、5 MHz步长、CH4数据分布、D8结构和训练规则 |
| 新训练 | D8随机初始化，FP32/no-AMP，batch=`8`，200 epoch，seed=`42` |
| 三轨 | `EE`：Exact-D8/Exact；`EC20`：Exact-D8/0.2 Coarse；`CC20`：新D8/0.2 Coarse |
| 资源停止 | 启动可用RAM≥14 GiB；RAM/GPU/磁盘硬线为8 GiB/14 GiB/100 GiB；Gate总硬超时180分钟 |
| 禁止范围 | 不读取旧0.3标签，不把ignore转正，不用Exact回退，不微调CH3或现有Exact-D8，不运行test |

### 12.3 执行流程和预设判定

```text
完整支持域审计
→ 8/8数据与训练单步pilot
→ 完整0.2 Coarse数据生成
→ EC20评估
→ Coarse-D8单次200 epoch训练
→ CC20独立评估
→ EE/EC20/CC20三轨判定
```

恢复比例定义为：

$$
\frac{R_{EC20}-R_{CC20}}{R_{EC20}-R_{EE}}.
$$

| 观察结果 | 预设判定 |
|---|---|
| 训练趋势关闭，CC20的RMSE/GOSPA均在EE的1.10倍内，100 m召回下降≤5个百分点 | `D8_DISTRIBUTION_SHIFT_DOMINANT_T020` |
| 恢复比例≥50%但未达到等效范围，或总体恢复但长尾未恢复 | `MIXED_CAUSES_T020` |
| 训练趋势关闭但恢复比例<50% | `REPRESENTATION_LIMIT_CANDIDATE_T020` |
| 趋势未关闭、有效学习失败或证据异常 | `INCONCLUSIVE_T020` |

### 12.4 执行结论

**结论：工程`PASS`，科学状态为`REPRESENTATION_LIMIT_CANDIDATE_T020`。降低阈值解决了“有没有频带”，但没有解决“频带信息是否足够准确”。**

| 关键证据 | 结果 | 说明 |
|---|---:|---|
| 支持域 | train 2560源、validation 640源全部有正子带，零空联合频带 | 0.2阈值确实闭合2 MHz下限 |
| 三轨RMSE | EE=`130.726516 m`；EC20=`162.551301 m`；CC20=`157.505013 m` | 重训D8只小幅改善Coarse输入 |
| 恢复比例 | `15.856%` | 低于50%混合原因门槛 |
| 等效门 | CC20/EE RMSE=`1.204844`；GOSPA比=`1.172947` | 两项均超过1.10 |
| 长尾 | CC20 P95=`224.7599 m`，高于EE的`89.7583 m` | 典型误差不差，但极端定位明显恶化 |

新Coarse-D8完成200 epoch，best位于epoch 93且末20轮无改善，排除了“训练明显不足”这一直接解释。当前证据更支持硬19子带表示仍是限制候选，而不是主要由D8输入分布失配造成。

本结论只适用于当前缩减数据、D8、0.2硬子带和单seed，不是信息论证明，也不排除其他模型或更强频带表示。暂不直接用0.2标签重训CH3；精确事实见`SYS-S2G4-R1-20260824`。

## 13. S2-G4-R1后形成的候选频带表示方法

> 本章保留R1后形成候选方法时的设计依据。Soft-19现已由R2实际验证，当前路线以第14.7节为准；本章其余方法仍未获批执行。

### 13.1 为什么需要改变频带表示

在R1完成时，0.2阈值已经让第四章2–20 MHz symbol-rate范围内的每个有效源获得正子带，但重新训练D8只恢复了Exact—Coarse RMSE缺口的15.856%。当时因此提出检查“频带怎样交给细DPD”。R2随后证明，R1还混入了主瓣与`BW_actual`定义差异，不能把R1结果单独解释成19窗口分辨率不足。

三种候选方法都不直接改变定位网络。它们改变的是频带先验到细DPD的转换方式：

```text
频带表示
→ 转换为4096点FFT频率掩模或权重
→ 从4站IQ中保留或加权相应频率成分
→ 计算401×401细DPD空间谱
→ D8预测信源位置
```

在候选位置$\mathbf{x}$上，DPD会按照该位置对应的站间时延差，对所选频点的互谱进行相位补偿和累积。可概括为：

$$
R_{mn}(\mathbf{x})=
\sum_{f\in\mathcal{F}}
X_m(f)X_n^*(f)
e^{j2\pi f\Delta\tau_{mn}(\mathbf{x})}.
$$

其中$\mathcal{F}$由频带接口决定。漏掉真实信号频点会损失相干定位信息；加入过多带外频点会引入噪声或其他信号；边界偏差还会改变细DPD的峰形、旁瓣和长尾。D8接收的是这种变化后的空间谱，而不是频带标签本身。

当前硬19子带把$[-50,50)$ MHz基带划分为19个重叠窗口：每个窗口宽10 MHz、步长5 MHz，相邻窗口重叠5 MHz。S2-G4-R1对每个源和每个窗口计算主瓣覆盖率：

$$
c_k=\frac{|B_{\mathrm{main}}\cap W_k|}{10\ \mathrm{MHz}}.
$$

再按$b_k=\mathbf{1}[c_k\ge0.2]$生成二值标签。所有源的正子带取并集后，正子带覆盖的全部FFT频点都被保留。该规则解决了“窄带源没有任何正子带”的支持域问题，却可能把很窄的真实频带扩展成完整10 MHz窗口，因此仍会带入较多无用频点。

### 13.2 方法一：软19子带

软19子带保留现有19个窗口，但不再把覆盖率立即压成0或1，而是保留连续值：

$$
c_k\in[0,1].
$$

例如，硬表示可能为`[0, 0, 1, 1, 0, ...]`，软表示则可以为`[0, 0.1, 0.7, 0.5, 0.05, ...]`。oracle实验中的$c_k$由真实频带与各窗口的重叠比例计算；未来实际级联时，可以由CH3输出子带概率或软覆盖值。

19个软值还要展开成4096点FFT权重。一个简单候选规则是对覆盖同一频点的窗口取最大权重：

$$
w(f)=\max_{k:f\in W_k}c_k.
$$

细DPD应使用$\sqrt{w(f)}X_m(f)$作为加权频谱，使站间互谱中的最终权重为$w(f)$；若直接用$w(f)$乘FFT，互谱权重会变成$w^2(f)$。R2已按此规则为`compute_fine_dpd()`增加默认关闭的显式权重接口，并用二值、全1和统一缩放三项回归验证语义。

软19子带的主要价值是消除阈值附近的突变，并保留一部分边界位置和相对覆盖信息。例如覆盖率0.199和0.201不再分别变成完全不同的0和1，而是保持为相近的连续值。它对CH3的改造也相对较小，可以继续使用19个输出。

它的限制是频率描述仍只有19维，分辨率仍受10 MHz窗口和5 MHz步长约束。两个不同的窄带信号仍可能产生相似的19维表示；多源频带求并集后，频带属于哪个信源的信息也可能丢失。

| Soft-19 Oracle结果 | 可支持的判断 |
|---|---|
| 明显接近Exact | 硬二值化是主要损失，19窗口可能仍够用 |
| 有改善但长尾仍差 | 连续权重改善了一般样本，但重叠、边界或多源样本仍受限 |
| 几乎不改善 | 19窗口分辨率或频带求并集更可能是主要限制 |

R2的实际结果是：Soft-19适配后没有优于同定义Hard-19，而Hard-19-Actual已经达到当前oracle候选水平。因此这里只能排除“软化是必要改进”，不能据此继续推出“必须提高到39/79子带”。

### 13.3 方法二：连续频带边界

连续边界不输出19个子带，而是直接描述每个信源的频率区间：

$$
[f_{\mathrm{low},s},f_{\mathrm{high},s}],
$$

或者等价地输出中心频率$f_{c,s}$和带宽$B_s$：

$$
f_{\mathrm{low},s}=f_{c,s}-\frac{B_s}{2},\qquad
f_{\mathrm{high},s}=f_{c,s}+\frac{B_s}{2}.
$$

这些区间的并集直接映射到4096点FFT频率轴。当前采样率为100 MHz、观测长度为4096，因此FFT频率间隔约为24.4 kHz，远细于当前5 MHz子带步长。

需要明确：S2-G2和S2-G3使用的`Exact-Oracle Band`已经是真实连续边界oracle。它根据每个源的真实中心频率和实际带宽生成FFT掩模，现有EE轨道已经给出了这种表示下的定位上限。因此，后续不需要重复运行相同的continuous-oracle训练来证明其上限；真正待解决的问题是CH3能否从粗DPD中预测足够准确的连续边界。

若走该路线，CH3需要新增或替换输出任务，例如保留源数分类，同时为每个有效源槽位回归中心频率和带宽，或直接回归左右边界。训练时还要处理空槽、边界合法性、源槽位排序、多源重叠和回归损失尺度。

连续边界的信息保留最充分，形成的细DPD也最接近第四章原始Exact输入；但它是三种方案中CH3改造最大、训练难度最高的一种。连续回归对标签定义和预测误差更敏感，频带边界究竟表示主瓣还是包含滚降带的实际带宽也必须先固定。

### 13.4 方法三：更细频率栅格

更细栅格保留“子带分类”框架，但减小窗口宽度和步长。例如：

| 方案 | 子带宽度 | 步长 | 子带数量 |
|---|---:|---:|---:|
| 当前硬19子带 | 10 MHz | 5 MHz | 19 |
| 细化一级 | 5 MHz | 2.5 MHz | 39 |
| 细化二级 | 2.5 MHz | 1.25 MHz | 79 |

以39子带为例，边界量化粒度由约5 MHz降低到约2.5 MHz。它仍可使用硬标签和子带并集生成FFT掩模，但每个被选窗口更窄，因此频带膨胀和带外噪声会减少，对窄带信号及相近频带也更友好。

对第四章而言，最终输入仍是一张`401×401`细DPD，D8的输入尺寸和主体结构不必改变；需要重新生成相应频带表示下的细DPD并重新训练D8。对第三章而言，粗DPD输入和输出的频率维都将由19增加到39或79，数据体积、粗DPD计算量、模型首层和输出头都需要同步调整。

若后续证据表明Hard-19-Actual仍不稳定，再考虑Hard-39时不应同时加入软权重。只有Hard-39在公平重复中明显优于Hard-19-Actual，才能把收益归因于频率分辨率提高；R2结果本身不要求自动进入Hard-39。

### 13.5 三种方法的关系

| 方法 | 保留的信息 | CH3改造 | CH4改造 | 主要回答的问题 |
|---|---|---|---|---|
| Soft-19 | 19窗口的连续覆盖程度 | 较小 | 增加加权细DPD并重训D8 | 二值化是否是主要损失 |
| 连续边界 | 每个源的精确左右边界 | 最大 | 可复用现有Exact链路 | CH3能否直接给出CH4所需精确频带 |
| Hard-39/79 | 更高分辨率的离散频带 | 中等 | 重算细DPD并重训D8 | 19窗口的频率分辨率是否不足 |

三者不是互斥的最终选择。Soft-19是最低成本的信息增强；Hard-39在保留分类范式的同时提高分辨率；连续边界的信息上限最高，但需要重新设计CH3输出任务。后续也可以在证据支持下组合为Soft-39，但不应在第一轮同时改变多个因素。

### 13.6 后续比较必须控制的两个因素

第一，必须统一物理频带定义。当前Coarse标签按symbol rate定义主瓣区间：

$$
B_{\mathrm{main},s}=
[f_{c,s}-R_s/2,\ f_{c,s}+R_s/2],
$$

而Exact掩模按`BW_actual`定义包含滚降部分的实际频带：

$$
B_{\mathrm{actual},s}=
[f_{c,s}-BW_{\mathrm{actual},s}/2,\ f_{c,s}+BW_{\mathrm{actual},s}/2].
$$

后续oracle比较如果继续沿用不同定义，就会同时改变“表示精度”和“物理频带范围”，无法把差异严格归因于软化或栅格分辨率。建议统一以`BW_actual`为基准，再分别生成Exact、Soft-19和Hard-39表示；若必须沿用CH3主瓣语义，则应另建Exact-Mainlobe对照，不能直接与现有Exact-Actual EE混比。

第二，必须检查多源频带求并集造成的信息损失。当前接口先把多个源的频带合并成一个FFT掩模，再计算一张细DPD。即使单源边界准确，求并集仍可能丢掉“哪个频带属于哪个源”的对应关系，并使不同源在同一细DPD中互相干扰。若Soft-19和Hard-39仍不能接近Exact，应进一步比较“联合频带单张细DPD”和“按源槽位分别生成细DPD”，而不是无限增加子带数量。

### 13.7 R2后路线的实际推进

```text
EE连续真实边界上限已复用
→ R2完成Hard/Soft-19-Actual公平比较
→ Soft-19停止，Hard-19-Actual进入候选
→ R3完成三seed确认
→ R4完成1k/4k/8k学习曲线
→ Hard-19-Actual固定为CH3接口目标
→ 转入S2-G5数据契约与predicted-band / predicted-K闭环
```

R3和R4已经补充跨seed与数据规模证据，原先“先跨seed还是直接做CH3标签”的分支已关闭。Hard-39、连续回归和逐源细DPD继续保留为备选，仅当S2-G5证明Hard-19-Actual预测接口本身不足时再考虑。

## 14. S2-G4-R2：Soft-19 oracle适配诊断

### 14.1 要回答的问题

在统一使用真实`BW_actual`定义物理频带后，Soft-19是否比Hard-19保留更多定位信息，并能否通过重新训练D8接近现有Exact上限？本Gate只研究oracle频带表示，不评价CH3当前预测能力。

### 14.2 表示定义与公平控制

对第$s$个源和第$k$个10 MHz窗口，使用实际频带计算覆盖率：

$$
c_{s,k}=\frac{|B_{\mathrm{actual},s}\cap W_k|}{10\ \mathrm{MHz}}.
$$

| 表示 | 子带规则 | FFT映射 |
|---|---|---|
| Hard-19-Actual | $b_{s,k}=\mathbf{1}[c_{s,k}\ge0.2]$，再对信源取并集 | 正子带覆盖频点权重为1，其余为0 |
| Soft-19-Actual | $q_k=\max_s c_{s,k}$ | $w(f)=\max_{k:f\in W_k}q_k$，FFT振幅乘$\sqrt{w(f)}$ |

Hard-19-Actual不是新的研究路线，而是Soft-19的公平控制。它与Soft-19使用完全相同的`BW_actual`、19个窗口、IQ、监督和训练配置，只改变“覆盖率是否二值化”。S2-G4-R1的Hard-19使用主瓣定义，只作为历史参照，不作为本Gate的主要因果对照。

### 14.3 方案卡

| 项目 | 固定方案 |
|---|---|
| 输入 | Gate 3A train/validation=`1024/256` IQ与Exact监督；S2-G2 epoch 93 Exact-D8 |
| 新数据 | 同一IQ分别生成Hard-19-Actual和Soft-19-Actual的`401×401`细DPD |
| 两项训练 | Hard-D8与Soft-D8均从相同seed随机初始化，D8、FP32/no-AMP、batch=`8`、200 epoch |
| 保持不变 | D8结构、Focal+offset、AdamW、lr=`1e-3`、完整余弦调度、oracle K和validation选best |
| 五轨 | `EE`、`EHA`、`HHA`、`ESA`、`SSA`；首字母为模型，后两字母为输入表示 |
| 主比较 | `SSA`对`HHA`判断软化收益；`SSA`对`EE`判断是否接近Exact上限 |
| 资源停止 | 启动可用RAM≥14 GiB；RAM/GPU/磁盘硬线为8 GiB/14 GiB/100 GiB；输出硬线6 GiB |
| 禁止范围 | 不修改或训练CH3，不运行test或predicted band/K，不调窗口、阈值、模型或epoch |

### 14.4 实现与验证要求

- `compute_fine_dpd()`新增默认关闭的非负频率权重接口；原`freq_mask`调用路径和位置参数保持兼容，两种输入互斥。
- 软权重必须有限、位于`[0,1]`且至少一个频点为正；使用平方根振幅权重，保证互谱中的相对权重为$w(f)$。
- 增加权重语义测试：二值权重与原布尔掩模一致；全1权重与全频带一致；统一缩放在功率归一化后保持输出一致。
- 两种新数据必须逐样本核对索引、源数、位置和监督与Exact一致，并记录非零频点、等效带宽和权重范围。
- 所有新功能通过S2-G4-R2专用入口显式启用；原CH3/CH4、S2-G4和S2-G4-R1入口默认行为不变。

### 14.5 执行流程

```text
身份、资源与权重语义预检
→ Hard/Soft各8+8条数据pilot
→ 两种表示各一次训练单步
→ 完整Hard/Soft train与validation生成
→ EHA与ESA输入失配评估
→ Hard-D8与Soft-D8各200 epoch训练
→ HHA与SSA独立评估
→ 五轨配对比较和总判定
```

数据生成、单次训练、单次评估和Gate总墙钟硬线分别为45、90、40和210分钟。只有硬超时自动结束进程树；科研语义、身份、非有限值或资源红线按既有规则停止。

### 14.6 预设判定

当$R_{HHA}>R_{EE}$时，Soft-19相对Hard-19的RMSE恢复比例定义为：

$$
\rho_{soft}=\frac{R_{HHA}-R_{SSA}}{R_{HHA}-R_{EE}}.
$$

| 观察结果 | 科学状态 |
|---|---|
| 两项训练趋势关闭且有效学习；`SSA/EE`的RMSE和GOSPA均≤1.10，100 m召回下降≤5个百分点 | `SOFT19_EQUIVALENT_CANDIDATE` |
| 未进入等效范围，但$\rho_{soft}\ge0.5$，且SSA的RMSE优于HHA | `SOFT19_EFFECTIVE_NOT_EQUIVALENT` |
| 两项趋势关闭，但$\rho_{soft}<0.5$或SSA不优于HHA | `SOFT19_INSUFFICIENT` |
| 任一训练趋势未关闭、有效学习失败、数据/权重语义异常或关键指标不可比 | `INCONCLUSIVE_SOFT19` |

同时报告mean GOSPA、100 m召回、median、P90/P95、极端错误、N=2/N=3和固定seed配对bootstrap。bootstrap只描述差值不确定性，不替代上述预设判定。

### 14.7 执行结论

**结论：工程`PASS`，科学状态为`SOFT19_INSUFFICIENT`。Soft-19没有优于同定义Hard-19，当前不值得据此改造CH3。**

| 关键问题 | 结果 | 含义 |
|---|---:|---|
| 两种表示是否公平可比 | 1280条监督逐字段相同；1280条DPD全部不同 | 主要变量确为Hard/Soft表示 |
| 训练是否充分 | 两者均完成200 epoch，Hard/Soft最佳epoch=`121/79`；趋势均关闭且有效学习 | Soft并非在79轮停止，不能用“训练轮数不足”解释结果 |
| 适配后RMSE | EE/HHA/SSA=`130.73/112.06/141.23 m` | Soft未优于Hard；Hard已达到当前oracle候选水平 |
| mean GOSPA | EE/HHA/SSA=`27.59/28.70/30.77 m` | Soft相对EE高11.52%，未通过10%等效门 |
| 100 m recall | EE/HHA/SSA=`95.63%/95.16%/93.44%` | Hard与EE接近，Soft略差 |
| 长尾 | HHA/SSA P95=`92.83/139.35 m` | Soft的主要问题仍在极端样本 |
| 源数分层 | HHA/SSA双源=`115.41/109.66 m`，三源=`109.78/158.83 m` | Soft不是全面退化，差距主要由三源长尾拉大 |
| 有效频率孔径 | Hard/Soft非零支持约=`1416/1558`频点，有效加权带宽约=`34.57/28.41 MHz` | Soft覆盖点更多，但低权重使有效带宽反而缩小 |

本结果推翻了“R1已经证明19窗口分辨率本身是主要限制”的强解释。R1使用`symbolRate`主瓣定义，R2公平对照统一使用`BW_actual`；Hard-19-Actual反而取得更低RMSE，说明物理频带定义是此前结果的重要混杂因素。单seed下HHA优于EE不能解释为Hard-19理论上优于连续Exact，只能说明它已达到值得继续验证的候选水平。

Soft并不是在Hard之外“增加置信度信息”，而是在逐站功率归一化前重新分配各频率的相对贡献。当前实现还对多源覆盖率取`max`，并在每个10 MHz窗口内使用常数权重；它既不保留频带属于哪个信源，也不是按SNR或定位信息设计的最优权重。因此Soft可能压低对时延分辨率有用的频带边缘，削弱第三个信源的DPD峰，并在oracle `K=3`强制取三个峰时产生少量严重误定位。该机制与三源和P95退化一致，但目前频带宽度比例与Soft退化没有明确单调相关性，仍属于待验证解释。

逐样本上Soft优于Hard的比例约44.5%，RMSE差值中位数仅约`+0.36 m`，说明典型样本接近，整体差距由少数灾难性错误主导。Soft/Hard配对bootstrap的RMSE均值差约`+10.25 m`，95%区间为`[-4.56, 26.21] m`；GOSPA差约`+2.07`，95%区间为`[-1.13, 5.34]`，均跨0。因此不能声称Soft稳定显著更差；单seed还无法覆盖初始化和优化路径方差。当前最合理判断是：训练随机性可能参与排名，但“Soft只是较早收敛”证据不足，频率重加权、有效带宽缩小和三源峰竞争是更值得检查的机制。

按事先等效门，本Gate仍判为`SOFT19_INSUFFICIENT`。随后R3已完成配对跨seed确认，R4又完成数据规模学习曲线；两者均未提供继续Soft路线的充分依据。精确事实见`SYS-S2G4-R2-20260825`。

## 15. S2-G4-R3：Exact/Hard/Soft配对跨seed确认

### 15.1 要回答的问题

R3用于区分单seed训练方差与表示差异：确认Hard-19-Actual相对Exact是否稳定，Soft劣于Hard是否只是seed 42的偶然现象，以及Soft的退化是否持续集中在三源长尾。本Gate仍只使用缩减validation、oracle频带和oracle K，不构成论文正式性能实验。

### 15.2 配对设计

| 项目 | 固定方案 |
|---|---|
| 表示 | Exact、Hard-19-Actual、Soft-19-Actual |
| seeds | `42、1042、2042`；seed 42复用已有证据 |
| 新训练 | seed 1042和2042各训练Exact/Hard/Soft，共6次 |
| 数据 | 只读复用现有1024/256三种DPD及完全相同监督，不重新生成 |
| 模型与训练 | D8、随机初始化、FP32/no-AMP、batch 8、200 epoch、完整余弦调度 |
| 配对控制 | 同一seed三种表示使用相同初始参数、样本顺序和确定性配置 |
| checkpoint | 只依据validation RMSE选择best |
| 禁止范围 | 不修改CH3，不运行test或predicted band/K，不调表示、模型、loss或epoch |

运行顺序固定为seed 42历史`Exact→Hard→Soft`、seed 1042 `Hard→Soft→Exact`、seed 2042 `Soft→Exact→Hard`，降低固定训练顺序与表示完全重合的风险。新功能使用独立R3入口，原S2-G2、S2-G4和R2的seed门禁及默认行为保持不变。

### 15.3 执行流程

```text
代码、数据与seed 42证据身份预检
→ seed切换和相同初始化pilot
→ seed 1042三项配对训练与评估
→ seed 2042三项配对训练与评估
→ 三seed逐项配对分析
→ 三源长尾和最差样本诊断
→ 结构化报告与Gate判定
```

任一训练崩溃、出现非有限值、身份不一致或确定性配置失败时停在当前层，不自动换seed补位。每个seed内部报告配对样本bootstrap；跨seed只报告方向、median和range，不用3个seed进行显著性检验，也不把validation结果表述为无偏test性能。

### 15.4 指标与预设判定

主指标为定位RMSE，同时报告mean GOSPA、100 m recall、median、P90/P95、`>100 m`错误数、N=2/N=3、best epoch和训练趋势。每个seed的Hard等效条件为RMSE比和GOSPA比均不高于`1.10`，且100 m recall下降不超过5个百分点。

| 状态 | 判定 |
|---|---|
| `HARD19_STABLE_CANDIDATE` | 至少2/3 seed通过等效条件且跨seed中位数通过；不存在RMSE/GOSPA恶化超过25%或recall下降超过10个百分点的极端seed |
| `HARD19_NOT_SUPPORTED` | 至少2/3 seed中Hard相对Exact恶化超过10%，且跨seed中位数不通过 |
| `HARD19_SEED_SENSITIVE` | 结果方向明显不一致，或总体判断被单个严重异常seed主导 |
| `INCONCLUSIVE_R3` | 训练、身份、确定性或指标不可比 |

Soft只作为次要判断：若至少2/3 seed优于Hard、RMSE中位改善达到5%且GOSPA不恶化，才重新考虑Soft；若持续只在三源P95和严重错误上退化，则转向有效带宽和第三峰竞争诊断。结果不自动触发Soft调权、Hard-39或增加到5个seed。

### 15.5 资源与监控

| 项目 | 启动/预警 | 红线 |
|---|---:|---:|
| 启动可用RAM | `≥12 GiB` | — |
| 运行可用RAM | `<8 GiB` | `<6 GiB` |
| 进程树RSS | `>6 GiB` | `>10 GiB` |
| GPU总占用 | `>13 GiB` | `>15 GiB` |
| 磁盘剩余 | `<50 GiB` | `<25 GiB` |
| R3输出 | `>6 GiB` | `>10 GiB` |

R3不设置数据、训练、评估或Gate墙钟硬超时，预计时长只用于调度。监控固定10秒采样；进程无新输出仅产生一次提示，不自动结束。只有非零退出、CUDA OOM、非有限值、数据门禁失败或用户明确终止才结束当前任务。控制台使用摘要模式，完整逐epoch输出保留在`stage.log`。

### 15.6 执行结论

**结论：工程`PASS`，科学状态为`HARD19_STABLE_CANDIDATE`。Hard-19-Actual在三个seed的RMSE均不差于Exact，已具备进入CH3 Actual标签接口设计的稳定性证据；Soft-19没有跨seed稳定优势。**

| seed | Exact RMSE | Hard RMSE | Soft RMSE | Hard完整等效门 | Soft相对Hard |
|---:|---:|---:|---:|---|---|
| 42 | 130.73 m | 112.06 m | 141.23 m | 通过 | 变差26.03% |
| 1042 | 107.62 m | 104.14 m | 128.75 m | GOSPA比1.124，未通过 | 变差23.63% |
| 2042 | 140.76 m | 135.00 m | 122.96 m | 通过 | 改善8.91% |

Hard/Exact RMSE比的三seed中位数为`0.9590`、范围为`[0.8572,0.9676]`；GOSPA比中位数为`1.0401`，100 m recall下降中位数仅`0.00469`。2/3 seed通过联合等效门，且没有RMSE/GOSPA恶化超过25%或recall下降超过10个百分点的极端seed，满足预设稳定候选条件。这里的“稳定”只表示当前缩减数据和三seed筛查下，Hard没有依赖seed 42的偶然优势；不能解释为离散Hard频带理论上优于Exact。

Soft只在seed 2042优于Hard，三seed中位RMSE改善为`-23.63%`，GOSPA比中位数为`1.0722`，未通过重新考虑Soft的门槛。R2观察到的三源长尾在seed 42和1042重复，但seed 2042中Soft的N=2/N=3 RMSE都优于Hard，说明Soft的不利机制具有明显训练seed依赖，不能再表述为“Soft必然削弱第三峰”。当前更稳妥的结论是：Soft重加权增加了优化方差或与D8的适配不稳定，没有提供足以承担接口改造成本的稳定收益。

三seed的Soft−Hard样本bootstrap区间均跨0；跨seed只有3个配对，不进行显著性检验。统计谬误11项扫描已覆盖：需要持续警惕聚合结果掩盖seed方向差异、探索性多指标和将validation诊断外推为理论或test结论；其余类型不适用或未见证据。随后R4已完成数据规模确认，当前路线转入S2-G5，不再自动调Soft权重、增加seed或进入Hard-39。精确事实见`SYS-S2G4-R3-20260825`。

## 16. S2-G4-R4：数据规模学习曲线与场景分层诊断

### 16.1 要回答的问题

R4不再增加Soft训练seed，而是扩大共享数据并观察结论是否随数据规模稳定。它回答三件事：Hard相对Exact的优势是否只是小数据偶然现象；Soft与Hard的排序能否在更丰富场景中稳定；哪些带宽、SNR、源数和空间条件导致三种表示出现差异。

### 16.2 数据与公平控制

| 项目 | 固定方案 |
|---|---|
| 共享IQ | MATLAB seed=`20260826`；train/val/test=`8192/2048/2048` |
| 物理分布 | 保持第四章原版生成分布；N=2/3各半，symbol rate=`2–20 MHz`，SNR=`-10–10 dB` |
| 三种表示 | Exact-Actual、Hard-19-Actual（阈值0.2）、Soft-19-Actual |
| 嵌套训练规模 | `1024 ⊂ 4096 ⊂ 8192`；相同规模三种表示使用相同IQ、标签和样本索引 |
| validation | `val_select=1024`用于checkpoint选择，`val_compare=1024`只用于冻结后的规模比较 |
| test | 只生成并锁定原始IQ，本Gate不生成test DPD、不评估test |
| 模型 | D8、FP32/no-AMP、batch 8；不修改CH3、loss或表示定义 |

Exact数据使用默认关闭的`d8_minimal`存储，只省略D8不读取的`hyp_mask`和`gauss_multi`；原入口默认文件字段保持不变。Hard/Soft继续复用Exact的位置与热力图监督，避免三套标签漂移。

### 16.3 分步执行

```text
R4-A 复读R3并做场景分层
→ R4-B 生成并审计共享IQ
→ R4-C 建立嵌套manifest与冻结validation
→ R4-D 生成并审计Exact/Hard/Soft细DPD
→ R4-E Codex按1k、4k、8k运行训练
→ R4-F 冻结模型后比较学习曲线和场景长尾
```

用户后续明确授权Codex执行R4-E/R4-F。九组训练严格按`1k→4k→8k`、同规模`Exact→Hard→Soft`完成，没有使用test或修改科研配置。

### 16.4 训练预算与判定边界

| 规模 | 最大epoch | 用途 |
|---:|---:|---|
| 1024 | 200 | 与R3小规模结论衔接 |
| 4096 | 160 | 判断增加多样性后排序是否稳定 |
| 8192 | 80 | 在可接受成本下观察学习曲线趋势 |

checkpoint只按`val_select`选择，`val_compare`不参与调参。主比较是三种表示在相同规模下的配对差异，以及同一表示随规模增长的变化；同时报告N=2/N=3、SNR、实际带宽、频带并集宽度、最小源间距和边缘场景。R4不能证明正式论文配置最优，也不把validation结论当作test性能。

若Hard优势随规模缩小或消失，优先解释为Exact在小数据下更难学习；若Hard持续稳定，则冻结Hard-19作为CH3接口候选；若Soft仍无稳定优势，则停止Soft路线。三种表示都不稳定时，先检查数据覆盖与长尾，不直接修改CH3或继续增加seed。

### 16.5 资源与停止规则

监控固定10秒采样，不设置墙钟硬超时。GPU占用13/15 GiB、系统可用RAM 8/6 GiB、进程树RSS 10/14 GiB、磁盘剩余100/50 GiB、R4输出40/60 GiB分别作为预警/红线；启动8k训练前可用RAM应不少于16 GiB。只有非零退出、CUDA OOM、非有限值、数据身份门禁失败、资源红线或用户终止才停止当前阶段。

### 16.6 R4执行结论

**结论：R4-A至R4-F全部`PASS`。增加数据可以消除Hard在1k上的明显劣势，但没有得到Hard或Soft稳定优于Exact的证据。Hard-19已足以作为CH3接口目标，Soft路线停止。**

| 问题 | 结论 | 关键证据 |
|---|---|---|
| 为什么扩大数据 | 旧256条validation上的逐样本胜负随seed改变，无法从小样本稳定解释Hard/Soft排序 | Hard优于Exact的样本比例随seed约为52.3%/44.9%/52.7%；Soft优于Hard约为44.5%/38.7%/45.7% |
| 共享IQ是否合格 | 合格，且test仅生成锁定、未进入DPD或评估 | train/val/test=`8192/2048/2048`，每个split的N=2/N=3严格各半；seed=`20260826` |
| 嵌套清单是否公平 | 合格 | `1k⊂4k⊂8k`；`val_select`与`val_compare`各1024、互不重叠，所有清单N=2/N=3各半 |
| 三表示能否公平训练 | 可以 | Exact/Hard/Soft的train/val均为8192/2048，位置、源数、热力图和索引逐字段相同，所有DPD有限 |
| 是否出现异常相同DPD | 只有可解释的数学等价，不是生成错误 | Exact与Hard全部不同；Hard与Soft仅train样本382/1892和val样本152相同，因为唯一频带支持内Soft仅为统一缩放，随后被DPD功率归一化抵消 |
| 本机资源是否足够 | 足够完成R4规模训练 | 18个训练/评估监控阶段全部PASS；GPU峰值约6.34 GiB，进程树RSS峰值约11.78 GiB，无资源红线 |

| 规模 | Exact RMSE / GOSPA | Hard RMSE / GOSPA | Soft RMSE / GOSPA |
|---:|---:|---:|---:|
| 1k | 121.22 / 25.38 | 176.90 / 29.38 | 131.98 / 25.11 |
| 4k | 112.26 / 21.40 | 113.12 / 20.92 | 98.78 / 20.61 |
| 8k | 88.20 / 18.60 | 93.09 / 19.36 | 95.61 / 19.79 |

1k时Hard显著劣于Exact；到4k后Hard与Exact已无稳定差异。8k时Exact点估最好，Hard的RMSE配对bootstrap区间仍跨0，Soft相对Hard也无稳定优势。场景分层中三种表示的优劣方向会随SNR、带宽和源间距改变，不存在某种表示在所有场景统治占优。

R4的最稳妥解释是：19子带Hard表示的信息上限足以支撑当前D8，早期性能下降主要是小数据与模型适配问题，而不是必须立即提高频率表示精度。后续固定Hard-19-Actual语义，按S2-G5依次建立统一数据契约、CH3预测接口和四轨闭环；Soft不再增加数据或seed。本Gate只有单个训练seed，且未运行test；结论是缩减数据上的接口设计证据，不是论文最终性能。精确路径、哈希、配对区间和资源见`SYS-S2G4-R4-20260826`。

## 17. S2-G5：统一数据与Hard-19-Actual端到端接口

### 17.1 总体目标

S2-G5不把现有CH3和CH4模型直接硬串联，而是先让两章共享同一物理数据契约和频带语义，再逐层隔离频带预测、源数预测和定位误差。最终目标是判断Hard-19-Actual能否成为稳定、可解释的CH3→CH4接口。

### 17.2 候选子阶段

| 子阶段 | 核心任务 | 主要输出 | 进入下一步的最低条件 |
|---|---|---|---|
| `S2-G5-R1` | 定义统一物理分布和Actual带宽标准；实现CH3 Hard-19-Actual标签 | 版本化数据契约、标签函数、自测与小样本审计 | 旧入口不变；标签可复算；每个有效源至少有一个正子带 |
| `S2-G5-R2` | 用1k→4k→8k嵌套共享数据训练CH3 | predicted band、predicted K及冻结8k validation checkpoint | 相对初始化有效学习；规模收益有配对证据；不看test |
| `S2-G5-R3` | 运行oracle/predicted四轨误差隔离 | oracle/oracle、oracle/predicted-K、predicted-band/oracle-K、predicted/predicted四轨报告 | 样本不遗漏；K=0、漏检和多检有明确处理 |
| `S2-G5-R4` | 加入频带/K不确定性与接口扰动训练 | 置信度传递、候选频带/K、扰动增强或拒识策略 | 只针对R3确认的主要误差，不盲目增加模块 |
| `S2-G5-R5` | 通过后扩展到16k并冻结test | 规模学习曲线、最终缩减配置和一次冻结test | 懒加载与资源门通过；配置不再由test选择 |

### 17.3 路线边界

```text
统一物理分布和Actual带宽标准
→ 实现CH3 Hard-19-Actual标签
→ 4k级共享数据训练CH3
→ oracle/predicted四轨误差隔离
→ 频带/K不确定性与接口扰动训练
→ 通过后扩展至16k并冻结test
```

各子阶段必须单独审批。R1只冻结契约和验证标签工程，不训练CH3/CH4；R2以前不生成大规模细DPD；R3以前不把oracle结果写成端到端结果；R4只有在R3能定位误差来源时才进入；R5不自动扩展到师弟原40k规模。

## 18. S2-G5-R1：统一IQ、Actual标签与按K分流契约

### 18.1 执行方案

R1不重新定义带宽，也不训练模型。`BW_actual=Rs(1+1.2α)`直接作为Actual频段宽度，Hard-19标签按Actual频段与每个10 MHz窗口的覆盖率计算，正例阈值固定为0.2。

| 项目 | R1固定方案 |
|---|---|
| 统一IQ入口 | 在`main31_loc_v3.m`增加显式`s2g5r1` profile，不另建物理生成器 |
| 源数 | CH3使用K=0/1/2/3；R1小样本中四类等量 |
| K=2/3 | 与G4-R4保持相同条件分布、波形和空间约束，但生成独立样本；现有Hard-D8无需重训 |
| K=0 | 检测后直接结束，不进入定位 |
| K=1 | 只保留`single_source_locator_pending`接口；调用定位函数时明确报未实现 |
| K=2/3 | 路由到`d8_hard19_actual` |
| 兼容性 | 默认入口和`s2g4r4` profile不改变；新增字段只作为数据契约元数据 |

```text
生成40/16/4条统一IQ
→ 独立复算Hard-19-Actual标签
→ 检查K=0/1/2/3分流
→ 生成19×81×81粗DPD
→ 用CH3原Dataset、Transformer和loss完成消费检查
→ 回归默认smoke入口
```

R1只验证工程契约，不通过小样本评价CH3精度，也不运行D8定位性能、正式训练或test性能评估。

### 18.2 执行结论

**结论：R1通过。统一生成器、Actual标签和分流接口已经闭合，可以进入4k级CH3训练设计。**

| 问题 | 结论 | 支撑证据 |
|---|---|---|
| 四类源数能否由同一入口生成 | 可以 | train/val/test=`40/16/4`，每个split的K=0/1/2/3严格等量 |
| Actual标签是否可靠 | 可靠 | MATLAB标签与Python独立复算逐位一致；所有有效源至少有一个正子带，空槽和K=0无标签 |
| K=1是否被临时算法替代 | 没有 | 路由保留为pending，定位接口明确抛出`NotImplementedError` |
| K=2/3是否需要重训D8 | 当前不需要 | 物理条件和Hard-19-Actual输入契约与G4-R4一致，只扩展源数混合并使用独立样本 |
| CH3能否消费新数据 | 可以 | 60条IQ全部生成粗DPD；原Dataset加载40条train，M=10 Transformer完成一次有限forward/loss |
| 原入口是否仍可运行 | 可以 | 默认`4/2/2` smoke仍为K=2/3各半、主瓣阈值0.3，兼容回归通过 |

R1没有证明现有CH3已经会预测Actual标签，也没有验证端到端定位性能；这些问题分别留给R2和R3。精确路径、哈希和检查事实见`SYS-S2G5-R1-20260827`。

## 19. S2-G5-R2：CH3统一数据嵌套规模训练

### 19.1 要回答的问题

R2先在同一4k训练池上比较1k和4k，判断CH3能否学习Hard-19-Actual与K，以及增加数据是否仍有明确收益。本Gate不确定论文最终规模，不运行test，不评估CH4定位，也不自动进入8k。

### 19.2 数据与公平控制

| 项目 | 固定方案 |
|---|---|
| 统一IQ | MATLAB seed=`20260828`；train/validation/占位test=`4096/2048/4` |
| 源数 | 每个集合K=0/1/2/3严格等量；占位test不生成粗DPD、不训练、不评估 |
| 标签 | `BW_actual=Rs(1+1.2α)`；Hard-19-Actual阈值0.2 |
| 嵌套训练 | `train_1k⊂train_4k`；每类分别为256/1024条 |
| validation | `val_select/val_compare=1024/1024`，互不重叠且每类各256条 |
| 模型 | Transformer、M=10、FP32、batch 64；保持原Focal BCE和ignore逻辑 |
| 公平性 | 两臂seed 42、初始参数身份、模型、loss、阈值和调度一致；4k不从1k续训 |

1k和4k使用相同epoch预算，表示每条样本经历相同的数据遍历次数；4k每轮optimizer step更多。结论因此是“标准训练预算下的数据规模效果”，不是等计算量的纯因果比较。

### 19.3 执行流程

```text
生成4096/2048统一IQ
→ 独立复算标签并生成粗DPD
→ 冻结嵌套manifest和两个validation子集
→ batch 64容量检查
→ 从头训练1k
→ 从相同初始化重新训练4k
→ 冻结best并在val_compare配对评估
→ 按预设规则判断8k
```

所有输出写入唯一目录；数据和checkpoint均保存SHA256。10秒监控、摘要输出，不设置墙钟硬超时。参数拼写、编码或报告字段等高置信工程小错可以最小修复，但失败证据必须保留，且不得改变科研配置。

### 19.4 训练与评价口径

| 项目 | 设置 |
|---|---|
| 优化 | AdamW，lr=`1e-3`，weight decay=`5e-4`，梯度裁剪1.0 |
| 调度 | 最大150 epoch，warmup 5，patience 25，最低lr=`1e-6` |
| checkpoint | 只由`val_select`最低loss选择；固定阈值0.5 |
| 计数指标 | balanced accuracy、各K准确率、混淆、MAE、漏计/多计、K=0误报 |
| 频带指标 | active-band macro-F1、逐源IoU、完全匹配率、空槽激活率 |
| 不确定性 | 在同一`val_compare`上按K分层做2000次配对bootstrap |

单臂沿用S2-G1有效学习门：loss相对初始化下降至少20%，balanced count accuracy和band macro-F1均相对初始化提高至少0.20且最终不低于0.50。早停、best不在最后15轮或尾段改善不足1%均可判为趋势关闭。

4k相对1k的两个主指标均提高且至少一个提高2个百分点，或一个配对区间完全大于0且另一项不劣1个百分点，同时无单类下降超过5个百分点、K=0误报增加不超过2个百分点时，判为`ENTER_8K`。8k仍需另行审批。

### 19.5 1k/4k阶段执行结论

**结论：R2工程`PASS`，预注册规模判定为`ENTER_8K`。CH3已经能学习统一Actual接口，且4k尚未进入数据规模平台。**

| 问题 | 结论 | 关键证据 |
|---|---|---|
| 数据与标签是否合格 | 合格 | train/validation=`4096/2048`；四类K等量；四个粗DPD子集shape、有限性、标签和manifest索引全部PASS |
| 两项训练是否充分 | 充分 | 1k/4k分别在145/127轮早停，best epoch=`120/102`；学习门和收敛门均通过 |
| 计数是否随规模改善 | 明确改善 | balanced accuracy=`0.7754→0.8320`，差值`+0.0566`，95%配对区间=`[0.0332,0.0801]` |
| 频带是否随规模改善 | 明确改善 | band macro-F1=`0.8504→0.8752`，差值`+0.0248`，区间=`[0.0150,0.0345]` |
| 改善来自哪里 | 主要来自K=1/2 | K=1=`0.7344→0.8125`，K=2=`0.5430→0.7109`；K=3下降1.56个百分点，未触发5个百分点停止线 |
| 频带结构质量 | 同步改善 | 逐源平均IoU=`0.7558→0.7983`，完全匹配率=`0.3548→0.4551` |
| K=0是否恶化 | 轻微但未越线 | 误报率=`0.0195→0.0234`，增加0.39个百分点 |
| 资源是否可承受 | 可承受 | 1k/4k训练约14.8/40.2分钟；4k GPU峰值约7.81 GiB、RSS约4.05 GiB，全部有效监控阶段0 warning/0 red flag |

两份best checkpoint独立重载的主指标与最终分析完全一致，训练前后数据SHA未变化。一次独立评估曾因外层把`eval`误写为`evaluate`而立即失败，未写科研结果；失败目录保留，修正命令后在新监控目录通过。精确路径、哈希、资源和完整指标见`CH3-S2G5-R2-20260827`。

1k/4k比较仍只有单个training seed和validation证据，因此`ENTER_8K`只授权第19.6节的条件扩展，不代表8k自动成为论文最终规模，也不授权R3或test。

### 19.6 8k条件扩展执行方案

8k扩展只回答“统一数据从4k增加到8k后，CH3的计数和Hard-19-Actual频带预测是否继续获得值得付出计算成本的改善”。它不确定论文最终数据规模，不运行test、R3或D8，也不改变模型、loss、阈值和优化器。

不能把原MATLAB样本数直接改成8192并假定前4096条仍是R2旧数据，因为`randperm(N_trials)`会改变整条随机序列。实际采用严格增量方式：

```text
冻结R2旧4k和两个validation
→ 用新seed生成额外4k
→ 独立复算Actual标签
→ 只为新增4k计算粗DPD
→ 分块合并旧4k与新增4k
→ 审计旧4k是8k严格前缀
→ 从相同初始化训练8k
→ 在原val_compare上配对比较4k与8k
```

| 项目 | 固定设置 |
|---|---|
| 新增数据 | MATLAB seed=`20260830`；train=4096且K=0/1/2/3各1024条；val/test只生成4条占位数据并不再消费 |
| 合并数据 | `train_8k=旧4k+新增4k`，每个K各2048条；`sample_idx_all`重排为0–8191 |
| validation | 原`val_select/val_compare=1024/1024`，文件和SHA保持不变 |
| 标签 | `BW_actual=Rs(1+1.2α)`；Hard-19-Actual阈值0.2 |
| 模型 | Transformer、M=10、FP32、batch 64；seed 42及初始化SHA与R2一致 |
| 训练 | AdamW、lr=`1e-3`、weight decay=`5e-4`、最大150 epoch、warmup 5、patience 25 |
| 评价 | balanced count accuracy、band macro-F1、各K准确率、K=0误报、IoU、完全匹配率；按K分层2000次配对bootstrap |
| 输出 | 在既有`outputs/s2g5r2_ch3/20260827_191207`中新增`8k`相关目录和结果，不新建R2根目录 |

若两项主指标均不下降、至少一项提高1个百分点且其配对区间下限大于0，同时另一项区间下限不低于−1个百分点，并且没有单类下降超过5个百分点或K=0误报增加超过2个百分点，则冻结8k供R3使用。若两项均无改善或触发安全退化线，保留4k；其他混合情形记为规模证据不确定，工程上按简约原则仍使用4k。任何判定都不自动增加seed、运行16k或进入R3。

监控沿用10秒采样和摘要输出，不设置普通墙钟硬超时。新增结果全部写入新文件或新子目录，旧4k、validation、checkpoint和R2分析报告保持只读。最终执行后，在本章最后新增统一的1k/4k/8k结论小节；精确指标仍以`实验结果记录.md`为唯一事实源。

### 19.7 1k/4k/8k统一执行结论

**结论：S2-G5-R2整体工程`PASS`，预注册规模判定为`FREEZE_8K_FOR_R3`。CH3在统一Hard-19-Actual数据上随1k→4k→8k连续改善，8k冻结为下一阶段接口checkpoint。**

| 指标（固定`val_compare`） | 1k | 4k | 8k |
|---|---:|---:|---:|
| Balanced count accuracy | 0.7754 | 0.8320 | **0.8867** |
| Band macro-F1 | 0.8504 | 0.8752 | **0.9058** |
| 逐源平均IoU | 0.7558 | 0.7983 | **0.8387** |
| 逐源完全匹配率 | 0.3548 | 0.4551 | **0.5391** |
| Count MAE | 0.2305 | 0.1729 | **0.1182** |
| K=0误报率 | 0.0195 | 0.0234 | **0.0117** |

4k→8k的balanced count accuracy提高5.47个百分点，95%配对区间为`[3.42,7.32]`个百分点；band macro-F1提高3.06个百分点，区间为`[2.25,3.91]`个百分点。K=0/1/2/3准确率均提高，其中K=2提高10.94个百分点；没有触发单类退化或K=0误报停止线。4k在同一入口重新评估时精确复现R2指标，8k checkpoint独立重载在`1e-12`容差内一致。

8k由R2旧4k和新seed生成的新增4k组成，每个K各2048条。旧4k的DPD、标签、位置、SNR、频带和源数等9个逐样本字段与8k前4096条逐块完全一致，两个validation文件及原R2数据SHA未变化。8k在130轮早停，best epoch为105，训练约69.4分钟；监控GPU峰值约7.84 GiB、进程树RSS峰值约11.08 GiB，只有一次超过10 GiB提示线的RAM告警，没有红线、OOM或数值异常。

本结果仍是单training seed、固定validation上的缩减规模证据，不是test或论文正式性能。它证明8k在当前配置下比4k更适合作为R3工程接口checkpoint，但不能证明8k是最终最优规模，也不能替代后续正式多seed/test设计。精确配置、哈希、资源和运行目录见`CH3-S2G5-R2-20260827`。

## 20. S2-G5-R3：Oracle/Predicted四轨级联误差隔离

### 20.1 要回答的问题

R3使用冻结CH3和Hard-D8，不训练模型，只回答频带预测、源数预测及二者交互分别造成多少定位损失，并判断现有级联能否直接使用。

### 20.2 固定方案

| 项目 | 固定内容 |
|---|---|
| CH3 | R2冻结8k Transformer、M=10、阈值0.5 |
| CH4 | R4冻结8k Hard-19-Actual D8、FP32/no-AMP |
| 数据 | R2固定`val_compare`；全1024条做分流，真值K=2/3的512条做四轨定位 |
| 频带 | `BW_actual=Rs(1+1.2α)`，Hard-19覆盖阈值0.2 |
| K错误 | K=0输出空集合；K=1用D8 Top-1作反事实诊断；不裁剪或oracle回退 |
| 评价 | GOSPA及分量、100 m召回为主；辅以10/30/50 m、匹配误差和K=2/3分层 |
| 禁止范围 | 不运行test、不训练、不调阈值、不实现真值K=1定位、不自动进入R4或16k |

### 20.3 四轨定义

| 轨道 | 细DPD频带 | 解码K | 作用 |
|---|---|---|---|
| `OB-OK` | Oracle Hard-19-Actual | Oracle K | 当前接口表示下的定位基准 |
| `OB-PK` | Oracle Hard-19-Actual | Predicted K | 隔离计数误差 |
| `PB-OK` | Predicted Hard-19 | Oracle K | 隔离频带误差 |
| `PB-PK` | Predicted Hard-19 | Predicted K | 当前完整级联诊断 |

只生成Oracle-Band和Predicted-Band两套细DPD。每套只运行一次D8前向，再用两种K解码。预测频带为空时输出空位置集合，禁止用真值补齐。

### 20.4 执行与判定

```text
冻结输入并核对SHA
→ 4样本pilot
→ 全1024条CH3推理并复现R2
→ 生成两套512条细DPD
→ 四轨解码与独立重载
→ 按K分层配对bootstrap
→ 形成R3归因和下一步建议
```

配对bootstrap固定2000次、seed=`20260831`。若`PB-PK`相对`OB-OK`的GOSPA增幅区间上限不超过10%，且100 m召回差区间下限不低于−5个百分点，判为`DIRECT_CASCADE_SUPPORTED`。否则比较`PB-OK`和`OB-PK`：稳定更差的一侧成为R4优先路径；差异区间跨0则判为联合或不确定问题。交互项区间完全大于0时另记`INTERACTION_AMPLIFICATION`。

输出使用唯一目录`outputs/s2g5r3_cascade/<timestamp>`。监控固定10秒采样、摘要输出，不设置普通墙钟硬超时；启动可用RAM至少10 GiB，可用RAM 8/6 GiB、RSS 10/14 GiB、GPU 13/15 GiB、磁盘50/25 GiB和输出2/4 GiB分别为预警/红线。

### 20.5 执行结论

**结论：R3工程`PASS`，但完整级联未通过GOSPA等效门；下一步应优先解决源数判决，而不是继续提高Hard-19频带精度。**

| 轨道 | mean GOSPA | 100 m召回 | 直观含义 |
|---|---:|---:|---|
| `OB-OK` | 20.12 m | 97.58% | Hard接口与D8基准 |
| `OB-PK` | 25.75 m | 95.00% | 只加入K错误后明显退化 |
| `PB-OK` | 19.62 m | 97.58% | 预测频带没有稳定损失 |
| `PB-PK` | 25.18 m | 95.16% | 当前完整级联诊断 |

频带路径使GOSPA变化`−0.51 m`，95%区间为`[−1.64,0.57] m`，不能认为有稳定损失；K路径使GOSPA增加`5.63 m`，区间为`[4.06,7.37] m`。完整级联相对基准增加`5.06 m`，相对增幅为25.14%，其区间为`[15.55%,36.82%]`，因此没有通过10%等效门。100 m召回只下降2.42个百分点并通过5个百分点门，说明主要代价来自GOSPA中的漏检和误报基数惩罚，而不是已匹配位置普遍变差。

交互项为`−0.06 m`，区间`[−0.79,0.67] m`，没有误差放大证据。K=2/3的512条样本中，CH3有442条计数正确；预测K分布为0/1/2/3=`2/18/232/260`。因此R4应优先研究K置信度、候选K或稳健解码，不应先继续Soft、Hard-39或频带网格细化。

四轨样本身份完全一致，CH3逐样本精确复现R2，D8独立重载在`1e-12`内一致；Oracle/Predicted细DPD各512条，Predicted有2条空频带且按预案保留。未训练模型、未调阈值、未读取test，K=1定位仍未实现。精确路径、哈希、资源和完整指标见`SYS-S2G5-R3-20260828`。

## 21. S2-G5-R4：16k规模扩展与K瓶颈复核

### 21.1 要回答的问题

R3只证明当前8k CH3的级联损失主要来自K错误，不能证明必须修改K算法。R4先保持CH3、D8和接口算法不变，将CH3训练规模扩展至16k，判断K误差能否随数据自然下降；同时检查剩余错误是否具有低置信、Top-2可恢复特征，为后续是否研究候选K或稳健解码提供依据。

### 21.2 固定方案

| 项目 | 固定内容 |
|---|---|
| 数据 | `train_16k=冻结旧8k+新增8k`；K=0/1/2/3各4096条；旧8k必须为严格前缀 |
| 物理与标签 | 沿用统一分布、`BW_actual=Rs(1+1.2α)`和Hard-19-Actual阈值0.2 |
| validation | 原`val_select/val_compare`保持只读；前者选checkpoint，后者只做最终比较 |
| CH3 | Transformer、M=10、FP32、batch 64、seed 42，从相同初始化重新训练 |
| 训练 | AdamW、lr=`1e-3`、weight decay=`5e-4`、150 epoch、warmup 5、patience 25 |
| CH4 | 冻结R4 Hard-D8，不训练或微调；复用R3 Oracle细DPD |
| 禁止范围 | 不运行test、不调0.5阈值、不修改模型/loss、不自动执行32k或K改进 |

### 21.3 执行流程

```text
冻结8k输入与SHA
→ 新增8k统一IQ和Actual标签
→ 生成新增8k粗DPD
→ 建立严格嵌套16k训练集
→ 容量pilot
→ 从头训练16k CH3
→ 8k/16k配对比较与置信度诊断
→ 更新四轨级联
→ 形成R4路线判定
```

本轮按用户要求执行到正式16k长训练前暂停。长训练由用户决定在PyCharm点击运行，或另行授权Codex代为执行。

### 21.4 K置信度与Top-2诊断

当前CH3没有独立K分类头。对槽位`s`定义`q_s=max_b sigmoid(logit_s,b)`，现有计数为`K_hat=sum(q_s>0.5)`。R4使用`min_s |q_s-0.5|`描述最近翻转边界，并将最接近0.5的槽位翻转后所得相邻K作为第二候选。只报告错误置信度分布、risk–coverage、Top-2真值覆盖率和oracle候选上限；不把该分数称为校准概率，不调温度或阈值，不把oracle选择写成可部署结果。K>3按接口越界记录，禁止裁剪或真值回退。

### 21.5 评价与预注册判定

规模比较沿用balanced count accuracy、Band macro-F1、各K准确率、Count MAE、K=0误报、IoU和完全匹配率，并做按K分层的2000次配对bootstrap。规模收益门要求：两项主指标点估计均不下降，至少一项提高1个百分点且其区间下限大于0，另一项区间下限不低于−1个百分点；任一K下降不超过5个百分点，K=0误报增加不超过2个百分点。

16k完成后复用R3四轨：`OB-OK`、`OB-PK16`、`PB16-OK`和`PB16-PK16`。完整级联仍使用GOSPA增幅区间上限不超过10%、100 m召回差区间下限不低于−5个百分点的等效门。

| 判定 | 条件 | 后续 |
|---|---|---|
| `DIRECT_CASCADE_SUPPORTED_16K` | 完整级联通过两项等效门 | 不添加K机制，冻结16k工程基线 |
| `RECOMMEND_32K_SCALE` | 规模门通过，且K路径损失相对8k稳定下降、点估计至少降低20% | 单独审批32k，不改模型 |
| `ENTER_K_CANDIDATE_GATE` | 级联仍失败且规模收益弱或K损失持续；Top-2错误覆盖率≥80%，候选上限恢复≥50%的K损失 | 后续研究候选K、置信度和稳健解码 |
| `ENTER_CH3_MODEL_DATA_DIAGNOSIS` | Top-2覆盖低或高置信错误占主导 | 优先检查困难数据或CH3训练/结构 |
| `INCONCLUSIVE_NO_AUTO_ACTION` | 不满足上述清晰条件 | 停止自动推进并重新审批 |

### 21.6 资源、输出与停止边界

输出使用唯一目录`outputs/s2g5r4_ch3_scale/<timestamp>`，旧R2/R3证据只读。监控固定10秒采样、摘要输出，不设置普通墙钟硬超时。启动要求可用RAM至少10 GiB、磁盘至少60 GiB；可用RAM 8/6 GiB、RSS 16/20 GiB、GPU 13/15 GiB、磁盘40/25 GiB和R4根目录25/40 GiB分别作为预警/红线。

依据8k实测，新增8k IQ、粗DPD和构建预计约35–45分钟，16k训练约2–2.5小时。容量pilot必须先完成Dataset实例化、一次forward/loss/backward和validation；若内存红线失败，不用缩小batch掩盖常驻数据问题，也不自动修改为懒加载。结果完成后再在本节新增简明结论，精确配置、资源、路径和指标仍以`实验结果记录.md`为唯一事实源。

### 21.7 长训练前执行进度

**结论：新增数据和16k构建均通过，但容量门未通过；正式训练不得直接启动。**

| 检查 | 结果 | 含义 |
|---|---|---|
| 默认入口兼容 | `PASS` | 原`[4,2,2]` smoke和seed未变；R4错误规模会被拒绝 |
| 新增8k IQ/标签 | `PASS` | K=0/1/2/3各2048条，统一物理与Actual标签独立复算通过 |
| 新增8k粗DPD | `PASS` | 8192条全部有限，无监控异常 |
| 严格嵌套16k | `PASS` | 每个K各4096条，旧8k的9个科研字段逐块保持不变 |
| batch 64计算链 | 计算本身`PASS` | forward、loss、backward和validation均可完成 |
| 系统RAM门 | **未通过** | 最低可用RAM仅约0.40 GiB，低于6 GiB红线；进程树RSS峰值约15.42 GiB |

当前训练入口被设计为读取容量监控状态；在现有红线证据下会拒绝启动。这里的失败来自16k数据被原Dataset一次性载入内存，不是batch、GPU、模型或数值问题。下一步应先审批“关闭足够多后台软件后重测容量”或“增加R4专用、逐样本等价审计的懒加载器”；不能仅在PyCharm或Codex之间选择运行主体后直接训练。精确事实见`CH3-S2G5-R4-PRETRAIN-20260828`。

### 21.8 R4专用懒加载最小改造

**目标：只改变频谱的存储与读取方式，不改变数据、预处理、增强、模型、loss和训练配置。** 16k频谱先按原版`transpose → log(x+1) → 单样本z-score`顺序生成float32 `.npy`缓存，训练时通过只读NumPy memmap按样本读取；体积较小的标签仍载入RAM。原Dataset和默认训练入口保持可用，懒加载只由R4专用入口启用。

| 门禁 | 做法 | 通过要求 |
|---|---|---|
| 缓存身份 | 记录源MAT与缓存SHA、shape、dtype和预处理版本 | 输入变化时拒绝复用 |
| 频谱抽样 | 固定512条，覆盖K=0/1/2/3、旧/新增8k、首尾及分块边界 | 优先逐元素相同；否则最大误差≤`1e-7`且均值误差≤`1e-9` |
| Dataset/增强 | 固定seed比较未增强样本、shuffle和前5个增强batch | 输入与标签一致 |
| 短程训练 | 同一1k数据，Eager/Lazy各从头训练3 epoch | 训练历史和最终模型/优化器/调度器一致，浮点误差不得超过`1e-7` |
| 16k容量 | batch 64执行forward、loss、backward和optimizer step | 最低可用RAM≥8 GiB、RSS<16 GiB、GPU<13 GiB |
| 16k吞吐 | 完整训练遍历1 epoch，不保存正式checkpoint | 无数值/资源错误；预计超过4小时只作速度提醒 |

```text
静态与原入口回归
→ 生成16k及1k验证缓存
→ 512条频谱抽样核对
→ Dataset与增强等价检查
→ 1k三轮Eager/Lazy配对训练
→ 16k容量和单epoch吞吐验证
→ 停止并由用户选择正式训练运行主体
```

所有新增证据继续写入当前R4根目录的`lazy_cache/`、`audit/lazy_*.json`和`monitor/lazy_*`，不得覆盖第21.7节保留的eager容量失败证据。只有状态为`LAZY_READY_FOR_TRAINING`时，PyCharm固定入口才允许启动正式16k训练；本轮执行到该门禁即停止，不运行正式训练或test。

### 21.9 懒加载执行结论

**结论：懒加载在数据、随机增强和短程训练上与原eager方式严格一致，并通过16k资源与吞吐门；正式训练仍未启动。**

| 证据 | 结果 | 说明 |
|---|---|---|
| 512条分层频谱 | 逐元素零误差 | 覆盖四类K、旧/新增8k及首尾和拼接边界 |
| Dataset与增强 | `PASS` | 固定seed下未增强样本及前5个shuffle+增强batch摘要完全相同 |
| 1k三轮配对训练 | 严格一致 | history、模型、优化器和调度器最大误差均为0 |
| 16k容量 | `PASS` | batch 64计算链正常，无数值或资源红线 |
| 16k单epoch | 约38.1秒 | 完整遍历256个batch；150轮纯训练粗估约95分钟，不含validation和保存 |
| 最终门禁 | `LAZY_READY_FOR_TRAINING` | 无慢速提醒；PyCharm固定入口已解锁 |

完整epoch给出的保守资源包络为：系统最低可用RAM约15.14 GiB、进程树RSS峰值约7.89 GiB、GPU总占用峰值约7.33 GiB，均处于预设安全边界。缓存只改变频谱存储与读取位置，未修改16k数据、标签、模型、loss、阈值、seed或150 epoch正式配置。精确路径、SHA和监控事实见`CH3-S2G5-R4-LAZY-20260828`。下一步只需由用户选择在PyCharm点击固定入口，或授权Codex执行正式16k训练。
