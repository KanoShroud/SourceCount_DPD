# 实验结果记录

> 本文件记录可追溯的正式实验事实、重要结果审查和具有独立研究意义的诊断结论。Gate 3A 与 Gate 3B 已录入为工程、训练与可重复性证据，不作为论文正式配置的性能结果。

## 记录原则

- 只记录实际运行实验、读取原始结果文件或明确来自用户反馈的事实，不编造指标、路径、权重或结论。
- 每条记录应标注运行类型、入口脚本、数据与结果路径、关键配置、指标、证据来源、可信度和复核需求。
- 用户口述结果标注“用户反馈/待复核”。
- 正式训练、完整评估、重要只读诊断、仅重绘或导出以及冒烟测试必须明确区分。
- 仅验证运行链路、结果字段或工程不变量的冒烟测试默认写入 `修改记录.md`，不作为性能证据。
- 单独执行 Python 语法编译、Ruff、MATLAB Code Analyzer 或代码结构检查，不作为实验结果。
- 声称结果可用于论文前，至少应能追溯实验设置、随机种子、数据、代码版本或指纹、原始输出和图表生成方式。

## 当前结果状态

| 字段 | 内容 |
|---|---|
| 已核验正式实验 | 暂无论文正式配置实验；已完成一组第四章缩减数据上的完整 60 epoch 训练。 |
| 已核验重要验证 | 第四章 Gate 3A 中等规模数据与短训练可重复性通过；Gate 3B 两次 D8 no-AMP 60 epoch 训练严格可重复、确认有效学习，冻结 checkpoint 的 validation/test oracle-K 独立重复评估通过。 |
| 已核验结果文件 | `outputs/gate3_ch4/20260821_152344/gate3a_gate_report.json`、`outputs/gate3b_ch4/20260821_173526/gate3b_gate_report.json`、`frozen_checkpoint.json`、`validation_comparison.json`、`test_comparison.json`。 |
| 用户反馈结果 | 暂无。 |
| 当前边界 | Gate 3B 结果来自 1024/256/256 缩减数据，且评估使用真实 `n_src` 的 oracle-K；best epoch=57 表明达到60 epoch上限附近仍可能改善。不能将其写成充分收敛、端到端 predicted-K 或论文指标复现。 |
| 后续动作 | 另行审批第三章小规模收敛、oracle-K/predicted-K 双轨评估和第四章延长 epoch 验证；正式论文配置、图表溯源及创新性核验仍为独立阶段。 |

## 2026-08-21 第四章 Gate 3A 中等规模可重复性验证

| 字段 | 内容 |
|---|---|
| 类型 | 重要可重复性验证与资源诊断；非正式性能实验。 |
| 入口 | `main31_loc_v3.m`、`gen_ch4_loc_data.py`、`train_yolo.py`、`gate3_ch4.py`、`gate3_stage_runner.py`。 |
| 数据与配置 | IQ/定位 train/val/test=`1024/256/256`；随机种子 MATLAB=`20260822`、Python=`42`；D8 `dualhead`、`dice_weight=0`、no-AMP、batch/val batch=`8/8`；pilot 训练 5 epoch，重复 2 次。 |
| 结果路径 | `outputs/gate3_ch4/20260821_152344`；总报告 `gate3a_gate_report.json`；完整定位数据与审计在 `05_full_loc_after_encoding_fix/`。 |
| 代码版本或指纹 | 当前未提交工作树；结构化 run config、完整 checkpoint、命令、日志和资源 CSV 均在运行目录。未建立 Git commit 指纹。 |
| 数据完整性 | N=2/3 各半；1536 个 IQ 样本哈希全部唯一且跨 split 无泄漏；定位任务共 1536 条、12 个分片、3,951,967,293 bytes；pilot/full 前缀全部张量和索引字段最大绝对差为 0。 |
| 可重复性结果 | 两次 5 epoch 运行的逐 epoch 指标、模型、优化器、调度器、随机状态及最终验证完全一致；三类 mismatch 数均为 0。 |
| 资源结果 | 完整 DPD train/val/test 总墙钟 839.130 s；纳入门禁的最低系统可用内存 13,033,127,936 bytes、峰值进程树 RSS 2,665,586,688 bytes、峰值系统总 GPU 已用显存 6957 MiB；无 warning/red flag。 |
| 预算结果 | pilot 稳态 epoch 中位数最大值 1.45815 s；按样本量 8 倍和 60 epoch 外推 699.912 s，即 11.665 min。该结果未实跑 60 epoch。 |
| 异常与证据保留 | 首次确定性比较 `CRASHED`，首次完整 DPD train 因日志编码/管道问题在 1500 s `TIMEOUT`；均经用户审批最小修复后在全新目录通过，失败目录未删除或覆盖。 |
| 结论 | Gate 3A=`PASS`：中等规模数据链、D8 pilot 完全确定性和本机资源可行性成立，可进入 Gate 3B 方案审批。 |
| 适用边界 | 不证明 60 epoch 实际耗时、收敛、泛化定位误差、论文图表、论文指标或端到端 predicted K；随机初始化/短训练 RMSE 不作性能解释。 |
| 复核状态 | 总门禁回读通过；25 个纳入判定的监控阶段均为 PASS，无 warning/red flag；formal 第四章目录不存在；交付前复核无遗留 Python/MATLAB 实验进程。 |

## 2026-08-21 第四章 Gate 3B 小规模完整训练与严格重复性验证

| 字段 | 内容 |
|---|---|
| 类型 | 缩减数据上的完整训练、严格可重复性验证和独立 held-out 评估；非论文正式配置性能实验。 |
| 入口 | `train_yolo.py`、`eval_ch4_checkpoint.py`、`gate3_stage_runner.py`、`gate3b_ch4.py`。 |
| 数据与配置 | 复用 Gate 3A 已审计定位数据 train/val/test=`1024/256/256`、8/2/2 分片；D8 `dualhead_std`、FP32/no-AMP、batch/val batch=`8/8`、AdamW、初始学习率 `1e-3`、CosineAnnealingLR、weight decay `5e-3`、dropout `0.4`、60 epoch、patience 60、seed 42、deterministic。 |
| 结果路径 | `outputs/gate3b_ch4/20260821_173526`；总报告 `gate3b_gate_report.json`。Gate 3A 历史根 `outputs/gate3_ch4/20260821_152344` 保留原名和原位，仅被只读复用。 |
| 代码版本或指纹 | 当前未提交工作树；总报告记录 `train_yolo.py`、`eval_ch4_checkpoint.py`、`gate3_stage_runner.py` 和 `gate3b_ch4.py` 的路径、大小和 SHA256。 |
| 数据完整性 | 12 个定位分片和 3 个 index 共 3,951,967,293 bytes；训练前后15个文件的大小和 SHA256 完全一致。 |
| 训练结果 | A/B 均完成60 epoch且未提前停止；墙钟分别 681.222/683.018 s。best epoch 均为57，best validation oracle-K RMSE均为150.791921 m，最终学习率均为 `1e-6`。 |
| 学习判定 | 未训练模型 validation loss/RMSE 为 7.011946/1075.637084 m；冻结 best 为1.016160/150.791921 m，两项均严格改善，A/B均为 `LEARNING_CONFIRMED`。 |
| 严格重复性 | 排除耗时和资源字段后，60 epoch历史、初始/最终validation、summary关键字段、best/last checkpoint中的模型、optimizer、scheduler、scaler及RNG状态全部逐值一致，所有 mismatch 列表为空。 |
| 冻结 checkpoint | 只依据 validation 从训练A选取 epoch 57；`best_yolo_dualhead_std.pth` SHA256=`8cf7f9541b3269539b7b16580d7a13dd2b9a9770afa1b0032b3ec491844f0153`；test 未参与选择。 |
| validation oracle-K | 两次独立进程完全一致并与训练 best 完全一致：RMSE 150.791921 m，mean 35.004812 m，median 4.695417 m，within 10/30/50 m=`0.6859375/0.878125/0.93125`。 |
| test oracle-K | 两次独立进程完全一致：loss 0.999282，RMSE 110.565991 m，mean 26.120635 m，median 4.740854 m，within 10/30/50 m=`0.7171875/0.8953125/0.93125`。N=2/N=3 RMSE为81.613676/126.230752 m。 |
| 资源结果 | 14个阶段监控报告均PASS且无warning/red flag；最低系统可用RAM 14,255,198,208 bytes，峰值进程树RSS 3,764,129,792 bytes，峰值系统总GPU已用显存7388 MiB；报告前输出615,897,001 bytes。 |
| 总门禁 | `PASS_WITH_MORE_EPOCHS_RECOMMENDED`；工程、重复性、有效学习、独立评估、数据完整性和隔离均通过，但best epoch=57落在最后5个epoch，趋势为 `STILL_IMPROVING_AT_LIMIT`。 |
| 适用边界 | 全部定位指标使用真实源数决定Top-K，是oracle-K；数据规模和配置不等于论文正式实验，未验证predicted-K、论文图表、论文指标、全量泛化或当前创新性。原始test JSON中的 `metrics.val_loss` 表示当前test split loss，总报告已映射为 `test_loss`。 |
| 复核状态 | 总报告回读、代码语法/Ruff、数据前后SHA、formal目录隔离和残留实验进程检查均通过；未发生崩溃、超时、自动重试或失败目录复用。 |

## 实验记录模板

### YYYY-MM-DD 实验或结果审查名称

| 字段 | 内容 |
|---|---|
| 类型 | 正式训练 / 完整评估 / 重要诊断 / 仅重绘或导出 / 用户反馈待复核 |
| 入口 | 待填写 |
| 数据与配置 | 待填写 |
| 结果路径 | 待填写 |
| 代码版本或指纹 | 待填写 |
| 关键指标 | 待填写 |
| 完整性与公平性 | 待填写 |
| 证据来源 | 日志、模型权重、MAT、CSV、JSON、图像或用户反馈等 |
| 结论 | 待填写 |
| 适用边界 | 待填写 |
| 复核状态 | 待填写 |

## 历史结果索引

- 第四章：2026-08-21 Gate 3A 中等规模数据、pilot 完全确定性与资源预算，`PASS`，非论文性能证据。
- 第四章：2026-08-21 Gate 3B 两次60 epoch严格可重复训练与独立oracle-K评估，`PASS_WITH_MORE_EPOCHS_RECOMMENDED`，缩减数据证据。
