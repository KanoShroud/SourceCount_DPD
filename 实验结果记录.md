# 实验结果记录

## E2E-G5-R5-20260922：保守身份纠错未获得检查集净收益

- 用户批准Codex执行；状态COMPLETED，最终审计PASS。证据根：`outputs_e2e/unified/e2e_g5_r5/20260922_152822/`；`contract.json`登记协议与身份，`calibration.json`保存阈值，`samples.jsonl`保存逐样本排列和指标，`comparison_report.json`与`final_audit.json`保存比较与审计，中文摘要为`运行结论.md`。
- 复用R4 `20260922_131120`缓存及两seed（20260921/20260922）显式匹配器epoch20，保留gamma符号和尺度；A为原绑定，B为评分器保守纠错，C为粗DPD局部频谱中心化余弦纠错。K、位置、频带数值固定，不训练、不重选峰、不读取val_compare/test。
- 512条val_select按真实K分层，用split seed20260922预先分成256校准与256检查，各含每种K的64条；两个模型seed使用同一划分。每方法共享一个阈值，按校准净联合命中选择，平局依次偏向少修改、更大阈值。联合命中沿用距离≤100 m且band F1≥0.8。方法与退化回退详见实现文档17.16。
- B选定阈值`0.6398383577664694`，校准两seed合计6次修改、净增4个联合命中；C选择正无穷阈值（JSON为null），即不修改。两方法都判为`NO_CHECK_GAIN`。

| 检查部分，每seed 256场景/384真源 | seed20260921 | seed20260922 | 两seed算术平均 |
|---|---:|---:|---:|
| A原绑定联合Recall | 75.2604% | 76.5625% | 75.9115% |
| B校准后联合Recall | 75.0000% | 76.5625% | 75.7813% |
| B相对A变化 | −0.2604 pp | 0 pp | −0.1302 pp |
| B无阈值联合Recall | 71.3542% | 74.7396% | 73.0469% |
| C校准后联合Recall | 75.2604% | 76.5625% | 75.9115% |
| C无阈值联合Recall | 64.3229% | 65.8854% | 65.1042% |
| 所有方法GOSPA（m） | 21.8336 | 21.3133 | 21.5735 |
| 所有方法定位RMSE（m） | 134.5144 | 106.5193 | 120.5168 |

- B检查部分分别修改5/2个场景：seed21为1次有益、2次有害、2次联合命中数不变，净少1个命中；seed22为2次命中数不变。计数正确子集分别234/233条，所选修改均在该子集内，结论相同。C校准后无修改。未触发退化向量或最优排列并列回退，C不修改由校准阈值决定。RMSE平均是两seed RMSE的算术平均，不是合并误差重算；本表检查子集不可与R4完整512条指标直接比较。
- 正式墙钟37.4319 s（不含实现），系统RAM采样峰值43.8%；32场景短测2.0128 s，保守总估时168.3075 s，未超3600 s预算。没有读取异常。粗DPD只读G5本地快照`input_snapshot/files_01_val_select.mat`，大小411133526字节，SHA256为`d4cb6af9e3f99c2162c306105b29f947e48ac85c38075acc8226b6403046d6ad`；分块校验消费及阶段前后身份复核通过。
- 独立回读复算1024条场景-seed记录的全排列、阈值最优性、联合Recall及修改收益；256/256划分互斥且跨seed一致，8份输出、66份输入和57份代码登记哈希匹配。B无阈值完整512条联合Recall为70.9635%/72.7865%，复现R4显式匹配epoch20。7项合成测试、语法编译和Ruff通过。
- 结论：B的校准收益未推广至检查部分，C当前直接读法未提供可用纠错收益；固定位置保护定位有效。停止R2–R5当前事后评分/阈值修补，G6另行制定审批。本轮不能推断粗DPD无身份信息、所有事后方法无效或架构存在不可突破的瓶颈，也未重新检验E2E相对SG收益。数据仍来自已使用的val_select，不是新独立测试集。

## E2E-G5-R4-20260922：固定位置保护定位，身份匹配训练有效但验证无净收益

- 用户批准Codex完整执行。证据根：`outputs_e2e/unified/e2e_g5_r4/20260922_131120/`；报告`report.json`、`final_audit.json`、`运行结论.md`；输入/代码合同`contract.json`；各seed/轨道的`training/`保存history与每2轮checkpoint；`cache/`保存固定位置、全排列评价表及校验索引。复用R3缓存，不重跑基础模型，不写原outputs，不读val_compare/test。
- A为R1联合解码原始绑定；B为拼接MLP，C为64维归一化点积匹配。仅重排已选位置与槽位的关系；评分器共享128→16投影初始值，相同seed与样本顺序。C使用无约束可训练gamma从0启动；首步仅gamma获得梯度，后续投影有梯度，测试通过。基础模型及位置/频带数值冻结。
- train4096、val_select512，seed20260921/22，20轮、每2轮同时评价train/val；batch4，AdamW lr1e-4、weight decay1e-4、clip10、FP32。最大联合命中的全部排列作为正例，多正例logsumexp监督，预测0/1源及所有排列质量相同的样本不训练排序。有效训练样本1650/1647，验证可区分样本211/207；同seed两轨有效optimizer步数完全一致，为17909/17846。训练后所有评价点未超过原绑定，四轨均选epoch0。
- 墙钟849.1291 s（14.15分钟），系统RAM采样峰值50.3%；最终审计PASS。回读重算63份登记JSON与44个评价checkpoint SHA全部匹配，无读取异常事件。每种位置排列及每次完整评价均检查位置指标不变；输入及缓存同字节校验消费、阶段后复核通过。4项合成测试、编译、Ruff通过。

| 两seed指标算术平均 | A原始绑定 / 最终所选模型 | B拼接MLP epoch20 | C显式匹配 epoch20 |
|---|---:|---:|---:|
| train联合Recall | 82.0557% | 89.8763% | 86.2467% |
| val联合Recall | 76.8229% | 65.0391% | 71.8750% |
| train可区分样本排列正确率 | 74.1893% | 92.5683% | 83.8954% |
| val可区分样本排列正确率 | 70.5783% | 47.6235% | 60.5387% |
| val空间匹配band F1 | 88.9954% | 83.1157% | 87.1148% |
| val GOSPA/m | 22.7196 | 22.7196 | 22.7196 |
| val matched RMSE/m | 147.6317 | 147.6317 | 147.6317 |
| val匹配覆盖率 | 97.2656% | 97.2656% | 97.2656% |

排列正确率按“被选排列是否属于联合命中最优集合”计算，仅统计有优劣排列的样本；epoch0全零分数由原排列优先规则复现原绑定，并非评分器已有识别能力。RMSE按逐seed计算后平均。val真值辅助固定位置联合Recall上限分别88.2813%/88.5417%，平均88.4115%，未直接套用旧三seed上限；最终所选模型oracle gap closure均0。C末轮各seed联合Recall为70.9635%/72.7865%，相对各自原绑定下降5.5990/4.2969 pp；B为63.5417%/66.5365%。

训练loss首轮→末轮：B为1.0599→0.4485、1.0588→0.5028；C为1.0599→0.6296、1.0587→0.6176。C末轮gamma为−1.1317/+1.1786（允许符号自由学习），不将负号认定为工程失败。训练排列明显改善而验证未超过原绑定，表现出明显训练—验证差距。

**补充R3训练排名回读。** 使用现存epoch0/20和train缓存，不重建中间checkpoint。两个seed各有2045/2046个可区分候选组合样本；R3初始严格排名正确率74.0342%/74.7312%，旧监督末轮69.4866%/70.4301%，组合监督末轮77.4572%/80.7918%。结合R3验证排名退化，进一步表明“训练loss下降”不应解释为验证身份能力改善。

**结论。** 固定位置策略成功隔离身份任务并保住定位结果；显式匹配相对拼接MLP改善验证表现，但仍不足以稳定替代原绑定。当前更直接的问题是身份匹配的泛化，以及如何利用原模型已有的绑定先验；不能继续把失败简单归为“训练集也学不会”或“特征完全没有身份信息”。训练集高准确率仍不证明可泛化的物理身份信息已被提取。后续方案另行审批，不自动加轮、引入新特征或恢复全网E2E。

## E2E-G5-R3-20260922：冻结评分器对照完成，组合监督未产生净收益

- 用户批准并由Codex完整执行。证据根`outputs_e2e/unified/e2e_g5_r3/20260922_115824/`；主要报告为`report.json`、`final_audit.json`、`运行结论.md`，配置及源码身份见`contract.json`，逐轨曲线与checkpoint见`training/<seed>/<old|joint>/`，固定候选及投影前特征见`cache/`。旧R2产物和原outputs均未修改。
- 两seed20260921/20260922，各自以G5 SG最佳checkpoint为固定基础；A无评分基线、B旧逐槽监督、C新组合监督。B/C评分器初始化及样本顺序相同，仅评分器投影和MLP训练；基础网络冻结。train4096、val_select512；20轮、每2轮评价、epoch0纳入选模；batch4、lr1e-4、AdamW、weight decay1e-4、clip10、网络FP32，组合分数延续原解码float64累加。B只用原关联loss，C只用组合loss；不读取val_compare/test。
- 组合正例采用“最大一对一联合命中，再按位置命中打破并列”，多正例logsumexp监督。原top8、30 m约束、K0/1及无合法组合回退保持不变。缓存的是投影前特征，128→16投影仍可训练。无有效监督的batch跳过optimizer更新；旧监督两seed有效步数20373/20372，新监督19231/19182，按既定20轮比较，不宣称步数完全一致。
- 总墙钟1953.2086 s（32.55分钟），系统RAM采样峰值57.0%。两seed四轨均完成20轮；五份输入审计及最终审计PASS，固定缓存消费范围阶段后复核通过。回读重算5份登记报告与8个最佳/末轮checkpoint SHA均匹配，无读取异常事件。零评分缓存前后输出一致；三个合成测试、编译与Ruff通过。投影层与末层参数均更新。

| val_select两个seed指标算术平均 | 联合Recall | GOSPA/m | matched RMSE/m | 匹配覆盖率 |
|---|---:|---:|---:|---:|
| A基线；B/C最终所选epoch0 | 76.8229% | 22.7196 | 147.6317 | 97.2656% |
| B旧监督epoch2 | 74.5443% | 23.9339 | 189.5522 | 97.2656% |
| C新监督epoch2 | 76.5625% | 23.2041 | 164.7048 | 97.2656% |
| B旧监督epoch20 | 69.2057% | 26.1296 | 263.7421 | 97.2656% |
| C新监督epoch20 | 69.2057% | 24.7192 | 240.4514 | 97.2656% |

RMSE为逐seed RMSE再平均。基线各seed联合Recall为76.5625%/77.0833%；旧监督末轮为69.1406%/69.2708%，新监督末轮为68.8802%/69.5313%。四轨全部选择epoch0，所有训练后评价点均未超过对应起点；末轮两组均值恰好相同不代表预测相同。旧loss首轮→末轮为1.9015→1.5808、1.9323→1.6152；新loss为1.6104→1.3286、1.6110→1.3468。优选组合最高分超过非优选组合最高分的比例，两seed平均从初始69.4118%降至旧监督55.4902%、新监督57.4510%；每seed该指标有255个可区分组合的validation样本。

结论：冻结基础模型及更换组合监督均未得到净收益。新监督减轻早期退化，末轮GOSPA/RMSE优于旧监督，但没有改善联合命中；本轮不支持仅靠目标对齐即可解决身份评分问题。loss下降和参数更新证明训练实际发生，不证明训练集联合排名已充分学会。下一步不自动扩展到全网E2E或增加轮数；若继续，应优先检查固定表征与评分器的身份区分能力，具体方案另行审批，不能把本次失败升级为“特征没有身份信息”。

## E2E-G5-R2-SCORE-TOGGLE-20260922：关联评分开关诊断

- 用户批准Codex执行；固定两个seed的C1/C2、epoch0/2/20，共12个checkpoint，在全部512条val_select上各前向一次，再用同一候选分别执行评分开/关解码。模型、候选、源数、频带、30 m排斥及其他规则不变；不训练、不调权、不读取val_compare/test。
- 有效证据：`outputs_e2e/unified/e2e_g5_r2/20260921_approved/diagnostics/score_toggle_20260922_104720/`，包含`protocol.json`、`report.json`、12份逐样本JSONL和`diagnostic_input_audit.json`。12个评分开启评价均复现历史validation联合Recall、GOSPA、RMSE（绝对容差1e-5）；epoch0开关输出相同。运行338.29 s，系统RAM采样峰值55.4%；1536个消费范围阶段后复核PASS，checkpoint前后身份复核通过，12份逐样本文件SHA再次核对通过，无读取异常事件。
- 首个尝试`score_toggle_20260922_104628/`主动中止并保留：候选事后标注的空频带F1边界从1对齐为原评价定义的0，真实频带阈值对齐为>0.5；该尝试不作为结论证据。原模型与正式评价函数均未改动。

| seed / 轨道 | epoch2联合Recall：关→开 | epoch20联合Recall：关→开 | epoch20 GOSPA/m：关→开 | epoch20 matched RMSE/m：关→开 |
|---|---:|---:|---:|---:|
| 20260921 C1 | 77.2135%→73.3073% | 76.9531%→70.3125% | 22.6281→24.9957 | 127.6498→220.5032 |
| 20260921 C2 | 76.1719%→74.7396% | 74.7396%→70.1823% | 23.3886→27.5013 | 146.5444→272.1614 |
| 20260922 C1 | 78.5156%→74.4792% | 74.7396%→70.1823% | 22.5228→26.2171 | 154.5853→267.0409 |
| 20260922 C2 | 76.8229%→74.0885% | 78.3854%→68.2292% | 23.1921→27.3814 | 143.1607→281.9877 |

epoch20匹配覆盖率按上表行序为96.3542%、96.0938%、97.0052%、96.8750%，同checkpoint开关相同。RMSE采用原匹配口径，非仅100 m内成功定位误差。以下四轨平均是描述性汇总，不把相同场景或相同seed当作独立重复：epoch0联合Recall76.8229%；epoch2关闭77.1810%、开启74.1536%；epoch20关闭76.2044%、开启69.7266%。末轮关闭相对起点下降0.6185 pp，而打开评分额外下降6.4779 pp。末轮四轨平均GOSPA为22.9329→26.5239 m，逐轨RMSE平均为142.9850→260.4233 m。

**分数证据。** 在预测K≥2且同槽候选中同时存在联合合格与不合格候选的子集，统计“最好合格候选的分数高于最好不合格候选”的比例。epoch20热图单独为74.67%—78.16%，关联评分单独仅36.78%—42.43%，相加后为63.74%—65.93%。合格指该槽位频带F1≥0.8且位置距任一相容真源≤100 m；该局部标注不替代最终一对一联合匹配，也不能将该比例直接与随机二分类50%比较。

**典型身份交换。** `20260921_c1_20_samples.jsonl`中的index279/raw_index331：真源约为(-70.1,-712.0)和(466.2,-618.7) m。关闭评分时两个槽位分别选到对应位置，联合命中2；开启后两槽位交换空间位置，位置仍靠近两真源，但频带绑定错位，联合命中0。正确组合热图分数和−1.66094，错误组合−1.70052；关联分数和却分别为−3.34223、−3.21471；相加后错误组合−4.91523超过正确组合−5.00317。此例直接显示身份评分覆盖了原有正确组合的排序优势。

**结论与边界。** 八个训练后检查点的开关实验一致证明：关联评分参与解码是当前退化的主要直接来源；它不只是数值尺度不匹配，身份排序本身也不可靠。关闭后仍有少量、依seed变化的训练漂移，未据此确定基础表示或梯度冲突的根因。诊断未证明“不一致的训练目标”是唯一底层原因，也未评估新的修复算法；下一步优先设计面向最终组合排序的身份监督，保留原热图基础，不先加轮、全解冻或盲调融合权重。科学方案另行审批。

## E2E-G5-R2-20260922：六轨完成，关联训练未改善验证表现，最终均选回初始模型

- 用户完成正式运行，本轮回读分析，不重训、不重跑网络、不读取test。证据根：`outputs_e2e/unified/e2e_g5_r2/20260921_approved/run/`；主要证据为`training_report.json`、`training/<seed>/<arm>/history.json`及`evaluation/`下的`comparison_report.json`、`final_audit_report.json`、`运行摘要.md`。
- 两个seed为20260921/20260922；C0为SG继续训练，C1为SG＋关联评分器，C2为E2E＋关联评分器。六轨均完成20轮、每轨20480次optimizer更新；未触发24轮条件延长。总墙钟23231.07 s（6.45小时），训练累计21094.92 s（5.86小时），系统RAM采样峰值63.9%。C0每轮约2.47分钟，C1/C2约3.15—3.17分钟，12小时预算未限制完成。
- 最终审计PASS；本轮重算18项最终产物SHA均匹配，选中及最新checkpoint身份匹配；13份输入审计均PASS，运行目录无读取异常事件文件。两seed各自C1/C2最终逐样本文件字节相同。

| val_compare，两个seed指标算术平均 | C0：SG继续训练 | C1：SG＋关联 | C2：E2E＋关联 |
|---|---:|---:|---:|
| 联合Recall（位置≤100 m且band F1≥0.8） | 76.3021% | 74.8047% | 74.8047% |
| GOSPA / m | 22.5474 | 22.5834 | 22.5834 |
| matched RMSE / m | 149.2410 | 156.4232 | 156.4232 |
| 匹配覆盖率 | 95.8333% | 95.9635% | 95.9635% |
| Recall@100m | 91.4388% | 91.4714% | 91.4714% |
| band-only F1 | 90.5185% | 90.6098% | 90.6098% |
| exact count | 89.3066% | 89.1602% | 89.1602% |

每seed评价1024场景、1536真实源；RMSE为逐seed RMSE再平均。报告中的K分层场景配对bootstrap使用2000次重采样、跨seed共用场景索引：C1−C0联合Recall为−1.4974 pp，95%区间[−2.7669, −0.2930] pp；GOSPA为+0.0360 m，区间[−0.3784, +0.4397] m；RMSE为+7.1822 m，区间[−4.6136, +20.1910] m。区间描述当前两个训练seed下的场景抽样不确定性。

**选模解释是本次结论的关键。** C0两个seed分别选epoch10、epoch4；四条C1/C2均选epoch0。初始评分器末层权重和偏置为零，两组前向相同。因此C2−C1所有指标为零来自共同初始模型，不能解释为训练后的反馈效果相同。C1−C0也应解释为“关联方案未产生优于起点的可选模型，而C0继续训练获得改善”，不能将−1.4974 pp全部归为训练后评分器造成的损害。

| val_select联合Recall | epoch0 | epoch2 | epoch20 | 最佳epoch |
|---|---:|---:|---:|---:|
| 20260921 C1 | 76.5625% | 73.3073% | 70.3125% | 0 |
| 20260921 C2 | 76.5625% | 74.7396% | 70.1823% | 0 |
| 20260922 C1 | 77.0833% | 74.4792% | 70.1823% | 0 |
| 20260922 C2 | 77.0833% | 74.0885% | 68.2292% | 0 |

四轨所有已评价训练后checkpoint均未超过各自起点。关联loss从首轮约1.91降至末轮1.59—1.62，末轮训练候选正样本覆盖约97.3%—97.6%；最新评分器参数确实更新，C1/C2训练后参数不同。由此确认训练实际发生，但训练目标下降没有转化为验证联合命中收益。当前曲线不支持优先追加轮数；C1在切断反馈时已退化，也不支持将E2E梯度冲突作为唯一原因。

下一步建议仅用已有训练后checkpoint，在val_select上保持权重和候选不变，比较关联评分开/关，并检查正确与错误候选的热图分数和身份分数。这可区分评分融合不当与训练过程中基础表示退化；尚未执行。优先假设是“单独监督身份分数、推理却与热图固定权重相加”的目标与排序不一致，其次是局部特征区分身份不足。具体综合判断见实现文档17.13.8。

## E2E-G5-R2-DIAG-20260921：固定位置与频带的身份排列诊断

- 登记上一轮只读分析：来源为`outputs_e2e/unified/e2e_g5_r1/20260920_approved/evaluation/`六个`*_samples.jsonl`。保持联合解码的位置集合、有效槽位及每条频带预测不变，枚举频带在预测位置间的排列，以真值计算最多可达的联合命中。位置阈值100 m、频带F1阈值0.8及ignore规则保持G5-R1定义；未重跑网络、训练或test。本轮只将已完成分析写入文档，不因目录整理重复验证旧产物。

| 三seed累计，真实源实例均4608 | SG | E2E |
|---|---:|---:|
| 位置命中 | 4200（91.1458%） | 4171（90.5165%） |
| 实际联合命中 | 3461（75.1085%） | 3525（76.4974%） |
| 仅重排频带可达联合命中 | 4071（88.3464%） | 4028（87.4132%） |
| 可恢复联合命中 | 610 | 503 |
| 占位置命中与联合命中缺口 | 82.5440% | 77.8638% |
| 至少增加一次联合命中的场景×seed数 | 392 | 327 |

- E2E各seed的实际/重排后联合命中依次为1185/1350、1182/1336、1158/1342；SG依次1148/1372、1150/1347、1163/1352。该诊断证实固定预测中存在大量身份配错的可修复空间，不证明可学习方法必然达到真值辅助上限，也不将剩余缺口全部归为频带本身错误。
- 结构解读：原训练对频带/热图/Offset采用同一匹配，已有联合监督；当前候选组合依据热图分数与空间排斥，尚无额外逐子带局部证据的身份评分。提出G5-R2作为单一关联模块验证，具体方案见实现文档17.13，尚未实施。

## E2E-G5-R1-20260920：联合选峰有效，反馈优势依赖解码，逐源关联仍待改进

- 用户在PyCharm完成正式运行。证据根：`outputs_e2e/unified/e2e_g5_r1/20260920_approved/evaluation/`；主要报告为`comparison_report.json`、`final_audit_report.json`、`运行摘要.md`，逐样本证据为六个`*_samples.jsonl`。入口收据`../exit_receipt.json`为COMPLETED，退出码0，墙钟152.1778 s；最终审计PASS，科学结果初始标记COMPLETE_FOR_REVIEW。未训练、未读取test，原G5证据未覆盖。
- 固定三个seed20260921/22/23的SG/E2E最佳checkpoint，32条val_select工程pilot后评价1024条val_compare，各K=0/1/2/3均256条；原/联合解码使用同一前向。联合规则为7×7局部峰、每槽位最多8候选、30 m排斥、峰值对数和最大、保留槽位频带，详见登记合同。联合命中为位置≤100 m且频带F1≥0.8的一对一最大匹配，沿用ignore规则。
- 本次回读验证：最终审计登记的25个产物SHA全部匹配；现行61项及原G5 v4的111项合同身份匹配。六轨共6144条预测完整；独立重算联合命中、候选组合最大值及源数/频带不变性通过，四组场景配对bootstrap汇总区间复现。运行报告记录历史logits全部精确复现、坐标差≤5e−5 m、GOSPA差≤1e−4 m；输入阶段后复核PASS，异常目录无事件。运行采样的系统RAM峰值53.6%，非持续采样绝对峰值。

| 全1024条，三个seed指标的算术平均 | SG原解码 | SG联合解码 | E2E原解码 | E2E联合解码 |
|---|---:|---:|---:|---:|
| GOSPA / m | 53.2812 | 22.4855 | 41.0239 | 23.8614 |
| Recall@100m | 63.0425% | 91.1458% | 75.3906% | 90.5165% |
| 联合Recall@100m且band F1≥0.8 | 57.9861% | 75.1085% | 68.7500% | 76.4974% |
| matched RMSE / m（逐seed RMSE再平均） | 510.6466 | 155.8533 | 398.8331 | 160.1172 |
| 匹配覆盖率 | 95.6597% | 95.6597% | 95.6380% | 95.6380% |
| 按空间匹配的band F1 | 84.6036% | 88.1112% | 86.8349% | 88.0919% |
| exact count | 89.0951% | 89.0951% | 89.5833% | 89.5833% |

band-only预测未变；空间关联指标的变化来自位置分配，而非频带数值改变。其他定位分层、分位数及GOSPA分量见原始比较报告。

| 配对变化（后者减前者） | GOSPA / m，95% CI | 联合Recall变化 / pp，95% CI |
|---|---|---|
| SG原→联合 | −30.795706 [−32.353891, −29.337511] | +17.122396 [+15.407986, +18.772244] |
| E2E原→联合 | −17.162516 [−18.454323, −15.908591] | +7.747396 [+6.553819, +8.941515] |
| 原解码SG→E2E | −12.257333 [−13.689552, −10.789463] | +10.763889 [+9.136285, +12.304688] |
| 联合解码SG→E2E | +1.375857 [+0.748998, +1.972606] | +1.388889 [−0.455729, +3.211806] |

- 三seed的联合解码E2E−SG GOSPA差依次为+0.634361、+1.616445、+1.876765 m，均偏向SG。联合Recall差依次+2.408854、+2.083333、−0.325521 pp，汇总区间跨0。联合解码下Recall@100m差−0.629340 pp，95% CI [−1.107313, −0.108507] pp；RMSE差+4.263884 m，95% CI [−8.964760, +18.111673] m。区间按K分层、2000次场景同步重采样，条件于现有三个训练seed，不等于三倍独立样本或新seed不确定性。
- 按样本×seed计数（每轨3072次场景预测），SG/E2E重复定位分别1215/801→0/0；存在匹配误差>500 m的场景分别786/532→104/111。E2E的665次GOSPA改善、57次变差，651次Recall@100m改善、0次变差；94次联合命中数下降，其中73次同时位置命中增加。因此平均定位改善不能替代逐源关联检查；重复率归零本身部分由排斥规则直接保证，收益证据应看真实源命中和GOSPA。
- E2E联合解码实际位置命中4171/4608=90.5165%，联合命中3525/4608=76.4974%。候选并集100 m覆盖96.1372%，保留槽位与30 m约束的真值辅助可行位置上限94.0755%、联合上限90.7335%。上限只用于解释已有候选的可用性，不进入推理、不作为模型成绩；结果支持优先研究槽位—空间候选的身份分配，同时保留候选缺失这一较小但真实的误差来源。
- 近源<30 m、30—100 m两组均无样本；多源场景最小实际源间距151.384369 m。因此本次不提供100 m内近源安全性结论。六轨无回退。案例图按固定seed/样本顺序取首个满足条件者，见`figure_manifest.json`与`figures/`；`near_harm=NO_CASE`源于该分层为空，不能解读为近源无损。

| 同一512条K=2/3开发样本 | GOSPA / m | Recall@100m | matched RMSE / m |
|---|---:|---:|---:|
| 原hard_base | 24.1530 | 95.1563% | 57.4522 |
| 原r5a_selected | 20.4453 | 97.0313% | 61.7176 |
| SG联合解码（三seed平均） | 31.0993 | 92.7083% | 129.5432 |
| E2E联合解码（三seed平均） | 33.4199 | 92.1875% | 133.3100 |

共同样本raw_index集合逐一核对一致，硬参考来自原报告，无新训练。不能拿全1024条的23.8614 m与硬级联512条的24.1530 m直接比较并声称超过硬级联。

综合结论：联合选峰的开发集修复作用得到六轨支持，独立选峰是当前重要瓶颈。原G5反馈收益在原解码下仍可复现，但采用相同联合解码后，E2E的GOSPA优势消失且略差，联合关联优势未确定。因此后续必须以SG＋联合解码为强配对对照，优先解决频带—位置绑定，再检验反馈是否带来额外收益；不直接扩大训练或宣布E2E方案完成。实现文档17.12.7记录研究解释，G6仍待方案审批。

## E2E-G5-ANALYSIS-20260920：完整执行结论与重复定位诊断

- 正式证据：`outputs_e2e/unified/e2e_g5/20260919_approved/`的`training_report.json`、`comparison_report.json`、`hard_reference_report.json`、`final_audit_report.json`。本次只回读原结果；没有重训、重选checkpoint或读取test。工程收尾身份见下方`E2E-G5-HARD-RECOVERY-20260920`。
- 六轨均完成64 epoch、65536 optimizer step；train/val_select/val_compare为4096/512/1024，各K均衡；batch4。SG/E2E每对共享初始化、样本顺序和预算，共用原CH3/D8 seed42预训练权重，新增接口随机初始化，不从G3/G4热启动。原硬级联不是等训练预算的因果对照；同前向SG才承担反馈因果对照角色。

| 三个seed指标的算术平均 | SG | E2E |
|---|---:|---:|
| 全val_compare GOSPA / m | 53.2812 | 41.0239 |
| Recall@100m | 63.0425% | 75.3906% |
| matched RMSE / m（逐seed RMSE再平均） | 510.6466 | 398.8331 |
| exact count | 89.0951% | 89.5833% |
| band F1（原联合匹配口径） | 90.3344% | 89.7306% |
| band-only F1 | 90.3518% | 90.0380% |

- E2E−SG GOSPA平均为−12.257333 m，配对样本bootstrap 95% CI `[−13.689552, −10.789463] m`；三个seed差值依次为−12.038809、−11.933701、−12.799490 m。区间仅描述给定三个训练seed下的样本不确定性，不把3072条预测当独立场景。没有触发预注册稳定实质损害项。
- 正式科研状态仍为`DIRECTIONAL_BUT_INCONCLUSIVE`，直接原因是`training_budget_unresolved=true`，不是定位增益的区间跨0。seed20260921/22/23的SG best分别64/64/60，E2E分别60/54/64；前两个SG和第三个E2E仍满足预算候选规则。支持“当前开发数据和预算下反馈收益三seed同向”，不宣称充分收敛、最终test成功或架构已超过硬级联。

| 同一512条K=2/3开发样本 | GOSPA / m | Recall@100m | matched RMSE / m |
|---|---:|---:|---:|
| 原hard_base | 24.1530 | 95.1563% | 57.4522 |
| 原r5a_selected | 20.4453 | 97.0313% | 61.7176 |
| G5 E2E（三seed指标平均） | 67.7483 | 74.0365% | 420.4563 |

硬参考匹配覆盖率分别96.4844%/98.6719%，RMSE不能脱离覆盖率、漏检和虚警比较。此处没有补做K=0/1硬参考，没有将原系统较充分的独立训练与新接口训练当等预算。

### G5-SLOT-DIAG：证据边界、选例与回放

- 最终诊断目录：`outputs_e2e/unified/e2e_g5_diagnosis/20260920_slot_analysis_v2/`。`report.json`保存全开发预测描述统计及四例中间表示；`probes/report.json`保存原D8同全频输入与候选联合解码探针。PNG/SVG在`figures/`，中间张量在四个NPZ。初版`20260920_slot_analysis/`保留，不作为最终图文引用源。
- 所用模型、数据、冻结特征均来自本地G5登记快照；读取与消费同一经SHA核对字节。正式v4合同111项身份通过；实际消费输入执行前后身份复核，诊断异常文件0。四例SG/E2E回放频带logits与原逐样本结果完全一致，最大坐标差小于0.00003 m；差异来自输出坐标FP32舍入。诊断不更新权重、不覆盖G5预测、不改变正式评价规则。
- 选例固定seed20260921：A取此前已讨论的raw30；B在K正确、低于−10 dB、有重复预测且误差>500 m的三源样本中取最大误差的中位样本；C取SNR最接近A的非负SNR三源全命中样本；D取SG严重失败而E2E三源全命中、SNR最接近A且不同于C的样本。这是目的性案例，不是随机性能样本。
- 绘图参考刘同振论文第3.5.2节、图3.12的子带顺序和空间激活对照形式（PDF第74—78页）；本图用统一色标、真实位置标记，粗特征保留11×11索引而不伪装成精确物理坐标。RMS是通道L2范数除以通道数平方根，不是注意力或信号功率；频谱为含噪接收IQ的4096点FFT、四站功率均值、相对其峰值的dB，不是单源干净频谱。频带图灰色为对应真值的ignore位置；仅按频带关联行不代表位置已关联正确。

| 案例 | raw_index / ordinal | SNR / dB | SG三个匹配误差 / m | E2E三个匹配误差 / m |
|---|---|---:|---|---|
| A 高SNR重复定位 | 30 / 770 | 5.0399 | 6.04 / 2.91 / 1331.80 | 8.91 / 7.21 / 1341.85 |
| B 低SNR重复定位 | 253 / 793 | −11.1701 | 1.93 / 1066.39 / 8.74 | 3.24 / 942.21 / 16.60 |
| C 高SNR成功 | 1999 / 1019 | 5.3215 | 6.62 / 5.51 / 1.67 | 3.65 / 3.43 / 2.69 |
| D 反馈改善位置集合 | 1462 / 940 | 5.6812 | 823.81 / 1221.53 / 6.12 | 14.88 / 23.47 / 9.08 |

### 全开发预测的描述性规律

“严重长尾”仅在本次诊断中定义为至少一个matched error>500 m；“重复”定义为预测中有一对位置相距<30 m。它们不是新Gate阈值，且matched error不涵盖所有纯漏检。

- 三seed共有532个严重长尾样本×seed实例，其中509个源数正确（95.68%），476个含重复位置（89.47%）。同一场景跨seed重复出现，不作为独立统计样本。
- K=1/2/3分别有41/132/359个严重长尾实例，每层分母768，对应5.34%/17.19%/46.74%。二/三源含重复组776例中474例严重长尾，不含重复组760例中17例严重长尾；这是相关性分组，不是消除重复后的因果收益预测。
- 三源SNR<−10 dB的93实例中64例严重长尾；SNR≥5 dB的111实例中49例严重长尾。低SNR加重但不能单独解释。
- 元数据frequency_overlap在二/三源全部为True，不能独立估计频带重叠影响。补充按“存在一对在共同有效子带上标签相同”分组：三源同标签组276实例中128例严重长尾（46.38%），不同标签组492实例中231例（46.95%）；重复位置比例分别71.38%/73.37%。同标签不是必要条件，不能把全局失败简单归为同频。

### 四例隔离探针及新限制

| 案例 | 原预训练D8＋相同全频细DPD：三个误差 / m | 相同E2E热图＋候选联合分配：三个误差 / m |
|---|---|---|
| A | 2.90 / 8.37 / 43.48 | 8.91 / 7.11 / 62.65 |
| B | 2.12 / 12.06 / 2.03 | 3.24 / 13.59 / 8.28 |
| C | 2.98 / 0.57 / 1.01 | 3.65 / 3.43 / 2.69 |
| D | 13.43 / 22.54 / 10.21 | 14.88 / 23.47 / 9.08 |

- 原D8探针复用同一冻结全频前缀，使用原D8自身预训练decoder/定位头和原多峰解码，K取E2E预测值（四例恰好都正确），不是原带限硬级联。四例均3/3命中100 m，不能据四例宣称全频与带限等效，但A/B不支持“全频DPD已经丢掉第三个源的可定位信息”作为唯一解释。
- 候选联合分配不使用真值：每个激活槽位取7像素局部极大抑制后的8个候选，组合中位置不得相距<30 m，最大化各槽位所选热图概率的对数和，offset沿用原值。A/B通过将重复槽位转向次峰恢复三源100 m命中；C/D位置不变。仅为四例事后可行性探针，不是验证集选优或正式解码修改，也不证明30 m排斥适用于近源场景。
- A中重复的Q1/Q3：anchor余弦0.5028、query余弦0.9283、热图余弦0.9832。频带加权粗特征槽位两两余弦0.9472—0.9951；但D成功位置集合的Q2/Q3 query/热图余弦亦达0.9672/0.9946，高相似度不是充分失败条件。attention按空间softmax后仅以`1+attention`增强，没有槽位间竞争或直接抑制；不能把它描述为逐源排他的空间掩码。
- 新增“按空间匹配后检查频带”仅为诊断，不替代既有F1：A联合解码后的三个频带F1为0.8571/0.8/1；B为0.8571/0.5714/0.8571；D原E2E与联合解码均为1/0/1。说明位置集合变好不等于每个位置绑定了正确频带。A原band-only F1全1不能证明槽位身份与位置联合正确；D标题“反馈改善”仅指位置集合。后续方案须同时验证频带—位置关联。
- 结论：最高优先级为槽位空间分工与最终候选分配，其次排查频带条件混合、attention不能抑制其他源、query身份弱化及联合关联监督；大范围输入噪声、冻结适配和预算仍是候选因素，尚无单一根因或已验证全局修复。方案与图解见实现文档17.10—17.12。

## E2E-G5-HARD-RECOVERY-20260920：参考坐标精度误判修复与完整收尾

- 用户实际运行`manual_20260919_235908`：六轨训练PASS，训练阶段53085.9899秒（约14.746小时）；三组seed20260921/22/23的SG、E2E均按成对规则完成64轮/65536步。训练后输入核对PASS，六轨val_compare预测及比较报告完成；失败发生在第5阶段Hard参考核对，不是训练失败。原日志、失败回执和`hard_reference/pb_base_samples.jsonl`均保留。
- 原错误`Reference truth/coordinate/GOSPA mismatch`来自两种FP32坐标表示：历史R5的真值经过`positions/EDGE → float32 pos_label → *EDGE`，G5 targets直接保存米制float32。使用已登记SHA的历史`val_compare_top4.jsonl`真值重算，两组各512条历史GOSPA逐条精确一致；真值最大分量差仅0.000030517578125米。直接拿G5真值核对历史标量时，hard_base有6条、r5a_selected有9条触发旧rtol/atol阈值；GOSPA最大差分别0.0000180519752035/0.0000245707580808米。诊断证据：运行根`engineering_v4/diagnosis.json`。不是本次读取SHA漂移或模型训练数值失败。
- 修复：先按登记历史真值精确复现原GOSPA，再通过Hungarian匹配核对真值，仅允许每坐标`4*float32_eps*max(1,abs(coordinate))`内的舍入差；不放宽GOSPA验证、不改变预测。参考对比行统一以G5米制真值重算GOSPA各分量、匹配误差和阈值匹配数，保留`historical_gospa_m`及差异上限。历史聚合另按历史行核对，不混用坐标表示。
- 工程v4合同独立冻结111项身份，链接v3并锁定已完成训练、checkpoint和六轨比较产物。修复产物写新`hard_reference_recovery_v4/`；旧失败目录不覆盖。一键入口在受验证的完整训练/比较证据存在时，只恢复Hard与final_audit，最终PASS后直接提示已完成，不再触发旧SG48身份或原训练重跑路径。
- 已执行收尾：`manual_20260920_155914_receipt.json`为EXECUTION_COMPLETE_ANALYSIS_PENDING，`hard_reference_report.json`与`final_audit_report.json`均PASS。最终审计记录恢复读取失败0、test_executed=false、累计墙钟92758.654秒（含准备及暂停，约25.77小时）。独立重算新参考报告1024条GOSPA与保存值逐条一致；最新`--check-only`识别已完成。静态编译/Ruff F,E9通过。没有重训、没有重新生成六轨预测，也没有重跑SG/E2E比较；其冻结身份复核通过。
- 科研状态仍为已保存的`DIRECTIONAL_BUT_INCONCLUSIVE`，不是工程失败。完整G5已具备分析材料，但本轮只诊断、修复和审计，不将其解释为端到端收益已获最终证实；下一步回读整体结果，test继续封存。

## E2E-G5-LIVELOG-20260919：剩余耗时外推与PyCharm实时日志

- 仅工程日志增强，未启动正式训练；证据为原G5运行根`engineering_v3/logging_validation.json`和`contract.json`。日志v3合同登记93项身份并链接v2，训练数值路径、样本顺序、资源门槛及预算检查均未改变，原恢复数值证据继续适用。
- 从已完成SG48开始，新加载器两次batch4预取短测按总样本/总耗时折算，SG/E2E每4096样本训练约162.167/161.211秒；验证耗时沿用原pilot。只执行基础48轮，剩余SG96轮＋E2E144轮，训练及模型选择外推11.190小时；若全部成对延长64轮，则剩余SG144轮＋E2E192轮，外推15.669小时。加25%吞吐波动余量及1小时输入复核/最终评价/保存规划余量，分别约14.988与20.587小时，即约15/21小时。余量为规划假设，不是测得的最终阶段耗时；短测含诊断开销且不能保证长期速度，不作为新预算批准。
- 一键入口启动时并列显示上述预计时间和原pilot保守预算需求34.31小时。真实只检查通过时剩余预算38.36小时；仍按原截止时间计费，不将15/21小时预测替换预算门禁。
- 日志：新`统一模型代码/common/g5_live_log.py`让子进程继续无缓冲写UTF-8文件，主进程每秒增量读取转发，不创建stdout管道或后台日志线程。每128个batch的既有进度记录转成中文seed/轨道/epoch/batch百分比/累计步数/本轮剩余时间；原epoch与最佳GOSPA输出实时转发，阶段每分钟报告存活及耗时。当前epoch ETA由相邻进度点计算，仅含训练剩余，不是整体完成时间。
- 验证：真实短子进程中文＋拆分UTF-8字节实时显示、退出前可见、文件内容一致通过；预期退出码3正确报错并留报告；模拟资源停止会终止子进程；重复进度去重和epoch ETA计算通过；一键六阶段分发为mock通过。语法编译、Ruff F/E9和真实`--check-only`通过。预期失败用例只在engineering_v3测试子目录，不是正式训练失败；没有运行完整训练或评价。

## E2E-G5-INTEGRATION-20260919：推荐加载配置接入与一键交付

- 状态：`READY_FOR_USER_RUN`；证据根`outputs_e2e/unified/e2e_g5/20260919_approved/engineering_v2/`，入口审查为`handoff_report.json`，数值恢复证据为`recovery_20260919_233234/report.json`。未启动正式训练、未计算验证/比较指标、未读取test；其他正式轨道仍无训练产物。既有空E2E目录不是训练完成证据。
- 接入：训练按样本SHA读取＋单工作线程/单batch预取；验证与比较按样本读取、不额外预取。batch4、2 GiB共享LRU、原初始化/SG-E2E断梯度语义/FP32/优化器/学习率/48或64轮/seed均不变。整机RAM占用85%预警即停止继续分配，替代v2运行的旧8 GiB空闲停止线；原manifest与原48小时开始时刻不修改。
- 索引：train4096、val_select512、val_compare1024逐样本范围从登记SHA匹配的原分片字节生成；不复制或重算大特征。原`training_code_contract.json`完整保留；新`engineering_v2/contract.json`登记89项身份及独立源码快照，包含原合同身份、新索引和恢复证据。冻结前对非本轮修改的原合同文件重新验SHA，三个受影响统一模块的旧源码在原snapshot中匹配登记SHA。
- 真实数值验证：SG从原epoch48/49152步`last.pt`恢复，E2E从原seed初始化；每轨对旧分片加载和新预取各执行8步短窗口，保存第4步状态并从新队列重放后4步。两轨均逐步loss/梯度范数、最终模型和优化器摘要完全一致；checkpoint重放一致、下一epoch样本排列一致。提前关闭活跃预取队列后重新取样一致。仅验证恢复不变量，不声称正式入口支持任意batch恢复；正式入口仍只保存完整epoch。
- 输入检查：恢复验证后3072个train分片与targets登记身份通过；val_select/val_compare各检查首、末、重复样本的三组特征，旧/新路径dtype、shape、bytes一致，仅工程数据核对，不运行指标或选模型。新工程目录读取异常事件0。正式点击时仍完整复核所有输入，不以本次抽样替代规定预检。
- 工程检查：六个本轮Python文件编译及Ruff F/E9通过；RAM84.9/85.0边界、关闭队列时待处理后台异常传播、系统锁互斥/释放通过。模拟测试确认跳过SG48、只在配对条件满足时两轨延长64轮、六阶段成功/失败回执、过期预算拒绝、已有评价拒绝。真实`运行入口/E2E/G5/G5一键继续.py --check-only`通过，UTF-8非ASCII子进程捕获无乱码。完整流水线未运行。
- 预算：交付回执时剩余38.62小时，入口仍采用旧pilot的保守恢复需求34.31小时，不因短测提速降低预算要求或重置截止时间。之后点击须再次满足实时预算；原截止为2026-09-21 14:13:23本地时间。延迟启动可能因剩余预算不足而拒绝，需要用户另批预算修订。
- 检查脚本纠正：首次交付断言把历史空E2E目录误当成新训练活动；改为检查实际文件，旧空目录保留。失败检查记录`handoff_probe_failed.json`，新通过回执`handoff_report.json`，不把失败断言当产品训练故障或PASS证据。
- 下一步：用户在PyCharm直接运行根目录`运行入口/E2E/G5/G5一键继续.py`，无需参数，读取新合同后自动继续训练、输入复核、比较、Hard参考及最终审计；完成或报错后回读最新manual回执。验证权重仅存于engineering_v2，不用于正式初始化。

## E2E-G5-PREFETCH-BATCH-20260919：单batch预取与batch吞吐筛选

- 类型：用户批准的真实前向/反向工程筛选，不是正式模型训练或收敛评价。成功证据：`outputs_e2e/unified/g5_prefetch_probe/20260919_230910/report.json`，状态PASS；协议和逐臂完整轨迹在同目录。未读取val_select、val_compare或test，未恢复正式G5。
- 配置：原G5初始化、seed=20260921、FP32、学习率/优化器/loss/冻结范围不变；逐样本已校验字节加载、共享2 GiB LRU；每臂128样本预热＋512样本计时；SG/E2E、batch=4/8、预取开关各两次，第二次反转batch及开关顺序。预取为单工作线程、最多一个待消费batch；主线程保持更新顺序，后台异常必须传回。消费数据摘要计算包含在计时中，结果不等于无诊断开销的正式吞吐。
- 用户批准整机RAM占用85%预警。本次探针达到预警即停止继续分配，替代探针旧空闲8 GiB门槛；不修改历史正式manifest。显存保留上限14 GiB，墙钟上限30分钟，磁盘空闲至少50 GiB。

| 配置 | SG合并吞吐（样本/秒） | E2E合并吞吐（样本/秒） | 张量峰值显存（GiB，SG/E2E） | PyTorch保留峰值（GiB） |
|---|---:|---:|---:|---:|
| batch4，无预取 | 18.288 | 19.194 | 2.193 / 2.198 | 3.914 |
| batch4，单batch预取 | 25.258 | 25.408 | 2.192 / 2.198 | 3.914 |
| batch8，无预取 | 19.654 | 19.938 | 4.261 / 4.271 | 7.543 |
| batch8，单batch预取 | 26.328 | 26.752 | 4.261 / 4.271 | 7.543 |

- 合并吞吐按两次共1024样本除以总计时时间计算。batch4预取相对同batch无预取，SG/E2E耗时减少27.60%/24.45%；在此基础上改batch8仅再减少4.06%/5.03%。两次复测不构成统计显著性或长期吞吐保证，不能与前次按样本读取试验百分比直接相加。
- batch16：SG/E2E首次无预取试跑分别14.755/17.259样本/秒；张量峰值8.398/8.419 GiB，但保留显存均14.773 GiB，超出门槛而淘汰。不将其作为可部署通过配置；没有完整复测、没有预取等价结论。batch32因同轨batch16已超资源门槛而跳过，**没有实测batch32，不能声称其发生OOM或绝对不可运行**。
- 有效16臂（两个repeat×两轨×两batch×两预取开关）完成；全部8个同batch配对的消费摘要、逐步loss/梯度范数、最终模型和优化器摘要完全一致。未逐步保存完整梯度摘要，也未在本轮重复checkpoint恢复验证；前次按样本读取恢复证据不能当作预取队列断点恢复证据。
- 成功候选训练循环中整机RAM采样峰值54.7%，未触发85%预警；并非系统级连续峰值测量。运行前后3072个train分片及targets身份核对通过，冻结代码及本轮源码身份复核通过，成功目录异常事件0。主路径保持校验与消费同字节，不代表已查明历史异常根因。
- 保留失败尝试：`outputs_e2e/unified/g5_prefetch_probe/20260919_230434/failure.json`，完成4臂后batch16触发保留显存上限。首次脚本错误地将单候选资源淘汰作为整个筛选终止；最小修复为保存淘汰事实并跳过同轨更大候选，重新从上述新目录运行。旧目录不作PASS证据。
- 结论：推荐正式接入候选为**按样本读取＋单batch预取＋batch4**；batch8仅作为小幅吞吐提升的备选，不以保留SG48为选择理由。用户已接受更优新配置下重跑SG，但现有证据不足以支持为几个百分点收益直接更改优化步数与收敛预算。不增加RAM缓存，不启用AMP，不改变学习率或epoch。正式接入及新合同、实际恢复边界验证尚未实施，旧一键入口仍为原实现。

> 本文件记录可追溯的正式实验事实、重要结果审查和具有独立研究意义的诊断结论。Gate 3A 与 Gate 3B 已录入为工程、训练与可重复性证据，不作为论文正式配置的性能结果。

## E2E-G5-RANGE-20260919：按样本范围读取的等价与速度验证

- 工程验证，不是新增研究Gate或正式训练。证据：`outputs_e2e/unified/g5_sample_range/20260919_223427/`，`protocol.json`、`sample_index.identity.json`、四份轨道报告及`report.json`。保留原冻结训练源码和SG48；没有后台预取，没有读取val_compare/test，没有恢复正式G5。
- 在现有4096条train上，从完整SHA匹配的NPY字节登记每条样本的偏移、长度、dtype、shape及SHA；只登记索引，不复制特征。相同seed20260921、相同前257个随机batch（256训练＋下一步恢复验证）、batch4、共享2 GiB缓存、原优化配置。SG先原后新，E2E先新后原。两个方向实际消费的数据逐字节一致；各256步loss、裁剪前梯度范数、裁剪后完整梯度摘要、最终参数及优化器摘要完全相同；双方保存恢复后的下一步完全一致。`report.json`判定`EQUIVALENT`。
- SG原整片90.9320365秒→范围55.2476488秒，新/旧0.6075707850，耗时减少39.24%；E2E原79.0679542秒→范围55.3963864秒，新/旧0.7006174241，耗时减少29.94%。计时包含每步用于等价核验的梯度摘要开销，不含建模、逐字节数据对照及checkpoint测试；两边口径相同，不能直接当完整正式训练耗时。
- 两轨各自的读取量原37214713728字节→新9749321728字节，比值0.2619749220，减少73.80%；统计含恢复验证的额外步骤，训练计时仅256步。原缓存峰值2146538752字节，新2146516992字节，均不超过2 GiB。候选首次epoch样本无重复，命中率不必比整片高；改善主要来自少读无关样本。
- 合成测试通过：乱序/重复索引、只读缓冲、LRU容量、Fortran序拒绝；模拟首次IO异常后仅重读一次成功，连续两次身份失败保存两次捕获并停止。合成异常位于临时测试目录，不是项目数据异常。真实验证结束输入身份复核另见`post_identity.json`。
- 首次启动`20260919_223408`在训练前因本机psutil不接受WindowsPath而退出，改为字符串后新目录运行；失败记录保留，不计通过证据。语法编译、Ruff F/E9通过。
- 结论：支持“按样本范围读＋同字节SHA”为优先候选；无需现在叠加预取。四次短测不证明长运行稳定性或保证整场提速。尚未接入原G5正式入口；接入前需登记新工程合同和正式train/val_select/val_compare范围索引，保留旧证据，不能绕过冻结代码检查。

## E2E-G5-SG48-PAUSE-20260919：首个SG完成，按用户要求暂停

- 证据根：`outputs_e2e/unified/e2e_g5/20260919_approved/`。首seed 20260921 的 SG 完成48轮、49152次更新；`training/20260921/sg/report_epoch48.json`为PASS。选中epoch48，val_select GOSPA=54.5959256018 m；`budget_candidate=false`，虽然best在末轮，但44至48净改善不足0.5 m，不满足全部延长条件。无E2E结果，不作反馈增益结论。
- 用户将执行范围改为首SG结束后停止。监测在完整报告落盘后终止原训练PID42704，监督程序非零退出4294967295为主动停止，不是训练数值失败，原日志保留。`user_pause_audit.json`判定`SG48_COMPLETE_STOPPED`：epoch0—48历史完整、每2轮验证、last/best身份及历史匹配、其他轨道文件为空、全输入复核PASS（`identity_user_pause.json`）、异常事件0、test未读取。没有六轨完成或最终比较证据。
- SG主体用时15092.9086603秒（4.1925小时）；其中训练14766.3504308秒，轮间验证318.7660738秒，剩余保存/调度等7.7921557秒（不含epoch0及建模等前置）。训练每epoch平均307.6323秒，范围290.9154—343.3876秒。训练阶段进程总墙钟15113.6800秒。最佳checkpoint SHA256：`e96b113579bfa1c5eca1870f0986ab9e74e8c8b5e01a8d87795aa2d22518e0a4`。
- 换入读取量7269285691520字节（6.6114 TiB），命中23891次/未命中574849次，命中率3.9902%；读取计时6929.3369611秒，占SG主体45.9112%。该字段包含SHA，不是纯磁盘时间；`sha_seconds=0`表示没有独立计时，不表示校验零成本。重复逻辑读取量不等于物理磁盘流量。train三类特征合计36.1781 GiB，共享缓存2 GiB。
- 短窗口资源采样：`performance_observations/20260919_214620/`，55.62秒、12点GPU利用率8%—91%，算术均值44.42%；进程CPU计时折算单核19.81%、整机28逻辑核0.707%。仅为短窗口和操作系统计数，不代表全程平均，不足以解释低CPU计数根因。
- **耗时外推而非测得结果**：按SG实测同速，六轨48轮主体约25.15小时，六轨64轮约33.54小时；已完成SG后剩余约20.96/29.35小时。运行开始至正式训练前约3.79小时（包含准备、校验、试跑、修复及等待），从零起本次流程约29/37.5小时，另为末端比较/审计预留0.5小时；实际E2E全轨耗时与延长数量未知。建议恢复时给剩余时间另留15%余量，即约24.6小时（全48）或34.3小时（全64），不是新的预算授权。已有数据无需再生成，原预算截止2026-09-21 14:13:23（本地时间），暂停不自动扣除。
- 初步瓶颈：随机顺序下4样本分片通常只取其中1个样本，整片读取校验造成近4倍读取放大；2 GiB难以覆盖36.18 GiB特征。前向按需同步加载三类特征，无批次预取；小batch、CPU匹配及同步也可能贡献空隙，尚未作完整算子剖析。没有在线生成细DPD，checkpoint保存不是当前主要耗时。
- 工程交付：根目录`运行入口/E2E/G5/G5一键继续.py`及`运行入口/E2E/G5/G5手动运行说明.md`；静态检查、重复进程拒绝、模拟预算过期/已有评价拒绝通过，停止后的真实`--check-only`通过（未训练、未评价）。完整恢复流水线未执行。当前先暂停，后续由用户决定是否恢复原版或先审批等价加速；不改变冻结训练代码。

## 记录原则

## E2E-G5-PILOT-20260919：准备完成，预算试跑STOP_RESOURCE

- 后续审批与恢复：用户明确批准48小时总墙钟上限，原开始时间和其余配置不变。原STOP_RESOURCE报告保留在`budget36_evidence/pilot_report.json`，当前`pilot_report.json`是复用同一实测量、计入审批等待耗时后的预算重判，不是重新试跑或篡改原门禁。差异见`budget_amendment.json`；输入复核使用新报告`identity_budget48.json`。
- 类型：执行前工程与资源验证，不作为模型性能结论。证据根`outputs_e2e/unified/e2e_g5/20260919_approved`，主报告`pilot_report.json`、`feature_manifest.json`、`identity_before_pilot.json`及`execution_status.json`。
- 4096/512/1024特征准备PASS；新生成3840条固定细DPD。归档首次因PyTorch动态模块虚拟文件名失败，已最小修复，新归档与输入复核PASS，原失败日志保留。未读取test。
- 两轨128步：SG 42.2027915秒，E2E 36.5717063秒；512条验证分别12.727892秒、12.3836578秒；同初始化摘要相同，保存/恢复后的下一步loss、梯度及参数摘要完全一致。短测顺序和系统缓存可能影响计时，不能据此断言E2E天然更快。
- 准备及已耗时12868.799秒；按六轨全部64轮及每轨33次验证投影136352.471秒（37.876小时），对剩余部分加15%预留后154875.022秒（43.021小时），超过36小时合同。新增文件66880383997字节（约62.29 GiB），试跑两轨GPU已分配峰值约2.193/2.198 GiB。
- 判定STOP_RESOURCE；恢复入口正常执行停止规则，正式六轨训练、比较和最终审计均未执行。未出现读取异常事件文件。保留试跑权重，但不得作为正式训练初始化；试跑预测指标不作科学证据。
- 待决策：保持4096条、三seed六轨及48/条件64轮不变，申请扩大墙钟预算；或另行审批等价工程优化与新预算试跑。不得把只跑48轮的预估替代已批准的最坏64轮准入条件。

### 以下为通用记录原则

- 只记录实际运行实验、读取原始结果文件或明确来自用户反馈的事实，不编造指标、路径、权重或结论。
- 每条记录应标注运行类型、入口脚本、数据与结果路径、关键配置、指标、证据来源、可信度和复核需求。
- 用户口述结果标注“用户反馈/待复核”。
- 正式训练、完整评估、重要只读诊断、仅重绘或导出以及冒烟测试必须明确区分。
- 仅验证运行链路、结果字段或工程不变量的冒烟测试默认写入 `修改记录.md`，不作为性能证据。
- 单独执行 Python 语法编译、Ruff、MATLAB Code Analyzer 或代码结构检查，不作为实验结果。
- 声称结果可用于论文前，至少应能追溯实验设置、随机种子、数据、代码版本或指纹、原始输出和图表生成方式。
- 每项重要实验使用稳定实验 ID；其他文档通过实验 ID 或相对路径引用，不复制完整指标表。
- 运行完成时记录的文件数量、目录体积和哈希属于当时快照；后续若经审批清理无效数据或逐字节重复载荷，保留结构化报告、日志、manifest、唯一有效副本和清理记录，不把清理后的目录现状误写成原始运行状态。
- 2026-08-27 经审批统一清理未被模型选择的 `last_*.pth` / `last_*.pt`；各实验中关于 `best` / `last` 状态比较的结论仍是清理前已完成回读的历史快照，当前仅保留被选择的 `best` / frozen checkpoint 与结构化比较结果。
- 2026-08-29 经审批清理暂停的 S2-G5-R6 大型半成品、当前重新产生的7个未选 `last` checkpoint及工程 smoke/cache；中止R6不构成性能证据，保留其JSON/CSV、日志和监控报告。既有正式结果中的目录体积、资源峰值及 `best` / `last` 比较继续表示清理前快照。

## 当前结果状态

| 字段 | 内容 |
|---|---|
| 已核验正式实验 | E2E已完成G0—G4；G3支持小规模跨seed反馈收益，G4选择A2部分解冻；尚无冻结test最终论文性能结论。 |
| 已核验重要验证 | 新增`E2E-LOADER-OPT-20260919`：512条读取筛选及两轨各256步更新等价通过，1.3倍速度目标未通过。历史Gate判定保持不变。 |
| 已核验结果文件 | G4见`outputs_e2e/unified/e2e_g4/20260905_164500/final_report.json`及`final_audit_report.json`；本轮见下文加载器记录。 |
| 用户反馈结果 | 暂无。 |
| 当前边界 | G5仅为待审批方案；未执行G5或test，加载器短测不证明历史异常根因已解决，也不恢复旧失效训练。 |

## E2E-LOADER-OPT-20260919：有界同字节校验加载优化

运行类型：用户批准的工程吞吐与训练更新等价验证，不是模型性能Gate。状态`CORRECT_BUT_SPEED_TARGET_UNMET`。

- 入口：`统一模型代码/common/diagnostics/optimize_feature_loader.py`；加载器：`统一模型代码/common/verified_feature_loader.py`。
- 输入：`outputs_e2e/unified/e2e_g4/20260905_164500`的train冻结边界特征，取前512条，包含此前历史异常诊断选取范围；三类特征96个16条分片。只用于加载路径诊断，不恢复G4旧失效训练。
- 输出：`outputs_e2e/unified/loader_optimization/20260919_01`；`contract.json`登记入口及加载器代码SHA；`shards_manifest.json`登记旧/新分片身份、逐样本摘要与原文件stat；`final_report.json`记录全部筛选、训练和复核结果。
- 新生成384个4条分片，写入后回读逐值相等再发布；原数据不修改。所有读取均核对登记身份。
- 筛选：相同512条、batch4、128个随机batch，各方案两次，第二轮逆序执行；不清空Windows文件缓存，因此不是裸盘冷读测速。计时不含逐样本诊断摘要计算。

| 加载方案 | 两次读取秒数 |
|---|---:|
| 原mmap | 1.5148 / 1.4915 |
| 旧16条分片＋256 MiB校验LRU | 62.3286 / 62.1516 |
| 按batch合并＋16条分片＋1 GiB | 40.0416 / 40.2358 |
| 按batch合并＋4条分片＋1 GiB | 11.0333 / 10.9950 |
| 按batch合并＋4条分片＋2 GiB | 9.1336 / 9.1444 |

4条/1 GiB换入比例超过50%且读取慢于mmap的1.3倍，按预案补测2 GiB并选择该候选。相对旧LRU读取时间约减少85.3%，仍明显慢于mmap；这不是完整训练加速倍率。

真实训练复用G4 A2配置、seed20260907及登记G3初始化，两个相同随机顺序epoch共256步。输入、loss、梯度范数逐步完全一致，初始/最终模型摘要一致；第64/128/192/256步checkpoint内全部模型和优化器状态逐值相等。阶段后96个原分片与384个新分片SHA通过，原分片stat不变；未复现异常。

| 训练项 | mmap | 优化校验LRU |
|---|---:|---:|
| 256步墙钟（秒） | 43.7887 | 59.6676 |
| 进程RSS采样峰值（GiB） | 6.288 | 3.794 |
| 系统可用内存采样最低（GiB） | 13.783 | 16.146 |
| GPU分配峰值（GiB） | 2.199 | 2.198 |

LRU实际占用峰值约1.999 GiB；训练期读取累计约23.268 GiB，命中1055次、换入1975次。分片读取/哈希/解析分别约5.252/9.549/0.190秒，SHA计算而非NPY解析是主要额外开销之一。RSS是进程采样值，不是Windows全部缓存占用，也不是连续峰值监测。

完整任务406.954秒。训练耗时比1.362626，比原1.3目标高0.062626，因此不判PASS；与旧诊断2.739倍的结果不能作严格同条件训练对比。本次训练只各计时一次、mmap先跑，存在运行顺序和系统负载影响，不提供长运行吞吐保证。

结论：保留“校验与使用同一份bytes”及有界懒加载，候选为4条分片/2 GiB。建议G5明确接受当前开销后做新规模成本投影，不继续无限微调。未执行G5、未读取test、未追查到历史根因；PID写入事件跟踪本轮未启用。此前失败训练仍作废。

## CH4-G3A-20260821：第四章 Gate 3A 中等规模可重复性验证

| 字段 | 内容 |
|---|---|
| 实验 ID | `CH4-G3A-20260821` |
| 类型 | 重要可重复性验证与资源诊断；非正式性能实验。 |
| 入口 | `main31_loc_v3.m`、`gen_ch4_loc_data.py`、`train_yolo.py`、`gate3_ch4.py`、`gate3_stage_runner.py`。 |
| 数据与配置 | IQ/定位 train/val/test=`1024/256/256`；随机种子 MATLAB=`20260822`、Python=`42`；D8 `dualhead`、`dice_weight=0`、no-AMP、batch/val batch=`8/8`；pilot 训练 5 epoch，重复 2 次。 |
| 结果路径 | `outputs/gate3_ch4/20260821_152344`；总报告 `gate3a_gate_report.json`；完整定位数据与审计在 `05_full_loc_after_encoding_fix/`。 |
| 代码版本或指纹 | 当前未提交工作树；结构化 run config、完整 checkpoint、命令、日志和资源 CSV 均在运行目录。未建立 Git commit 指纹。 |
| 数据完整性 | N=2/3 各半；1536 个 IQ 样本哈希全部唯一且跨 split 无泄漏；定位任务共 1536 条、12 个分片、3,951,967,293 bytes；pilot/full 前缀全部张量和索引字段最大绝对差为 0。 |
| 可重复性结果 | 两次 5 epoch 运行的逐 epoch 指标、模型、优化器、调度器、随机状态及最终验证完全一致；三类 mismatch 数均为 0。 |
| 资源结果 | 完整 DPD train/val/test 总墙钟 839.130 s；纳入门禁的最低系统可用内存 13,033,127,936 bytes、峰值进程树 RSS 2,665,586,688 bytes、峰值系统总 GPU 已用显存 6957 MiB；无 warning/red flag。 |
| 预算结果 | pilot 稳态 epoch 中位数最大值 1.45815 s；按样本量 8 倍和 60 epoch 外推 699.912 s，即 11.665 min。该结果未实跑 60 epoch。 |
| 异常与证据保留 | 首次确定性比较 `CRASHED`，首次完整 DPD train 因日志编码/管道问题在 1500 s `TIMEOUT`；均经用户审批最小修复后在全新目录通过，失败目录未删除或覆盖。 |
| 结论 | Gate 3A=`PASS`：中等规模数据链、D8 pilot 完全确定性和本机资源可行性成立，可进入 Gate 3B 方案审批。 |
| 适用边界 | 不证明 60 epoch 实际耗时、收敛、泛化定位误差、论文图表、论文指标或端到端 predicted K；随机初始化/短训练 RMSE 不作性能解释。 |
| 复核状态 | 总门禁回读通过；25 个纳入判定的监控阶段均为 PASS，无 warning/red flag；formal 第四章目录不存在；交付前复核无遗留 Python/MATLAB 实验进程。 |
| 后续证据瘦身 | 2026-08-24 经审批删除 Gate 3A pilot 与完整定位数据逐字节相同的 `03_pilot_loc/data/train/loc_train_000.pt`，以及 `01_gate2_regression` 内与 Gate 2 成功运行逐字节相同的定位分片、pilot 分片和 D8 checkpoint；完整定位数据、Gate 2 成功副本、index、JSON/CSV、日志和总报告保留。上述原始体积与文件数仍表示运行完成时快照。 |

## CH4-G3B-20260821：第四章 Gate 3B 小规模完整训练与严格重复性验证

| 字段 | 内容 |
|---|---|
| 实验 ID | `CH4-G3B-20260821` |
| 类型 | 缩减数据上的完整训练、严格可重复性验证和独立 held-out 评估；非论文正式配置性能实验。 |
| 入口 | `train_yolo.py`、`eval_ch4_checkpoint.py`、`gate3_stage_runner.py`、`gate3b_ch4.py`。 |
| 数据与配置 | 复用 Gate 3A 已审计定位数据 train/val/test=`1024/256/256`、8/2/2 分片；D8 `dualhead_std`、FP32/no-AMP、batch/val batch=`8/8`、AdamW、初始学习率 `1e-3`、CosineAnnealingLR、weight decay `5e-3`、dropout `0.4`、60 epoch、patience 60、seed 42、deterministic。 |
| 结果路径 | `outputs/gate3b_ch4/20260821_173526`；总报告 `gate3b_gate_report.json`。Gate 3A 历史根 `outputs/gate3_ch4/20260821_152344` 保留原名和原位，仅被只读复用。 |
| 代码版本或指纹 | 当前未提交工作树；总报告记录 `train_yolo.py`、`eval_ch4_checkpoint.py`、`gate3_stage_runner.py` 和 `gate3b_ch4.py` 的路径、大小和 SHA256。 |
| 数据完整性 | 12 个定位分片和 3 个 index 共 3,951,967,293 bytes；训练前后15个文件的大小和 SHA256 完全一致。 |
| 训练结果 | A/B 均完成60 epoch且未提前停止；墙钟分别 681.222/683.018 s。best epoch 均为57，best validation oracle-K RMSE均为150.791921 m，最终学习率均为 `1e-6`。 |
| 学习判定 | 未训练模型 validation loss/RMSE 为 7.011946/1075.637084 m；冻结 best 为1.016160/150.791921 m，两项均严格改善，A/B均为 `LEARNING_CONFIRMED`。 |
| 严格重复性 | 排除耗时和资源字段后，60 epoch历史、初始/最终validation、summary关键字段、best/last checkpoint中的模型、optimizer、scheduler、scaler及RNG状态全部逐值一致，所有 mismatch 列表为空。 |
| 冻结 checkpoint | 只依据 validation 从训练A选取 epoch 57；`best_yolo_dualhead_std.pth` SHA256=`8cf7f9541b3269539b7b16580d7a13dd2b9a9770afa1b0032b3ec491844f0153`；test 未参与选择。 |
| validation oracle-K | 两次独立进程完全一致并与训练 best 完全一致：RMSE 150.791921 m，mean 35.004812 m，median 4.695417 m，within 10/30/50 m=`0.6859375/0.878125/0.93125`。 |
| test oracle-K | 两次独立进程完全一致：loss 0.999282，RMSE 110.565991 m，mean 26.120635 m，median 4.740854 m，within 10/30/50 m=`0.7171875/0.8953125/0.93125`。N=2/N=3 RMSE为81.613676/126.230752 m。 |
| 资源结果 | 14个阶段监控报告均PASS且无warning/red flag；最低系统可用RAM 14,255,198,208 bytes，峰值进程树RSS 3,764,129,792 bytes，峰值系统总GPU已用显存7388 MiB；报告前输出615,897,001 bytes。 |
| 总门禁 | `PASS_WITH_MORE_EPOCHS_RECOMMENDED`；工程、重复性、有效学习、独立评估、数据完整性和隔离均通过，但best epoch=57落在最后5个epoch，趋势为 `STILL_IMPROVING_AT_LIMIT`。 |
| 适用边界 | 全部定位指标使用真实源数决定Top-K，是oracle-K；数据规模和配置不等于论文正式实验，未验证predicted-K、论文图表、论文指标、全量泛化或当前创新性。原始test JSON中的 `metrics.val_loss` 表示当前test split loss，总报告已映射为 `test_loss`。 |
| 复核状态 | 总报告回读、代码语法/Ruff、数据前后SHA、formal目录隔离和残留实验进程检查均通过；未发生崩溃、超时、自动重试或失败目录复用。 |

## CH3-S2G1-20260821-A：第三章首次缩减数据生成与完整性失败

| 字段 | 内容 |
|---|---|
| 实验 ID | `CH3-S2G1-20260821-A` |
| 类型 | S2-G1缩减数据生成、资源诊断和数据完整性审计；未进入训练。 |
| 入口 | `main30.m`、`s2g1_ch3.py`、`gate3_stage_runner.py`。 |
| 数据与配置 | train/val/test=`2048/512/512`；0/1/2/3源=`20%/20%/30%/30%`；seed=`20260823`；19子带、81×81粗网格。 |
| 结果路径 | `outputs/s2g1_ch3/20260821_204124`。 |
| 代码身份 | Git HEAD=`a18e36a`，工作树含本次已批准的增量S2-G1代码和文档修改；预检保存完整状态，未创建提交。 |
| 预检 | `PASS`；31次采样，可用RAM中位数16,169,549,824 bytes、最低16,060,497,920 bytes，GPU总占用峰值4131 MiB，磁盘最低545,248,833,536 bytes；仅有低于16 GiB提示，未触及14 GiB启动线。 |
| 数据生成 | MATLAB退出码0，墙钟537.635 s，内部总耗时529.8 s；三文件共1,241,624,245 bytes；类别数量精确符合410/410/614/614和102/102/154/154。 |
| 资源 | 数据生成最低系统可用RAM13,268,566,016 bytes，峰值进程树RSS2,795,159,552 bytes，GPU总占用峰值5250 MiB，输出峰值1,241,637,071 bytes；无warning/red flag。 |
| 数据失败 | train样本索引1989为2源样本，最小源间距124.982101 m，小于声明约束300 m；val/test最小间距分别301.039581/300.300293 m。结构化 `data_audit.json`=`FAIL`。 |
| 原因 | 原始 `gen_multi_source_pos_v2.m` 在5万次约束放置失败后，以不检查源间距的随机位置补齐；不是第一阶段路径/设备修改造成。 |
| 总判定 | `FAIL_DATA_INTEGRITY`；按预先规则停在数据层，不自动修复、重生成或进入训练。 |
| 未执行 | batch 64容量检查、两次100 epoch训练、checkpoint冻结、validation和test均未执行。 |
| 适用边界 | 只证明当前受控数据生成资源可行并暴露原生成器边界；不能判断第三章收敛或性能。 |
| 后续证据保留 | 2026-08-24 经审批删除三个不合格 MAT 文件，共1,241,624,245 bytes；运行根、`data_audit.json`、`data_manifest_before.json`、阶段报告、资源CSV和日志保留。上方数据生成体积、类别数量和违规样本事实是清理前已完成审计的历史快照，原始 MAT 已不可直接回读。 |

## CH3-S2G1-20260822-B：源间距门禁修复后的资源预检停止

| 字段 | 内容 |
|---|---|
| 实验 ID | `CH3-S2G1-20260822-B` |
| 类型 | S2-G1全新目录重跑的启动资源预检；未执行数据生成或训练。 |
| 结果路径 | `outputs/s2g1_ch3/20260822_120601`。 |
| 代码状态 | S2受控路径已增加300 m后验检查、最多100次位置重试和重试元数据；共享 `gen_multi_source_pos_v2.m` 未修改。 |
| 预检结果 | `FAIL`；60秒采样的可用RAM中位数低于14 GiB启动线。失败JSON未保留精确中位数；预检结束后的只读快照为空闲RAM约13.861 GiB。 |
| 过程边界 | 未启动MATLAB，未生成train/val/test，未启动容量检查或训练；结束后无S2-G1残留实验进程。 |
| 总判定 | `BLOCKED_RESOURCE_PREFLIGHT`；属于当前系统资源不足，不是数据、算法或模型失败；不自动重试。 |

## CH3-S2G1-20260822：第三章缩减数据收敛与严格重复验证

| 字段 | 内容 |
|---|---|
| 实验 ID | `CH3-S2G1-20260822` |
| 类型 | 重要缩减数据收敛、确定性和独立重载验证；不是论文正式配置实验。 |
| 入口 | `main30.m`、`s2g1_train_ch3.py`、`s2g1_ch3.py`、`gate3_stage_runner.py`。 |
| 数据与配置 | train/val/test=`2048/512/512`，MATLAB seed=`20260823`；Transformer、M=10、FP32、batch/val batch=`64/64`、Python seed=`42`、100 epoch、warmup 5、patience 25、固定阈值0.5。 |
| 结果路径 | `outputs/s2g1_ch3/20260822_122055`；总报告 `s2g1_report.json`。 |
| 数据完整性 | 三文件共1,241,665,513 bytes、3072条样本无重复或跨split泄漏；train/val/test最小源间距为301.627/300.396/303.188 m，标签复算不一致均为0；train拒绝1个违规布局并重试1次。训练前后文件SHA256完全一致。 |
| 容量检查 | batch 64前向、loss、反向和optimizer step通过；PyTorch峰值allocated/reserved约3.42/5.23 GiB，监控GPU总占用峰值8343 MiB。 |
| 训练与重复性 | A/B均完成100 epoch，内部耗时885.050/820.310 s；100个epoch科研字段、best epoch及best/last的模型、optimizer、scheduler和RNG状态逐值一致，scientific mismatch=`0`。 |
| 冻结模型 | 训练A第96 epoch；SHA256=`22efb069102fb2e2c083d2897528610986e3031049222efd7875b7df70a190fb`，大小10,052,783 bytes；仅由warmup后最低validation loss选择，test未参与。 |
| Validation | 两次独立重载完全一致：loss=0.002593，count accuracy=0.90625，balanced=0.91546，count MAE=0.09961，0源虚警率=0.00980，活跃槽位band macro-F1=0.93296。 |
| Test | 仅运行一次：loss=0.003085，count accuracy=0.88867，balanced=0.89919，count MAE=0.12109，漏计/多计率=0.05078/0.06055，0源虚警率=0，活跃槽位band macro-F1=0.92720。 |
| 学习与收敛门禁 | validation loss相对初始化下降0.98525，balanced accuracy和band macro-F1分别提高0.91546/0.69146；末10轮相对此前最佳改善0.00431，小于1%，两项门禁均通过。 |
| 资源与时长 | 60秒预检RAM中位数/最低约19.45/19.16 GiB；全阶段最低可用RAM14,768,390,144 bytes，进程树RSS峰值3,085,451,264 bytes，GPU总占用峰值8840 MiB；目录1,283,101,317 bytes。14个监控阶段合计2326.277 s，另有62.341 s预检；无warning/red flag。 |
| 总判定 | `PASS`；第三章在当前缩减配置下能够稳定学习、严格重复并独立重载。 |
| 适用边界 | 单一缩减数据、单一固定seed和固定阈值；不证明跨seed统计稳定性、数据规模充分性、论文全量指标或当前创新性。 |
| 复核状态 | 总报告、数据前后SHA、checkpoint SHA、两次validation、单次test、代码指纹、14份监控报告和残留进程均已回读；无自动重试或目录复用。 |

## CH4-S2G2-20260822：第四章缩减数据充分训练诊断与接口checkpoint建立

| 字段 | 内容 |
|---|---|
| 实验 ID | `CH4-S2G2-20260822` |
| 类型 | 缩减数据单次200 epoch充分训练诊断、统一validation checkpoint比较和独立重载；非论文正式性能实验。 |
| 入口 | `train_yolo.py`、`eval_ch4_checkpoint.py`、`s2g2_ch4.py`、`gate3_stage_runner.py`。 |
| 数据与配置 | 只读复用Gate 3A定位数据train/val/test=`1024/256/256`；D8 `dualhead_std`、FP32/no-AMP、batch/val batch=`8/8`、AdamW、lr=`1e-3`、CosineAnnealingLR `T_max=200`、patience 200、seed 42、deterministic；未运行test。 |
| 结果路径 | `outputs/s2g2_ch4/20260822_143749`；总报告`s2g2_report.json`。 |
| 数据完整性 | 15个文件共3,951,967,293 bytes，训练前后大小和SHA256完全一致；正式data/output目录不存在。 |
| 训练结果 | 完成200/200 epoch、每轮128 step，墙钟2383.900 s；初始validation loss/RMSE=7.011946/1075.637084 m，best位于epoch 93，loss/RMSE=1.086389/130.726516 m；最后20轮无新增收益，`TREND_CLOSED`。 |
| 训练诊断 | epoch 93后train loss由0.873114继续降至0.703357，而最终val loss/RMSE升至1.346190/233.486805 m，记录为可能过拟合模式；仅作描述，不改变预设checkpoint选择规则。 |
| checkpoint比较 | 同一validation评估器下，Gate 3B epoch 57 RMSE=150.791921 m，新epoch 93 RMSE=130.726516 m，降低13.31%，故判定`NEW_200_SELECTED`。冻结SHA256=`5d5224d8d6478739af5042391ef1e718c08e2f81565882e133372c9fbd116dfd`。 |
| 长尾诊断 | Gate 3B→新权重：median=4.695417→5.421757 m，P90=37.247806→38.947443 m，P95=84.823923→89.758282 m，>500 m错误数=15→9，最大误差=1749.104492→1695.470215 m；640源中293改善、347变差，说明RMSE收益主要来自减少部分灾难性错误，并非普遍精度提升。 |
| 分层结果 | 新权重N=2/N=3 RMSE=73.015178/157.886750 m；overall mean=30.875801 m，within 10/30/50 m=`0.678125/0.8609375/0.91875`。 |
| 独立重载 | 冻结checkpoint第二次独立validation的聚合指标、P90/P95和640条逐源误差与首次评估完全一致，`checkpoint_reload=EXACT`。200 epoch训练本身未做第二次严格重复。 |
| 资源 | 训练峰值系统GPU已用6621 MiB，进程树RSS峰值3,764,576,256 bytes，最低系统可用RAM14,280,052,736 bytes；4份有效监控报告均PASS且无warning/red flag，报告前输出309,951,692 bytes。 |
| 工程异常 | 首次S2预检因Git输出使用Windows默认代码页而在训练前失败，修复为显式UTF-8后使用全新根目录；最终根中的首次Gate 3B评估又因手写历史时间戳错误立即失败，保留失败目录后按checkpoint SHA定位真实路径并在新目录通过。总门禁显式区分4份PASS证据与1份保留失败尝试。 |
| 总判定 | `PASS`；缩减配置的200 epoch趋势已关闭，并建立可用于S2-G3的接口checkpoint。 |
| 适用边界 | 仅限固定缩减数据、单seed、oracle-K和一次200 epoch训练；不证明跨seed稳定性、正式训练epoch、predicted band/K、论文指标或当前创新性。 |
| 复核状态 | 总报告、输入前后SHA、两候选统一validation、冻结SHA、第二次重载、4份PASS监控、失败证据、formal隔离和残留进程均已回读；S2-G2未生成test结果。 |

## SYS-S2G3-20260824：跨章可组合性、分布契约与频带表示上限审查

| 字段 | 内容 |
|---|---|
| 实验 ID | `SYS-S2G3-20260824` |
| 类型 | 重要跨章兼容性与表示上限诊断；不是论文正式端到端性能实验。 |
| 入口 | `s2g3_compute_coarse_dpd.m`、`s2g3_composability.py`、`gate3_stage_runner.py`；复用原第三章DPD/Transformer和第四章D8实现。 |
| 固定输入 | 只读使用Gate 3A的256条validation共享IQ和定位数据，N=2/N=3各128条；第三章epoch 96 checkpoint SHA256=`22efb069102fb2e2c083d2897528610986e3031049222efd7875b7df70a190fb`；第四章epoch 93 D8 checkpoint SHA256=`5d5224d8d6478739af5042391ef1e718c08e2f81565882e133372c9fbd116dfd`。 |
| 固定配置 | 第三章Transformer、M=10、阈值0.5，预测K保留0–10；第四章D8 `dualhead_std`、FP32/no-AMP、batch 8；GOSPA `p=2,c=100 m,alpha=2`；未调阈值。 |
| 结果路径 | `outputs/s2g3_composability/20260824_132001`；解释修正版总报告`s2g3_report_interpretation_fix.json`，SHA256=`ba55cc2e9b7ca44cc883db19687cd0d7f5e49b899e27bca9b823a85f61095610`；初版总报告保留为被替代证据。 |
| 代码身份 | 新增MATLAB入口SHA256=`fc453f9cb0bd3e828404658f2a830f0273f0d24502a6066a822718cf1533b8a3`；Python编排入口最终SHA256=`d29dd7d901a5eaa444505727a9603c4d7e34d710f8ff8d2c7f0ec75146a73cc5`；原训练和模型入口未修改。 |
| 数据身份 | 第四章raw validation IQ SHA256=`6d1749ae89c00c5ce863e8e38e8007fd692b63ea58ad8e8aecb34328d509e9bb`；粗DPD `coarse_val.mat` SHA256=`be1cd6b376d2e54f894d60a9fe7c8b8f142e324ae9b7efcbade2b036e11a5212`；样本索引严格为0–255且全部`group_idx=0`。 |
| 分布契约 | 阵列、载频、采样率、4096点观测和19子带等基础参数一致；第三章固定10 MHz symbol rate/13 MHz实际带宽，第四章实际带宽约2.665–25.935 MHz；两章还在频率重叠拓扑、位置半径/间距、功率范围和SNR定义上存在分布或定义差异。 |
| 第三章跨分布计数 | 两次独立推理的logits、概率、频带和K逐元素一致；accuracy/balanced accuracy=`0.691406/0.691406`，Wilson 95%下界=`0.632342`，N=2/N=3准确率=`0.492188/0.890625`，MAE=`0.3125`，K=0/K>3比例=`0.003906/0`。 |
| 第三章跨分布频带 | 活跃槽位precision/recall/F1=`0.889203/0.854427/0.871468`；物理合并IoU中位数=`0.753765`，相对全频带平凡基线的bootstrap 95%下界=`0.477717`；真实频带覆盖率中位数=`1.0`，空频带比例=`0.003906`。全部预设模型门通过。 |
| Exact-Oracle回归 | 新评估器最终精确回归S2-G2：640条匹配误差，RMSE=`130.72651561958395 m`，绝对差为0；`recall@100 m=0.95625`。原S2-G2入口独立复算也得到完全相同RMSE。 |
| Coarse-Oracle表示 | 按第三章真值19子带标签映射回FFT频带后，无空频带；细DPD SHA256=`a968afe4514bbbbfee20650112a197d7d7e86f73e2ff9e0e47f29308d886802e`。oracle-K匹配RMSE=`164.148382 m`，相对Exact比值=`1.255662`；`recall@100 m=0.95`，下降0.00625。 |
| 表示门 | Exact回归、样本完整、非空频带和100 m召回门通过；RMSE增幅超过10%门槛，因此`representation_gate_pass=false`。退化主要表现为长尾放大，不能仅凭接近的100 m召回判定接口充分。 |
| 条件四轨 | 因表示门失败，按预案未生成predicted-band细DPD，未运行`OB-PK/PB-OK/PB-PK`；`OB-OK`只作为Exact回归，`CO-OK`只作为表示控制。 |
| 资源 | 有效60秒预检RAM中位数/最低约14.81/14.74 GiB、GPU总占用峰值3747 MiB；15份PASS阶段监控合计317.463 s，最低系统可用RAM14,043,648,000 bytes，进程树RSS峰值1,686,446,080 bytes，GPU总占用峰值4549 MiB；运行根188,781,798 bytes，无warning/red flag。 |
| 工程修复与失败证据 | 保留2份未作为通过证据的监控失败：预检被外层包装器误识别为残留进程、完整粗DPD首次引用错误历史路径；另有一次监控采样间隔参数在子进程启动前被拒绝。Exact评估先后修正逐样本归一化归约顺序和float32距离口径，最终精确回归；旧评估输出均保留为被替代证据。 |
| 禁止范围核对 | 未生成新IQ，未训练或微调，未运行test，未调整阈值，未修改模型、DPD定义或原训练入口；结束后无S2-G3残留Python/MATLAB进程。 |
| 总判定 | 工程`PASS`；科学状态`REQUIRES_INTERFACE_REDESIGN`。第三章冻结模型通过跨分布门，但当前19子带映射与冻结D8的组合不满足预设定位长尾门槛；本实验不能单独区分接口信息损失和下游输入分布失配。 |
| 后续决策 | 在继续完整端到端四轨前，先单独审批连续频带边界、概率/更细频率表示或下游接口适配的oracle pilot；本结果不得表述为论文端到端性能、信息论上限或两章方法失效。 |

## SYS-S2G4-20260824：Coarse-Oracle D8适配的数据支持域停止审查

| 字段 | 内容 |
|---|---|
| 实验 ID | `SYS-S2G4-20260824` |
| 类型 | S2-G4身份/资源预检、工程pilot和完整元数据支持域诊断；在数据门停止，不是模型训练或性能实验。 |
| 入口 | `s2g4_resource_preflight.py`、`s2g4_coarse_d8.py`、增量S2-G4训练入口和`gate3_stage_runner.py`。 |
| 固定输入 | 只读复用Gate 3A train/validation=`1024/256`原始IQ和Exact定位数据；Exact-D8 epoch 93 SHA256=`5d5224d8d6478739af5042391ef1e718c08e2f81565882e133372c9fbd116dfd`；第三章10 MHz子带、0.3正标签覆盖阈值保持不变。 |
| 结果路径 | `outputs/s2g4_coarse_d8/20260824_164707`；停止报告`s2g4_stop_report.json`，频带支持明细`band_support_audit.json`。 |
| 预检与pilot | 轻量60秒RAM预检中位数21,531,000,832 bytes，身份预检通过；train/validation各8条Coarse数据构造通过，一次真实forward/loss/backward/optimizer step、validation、保存和重载通过，仅证明工程链。 |
| 数据门失败 | 完整train构造在样本205触发空Coarse-Oracle频带断言；该样本两个源的symbol rate为2.95/2.45 MHz，均无法达到正标签所需的3 MHz最小主瓣宽度。未回退Exact频带、未把ignore改为正带、未调整阈值。 |
| 完整支持域审计 | train共1024样本、2560有效源，其中154源无任何正子带，且与154个`symbolRate<3 MHz`源完全一致；有1个整样本为空。validation共256样本、640有效源，其中38源无正子带且同样与`symbolRate<3 MHz`完全一致；因同样本其他源提供正带，整样本空频带数为0。 |
| 结构结论 | `REPRESENTATION_SUPPORT_GAP_CONFIRMED`：当前第三章正标签接口不覆盖第四章完整2–20 MHz symbol-rate域；S2-G3只检查联合频带非空，曾掩盖validation中38个未被表达的窄带源。 |
| 资源 | 触发科学停止的完整构造尝试最低可用RAM18,036,236,288 bytes、进程树RSS峰值1,934,385,152 bytes、GPU总占用峰值6132 MiB、输出峰值328,851,679 bytes，无warning/red flag；失败不是资源超限。 |
| 未执行 | 完整Coarse train/validation、200 epoch从头训练、60 epoch微调、六项Exact/Coarse交叉评估、test、predicted-band/K及CH3修改/训练均未执行。 |
| 总判定 | `STOPPED_AT_DATA_GATE`；工程`PARTIAL_PASS`。原科学问题“D8分布失配还是19子带信息不足”为`INCONCLUSIVE_FOR_D8_VS_REPRESENTATION`，不得用本次结果宣称D8适配成败。 |
| 后续决策 | 另行审批小型oracle接口定义pilot，比较当前正标签、正标签加ignore、物理相交硬标签、软重叠权重和Exact连续边界；先闭合支持域，再恢复D8适配诊断。 |
| 复核状态 | 停止报告、全量元数据审计、pilot 输出、监控报告和原始输入身份已在运行完成时回读；后续存储清理见下一行。 |
| 后续证据瘦身 | 2026-08-27 经审批删除 `pilot_data`、`pilot_train_run`、`coarse_data` 和 `coarse_data_after_wrapper_fix`；停止报告、频带支持审计、监控 JSON/CSV 与日志继续保留。本条目中的 pilot 与资源事实是删除前已完成回读的历史快照。 |

## SYS-S2G4-R1-20260824：0.2阈值Coarse-D8快速适配诊断

| 字段 | 内容 |
|---|---|
| 实验 ID | `SYS-S2G4-R1-20260824` |
| 类型 | 0.2 Coarse-Oracle支持域、完整缩减数据、单次200 epoch D8训练和EE/EC20/CC20三轨诊断；非论文正式性能实验。 |
| 入口 | `s2g4_resource_preflight.py`、`s2g4_coarse_d8.py`、`train_yolo.py --s2g4_scratch`和`gate3_stage_runner.py`。 |
| 固定输入 | 只读复用Gate 3A train/validation=`1024/256` IQ与Exact监督；S2-G2 epoch 93 Exact-D8 SHA256=`5d5224d8d6478739af5042391ef1e718c08e2f81565882e133372c9fbd116dfd`；19个10 MHz子带、5 MHz步长、覆盖阈值0.2。 |
| 标签边界 | 0.2标签由`fc_offset`、逐源`symbolRate`和子带边界独立重算，不读取旧0.3 `band_mask`，不把ignore转正，不使用Exact频带回退；原MATLAB默认阈值和CH3均未修改。 |
| 结果路径 | `outputs/s2g4r_threshold020/20260824_182523`；总报告`s2g4r_report.json`，SHA256=`96ce7f318934ddc37dc281f14a9c9ffb3b9a4e4bd1fbbfd0f783bf5a2800af1e`。 |
| 支持域与pilot | train的2560个源、validation的640个源全部至少有一个正子带，空联合频带均为0；train/validation各8条数据构造和一次forward/loss/backward/optimizer step通过。 |
| 完整数据 | 生成1024/256条0.2 Coarse细DPD，train/validation耗时596.201/147.536 s；分别8/2个紧凑分片，总运行根约1.21 GiB，无空频带或非有限值。 |
| 训练配置 | D8 `dualhead_std`、随机初始化、FP32/no-AMP、batch/val batch=`8/8`、AdamW、lr=`1e-3`、weight decay=`5e-3`、dropout=`0.4`、Focal+offset、dice=`0`、seed 42、deterministic、200 epoch、`T_max=200`。 |
| 训练结果 | 完成200 epoch，耗时2252.261 s；初始RMSE=1086.519373 m，best为epoch 93、RMSE=157.505013 m，最后20轮改善为0，`trend_closed=true`、`effective_learning=true`。best SHA256=`8bbeadc36439320a394ecb89a20fd78f1639641f4ab3eb30946b5066d92e2c10`。 |
| 三轨结果 | EE/EC20/CC20 RMSE=`130.726516/162.551301/157.505013 m`；mean GOSPA=`27.591597/29.530322/32.363494 m`；100 m recall=`0.95625/0.95/0.93125`。 |
| 长尾结果 | EE→EC20→CC20的median=`5.4218/5.1126/5.0440 m`，P90=`38.9474/44.9211/53.9678 m`，P95=`89.7583/101.5253/224.7599 m`；>100 m错误数=`29/33/44`，说明scratch的典型误差不差但长尾恶化。 |
| 因果判定 | RMSE恢复比例=`0.158565`，CC20/EE RMSE比=`1.204844`，mean GOSPA比=`1.172947`，100 m召回下降=`0.025`。RMSE和GOSPA等效门失败，召回门通过，科学状态=`REPRESENTATION_LIMIT_CANDIDATE_T020`。 |
| 资源 | 13个受监控阶段全部PASS且无warning/red flag；合计3098.437 s，最低系统可用RAM16,405,217,280 bytes，进程树RSS峰值3,298,168,832 bytes，GPU总占用峰值6318 MiB。 |
| 未执行 | 未运行test、predicted-band/K、CH3训练、Exact-D8微调或CE/FE/FC扩展轨道。 |
| 适用边界 | 只证明当前缩减数据、D8、0.2硬子带和单seed下，重新训练D8不足以恢复Exact性能；不是信息论证明，也不排除更大数据、其他模型、软子带或连续频带改善。 |
| 总判定 | 工程`PASS`；科学状态`REPRESENTATION_LIMIT_CANDIDATE_T020`。下一步优先验证更有表达力的oracle频带，不建议直接重训CH3采用0.2标签。 |
| 复核状态 | 支持审计、完整数据报告、三轨逐样本JSONL、200 epoch history、best SHA、总报告和13份监控报告均已回读；无失败重试和残留实验进程。 |

## SYS-S2G4-R2-20260825：Soft-19与Hard-19-Actual公平适配诊断

| 字段 | 内容 |
|---|---|
| 实验 ID | `SYS-S2G4-R2-20260825` |
| 类型 | 统一`BW_actual`物理频带下的Hard/Soft表示、两次200 epoch D8训练和五轨oracle-K诊断；非论文正式性能实验。 |
| 入口 | `s2g4r2_soft19.py`、增量`compute_fine_dpd(..., freq_weights=...)`、`train_yolo.py --s2g4_scratch`和`gate3_stage_runner.py`。 |
| 固定输入 | Gate 3A train/validation=`1024/256` IQ与Exact监督；S2-G2 Exact-D8 epoch 93 SHA256=`5d5224d8d6478739af5042391ef1e718c08e2f81565882e133372c9fbd116dfd`；19个10 MHz窗口、5 MHz步长。 |
| 表示定义 | Hard-19-Actual按逐源`BW_actual`覆盖率≥0.2二值化；Soft-19-Actual保留覆盖率并按源、重叠窗口取max，FFT振幅乘`sqrt(weight)`，使互谱相对权重为`weight`。 |
| 结果路径 | `outputs/s2g4r2_soft19/20260825_142311`；总报告`final_report.json`；Hard/Soft best SHA256分别为`9252a9a2389d5ddd08aef9103e8c38282edafc63085ea1c1648f49a89810fd75`和`1ab1d582f180959aa1082d9dafaad890c7aaf11383747dbb6396acfa8a7db50b`。 |
| 工程门 | 资源/身份/支持域和权重语义自测通过；二值权重与布尔mask、全1权重与全频带、统一缩放归一化均逐值一致。全部1280条Hard/Soft监督字段相同，全部DPD张量不同。 |
| 训练配置 | 两项均D8 `dualhead_std`、随机初始化、FP32/no-AMP、batch/val batch=`8/8`、AdamW、lr=`1e-3`、weight decay=`5e-3`、dropout=`0.4`、Focal+offset、seed 42、deterministic、200 epoch、`T_max=200`。 |
| 训练结果 | Hard/Soft均完成200 epoch且`trend_closed=true`、`effective_learning=true`；best epoch=`121/79`，validation RMSE=`112.062501/141.228942 m`，初始RMSE=`1103.060078/1102.642452 m`。 |
| 五轨RMSE | EE/EHA/HHA/ESA/SSA=`130.726516/165.899666/112.062501/154.968663/141.228942 m`。冻结Exact-D8上Soft优于Hard，但适配后Soft明显未优于Hard。 |
| 五轨mean GOSPA | EE/EHA/HHA/ESA/SSA=`27.591597/30.261761/28.698633/29.082168/30.771511 m`。SSA/EE=`1.115249`，未通过预设1.10等效门。 |
| 召回与长尾 | EE/HHA/SSA的100 m recall=`0.95625/0.9515625/0.934375`；median=`5.4218/4.6339/4.9893 m`，P95=`89.7583/92.8251/139.3486 m`，>100 m错误数=`29/31/43`。Soft主要退化仍在长尾。 |
| 配对bootstrap | SSA−HHA样本RMSE均值差=`10.2515 m`，95% CI=`[-4.5583,26.2091]`；GOSPA差=`2.0729 m`，CI=`[-1.1288,5.3373]`。区间跨0，不能声称Soft显著更差，但没有预设方向性收益。 |
| 预设判定 | 两项训练有效且趋势关闭；Soft未优于Hard，SSA虽满足RMSE与100 m recall门，但GOSPA等效门失败，科学状态=`SOFT19_INSUFFICIENT`。因`R_HHA<R_EE`，原定Hard→Exact恢复比例不适用。 |
| 因果修正 | R1使用`symbolRate`主瓣，R2统一使用`BW_actual`。HHA达到候选水平说明物理频带定义是R1的重要混杂因素，不能再把R1结果强解释为19窗口分辨率本身受限。单seed下HHA优于EE不构成Hard-19理论优势。 |
| 资源与工程事件 | 24个监控阶段中23个PASS；Hard train数据生成的计算、8分片和PASS报告完成后，外层120秒等待超时遗留子进程，清理时该监控报告记为CRASHED。其数据随后由全量Hard/Soft逐字段比较完整回读并通过。总受监控墙钟约6962.66 s，最低系统可用RAM14,088,773,632 bytes，RSS峰值3,300,499,456 bytes，GPU占用峰值6885 MiB，结果根峰值2,593,039,558 bytes；无warning/red flag。 |
| 未执行 | 未运行test、predicted band/K、CH3修改或训练、Hard-39、逐源细DPD或阈值/模型/epoch调优。 |
| 适用边界 | 只适用于当前1024/256缩减数据、D8、单seed和oracle-K；不是论文正式性能，不证明CH3能输出Actual标签，也不能用单seed比较宣称表示显著优劣。 |
| 总判定 | 工程`PASS`；科学状态`SOFT19_INSUFFICIENT`。不采用Soft-19；Hard-19-Actual进入候选，但下一步需单独审批稳定性确认或CH3 Actual标签适配。 |
| 复核状态 | 总报告、五轨JSON/JSONL、两项200 epoch history与checkpoint SHA、全量数据比较、24份监控报告、资源和禁止范围均已回读；结束后无残留实验进程。 |

## SYS-S2G4-R3-20260825：Exact/Hard/Soft配对跨seed确认

| 字段 | 内容 |
|---|---|
| 实验 ID | `SYS-S2G4-R3-20260825` |
| 类型 | 固定缩减validation上的Exact/Hard/Soft三seed配对训练与oracle-K诊断；非论文正式性能实验。 |
| 入口 | `train_yolo.py --s2g4r3_scratch`、`s2g4r3_multiseed.py`和`gate3_stage_runner.py`。 |
| 固定输入 | 只读复用Gate 3A Exact及R2 Hard/Soft train/validation=`1024/256`数据；三种表示的1280条位置、源数、距离场、offset、样本索引逐字段相同。 |
| 配对配置 | seeds=`42/1042/2042`；同一seed三臂初始模型SHA相同，样本顺序及确定性配置相同；D8、FP32/no-AMP、batch=`8`、AdamW、lr=`1e-3`、200 epoch、`T_max=200`，只由validation RMSE选best。 |
| 结果路径 | `outputs/s2g4r3_multiseed/20260825_182925`；总报告`final_report.json`，SHA256=`fd049bf713be6ba17e0e36297965d88192a6c8f36f4b5d8657eaf813aeccf892`。 |
| 新训练完整性 | seed 1042/2042的六次训练均完成200 epoch、`trend_closed=true`、`effective_learning=true`；best epoch依次为Exact=`93/66`、Hard=`129/114`、Soft=`45/57`。 |
| 三seed RMSE | seed 42 Exact/Hard/Soft=`130.7265/112.0625/141.2289 m`；1042=`107.6230/104.1401/128.7466 m`；2042=`140.7625/134.9957/122.9635 m`。Hard在三个seed均低于Exact；Soft只在2042优于Hard。 |
| Hard等效结果 | Hard/Exact RMSE比中位数=`0.959032`、范围=`[0.857229,0.967637]`；GOSPA比中位数=`1.040122`；100 m recall下降中位数=`0.004688`。seed 42/2042通过联合门，1042因GOSPA比=`1.124295`未通过；无预设极端seed。 |
| Soft结果 | Soft优于Hard的seed数=`1/3`；Soft相对Hard RMSE改善中位数=`-0.236283`，GOSPA比中位数=`1.072229`，重新考虑门失败。N=3退化在seed 42/1042重复，但2042中Soft的N=2/N=3均优于Hard，不能声称Soft必然削弱第三峰。 |
| 配对不确定性 | 三个seed的Soft−Hard样本RMSE bootstrap 95% CI分别为`[-4.5583,26.2091]`、`[-4.1817,17.8769]`、`[-14.2824,4.0607] m`，均跨0。bootstrap描述固定模型下的样本波动，不替代seed重复。 |
| checkpoint SHA | seed 1042 Exact/Hard/Soft=`b6b55ae1…/b19268f0…/8a5028ed…`；seed 2042=`06d8ce0d…/e78249b0…/a2a9e35b…`；完整值保存在各评估JSON。 |
| 资源 | 17个监控阶段全部PASS且无warning/red flag；受监控时长合计13,690.138 s；最低系统可用RAM15,709,147,136 bytes，RSS峰值3,774,312,448 bytes，GPU总占用峰值6463 MiB，结果根峰值1,851,323,178 bytes；全部阶段`hard_timeout_enabled=false`。 |
| 统计边界 | 跨seed仅3个配对，只报告方向、median和range，不做显著性检验；同一validation既选checkpoint又诊断，不等同无偏test。11类谬误扫描中主要风险为聚合掩盖seed异质性、探索性多指标和过度外推，其余不适用或未见证据。 |
| 未执行 | 未运行test、predicted band/K、CH3修改或训练、Hard-39、Soft调权、增加到5个seed或完整级联。 |
| 总判定 | 工程`PASS`；科学状态=`HARD19_STABLE_CANDIDATE`。Hard-19-Actual固定为下一CH3 Actual标签接口候选；Soft不再优先。 |
| 复核状态 | 资源基线、数据/身份预检、三seed初始化哈希、六次history/checkpoint、六次新增JSON/JSONL、17份监控报告、manifest和总报告均已回读；无失败重试及残留训练进程。 |

## SYS-S2G4-R4-D-20260826：共享数据规模池与三表示DPD生成

| 字段 | 内容 |
|---|---|
| 实验 ID | `SYS-S2G4-R4-D-20260826` |
| 类型 | R3旧validation场景分层、R4共享IQ生成、嵌套manifest冻结和三表示细DPD完整生成/审计；尚未训练新模型，不是性能实验。 |
| 入口 | `main31_loc_v3.m`的显式`s2g4r4` profile；`gen_ch4_loc_data.py --d8_minimal`；`s2g4r2_soft19.py build --expected_samples`；`s2g4r4_scale_study.py`。 |
| 结果路径 | `outputs/s2g4r4_scale/20260826_132829`；主审计`08_dpd_audit_after_equivalence_check.json`，R4-A分析`08_r4a_existing_scenario_analysis.json`。 |
| 共享IQ | MATLAB seed=`20260826`；train/val/test=`8192/2048/2048`，每个split的N=2/N=3严格各半；SHA256分别为`3b70dc1a…/f4888f5d…/3771b7f8…`。test只生成并锁定，未生成DPD或评估。 |
| manifest | `train_1024⊂train_4096⊂train_8192`；`val_select/val_compare=1024/1024`且互不重叠；每份清单N=2/N=3各半。完整SHA保存在`06_manifests/manifest_audit.json`。 |
| DPD数据 | Exact/Hard-19-Actual/Soft-19-Actual均生成train/val=`8192/2048`；Exact采用D8最小字段存储。三套监督、样本索引和分组逐字段相同，全部DPD有限且顺序连续。 |
| 表示差异 | Exact–Hard在train/val全部样本不同；Hard–Soft分别有2/1条相同。抽查确认这些样本的Soft在唯一有效支持内只是统一缩放，经过DPD功率归一化后与Hard数学等价，不是生成错误。 |
| R4-A事实 | 在旧256条validation上，Hard优于Exact的逐样本比例随seed为`0.5234/0.4492/0.5273`，Soft优于Hard为`0.4453/0.3867/0.4570`；逐样本排序具有明显seed异质性，支持扩大数据而不是继续堆Soft seed。 |
| 时长 | MATLAB三split共454.5 s；Exact train/val=`4657.8/1107.3 s`，Hard=`5106.1/1213.4 s`，Soft=`5402.0/1302.6 s`；六个DPD阶段合计约5.22 h。 |
| 资源 | 六个DPD阶段全部PASS且无warning/red flag；最低系统可用RAM约11.24 GiB、进程树RSS峰值约3.09 GiB、GPU占用峰值约7.81 GiB、最低磁盘剩余约481.06 GiB；最终R4根目录约19.81 GiB。 |
| 工程事件 | 两次pilot分别因外层短超时和旧IQ路径错误失败，失败目录保留；修正执行方式/路径后在全新目录重跑。初版审计把Hard–Soft逐样本必须不同设得过严，定位统一权重归一化等价后改为显式记录等价样本。均未修改算法、数据分布或表示定义。 |
| 未执行 | R4-E任何训练、validation模型比较、test DPD/test评估、CH3修改/训练、predicted band/K、Hard-39和完整级联均未执行。 |
| 总判定 | R4-A至R4-D工程`PASS`；新数据具备公平规模学习曲线条件，但尚无新定位性能或最终表示排序结论。 |
| 复核状态 | MATLAB profile/字段/数量/SHA、五份manifest、六份DPD报告与监控、三表示逐分片监督/有限性/差异、PyCharm入口语法和旧入口默认兼容性均已核对；新Exact/1k清单完成一次batch 2 forward/loss/backward/optimizer step，loss有限，仅作为入口pilot。 |

## SYS-S2G4-R4-20260826：三表示数据规模学习曲线

| 字段 | 内容 |
|---|---|
| 实验 ID | `SYS-S2G4-R4-20260826` |
| 类型 | D8缩减数据规模训练、冻结validation比较、配对bootstrap与场景分层；不是test或论文正式性能。 |
| 入口 | `train_yolo.py`、`s2g4r4_scale_study.py evaluate/finalize`和`gate3_stage_runner.py` |
| 数据与配置 | Exact-Actual、Hard-19-Actual、Soft-19-Actual共享IQ/监督/清单；train=`1024/4096/8192`，`val_select/val_compare=1024/1024`；D8、FP32、batch 8、seed 42，epoch=`200/160/80`。 |
| 结果路径 | `outputs/s2g4r4_scale/20260826_132829`；总报告`11_analysis/final_report.json`。 |
| 完整性 | 9次训练和9次`val_compare`评估全部PASS；checkpoint只由`val_select`选择；1024条比较样本全部纳入；未生成test DPD、未运行test。 |

| train规模 | Exact RMSE / GOSPA | Hard RMSE / GOSPA | Soft RMSE / GOSPA | best epoch E/H/S |
|---:|---:|---:|---:|---:|
| 1024 | 121.22 / 25.38 | 176.90 / 29.38 | 131.98 / 25.11 | 82 / 81 / 89 |
| 4096 | 112.26 / 21.40 | 113.12 / 20.92 | 98.78 / 20.61 | 77 / 86 / 78 |
| 8192 | 88.20 / 18.60 | 93.09 / 19.36 | 95.61 / 19.79 | 38 / 59 / 46 |

| 配对不确定性 | 内容 |
|---|---|
| 1k | Hard−Exact RMSE=`+55.67 m`，95% bootstrap CI=`[33.84,78.62]`；Soft−Hard=`-44.92 m`，CI=`[-70.12,-21.98]`。Hard在小数据上明显劣化。 |
| 4k | Hard−Exact=`+0.86 m`，CI=`[-25.97,27.48]`；Soft−Hard=`-14.34 m`，CI=`[-40.47,12.48]`。三者无稳定RMSE排序。 |
| 8k | Hard−Exact=`+4.88 m`，CI=`[-18.02,27.16]`；Soft−Hard=`+2.52 m`，CI=`[-18.13,24.31]`。Soft−Exact的GOSPA和100 m recall劣势区间不跨0，但RMSE仍被少数极端样本主导。 |
| 分层结果 | N=2/N=3与SNR、带宽、频带并集、最小源间距和位置半径分层中，Hard/Exact与Soft/Hard差异方向均有反转；无全场景统治优势。 |
| 资源 | 9次训练合计`50,362.1 s`（约14.0 h）；18个训练/评估监控阶段全部PASS，6个warning均来自8k训练的RAM/RSS预警，0个red flag；最低可用RAM 6.16 GiB、RSS峰值11.78 GiB、GPU峰值6.34 GiB。 |
| 统计边界 | 2000次配对sample bootstrap只衡量固定training seed下的样本不确定性，不衡量训练seed方差。11类谬误扫描已完成；主要保留风险为多指标探索、分析路径自由度和validation结论外推。 |
| 总判定 | 工程`PASS`；科学状态=`HARD19_INTERFACE_SUPPORTED`。Hard-19在4k/8k已与Exact逼近，支持将CH4适配到CH3的19子带语义；Soft没有稳定收益，停止Soft路线。 |
| 下一步 | 固定Hard-19-Actual接口契约，设计CH3 Actual标签与predicted-band/K验证Gate；不继续增加Soft seed或盲目提高频率网格。 |

## SYS-S2G5-R1-20260827：统一IQ、Actual标签与分流契约

| 字段 | 内容 |
|---|---|
| 实验 ID | `SYS-S2G5-R1-20260827` |
| 类型 | 小样本数据/标签/接口工程验证；不是训练或性能实验。 |
| 入口 | `main31_loc_v3.m`的显式`s2g5r1` profile、`build_hard19_actual_labels.m`、`s2g3_compute_coarse_dpd.m`和`s2g5_r1_contract.py`。 |
| 结果路径 | `outputs/s2g5r1/20260827_165945`；默认入口回归位于`outputs/s2g5r1/compat_default_20260827_170336`。 |
| 数据 | seed=`20260827`，train/val/test=`40/16/4`；各split中K=0/1/2/3严格等量。条件分布沿用G4-R4，标签改为Hard-19-Actual阈值0.2。 |
| 标签审计 | 三split的正例/ignore均与Python独立复算逐位一致；所有有效源至少一个正子带，空槽和K=0标签全零，标签二值且互斥。 |
| 分流契约 | K=0=`stop_no_source`；K=1=`single_source_locator_pending`且调用明确报未实现；K=2/3=`d8_hard19_actual`。 |
| CH3消费 | 60条IQ全部生成19×81×81粗DPD；train 40条由原Dataset以M=10加载，Transformer完成batch 2 forward/loss，输出shape=`[2,10,19]`、loss有限。该loss仅证明链路可消费。 |
| CH4兼容性 | K=2/3的采样率、符号率、Actual公式、SNR、功率差、空间约束及频率拓扑与G4-R4一致；R1不要求重训已有Hard-D8。新样本与R4训练样本独立，避免数据泄漏。 |
| 默认入口回归 | 无profile的`4/2/2` smoke仍为K=2/3各半、主瓣阈值0.3、`mainlobe_t030`，运行通过。 |
| 资源 | 主结果目录31,671,291 bytes；MATLAB统一IQ约4.0 s，粗DPD约12.3 s；未启动训练。 |
| 关键哈希 | train IQ=`2133304e…`；train coarse=`74934ab6…`；CH3消费报告=`02dc03df…`。 |
| 总判定 | 工程`PASS`。R1契约闭合，可以进入R2的4k级CH3训练方案设计；未验证CH3预测精度、D8定位性能或端到端结果。 |

## CH3-S2G5-R2-20260827：统一数据1k→4k→8k嵌套规模诊断

| 字段 | 内容 |
|---|---|
| 实验 ID | `CH3-S2G5-R2-20260827` |
| 类型 | CH3统一物理分布上的单seed嵌套规模训练、独立validation比较与配对bootstrap；先完成1k→4k，再按预注册规则扩展8k；不是test或论文正式性能。 |
| 入口 | `main31_loc_v3.m`的`s2g5r2` profile、`s2g3_compute_coarse_dpd.m`、`s2g5_r2_materialize_subset.m`、`s2g1_train_ch3.py`和`s2g5_r2_ch3.py`。 |
| 结果路径 | 统一根目录`outputs/s2g5r2_ch3/20260827_191207`；1k/4k主结果为`analysis/final_report.json`，8k结果为`analysis/8k/compare_4k_8k.json`，最终8k门禁为`analysis/8k/final_gate_audit_v3.json`。 |
| 统一数据 | MATLAB seed=`20260828`；train/validation/占位test=`4096/2048/4`；各split K=0/1/2/3等量。占位test未生成粗DPD、未训练、未评估。 |
| 标签与粗DPD | Hard-19-Actual阈值0.2；MATLAB标签与Python逐位复算一致；train/validation全部生成19×81×81粗DPD，SHA256=`b448c934…/4e6d7c3c…`。 |
| 嵌套划分 | `train_1k⊂train_4k⊂train_8k`，样本数=`1024/4096/8192`；8k由冻结旧4k和MATLAB seed=`20260830`新增4k组成；`val_select/val_compare=1024/1024`且互不重叠，所有训练规模及validation均按K严格等量。 |
| 固定配置 | Transformer、M=10、FP32、batch/val batch=`64/64`、AdamW、lr=`1e-3`、weight decay=`5e-4`、Focal BCE、阈值0.5、seed 42、deterministic、150 epoch、warmup 5、patience 25。 |
| 初始化身份 | 相同seed重复构造的初始模型SHA256均为`72ac86f6e56cbca3661c6b31786272cbd72ab9e03ed3bc268b5cc61c5764d01e`；4k与8k均从相同seed 42随机初始化开始，不从较小规模checkpoint续训。 |
| 训练结果 | 1k/4k/8k分别在145/127/130轮早停，best epoch=`120/102/105`，训练用时约14.8/40.2/69.4分钟；三项学习门和收敛门均PASS。 |
| 冻结checkpoint | 1k SHA256=`c5e81094b738111670dca0c98ce519003ef8a60fa2be336701150b9a1dc31c6a`；4k=`72e4baf82f66781dfd505f3ee94e19264d970de18cc3c988a152868ea1e08ba2`；8k=`291ee9bce04b3a5a603568285d8505fd042d0937cc3ab48f6415ed0f24b80e2c`。均只由`val_select`最低loss选择；最终冻结8k供R3使用。 |
| 1k/4k计数 | balanced accuracy=`0.775391/0.832031`，4k−1k=`+0.056641`；K=0/1/2/3准确率分别为`0.9805/0.7344/0.5430/0.8438`与`0.9766/0.8125/0.7109/0.8281`。 |
| 1k/4k频带 | active-band macro-F1=`0.850411/0.875181`；逐源平均IoU=`0.755805/0.798311`；完全匹配率=`0.354818/0.455078`。K=0误报率=`0.019531/0.023438`。 |
| 1k→4k配对证据 | 按K分层2000次bootstrap；balanced accuracy差值95%区间=`[0.033203,0.080078]`，band macro-F1差值区间=`[0.015034,0.034490]`，均完全大于0。 |
| 1k→4k判定 | 工程`PASS`；预注册规模决策=`ENTER_8K`。两项主指标均提高且至少一项超过2个百分点；单类最大下降为K=3的1.56个百分点，K=0误报增加0.39个百分点，均未触发停止线。 |
| 8k数据完整性 | 新增4k的K=0/1/2/3各1024条，IQ标签独立复算PASS；合并8k每个K各2048条，SHA256=`350dcb1e…`。旧4k的9个逐样本科研字段与8k前4096条逐块一致；原4k及两个validation SHA未变化。 |
| `val_compare` 8k结果 | balanced count accuracy=`0.886719`，band macro-F1=`0.905776`，逐源平均IoU=`0.838691`，完全匹配率=`0.539063`，count MAE=`0.118164`，K=0误报率=`0.011719`；K=0/1/2/3准确率=`0.9883/0.8320/0.8203/0.9063`。 |
| 4k→8k配对证据 | balanced accuracy提高`+0.054688`，95%区间=`[0.034180,0.073242]`；band macro-F1提高`+0.030595`，区间=`[0.022472,0.039136]`。两项区间均完全大于0，所有K准确率均提高，K=0误报下降1.17个百分点。 |
| 最终判定 | 8k扩展工程`PASS`；预注册决策=`FREEZE_8K_FOR_R3`。这只冻结R3工程接口checkpoint，不把8k声明为论文最终规模。 |
| 资源 | 原R2的8个有效阶段全部PASS。8k扩展的8个科研阶段全部PASS；8k训练约69.5分钟，GPU总占用峰值约7.84 GiB、进程树RSS峰值约11.08 GiB、系统最低可用RAM约9.74 GiB。仅出现一次RSS超过10 GiB提示线，0 red flag。完整R2根目录唯一文件约9.27 GiB。 |
| 工程事件 | 首次独立重载命令将已有子命令`eval`误写为`evaluate`，2.4 s后退出，未写科研输出；失败监控目录保留，修正后在新目录重跑PASS。子集审计器曾错误要求非连续manifest索引连续，最小修复为逐位核对manifest后重审PASS。 |
| 8k工程事件 | 首次profile拒错pilot已正确退出，但MATLAB默认日志未打印ASCII identifier，随后在新目录补充identifier包装复核；正式IQ启动时外层工具等待误设10秒，但监控器与MATLAB子进程未中断并连续运行至PASS，未重启或复用半成品；首次最终审计因两个F1实现相差`2.22e-16`而被浮点逐位相等断言拒绝，改为绝对容差`1e-12`后在新目录通过。 |
| 完整性 | 训练前后旧4k、新增4k、合并8k及validation SHA未变化；4k重评精确复现R2指标，8k独立重载在绝对容差`1e-12`内一致；最终门禁PASS。 |
| 适用边界 | 单training seed、固定validation和缩减数据；bootstrap不包含训练seed方差。未运行test、R3四轨、CH4定位、K=1定位、额外seed或16k。 |

## SYS-S2G5-R3-20260828：Oracle/Predicted四轨级联误差隔离

| 字段 | 内容 |
|---|---|
| 实验 ID | `SYS-S2G5-R3-20260828` |
| 类型 | 冻结validation上的跨章级联、反事实四轨误差隔离和配对bootstrap；不是训练、test或论文正式性能。 |
| 入口 | 新增`第四章代码/s2g5r3_cascade.py`，复用`gate3_stage_runner.py`、CH3 Dataset/Transformer、细DPD、D8解码和GOSPA实现。 |
| 结果路径 | `outputs/s2g5r3_cascade/20260828_131043`；权威总报告`final_report.json`，SHA256=`065614de5ac8d6309cd4be80f82b83ee8312ef912e1aaa7c6a8cf84c388932fa`。 |
| 冻结输入 | CH3 8k checkpoint SHA256=`291ee9bce04b3a5a603568285d8505fd042d0937cc3ab48f6415ed0f24b80e2c`；Hard-D8 8k checkpoint=`4caaf2b96c2f8eb666b417f0cffe4ab90760315f9bd92c4d6ce4afcd425e0e7b`；raw validation=`84c5382e…`；`val_compare`=`a35cb199…`。 |
| 样本契约 | 全1024条K=0/1/2/3等量样本用于CH3分流；真值K=2/3各256条、共512条进入四轨定位。四轨local/raw索引和真值位置完全一致。 |
| CH3复现 | 8k checkpoint的二值频带、K和聚合指标逐样本复现R2；balanced count accuracy=`0.886719`，band macro-F1=`0.905776`。全1024条K混淆为`[[253,3,0,0],[29,213,13,1],[2,16,210,28],[0,2,22,232]]`。 |
| 细DPD | Oracle/Predicted Hard-19-Actual各生成512条、8个64样本分片；Oracle空频带0条，Predicted空频带2条，与定位子集`K_pred=0`一致；生成用时约329.3/328.5 s。 |
| 四轨主结果 | `OB-OK/OB-PK/PB-OK/PB-PK` mean GOSPA=`20.1240/25.7546/19.6151/25.1826 m`；100 m召回=`0.975781/0.950000/0.975781/0.951563`。 |
| 误差分解 | 频带路径`PB-OK−OB-OK=−0.5089 m`，95% CI=`[−1.6411,0.5722]`；K路径`OB-PK−OB-OK=+5.6306 m`，CI=`[4.0574,7.3682]`；两者差异稳定指向K路径。 |
| 完整级联 | `PB-PK−OB-OK=+5.0586 m`；相对GOSPA增幅=`25.137%`，bootstrap CI=`[15.555%,36.816%]`，未通过10%等效门。100 m召回差=`−2.422`个百分点，CI=`[−3.439,−1.406]`个百分点，通过5个百分点门。 |
| 交互与分层 | 交互项=`−0.0631 m`，CI=`[−0.7902,0.6719]`，无稳定放大。K=2的完整/基准GOSPA=`26.29/19.39 m`，K=3=`24.07/20.86 m`，两类均呈K错误主导。 |
| 指标解释 | `OB-PK/PB-PK`匹配RMSE低于oracle-K，是漏掉较难目标后的选择效应，不能解释为定位更准；GOSPA中的missed/false分量保留了该代价。 |
| 预注册判定 | 工程`PASS`；接口状态=`DIRECT_CASCADE_NOT_YET_SUPPORTED`；R4优先级=`COUNT_PATH_PRIORITY`；`interaction_amplification=false`。 |
| 确定性与完整性 | D8重新加载后四轨逐样本结果和指标最大差异≤`1e-12`；全部冻结输入SHA在结束后未变；权威评估目录为`05_four_tracks_after_gpu_memory_fix`。 |
| 资源 | 8个权威PASS监控阶段合计约729.9 s；最低系统可用RAM约16.26 GiB，进程树RSS峰值约1.65 GiB，GPU总占用峰值6618 MiB；权威阶段0 warning/0 red flag；根目录约333.51 MB。 |
| 工程事件 | 首次preflight因float32的0.2被逐位比较拒绝，改为绝对容差`1e-7`后新目录通过。首次四轨评估在同一进程连续重载D8导致GPU总占用15896 MiB并触发红线；显式释放模型/缓存后在全新权威目录重跑，GPU峰值1580 MiB。两份失败监控与旧四轨输出均保留且未用于结论。Oracle细DPD外层等待窗口曾返回超时，但监控器和子进程持续运行并最终PASS，未重启。 |
| 代码身份 | `s2g5r3_cascade.py` SHA256=`a633c8b79ab01a68cb0902ad1784cdbcdbc9940ede6b7109af5242787a6d022b`；语法、Ruff F/E9和`--help`通过。 |
| 适用边界 | 单CH3/D8训练seed、固定validation和K=2/3定位；sample bootstrap不含训练seed方差。真值K=1定位仍为pending，不能宣称K=0–3完整可部署系统。未运行test、训练、阈值调优、R4或16k。 |

## CH3-S2G5-R4-PRETRAIN-20260828：16k数据构建与容量门

| 字段 | 内容 |
|---|---|
| 实验 ID | `CH3-S2G5-R4-PRETRAIN-20260828` |
| 类型 | S2-G5-R4长训练前的数据生成、严格嵌套审计和容量pilot；不是训练或性能实验。 |
| 结果路径 | `outputs/s2g5r4_ch3_scale/20260828_151735`；状态报告`pretrain_status.json`。 |
| 冻结输入 | 旧8k粗DPD SHA256=`350dcb1e…`、8k checkpoint=`291ee9bc…`、固定`val_compare=a35cb199…`均与R2/R3证据一致。 |
| 新增8k | MATLAB profile=`s2g5r4_16k_add`、seed=`20260901`；K=0/1/2/3各2048条。IQ物理契约、Hard-19-Actual标签、最小源间距/源站距和粗DPD有限性均通过；新增粗DPD SHA256=`50e656bd…`。 |
| 16k构建 | 合并16384条、K=0/1/2/3各4096条；旧8k为严格前缀，9个逐样本科研字段逐块相同；16k粗DPD SHA256=`594c0fd6…`，训练视图使用硬链接。 |
| 容量计算 | batch 64、Transformer、M=10、FP32、cuda:0的一次forward/loss/backward及validation计算完成，容量JSON状态`PASS`；输入/输出shape=`[64,19,81,81]/[64,10,19]`，无NaN、OOM或GPU红线。 |
| 容量门 | 监控状态=`COMPLETED_WITH_RED_FLAGS`；最低系统可用RAM=`424,955,904 B`（约0.40 GiB），进程树RSS峰值=`16,561,369,088 B`（约15.42 GiB），低于预注册6 GiB可用RAM红线；GPU峰值2254 MiB。正式训练未启动。 |
| 阶段资源 | IQ约187.3 s；新增粗DPD约1407.3 s；16k构建/全量审计约380.0 s；R4根目录逻辑文件总量约17.87 GB（包含硬链接重复计数），磁盘空间充足。 |
| 工程事件 | IQ启动时外层工具等待误设过短并返回超时，但监控器和MATLAB子进程保持连续运行且最终PASS，未重启或复用半成品。日志控制台中文乱码只作为转码现象处理。 |
| 当前判定 | `STOPPED_AT_CAPACITY_GATE`；数据和算法链通过，但现有eager Dataset不满足RAM安全边界。需先审批释放后台RAM后重测，或R4专用懒加载改造；不能直接启动长训练。 |
| 适用边界 | 未训练16k模型，未运行test、调阈值、修改模型/loss、进入32k、校准置信度或运行新四轨。 |

## CH3-S2G5-R4-LAZY-20260828：16k懒加载等价性与资源门

| 字段 | 内容 |
|---|---|
| 实验 ID | `CH3-S2G5-R4-LAZY-20260828` |
| 类型 | R4专用数据读取改造的等价性、容量与吞吐验证；不是正式训练或性能实验。 |
| 结果路径 | 复用`outputs/s2g5r4_ch3_scale/20260828_151735`；总状态`lazy_pretrain_status.json`。 |
| 缓存 | 源16k SHA256=`594c0fd6…`；缓存shape=`[16384,19,81,81]`、float32、大小=`8,169,652,352 B`、SHA256=`a455fe55…`；生成约48.9 s。另建同逻辑1k缓存用于配对训练。 |
| 抽样等价 | 固定512条，覆盖K=0/1/2/3、旧/新增8k、首尾和8192拼接边界；512/512逐元素完全相同，最大和平均绝对误差均为0。 |
| Dataset等价 | 8个未增强样本逐tensor相同；seed 42、`num_workers=0`下前5个shuffle+增强batch的输入、源数、band和ignore摘要完全一致。 |
| 短程训练 | 同一1k数据、相同seed与配置分别使用eager/lazy训练3 epoch；epoch history完全一致，最终模型、优化器和调度器状态最大绝对误差均为0。 |
| 容量与吞吐 | 16k、batch 64的forward/loss/backward/step通过；完整训练epoch遍历16384条、256 batch约38.07 s。保守监控包络：最低可用RAM=`16,256,770,048 B`（约15.14 GiB），RSS峰值=`8,471,281,664 B`（约7.89 GiB），GPU总占用峰值=`7503 MiB`。 |
| 时间估算 | 150 epoch纯训练按单epoch线性粗估约5710 s（95.2 min），尚不含逐epochvalidation、checkpoint和早停开销；未触发4小时慢速提醒。 |
| 原版兼容 | 原`s2g1_train_ch3.py`默认Dataset路径仍通过1k容量回归；新PyCharm入口仅在`LAZY_READY_FOR_TRAINING`时调用R4专用lazy训练。 |
| 最终判定 | `LAZY_READY_FOR_TRAINING`；正式16k训练未启动，等待用户选择运行主体。 |
| 适用边界 | 等价性证据来自512条全量分层抽样和1k三轮训练，不是16k双份完整训练；未运行正式150 epoch、validation规模比较、四轨更新或test。 |

## CH3-S2G5-R4-20260828：16k规模、K置信度与级联复核

| 字段 | 内容 |
|---|---|
| 实验 ID | `CH3-S2G5-R4-20260828` |
| 类型 | 正式16k缩减规模训练、固定validation配对规模诊断、四轨级联更新和候选K oracle上限；不是test或论文最终性能。 |
| 结果路径 | `outputs/s2g5r4_ch3_scale/20260828_151735`；权威总门禁`analysis/16k/final_gate_audit.json`，级联总报告`cascade_16k/final_report.json`。 |
| 正式训练 | Transformer、M=10、FP32、batch 64、seed 42、最多150 epoch、warmup 5、patience 25；epoch 103早停，validation-best在epoch 78，训练约4125.8 s。学习门与收敛门均PASS。 |
| 冻结checkpoint | `train_16k/best_model_v26_B_M10.pth`，SHA256=`f2f7a7c345f1866b871282670f45671d930de34bb06493a9828a9d04a38699c4`；独立双重加载二值预测完全一致。 |
| 规模比较 | 8k→16k balanced count accuracy=`0.886719→0.891602`，差值bootstrap 95%区间=`[-0.012695,0.022461]`；Band macro-F1=`0.905776→0.923818`，区间=`[0.011666,0.024641]`。严格规模门因balanced区间下限略低于−1个百分点而未通过。 |
| 分层与误差 | K=0/1/2/3准确率变化=`−1.56/+1.56/+1.95/0.00`个百分点；K=0误报增加1.56个百分点；总K错误`116→111`。错误样本Top-2覆盖=`0.909910`，K>3预测0条。 |
| 16k四轨 | `OB-OK/OB-PK/PB-OK/PB-PK` mean GOSPA=`20.1240/25.7546/19.1438/24.1530 m`；100 m召回=`0.975781/0.950000/0.978125/0.951563`。 |
| 级联效应 | 频带路径=`−0.9802 m`，CI=`[-1.9208,-0.1342]`；K路径=`+5.6306 m`，CI=`[4.0574,7.3682]`；完整级联相对GOSPA增加=`20.021%`，CI=`[11.677%,29.909%]`，GOSPA等效门失败；召回差CI=`[-3.438%,−1.484%]`，召回门通过。 |
| 候选K上限 | Oracle在预测K与相邻候选K之间按真值GOSPA择优时，`OB-CKO=22.0147 m`，点估恢复K路径损失`66.42%`；2000次bootstrap均值`66.64%`，CI=`[49.53%,85.59%]`。这是上限诊断，不是部署算法。 |
| 资源 | 正式训练监控PASS、0 warning/0 red flag；最低可用RAM约12.24 GiB、RSS峰值约9.77 GiB、GPU总占用峰值8195 MiB。级联权威batch 8评估GPU峰值2472 MiB。 |
| 工程事件 | 首次四轨候选评估使用batch 64，计算完成但GPU总占用15773 MiB触发13 GiB红线；失败输出和监控保留且不用于结论。仅将D8推理batch降为8，在新权威目录重跑并完成两次严格重载，0 warning/0 red flag。 |
| 最终判定 | 总门禁`PASS`；科学决策=`ENTER_K_CANDIDATE_GATE`。不推荐直接进入32k，也不能宣称现有16k链路已达到直接级联等效。 |
| 适用边界 | 单training seed、固定validation和固定D8；bootstrap不含训练seed方差。未运行test、调阈值、修改模型/loss、实现可部署候选K选择或进入32k。 |

## SYS-S2G5-R5A-20260828：有序候选K与D8空间证据联合判决

| 字段 | 内容 |
|---|---|
| 实验 ID | `SYS-S2G5-R5A-20260828` |
| 类型 | 冻结CH3/D8的validation后处理、候选选择和端到端配对诊断；不是模型训练、test或论文最终性能。 |
| 结果路径 | `outputs/s2g5r5_candidate_k/20260828_222900`；总报告`final_report.json`，主分析`05_selector_analysis/r5a_analysis.json`。 |
| 冻结输入 | 16k CH3 SHA256=`f2f7a7c…`；8k Hard-D8 SHA256=`4caaf2b…`；Hard-19-Actual、原0.5规则和D8 batch 8均未修改。 |
| 数据隔离 | probe在16k训练logits上拟合；单温度和D8选择器只用`val_select`；`val_compare`仅评价一次。两个validation各1024条、K=0/1/2/3各256条，原始样本索引完全不重叠。 |
| 有序probe | 固定30维槽位特征、三个条件L2逻辑回归，温度=`1.3989`。`val_compare` Top-1准确率=`0.889648`，略低于原规则`0.891602`；相邻Top-2全样本覆盖=`0.990234`，Top-1错误覆盖=`0.911504`。 |
| K=2/3候选 | 相邻候选覆盖=`0.986328`。原规则、probe Top-1、CH3+D8选择的K=2/3准确率分别为`0.873047/0.869141/0.955078`；选择后K=2/3准确率分别为`0.949219/0.960938`。 |
| 级联点估 | `OB-OK/PB-BASE/PB-PROBE/PB-SELECTED/PB-CANDIDATE-ORACLE` mean GOSPA=`20.1240/24.1530/24.1299/20.4453/18.3232 m`；100 m召回=`0.975781/0.951563/0.949219/0.970313/0.974219`。 |
| 配对判定 | 2000次按K=2/3分层bootstrap：联合选择相对`OB-OK`的GOSPA增幅区间=`[-3.732%,7.379%]`，召回差区间=`[-1.172%,0]`；相对原`PB-BASE`的GOSPA差均值=`−3.6992 m`，区间=`[−5.2082,−2.2699] m`。两项直接级联门均通过。 |
| 选择器 | 只用K概率比、争议槽位分数、三个D8峰值关系和空间间距；`val_select`可用508/512条，`val_compare`使用509条并回退3条。真值位置、GOSPA和oracle候选不作为输入。 |
| 确定性 | `val_select`和`val_compare`的D8 Top-4峰在独立重载后最大差异均为0；选择器重载的512条输出完全一致。 |
| 资源 | 8个监控阶段全部PASS、0 warning/0 red flag；最低可用RAM约12.08 GiB、进程树RSS峰值约9.67 GiB、GPU总占用峰值约5.05 GiB；R5根目录约0.182 GiB。细DPD阶段约344.5 s。 |
| 工程事件 | 冻结推理首次外层等待窗口提前返回，但内部监控器和子进程未中断，并在同一证据链42.1 s正常PASS；不是实验失败，未重启或复用半成品。 |
| 最终判定 | 总门禁`PASS`；科学决策=`DIRECT_CASCADE_SUPPORTED`。按预注册停止规则不启动R5-B新增有序计数头。 |
| 适用边界 | 单CH3/D8 training seed、固定validation和真实K=2/3；sample bootstrap不含训练seed方差。K=1定位仍为空，未运行test、32k、阈值搜索或正式论文配置。 |
| 代码身份 | `第四章代码/s2g5r5_candidate_k.py` SHA256=`1bff3361478cd4b33e0ee298453a22778aebe7f0b90ecd2d2b566192b2667fe5`；语法、Ruff F/E9和CLI help通过。 |

## SYS-S2G5-R6A-20260829：当前成功配置的三seed稳定性确认

| 字段 | 内容 |
|---|---|
| 实验 ID | `SYS-S2G5-R6A-20260829` |
| 类型 | 两次CH3与两次D8正式训练、固定validation三seed主对角评价和3×3交叉组合诊断；不是test或论文最终性能。 |
| 结果路径 | `outputs/s2g5r6/20260829_170441`；原权威摘要`validation/r6a_summary.json`；RMSE补充摘要`validation/r6a_summary_rmse.json`，SHA256=`38fdcd79f633f6ce8dff45fc961e1a71cb9100140be61e213e357a29da78505d`；完整报告`validation/final_report.json`，SHA256=`df7e87c0fc2e92463234cd4e50e4a56f1bba749320c0a13084a594b73d502dd0`。 |
| 冻结输入 | CH3直接复用R4的16k MAT、validation、NPY缓存和manifest；D8直接复用R4的8k Hard-19-Actual分片及train/validation manifest。全部大小和SHA与冻结值一致，四次训练前后复核均未变化；未生成、复制、转存或重建数据。 |
| 数据契约 | CH3训练16,384条，K=0/1/2/3各4,096条；D8训练8,192条，K=2/3各4,096条，validation各512条。`val_select`与`val_compare`互斥；test未读取。 |
| 初始化与pilot | CH3和D8的42/1042/2042初始化哈希各不相同；真实CUDA one-batch forward/loss/backward/optimizer step全部通过。CH3 batch 64输入/输出为`[64,19,81,81]/[64,10,19]`；D8 batch 8输出heatmap/offset为`[8,1,401,401]/[8,2,401,401]`。 |
| CH3训练 | 固定16k、Transformer、M=10、FP32、batch 64、最多150 epoch、warmup 5、patience 25。seed 1042在103轮早停、best=78、checkpoint SHA=`baecd8b0…`；seed 2042在115轮早停、best=90、SHA=`7ed80117…`。两次约72.3/80.4 min。 |
| D8训练 | 固定8k、D8、Hard-19-Actual、FP32/no-AMP、train/validation batch 8、80 epoch。seed 1042 best=28、validation RMSE=`80.152 m`、SHA=`a4f71e82…`；seed 2042 best=63、RMSE=`79.762 m`、SHA=`78d74067…`。两次约124.4/124.5 min。 |
| CH3三seed | balanced count accuracy=`0.8916/0.8984/0.9004`；Band macro-F1=`0.9238/0.9219/0.9235`；真实K=2/3的Top-2覆盖三者均为`0.9863`，选择后K准确率=`0.9551/0.9414/0.9551`。 |
| 主对角定位 | seeds 42/1042/2042的`OB-OK` GOSPA=`20.124/22.591/17.968 m`，`PB-BASE`=`24.153/27.197/22.903 m`，`PB-SELECTED`=`20.445/23.800/18.912 m`。3/3均优于原始完整级联。 |
| 主对角匹配RMSE | seeds 42/1042/2042的`OB-OK`=`88.708/87.508/79.310 m`，`PB-BASE`=`57.452/65.371/54.173 m`，`PB-SELECTED`=`61.718/58.130/47.579 m`。对应真值信源匹配覆盖率：`OB-OK`均为100%，`PB-BASE`=`96.48%/96.88%/96.48%`，`PB-SELECTED`=`98.67%/98.28%/98.59%`。 |
| RMSE解释边界 | RMSE仅对成功匹配的信源对计算，不惩罚未匹配目标；`OB-OK`还受到少量公里级离群误差影响。因此预测轨RMSE低于oracle轨不代表完整系统更好。候选选择相对baseline的RMSE为2/3主seed下降、1/3上升；应与覆盖率和GOSPA联合解释。 |
| 严格直接门 | seeds 42/1042/2042分别为通过/未通过/未通过。相对`OB-OK`的bootstrap 95%区间分别为`[-3.73%,7.38%]`、`[0.93%,10.17%]`、`[-0.27%,11.03%]`；后两者越过10%上限，但均未达到15%灾难性退化。 |
| 3×3诊断 | 9/9个CH3/D8组合的`PB-SELECTED` GOSPA均低于各自`PB-BASE`，匹配RMSE为5/9下降、4/9上升；直接门矩阵为`[[通过,未通过,未通过],[通过,未通过,未通过],[未通过,通过,未通过]]`，无单一CH3行或D8列依赖。全部9组RMSE写入`r6a_summary_rmse.json`。 |
| 资源 | CH3两次训练RSS峰值约9.77 GiB、GPU峰值7,573/7,601 MiB；D8约11.73 GiB、GPU峰值5,779/5,676 MiB。四次训练及validation准备、主对角和交叉评价均0 warning/0 red flag，监控间隔10秒且无普通墙钟硬超时。 |
| 工程修改 | R6增量入口补齐冻结manifest和每K数量门禁、三seed初始化身份、真实CUDA pilot、每次训练前后SHA复核及validation-only三seed评价。仅自动修复新评价入口的未使用导入；未改变科研配置。 |
| 最终判定 | `R6A_MIXED`：3/3改善、仅1/3通过直接级联门、无灾难性退化、无单一组合依赖。依预注册规则停止在R6-B与test之前。 |
| 适用边界 | 固定16k CH3、8k D8、同一validation及3个training seed；3×3组合不是9个独立seed。没有运行R6-B至R6-E、K=1定位、32k或test。 |

## CH4-S2G5-R6B0-20260830：D8论文控制条件复现诊断

| 字段 | 内容 |
|---|---|
| 实验 ID | `CH4-S2G5-R6B0-20260830` |
| 类型 | 三套冻结D8在论文4A/4B控制条件下的oracle-K重要诊断；不是训练、冻结test或完整4A–4D复现。 |
| 结果路径 | `outputs/s2g5r6/20260830_132057`；最终报告`evaluation_extended_final/final_report.json`，SHA256=`def288dc2a4e1b63261ea9c98f8d5b1100a1c4ff6f4d3ae5a2aa6c9651ba1474`；精简摘要SHA256=`1a327e2f70255bff68b7c2bcc3940d65eaf65b7c9db3f369a30c8655e1806481`。 |
| 冻结模型 | 复用D8 seeds `42/1042/2042`，checkpoint SHA256分别为`4caaf2b9…/a4f71e82…/78d74067…`；评价前逐一核对身份，不重新训练或选择checkpoint。 |
| 控制数据 | 新生成12,000条独立IQ：K=2/3，10 MHz符号率、13 MHz `BW_actual`、严格同频、四站500 m环形阵列、10 m细网格。4A为`−10/−6/0/+6 dB`且距离800 m；4B为距离`800/1000 m`且SNR 0 dB。IQ审计全部通过，manifest SHA256=`2f5ece9e…`。 |
| 评价规模 | 12个锚点先各评价512条；按预案只把`4B_K2_DIST_1000`、`4B_K3_DIST_800`、`4B_K3_DIST_1000`扩到1000条。Exact和Hard-19-Actual使用相同IQ、位置、oracle-K和D8；共计算15,216个细DPD任务。 |
| 论文兼容门 | 每个锚点容差为`max(5 m, 论文RMSE的25%)`；三seed至少2个在上界内且三seed中位RMSE在上界内才算兼容；中位RMSE大于论文两倍记为明显偏离。样本bootstrap按整条样本重采样，不把同一样本内多个源伪装成独立观测。 |

Exact主轨最终结果：

| 论文锚点 | 论文RMSE | seeds 42/1042/2042 RMSE | 三seed中位 | 判定 |
|---|---:|---:|---:|---|
| 4A K2，−10 dB | 97.4 m | 283.68 / 286.99 / 208.32 m | 283.68 m | 明显偏离 |
| 4A K2，−6 dB | 17.1 m | 16.19 / 18.07 / 19.84 m | 18.07 m | 兼容 |
| 4A K2，0 dB | 12.5 m | 12.24 / 14.58 / 12.02 m | 12.24 m | 兼容 |
| 4A K2，+6 dB | 11.3 m | 13.36 / 14.41 / 11.86 m | 13.36 m | 兼容 |
| 4A K3，−10 dB | 166.0 m | 340.62 / 341.80 / 290.89 m | 340.62 m | 明显偏离 |
| 4A K3，−6 dB | 18.1 m | 16.40 / 18.02 / 38.93 m | 18.02 m | 兼容，存在seed长尾 |
| 4A K3，0 dB | 13.7 m | 13.12 / 14.61 / 12.59 m | 13.12 m | 兼容 |
| 4A K3，+6 dB | 12.8 m | 13.08 / 14.91 / 12.04 m | 13.08 m | 兼容 |
| 4B K2，800 m | 15.4 m | 15.28 / 16.37 / 14.73 m | 15.28 m | 兼容 |
| 4B K2，1000 m | 18.9 m | 21.78 / 77.65 / 20.94 m | 21.78 m | 兼容，存在seed长尾 |
| 4B K3，800 m | 19.9 m | 33.34 / 43.93 / 34.14 m | 34.14 m | 未兼容 |
| 4B K3，1000 m | 40.3 m | 70.65 / 93.73 / 58.74 m | 70.65 m | 未兼容 |

| 补充事实 | 内容 |
|---|---|
| Hard/Exact关系 | 多数正常条件接近，但Hard在4A K3、−6 dB的三seed中位RMSE为59.21 m、Exact为18.02 m，在4B K2、1000 m为34.95/21.78 m；Hard可能放大部分场景的长尾，但不能解释Exact在−10 dB及K=3距离条件也存在的差距。 |
| 训练支持域 | 当前8k D8训练集中符号率2–20 MHz、`BW_actual` 2.6–26 MHz、平均SNR约−15.47至9.74 dB、源半径约100.13–999.99 m，边际范围覆盖论文锚点；但严格10 MHz/13 MHz、同频、固定800 m与0 dB的联合组合没有原样出现。 |
| 最终判定 | `PAPER_CONDITION_MIXED`：Exact为8/12兼容、2/12明显偏离、2/12未兼容。当前D8在中等SNR与K=2距离条件达到论文量级，但没有复现极低SNR及K=3距离鲁棒性。 |
| 不确定性 | 三个预注册歧义距离条件已扩到方案上限1000条；K3的800/1000 m区间仍跨兼容边界，记录为剩余不确定性，不再自动扩样本。论文硬编码参考值没有原始样本或置信区间。 |
| 资源与完整性 | 正式IQ生成150.5 s、主DPD 7,172.1 s、扩展DPD 1,713.4 s、最终评价201.2 s；有效阶段均0 warning/0 red flag。峰值GPU约3.89 GiB、进程树RSS约1.71 GiB、最小系统可用RAM约17.83 GiB；运行根目录约5.99 GiB。 |
| 工程修复 | 首次MATLAB pilot因新函数未加入路径而停止，保留失败目录；仅修正调用路径和日志解码后在全新pilot目录通过。扩样本后增量加入基础/扩展DPD索引合并，并修正“已达到1000条上限仍建议继续扩展”的报告语义；科研配置未改变。 |
| 代码身份 | `s2g5r6b0_paper_conditions.py` SHA256=`6760631b7e221726f456faa9160f3931eb33ece660a3f21dfb7d794e1d07718e`；`s2g5r6b0_generate_control.m` SHA256=`8d5b12daac8668d68ec078b9918b375fbc4e4428912bddcefa0f283894779300`。 |
| 统计审查 | 11类常见谬误检查覆盖`11/11`；明确保留选择范围、论文无CI、三个training seed只作描述汇总、非冻结test及非完整复现等边界。 |
| 下一步含义 | 在R6-B中优先诊断D8的联合分布覆盖、低SNR/K=3长尾及训练规模，而不是继续修改CH3、读取test或把B0写成完整论文复现。任何新增训练需另行审批。 |

## E2E-G0-20260831：Soft连续桥完整分辨率可行性与梯度审计

| 字段 | 内容 |
|---|---|
| 实验 ID | `E2E-G0-20260831` |
| 类型 | 16条冻结validation清单下的二值等价、边界稳定、算子/系统梯度、Hard解码和401×401资源审计；不训练、不读取test、不产生性能比较结论。 |
| 正式结果 | `F:\SourceCount_DPD_E2E\outputs_e2e\smoke\unified\e2e_g0\20260831_235442`；唯一Gate状态=`STOP_GRADIENT`，`g1_unlocked=false`。 |
| 输入身份 | coarse validation SHA256=`d4cb6af9e3f99c2162c306105b29f947e48ac85c38075acc8226b6403046d6ad`；raw validation SHA256=`84c5382ec3a05e16aeb038e86e9a6df4e14784f4005bc75e5963235c83551789`。只读CH3/D8权重SHA256分别为`f2f7a7c345f1866b871282670f45671d930de34bb06493a9828a9d04a38699c4`、`4caaf2b96c2f8eb666b417f0cffe4ab90760315f9bd92c4d6ce4afcd425e0e7b`。 |
| 样本与停止顺序 | 运行模型前固定16条validation，K=0/1/2/3各4条；完整反向代表样本分别为local index `1/478/227/150`。系统梯度失败后按预注册顺序停止，因此16样本冻结前向未执行，`frozen_forward.json`不存在且不计为通过。 |
| 二值与稳定性 | 四类二值输入均通过。最大DPD相对差=`1.1282503465e-7`；Hard峰值索引完全一致，最大坐标差=`0.000732421875 m`。全零、近零、单带、相邻重叠、全一和普通连续案例全部有限；全零DPD经z-score为精确零。 |
| 算子梯度 | 21×21算子标量的190/190梯度有限非零；中心差分步长`0.01/0.003`相对误差=`0.001058/0.020703`且同号，通过5%门；`0.001`受FP32分辨率影响未通过但不改变预注册总判定。 |
| 完整梯度 | K=0/1/2/3的401×401梯度均有限且190/190非零，L2范数分别为`8.95394e-5/1.72065/0.210566/0.00162033`。但K=2完整`CH3 logits→桥→DPD→D8 loss`在步长`0.01/0.003`的有限差分方向导数为`-0.005805/-0.013550`，autograd为`+0.007192`，两次均异号，触发`STOP_GRADIENT`。 |
| 资源与确定性 | K=3两次完整forward+backward的loss、梯度、Heatmap和offset逐值一致；耗时`14.684/7.608 s`。峰值GPU allocated/reserved=`548,808,192/633,339,904 bytes`，峰值进程RSS=`2,038,267,904 bytes`，均远低于14 GiB、28 GiB和300 s停止线。 |
| Hard解码 | K=0/1/2/3的输出长度、空集合、稳定tie-break、坐标和offset契约通过；`hard_count_mismatch`只记录不隐藏修复。 |
| 失败尝试 | `20260831_204255`因manifest含`inf`无法写严格JSON，改为`null`后新目录重跑；`20260831_204423`与`20260831_204752`暴露二值等价夹具把浮点求和顺序差异当成dense绝对失败。经用户批准改为DPD相对误差＋Hard峰值/坐标判据后，正式目录通过该门。失败目录均保留，不作PASS证据。 |
| 科研结论 | 在线细DPD反向图在当前硬件上可连接且资源可行，但完整定位loss梯度方向尚不可信；“梯度非零”不能解锁联合训练。首要路线在G0停止，G1不得启动。 |
| 后续边界 | 不自动平滑最大特征值、不替换loss、不放宽梯度门。后续需另行审批受控梯度机制诊断/新G0，或转入双编码器＋逐源中期融合次要路线。 |

## E2E-G0D-20260901：完整系统梯度根因诊断

| 字段 | 内容 |
|---|---|
| 实验 ID | `E2E-G0D-20260901` |
| 类型 | 对G0失败点进行重要只读诊断；不训练、不读取test、不评价模型性能。 |
| 入口 | `统一模型代码/gates/g0/e2e_g0d.py`、`e2e_g0d_fp64.py`、`e2e_g0d_mixed.py`、`e2e_g0d_double.py`。 |
| 固定输入 | 继承G0 validation样本与方向；重点样本为K=2 local index `227`，跨样本补充K=1 index `478`与K=3 index `669`；冻结CH3/D8权重和数据身份不变。 |
| 证据目录 | 主诊断`20260901_112416`；FP64物理链`20260901_113322`；FP64物理链接FP32 D8`20260901_114150`；混合逐层与full-double确认`20260901_115115`。共同父目录为`outputs_e2e/smoke/unified/e2e_g0d/`。 |
| 原失败复现 | 原G0 K=2完整loss异号可复现：autograd方向导数`+0.00719244`；中心差分在`h=0.01/0.003`为`-0.00580549/-0.0135501`。该步仅证明诊断覆盖原故障。 |
| checkpoint排除 | 401×401 reentrant与nonreentrant梯度余弦=`1`、相对L2差=`0`、最大绝对差=`0`；21×21 off/reentrant/nonreentrant亦一致，状态=`PASS`。 |
| FP32阶梯 | 频率权重层多方向通过；原始DPD标量约`1e7`，从该层起微小loss变化受FP32量化，K=1/2/3跨样本没有形成稳定反向证据，主诊断状态=`INCONCLUSIVE_NUMERIC`。 |
| FP64物理层 | bridge、DPD、`log1p`和z-score使用float64/complex128。K=1/2/3各5个方向共15/15为`PASS_SMOOTH`、0稳定不一致；60个有效步长相对误差最大`4.00784e-4`。状态=`PHYSICS_GRADIENT_PASS`，因此未触发特征值间隔诊断。 |
| 混合精度定位 | FP64物理链接原FP32 D8时，K=2逐层阶梯的z-score目标通过，首次不可判定层为`L3_heatmap_logits`；FP32与混合前向的loss绝对差在三样本中为`1.49e-4`至`1.10e-3`，不作为性能差异。 |
| full-double确认 | 将同一D8权重、D8计算及相同Focal/offset公式转为double后，K=2五方向及K=1/K=3各一方向共7/7为`PASS_SMOOTH`、0稳定不一致；28个有效步长相对误差最大`3.00311e-3`。 |
| 唯一判定 | `IMPLEMENTATION_FIX / FP32_D8_FINITE_DIFFERENCE_RESOLUTION`，`g1_unlocked=false`。原G0异号来自FP32整链有限差分的分辨率不足及分段激活影响，不是checkpoint错误、autograd公式错误或最大特征值DPD梯度阻塞。 |
| 后续边界 | 当时要求另行审批分段混合精度重验且不得直接进入G1；该入口随后已由`E2E-G0-R1-20260901`完成。 |

## E2E-G0-R1-20260901：分段混合精度完整G0重验

| 字段 | 内容 |
|---|---|
| 实验 ID | `E2E-G0-R1-20260901` |
| 类型 | 16条冻结validation清单下的分段混合精度数值、梯度、资源与冻结前向重验；不训练、不读取test。 |
| 有效结果 | `F:\SourceCount_DPD_E2E\outputs_e2e\smoke\unified\e2e_g0r1\20260901_130737`；最终状态=`G0_PASS`，`g1_unlocked=true`。 |
| 精度合同 | CH3及sigmoid概率FP32；频带—FFT映射、在线DPD、`log1p`、z-score为FP64/complex128；归一化后转FP32进入冻结D8，Heatmap/offset及Focal+offset loss保持FP32；full-double D8仅用于审计。 |
| 全部门禁 | 二值等价、连续稳定性、算子梯度、Hard解码、资源/确定性、分层系统梯度和16样本冻结前向均为`PASS`。 |
| 二值兼容 | 四类二值输入的最大DPD相对差=`2.42222e-7`，Hard峰值索引全部一致，最大坐标差=`0.00146484 m`；归一化输入、Heatmap和offset最大绝对差分别为`1.99908e-4/0.00330925/0.000438124`。 |
| 生产图梯度 | K=0/1/2/3的190/190个band-logit梯度均有限非零，L2范数分别为`8.95117e-5/1.72028/0.210555/0.00162017`；K=3两次运行的loss、梯度、Heatmap和offset差均为0。 |
| 分层梯度审计 | FP64物理链K=1/2/3共15/15方向通过、0稳定不一致，60个步长最大中心相对误差=`4.00784e-4`；full-double完整D8/loss链7/7通过、0稳定不一致，28个步长最大中心相对误差=`3.00311e-3`。 |
| 资源 | K=3两次完整生产图forward+backward耗时=`9.444/8.465 s`；峰值GPU allocated/reserved=`551,581,696/618,659,840 bytes`，峰值进程RSS=`2,033,143,808 bytes`，均低于既有停止线。分层梯度审计耗时=`692.102 s`。 |
| 冻结前向 | 16/16样本全部有限，非零源样本0塌缩；Soft/Hard归一化DPD相关系数在可定义样本中为`0.637504—0.999989`，9/16存在CH3硬计数与软基数解码不一致。GOSPA和相关性仅登记初始化风险，不用于性能选择。 |
| 失败尝试与修复 | `20260901_125329`的前六项均通过，冻结前向首次运行时因19维硬子带掩码直接传入4096维FFT接口而`EXECUTION_ERROR`。仅通过冻结子带—FFT覆盖矩阵展开为4096维支持域，语法、Ruff和形状检查通过后在全新目录完整重跑；失败目录保留且不作PASS证据。 |
| 科研结论 | 第一版分段混合精度合同满足进入G1单seed因果训练的最低数值、梯度和资源条件。该结论不证明Soft-E2E优于Soft-SG或历史硬级联。 |
| 后续边界 | G1只解锁至准确方案审批；须保持同前向`Soft-SG(detach)`直接对照，不增加空间计数头、不读取test。 |

## 实验记录模板

## E2E-G1-20260901：Soft定位反馈配对因果训练与完整性恢复

| 字段 | 内容 |
|---|---|
| 实验 ID | `E2E-G1-20260901` |
| 类型 | 单seed配对因果训练、冻结validation评价及恢复性完整性审计；非论文正式实验 |
| 入口 | `统一模型代码/gates/g1/e2e_g1.py`；恢复入口`materialize_reference_snapshot.py`、`recover_g1_finalize.py` |
| 数据与配置 | train 256条、`val_select` 128条、`val_compare` 512条，K=0/1/2/3分层；两轨各4 epoch、256次optimizer step；物理链FP64/complex128，CH3/D8 FP32；无AMP、无test |
| 结果路径 | `outputs_e2e/unified/e2e_g1/20260901_145307`；权威总报告`final_report.json`，输入恢复报告`input_integrity_recovery_report.json` |
| 冻结checkpoint | `Soft-SG`和`Soft-E2E`均由`val_select`选择epoch 1；SHA256分别为`0771c909c276716be8c469ecc932538d7e18b6a1941bf7f61dbef9892da707c2`和`37fa2dffb0d0acb165968a23f5019b6993f8260e2cd24fcf53c1e7b820f879f9` |
| 梯度隔离 | 两轨均记录256步；`Soft-SG`定位梯度非零率0%，`Soft-E2E`为100%，预设因果隔离成立 |
| 主结果 | Hard-frozen/Soft-SG/Soft-E2E mean GOSPA=`18.771545/43.031741/43.053900 m`；`E2E-SG=+0.022159 m`，95%分层配对bootstrap区间=`[+0.000030,+0.065239] m`；Recall@100 m和exact cardinality差均为0 |
| 章级指标 | band macro-F1为`0.918398/0.919476`，balanced count accuracy均为`0.896484`；章级非劣与D8 oracle门通过，但主定位收益门失败 |
| 训练完整性 | `Soft-SG`在epoch 4中途被外部中断，从epoch 3完整checkpoint恢复优化器和Python/NumPy/Torch/CUDA随机状态并完整重放epoch 4；半程日志另存。最终选择epoch 1，早于中断点。`Soft-E2E`未中断。两轨实际均为4 epoch、1024次样本呈现、256次optimizer step和256条梯度记录 |
| 完整性恢复 | 普通读取曾对16k粗DPD和首段raw IQ给出漂移SHA及gzip解码失败；7个数据文件和2个参考checkpoint随后全部固化至`reference_snapshot`，9项无缓冲副本的大小和SHA逐项匹配冻结manifest。既有checkpoint、梯度日志、三轨评价和统计复核通过；已知异常索引均不在256条训练子集中，未重跑训练 |
| 根因结论 | 真正原因尚未唯一定位。G1只以`h5py.File(...,"r")`读取参考数据，没有改写证据；整文件SHA由普通二进制读取计算，因此不能归因于MATLAB→Python的数组转置或HDF5解码语义。现阶段分类为`READ_PATH_INCONSISTENCY`，优先排查重复读取、并发访问、硬链接/路径别名、系统缓存和过滤驱动，不把硬件作为首要解释 |
| G1流程复盘 | G1直接读取原目录，并在prepare、预检、两轨训练、三轨评价和结束门禁多次扫描大型原文件SHA；这不会正常地改变文件，但增加了无必要I/O和缓存压力，也使异常分类过度依赖单次整文件SHA。属于完整性流程设计不足，不是模型、loss、数据划分或评价算法错误 |
| 历史相似性 | 用户反馈原项目曾出现SHA不一致并被判为数据文件损坏，但相关对话和本地记录已删除，现无法复核是否同源。当前本地可检索材料没有保存同类gzip错误证据，因此该历史事实标记为“用户反馈/待复核”，不再表述为从未发生 |
| 总判定 | `G1_NO_GO`，`g2_unlocked=false`；第一版实现停止，不调参、不扩seed、不读取test；后续修正版须另立获批Gate |
| 适用边界 | G1按预注册方案本来就是单seed、小规模pilot；当前恢复的是该方案的完整结果，不是缩减版补救。未记录训练时每次输入张量的逐样本哈希，因此不能提供逐读取字节的绝对证明，但前置全文件身份、无解码错误、已知异常索引排除和完整输出共同支持无需重训恢复 |
| 复核状态 | 最终报告、9项无缓冲快照、两条训练summary、512步梯度日志、三轨评价和2000次bootstrap已回读；新增分级工具通过精确文件匹配、容器漂移但逻辑相同、科研数据变化三类检查 |

## E2E-G1-D1-20260902：原CH3硬槽位计数最终解码诊断

| 字段 | 内容 |
|---|---|
| 实验 ID | `E2E-G1-D1-20260902` |
| 类型 | G1失败后的确定性重解码与重要只读诊断；非训练、非新模型推理、非正式性能Gate |
| 入口 | `统一模型代码/gates/g1/diagnostics/diagnose_g1_hard_count_decode.py` |
| 数据与配置 | 复用G1 epoch 1两轨各512条有序Top-K输出；最终K改为已保存的原CH3硬槽位计数，按Top-K前缀精确截断；K=0/1/2/3各128条，2000次分层配对bootstrap |
| 结果路径 | `outputs_e2e/unified/e2e_g1_hard_count_decode/20260902_143158`；主报告`diagnostic_report.json`，统计审查`validation_report.json` |
| 主结果 | Soft-SG GOSPA `43.031741→18.343600 m`，变化`−24.688141 m`、95% CI `[−26.499691,−22.745981]`；Soft-E2E `43.053900→18.373396 m`，变化`−24.680504 m`、95% CI `[−26.485171,−22.746054]` |
| 计数与基线 | 两轨exact cardinality均由`56.25%→89.65%`；移除原Soft—Hard GOSPA差距的`101.76%/101.64%`。相对Hard-frozen的GOSPA差为`−0.427946/−0.398150 m`，两条95%区间均跨0，只支持恢复至同一量级 |
| E2E因果量 | 硬计数后`Soft-E2E−Soft-SG=+0.029796 m`，95% CI `[+0.000032,+0.088273]`；Recall@100 m和exact cardinality差均为0，定位反馈仍无收益 |
| 分层结果 | Soft-SG的K=0/1/2 GOSPA分别改善`69.83/11.77/21.01 m`，K=3增加`3.87 m`；不能把总体恢复写成所有K均改善 |
| 完整性 | 阶段前后复核7个数据快照和2个checkpoint；源summary、checkpoint、评价报告和逐样本文件身份登记；512/512条均满足硬K是原有序Top-K前缀，独立复算均值逐值一致；无test |
| 统计审查 | 11/11 fallacy scan完成；主要警示为固定均衡K先验、K=3反向、诊断为看到G1结果后的post-hoc validation分析，不得升级为预注册Gate PASS |
| 结论 | `SOFT_COUNT_FAILURE_CONFIRMED`：第一版Poisson-binomial Soft计数是绝对性能崩溃主因；但原`G1_NO_GO`、G2锁定及“定位反馈无收益”结论不变 |
| 适用边界 | 硬计数是候选最终解码和故障隔离，不是已冻结的新模型；继续首要路线须另批修正版小规模配对Gate，不加入空间计数头 |

## E2E-G1-R1-20260902：语义正确槽位存在修正版配对Gate

| 字段 | 内容 |
|---|---|
| 实验 ID | `E2E-G1-R1-20260902` |
| 类型 | 单seed修正版配对训练、独立validation评价和梯度诊断；非论文正式实验 |
| 入口 | `统一模型代码/gates/g1/e2e_g1_r1.py`，核心复用`统一模型代码/gates/g1/e2e_g1.py`；配置`统一模型代码/configs/e2e_g1_r1.json` |
| 算法改动 | 槽位存在由19个相关band概率的Noisy-OR改为`max-logit`；`L_exist`改为正负槽位均衡的直接BCE；最终K改为原CH3硬槽位计数。Soft频带并集、在线细DPD、D8、其余loss和开放层不变 |
| 数据与配置 | 复用G1相同256条训练样本；新`repair_select` 128条和`repair_compare` 512条均衡K并排除旧G1对应样本；两轨各4 epoch、256步；2000次按K分层配对bootstrap；无test |
| 输入 | 只读G1本地`reference_snapshot`的7个数据文件和2个checkpoint；阶段入口与finalize均复核manifest身份，不重新直接训练读取原`outputs` |
| 结果路径 | `outputs_e2e/unified/e2e_g1_r1/20260902_153159`；权威报告`final_report.json`，P0/P1报告`preflight_report.json` |
| P0/P1 | 低尾值不累积、Noisy-OR对照、硬边界、排列不变、频率桥不变、有限梯度、基数归一化7/7通过；K=0/1/2/3各4条两轨全链路通过，预计总墙钟28553.78 s，低于43200 s停止线 |
| 训练完整性 | 两轨均完成4 epoch、1024次样本呈现、256次optimizer step；无恢复、重跑或test。SG/E2E均由`repair_select`选择epoch 0，checkpoint SHA分别为`567898a9dda4d600768035900213483d0f247f7a176393b15e76da8be5cb5aca`和`c21b814cffdfa981a641b49925a36d66aaf1ab1ddee9e4753c597754f1b1c38d`；哈希差异含轨道/优化器元数据，所选模型状态与逐样本输出相同 |
| 主结果 | Hard-frozen/Soft-SG/Soft-E2E GOSPA=`17.143584/17.384145/17.384145 m`；exact count=`89.2578%/89.2578%/89.2578%`；Recall@100 m=`93.3594%/93.4896%/93.4896%`；band macro-F1三轨均为`0.920059` |
| 绝对恢复 | Soft-SG/Hard-frozen GOSPA比=`1.01403`，低于1.05门；exact count完全相同。Hard与Soft的计数混淆矩阵均为`[[125,3,0,0],[11,108,9,0],[2,6,110,10],[0,1,13,114]]`，说明第一版Soft计数崩溃已被修复 |
| 分K GOSPA | Hard-frozen K=0/1/2/3=`1.6573/21.3931/22.3590/23.1650 m`；Soft-SG/E2E=`1.6573/22.3546/22.6472/22.8775 m`。K=3通过预设5%非劣门 |
| E2E因果量 | 两轨正式`samples.jsonl`的SHA均为`4ceb234cae46cd61d100965d0f40d3c31452d036d75fc2197ba40137c7e810a8`；GOSPA、Recall和exact count差均为0，三项2000次bootstrap区间均为`[0,0]`，不满足改善门 |
| Oracle | 两轨oracle-band GOSPA均为`13.153849 m`，D8 oracle非劣门通过；一致性来自两轨最终均选择相同初始化模型，不能解释成E2E训练达到相同上限 |
| 梯度 | SG定位梯度非零率0%；E2E为256/256。E2E定位梯度范数中位数`0.090767`、P95=`2.534753`、最大=`5.672767`；定位/章节范数比中位数`0.424641`；余弦均值`0.002173`、负方向率`49.6094%`；CH3裁剪率`10.9375%` |
| 墙钟 | P0/P1约200.4 s；SG/E2E训练约5761.3/12426.5 s；Hard/SG/E2E评价约1576.2/3191.4/3221.9 s；合计约7.33 h，不含轻量文档与静态检查 |
| 统计审查 | 11/11 fallacy scan完成。主要边界：单training seed、均衡K先验、固定小规模训练；sample bootstrap不含seed方差；两轨差为0源于共同选择epoch 0，不是“E2E训练等效”；validation不能升级为test或所有端到端架构结论 |
| 总判定 | `G1_R1_NO_GO`，`g2_unlocked=false`。语义修复成功解决绝对计数故障，但当前首要实现没有可选择的定位反馈收益；不扩seed、不进入G2、不增加空间计数头 |
| 下一步边界 | 是否停止当前首要实现并切换“双编码器＋逐源中期融合”由用户确认；不得把本结果自动转成调loss、解冻更多层或读取test的授权 |
| 复核状态 | `final_report.json`、两条history/summary、512步梯度日志、三轨评价、两条oracle、逐样本SHA、分K结果和全部Gate checks已回读；输入身份在finalize时再次通过 |

## E2E-G2-20260903：逐源频率—空间潜在融合可行性门

| 字段 | 内容 |
|---|---|
| 实验 ID | `E2E-G2-20260903` |
| 类型 | 单seed、小规模张量接口、固定全频输入兼容、32样本强过拟合与梯度审计；非论文正式实验 |
| 入口 | P0/P1：`统一模型代码/gates/g2/e2e_g2_preflight.py`；P2：`统一模型代码/gates/g2/e2e_g2_latent_fusion.py`；模型：`统一模型代码/models/e2e_latent_fusion.py`；配置：`统一模型代码/configs/e2e_g2_latent_fusion.json` |
| 有效路径 | `outputs_e2e/unified/e2e_g2_latent_fusion/20260903_153441`；权威汇总`final_report.json`。`20260903_153008`为审计字段补齐前的先导目录，不作为正式Gate结论 |
| 输入与完整性 | 只读复用G1本地冻结快照及CH3/D8 checkpoint；P1的32张固定全频DPD和P2的32张固定全频DPD均写入`outputs_e2e`，逐文件登记shape、dtype、有限性、非恒定性和SHA；未读取test |
| P0 | K=0/1/2/3组成batch=4；CH3空间特征/token/global/head hidden/logits及D8 `d0/Heatmap/offset`形状与有限值均通过。CH3 logits、D8 Heatmap和offset的旧新旁路最大绝对误差均为0，判定`PASS` |
| P1输入兼容 | K平衡32条validation上，原分组/固定全频GOSPA=`17.239729/18.597859 m`，比值`1.078779`；Recall@100m=`95.8333%/93.7500%`，下降`2.0833 pp`，未触发联合止损，判定`PASS` |
| P1资源 | 32张固定全频DPD计算`72.35 s`，完整P1约`113.85 s`；按同一实现投影896张缓存约`3187.81 s`（53.13 min），低于2 h停止线。磁盘读取为亚毫秒量级，说明训练阶段可避免每epoch重复生成DPD |
| P2数据与训练 | 32条train样本按K各8条；K=2/3共16条全部为实际频率重叠案例。冻结CH3 CNN/Transformer及D8 backbone/PAN/decoder trunk，只训练前三个band head和批准的query/splitter/FiLM/共享逐源头；120 epoch、960 optimizer step；执行命令墙钟约233.6 s，低于30 min停止线 |
| P2计数与频带 | final exact count=`32/32`，active-band macro-F1=`1.0`；表明存在性和频带监督能够被当前开放参数记忆 |
| P2定位 | Recall@10m最高`64.5833%`（epoch 110），final=`58.3333%`，远低于95%门；final仍有1个多源样本query间距小于10m。重叠样本attention平均L1从初期约`2.41e-4`增至`1.302e-2`，出现区分但不足以稳定拆源 |
| 梯度 | E2E定位loss到band logits/query/spatial attention梯度范数=`0.043697/0.049487/0.310652`，均有限非零；SG时band logits/query严格为0，splitter内部attention仍接收定位梯度，符合“只阻断定位反馈到CH3/query”的对照语义 |
| 冻结D8诊断 | 同一32张固定全频DPD、真实K下，原D8头Recall@10m=`66.6667%`、Recall@100m=`95.8333%`、GOSPA=`18.390279 m`。新增头最高10m召回接近原D8水平，故不是完全退化，但也未证明小样本可强过拟合 |
| 总判定 | `G2_NO_GO_REPRESENTATION`。P0/P1和梯度路径通过；第一版query＋Soft频带加权粗空间图＋FiLM逐源头未通过P2表示/优化能力门，按预注册顺序不执行P3 |
| 结论边界 | 不能据此比较FS-E2E与FS-SG的实际收益，也不能否定固定全频细DPD或所有逐源潜在融合。下一步若继续，须先另行审批source splitting修正并重做小规模P2；不得直接扩256样本、调loss权重、解冻更多层或读取test |

## E2E-G2-R1-20260903：混杂消解与小样本能力恢复门

| 字段 | 内容 |
|---|---|
| 实验 ID | `E2E-G2-R1-20260903` |
| 类型 | 单seed、32条train样本的正式小样本能力诊断；包含输入对照、正源梯度审计和最长300 epoch强过拟合，不是论文性能或泛化实验 |
| 入口与配置 | `统一模型代码/gates/g2/e2e_g2_r1.py`；`统一模型代码/configs/e2e_g2_r1.json` |
| 有效路径 | `outputs_e2e/unified/e2e_g2_r1/20260903_183506`；权威汇总为`final_report.json`，最终状态`G2_R1_SPLIT_PASS_FINE_FAIL` |
| 完整性 | 原G2 manifest、checkpoint和32张固定全频缓存身份在prepare/finalize均通过；`source_run_unchanged=true`；未写原`outputs`，未读取test。G2-R1 manifest SHA256=`55f106f05999f5744c17da350b7d1e0ef700fe4e22d428775bad64ee3d59ed67` |
| P0输入对照 | 同一32条样本和冻结D8下，真实频率分组DPD的Recall@10/30m=`77.0833%/95.8333%`、GOSPA=`10.111361 m`；固定全频DPD为`66.6667%/87.5000%`、`18.390501 m`。K=2/3的16条样本均为单一重叠频率组，说明该对照包含真实重叠拓扑 |
| P0训练合同 | 恢复D8原`focal_loss_hm`、实际应用四项loss权重并保存真实best。K=1/2/3正源batch中Heatmap、offset及组合定位loss到开放路径的梯度均有限；组合loss到band logits梯度范数=`15.782693`，offset单项为`0.001390`；SG侧band/query严格为0。P0判定`PASS` |
| P1训练 | 保持原G2网络、32条样本、固定全频DPD、checkpoint、seed、冻结范围、query/splitter/FiLM/Hungarian不变；epoch 1—120学习率`1e-3`，121—200为`3e-4`，201—300为`1e-4`；完成300 epoch、2400 optimizer step，墙钟`408.37 s`，未超时 |
| P1最佳结果 | epoch 290：exact count=`32/32`，active-band macro-F1=`0.980471`，Recall@10/30/50/100m=`79.1667%/100%/100%/100%`，GOSPA=`7.432876 m`，多源坍缩样本=`0`。分K Recall@10m：K=1 `62.5%`、K=2 `81.25%`、K=3 `83.3333%` |
| Heatmap与offset | 只按Heatmap网格中心解码时Recall@10/30m=`62.5%/100%`；加入offset后为`79.1667%/100%`。正确网格被offset推坏的源数为0，说明offset总体有帮助，但仍不足以达到10 m门。最佳epoch相对前次评价的assignment flip rate为0；目标像素峰排名第一比例`31.25%`、中位排名4 |
| 顺序停止 | P1达到Recall@30m至少95%、无多源坍缩，但Recall@10m低于95%，命中预注册`G2_R1_SPLIT_PASS_FINE_FAIL`。仅为`MULTISOURCE_UNRESOLVED`准备的K=1全频/oracle P2因此不执行；这不是任务遗漏 |
| 结论 | 原G2的`G2_NO_GO_REPRESENTATION`不能再解释为“第一版splitter无法分开多源”。修正训练合同后，该结构已在这32条训练样本上实现30 m尺度的逐源分离；剩余失败集中在Heatmap峰的10 m级精度和offset细化，不应先重构splitter |
| 统计与适用边界 | 11/11统计谬误扫描完成。样本是固定、K平衡、困难重叠子集，且以训练集最佳epoch作能力门；只有一个初始化seed，也同时修正了loss、学习率调度和best记录。因此结果只能证明当前实现存在粗分离能力，不能证明泛化、定位反馈收益或某一项修正的独立因果贡献 |
| 复核状态 | P0/P1/final JSON和两个best checkpoint已回读；`p1_best_recall10.pt` SHA256=`2574f60e00592b33547624f3363442072253d42a75a2991eb8069cbc231e3b8e`；最终报告SHA256=`3f8b2ed9dba072dd2e7446a982e76f89a67acb08980cf45495e2550fdae17da6` |

## E2E-G2-R2-20260904：定位反馈配对因果门

| 字段 | 内容 |
|---|---|
| 实验 ID | `E2E-G2-R2-20260904` |
| 类型 | 单training seed、固定前向的`FS-SG/FS-E2E`配对训练与独立validation比较；非论文最终实验 |
| 入口与配置 | `统一模型代码/gates/g2/e2e_g2_r2.py`；`统一模型代码/configs/e2e_g2_r2.json` |
| 有效路径 | `outputs_e2e/unified/e2e_g2_r2/20260904_163951`；权威汇总`final_report.json`，状态`G2_R2_AUX_REGRESSION` |
| 数据合同 | train/val_select/val_compare=`256/128/512`，各自K=0/1/2/3均衡且角色互斥；test=`0`。896张固定全频DPD由两轨共享，缓存共`576425472`字节 |
| P0/P1 | 合同检查通过；两轨初始前向五组张量最大差均为0。E2E定位loss到band logits/query梯度范数=`0.049691/1.149805`，SG严格为0；splitter和source head两轨均为`29.271663/94.333642` |
| 两轨训练 | 两轨使用同一初始状态SHA256=`3384a074…`、数据顺序、四项单位loss和2432 optimizer step。SG/E2E分别选择epoch 28/36；epoch循环用时`458.67/444.98 s`，checkpoint SHA256=`2a4ec116…/824c51f1…` |
| 定位主结果 | SG/E2E GOSPA=`62.241181/59.217239 m`；逐样本差`-3.023942 m`，按K分层2000次bootstrap 95%区间`[-5.140442,-0.860844] m`。Recall@100m=`52.6042%/56.1198%`，差`+3.5156 pp`，区间`[1.3021,5.7292] pp` |
| 分K与分辨率 | GOSPA差K=0/1/2/3分别为`-0.5524/-0.2348/-4.3700/-6.9386 m`。Recall@10/30/50m由`38.1510/50.7812/51.9531%`提高到`42.3177/52.9948/55.0781%` |
| 辅助指标 | exact count=`89.4531%/88.8672%`，下降`0.5859 pp`；band macro-F1=`87.3215%/86.1942%`，下降`1.1273 pp`；band macro-IoU=`82.1252%/80.7630%`，下降`1.3622 pp`。后两项超过预注册1个百分点护栏 |
| 梯度机制线索 | SG定位上游梯度始终为0。E2E的10个记录点中，定位/辅助梯度范数比中位数`3.5718`，余弦均值`-0.0654`、负方向率`80%`；梯度裁剪率SG/E2E=`8.3882%/11.4720%`。与任务冲突相容，但不足以单独确认根因 |
| 工程恢复 | 首次目录`20260904_154608`在896张缓存生成完成后因梯度审计调用少传`config/loss_part`停止，未启动训练。最小修复通过静态检查后，新目录登记源缓存manifest SHA并复制896张缓存，再逐张加载复核；没有重算约51.7分钟DPD，失败目录保留 |
| 资源与完整性 | 含首次缓存、恢复、两轨和最终比较的实际执行约80分钟，低于2小时线；finalize确认源manifest与R1报告未变化，未写原`outputs`、未读取test。有效manifest/final SHA256=`7d156b4a…/4efc19f8…` |
| 总判定 | `G2_R2_AUX_REGRESSION`。定位反馈相对相同前向SG产生明确且方向一致的定位收益，但当前四项联合训练以轻微频带语义退化换取该收益，未满足完整PASS合同 |
| 统计边界 | 11/11统计谬误扫描完成。主指标预先指定且区间不跨0，分K方向无Simpson反转；但只有一个training seed，sample bootstrap不包含训练方差，分K结果未单独做多重比较推断，validation不能升级为test或稳定泛化结论 |
| 下一步边界 | 暂不进入G3、不调loss、不全解冻；先另行审批任务冲突最小诊断。当前band指标采用band＋Heatmap联合Hungarian匹配，需先区分band logits本身退化与匹配变化，再确认分层和梯度竞争是否可重复 |
| 复核状态 | P0/P1、两轨training report/best、P3逐样本结果和final均已回读；`source_unchanged=true`、`test_executed=false` |

## E2E-G2-R2-D1-20260904：辅助退化来源诊断

| 字段 | 内容 |
|---|---|
| 实验 ID | `E2E-G2-R2-D1-20260904` |
| 类型 | 现有R2 checkpoint的无重训匹配、校准、配对统计和条件梯度诊断 |
| 入口与配置 | `统一模型代码/gates/g2/e2e_g2_r2_d1.py`；`统一模型代码/configs/e2e_g2_r2_d1.json` |
| 有效路径 | `outputs_e2e/unified/e2e_g2_r2_d1/20260904_180714`；final状态`TRUE_BAND_REGRESSION`，解释附加`MATERIALITY_UNCERTAIN` |
| 无更新合同 | 复用R2的128条`val_select`、512条`val_compare`、固定缓存和SG/E2E best；`optimizer_steps=0`、`weights_updated=false`、`test_executed=false`，R2源身份未变化 |
| 匹配解耦 | joint/band-only F1差=`-1.1273/-1.1214 pp`，IoU差=`-1.3622/-1.3703 pp`；SG/E2E的joint与band-only匹配差异率仅`3.65%/5.21%`。下降不是联合Hungarian假象 |
| 分层 | band-only F1差K=1/2/3=`-0.1192/-1.4567/-1.2320 pp`；非重叠/重叠=`-0.1192/-1.3218 pp`。当前样本中K=2/3全部属于frequency-overlap，无法进一步比较多源非重叠情况 |
| 配对区间 | band-only F1差95%区间=`[-2.2398,-0.1037] pp`，IoU=`[-2.7540,-0.0740] pp`；下降方向成立，但两区间均跨`-1 pp`容限，不能证明非劣，也不能证明损害稳定超过容限。exact count差=`-0.5859 pp`，区间=`[-2.1484,0.9766] pp` |
| 概率指标 | band-only BCE SG/E2E=`0.16651/0.14403`，差`-0.02248`，95%区间=`[-0.03513,-0.00945]`；E2E概率loss反而改善，故不是频带概率质量全面退化 |
| 阈值诊断 | `val_select`独立选择SG/E2E阈值`-0.35/-1.05`，冻结应用于`val_compare`后F1=`87.5250%/86.1715%`；差距未恢复，排除简单全局阈值偏移 |
| 梯度审计 | 64条K平衡train、16个batch、epoch0/SG best/E2E best只反向审计。E2E best上游范数比中位数=`4.8108`，余弦均值=`+0.0852`、负方向率=`31.25%`；不支持普遍梯度冲突。K=3负方向率`75%`仅为局部候选，K=2为`25%` |
| 总判定 | `TRUE_BAND_REGRESSION / MATERIALITY_UNCERTAIN`。本次固定模型的Hard频带F1/IoU轻微下降真实存在并集中于重叠多源，但是否稳定超过1个百分点实际容限仍未知；梯度冲突不是已确认根因 |
| 统计边界 | 11/11扫描完成。样本bootstrap不含training seed方差；K与overlap在当前抽样中混杂；分层仅作机制诊断；不得把validation或无重训梯度审计升级为跨seed因果结论 |
| 完整性 | manifest/final/逐样本SHA256=`714bc595…/6e4c68b3…/9d02f365…`；运行约`525.75 s`，源证据未变化 |
| 下一步边界 | 原R2判定不变。若继续，优先原样重复独立training seed并预注册区间非劣规则；不先调loss、上梯度手术、全解冻或读取test |

## E2E-G3-20260905：定位反馈跨seed稳定性门

| 字段 | 内容 |
|---|---|
| 实验 ID | `E2E-G3-20260905` |
| 类型 | 两个预注册新training seed的`FS-SG/FS-E2E`配对训练、预算自适应延长和冻结validation比较；非论文最终实验 |
| 入口与配置 | `统一模型代码/gates/g3/e2e_g3.py`；`统一模型代码/configs/e2e_g3.json` |
| 有效路径 | `outputs_e2e/unified/e2e_g3/20260905_135720`；final状态`G3_FEEDBACK_STABLE_PASS`，辅助`AUX_UNCERTAIN`，预算`TRAINING_BUDGET_UNRESOLVED` |
| 数据与公平合同 | 固定复用R2的train/val_select/val_compare=`256/128/512`及896张全频细DPD；新增seed=`20260905/20260906`。同seed两轨初始化、顺序、loss、优化器和选模相同，唯一区别为定位梯度是否进入CH3/query；test=`0` |
| P0 | 896张缓存逐文件SHA全部通过；两seed的SG/E2E初始状态与五组前向逐值相同。SG上游定位梯度为0，E2E范数分别为`10.7889/11.5362` |
| seed 20260905训练 | SG/E2E在38 epoch内best=`12/24`，不触发延长；epoch循环用时`455.72/476.67 s`，best SHA256=`039b68f8…/6ef38b16…` |
| seed 20260906训练 | 38 epoch时SG/E2E best=`20/36`，触发两轨成对延长至60；最终best=`60/60`，标记`TRAINING_BUDGET_UNRESOLVED`。38 epoch循环用时`463.98/473.59 s`，延长阶段用时`261.29/270.97 s`；best SHA256=`38c9b59c…/790407b7…` |
| seed 20260905定位 | SG/E2E GOSPA=`64.699145/61.034168 m`，差`-3.664978 m`，95%区间`[-5.641863,-1.708006] m`；Recall@100m=`50.5208/55.5990%`，差`+5.0781 pp`，区间`[3.1250,7.0313] pp` |
| seed 20260906定位 | SG/E2E GOSPA=`61.584105/58.813425 m`，差`-2.770681 m`，95%区间`[-5.012375,-0.560794] m`；Recall@100m=`53.9063/56.9010%`，差`+2.9948 pp`，区间`[0.7813,5.4688] pp` |
| 跨seed主判定 | 两个新seed的GOSPA差均值=`-3.217829 m`，两条样本bootstrap区间均低于0且Recall保护门通过；连同历史R2形成3/3 seed总体定位同向改善，预注册判定=`G3_FEEDBACK_STABLE_PASS` |
| K分层 | seed `20260905`的K=0/1/2/3 GOSPA差=`+1.1049/+0.6471/-7.0542/-9.3577 m`；seed `20260906`=`+0.5524/+1.3983/-7.9316/-5.1019 m`。两个新seed总体收益都来自K=2/3，K=0/1轻微变差；历史seed四个K均改善，故只确认异质性，不认定K=0/1稳定退化 |
| 辅助输出 | seed `20260905/20260906` exact count差=`+0.5859/-0.3906 pp`；band-only F1差=`-1.3528/-1.4318 pp`，区间=`[-2.1759,-0.5877]/[-2.3290,-0.5132] pp`；IoU差=`-1.9209/-1.8874 pp`，区间=`[-2.9545,-0.9341]/[-3.1176,-0.6584] pp`；BCE差=`+0.00636/-0.03547` |
| 辅助判定 | Hard频带点估计在两个新seed均下降超过1个百分点，现象没有消失；但各区间均跨`-1 pp`实质损害边界，不满足“两个seed各自支持超过容限”的`MATERIAL_AUX_HARM`条件，判为`AUX_UNCERTAIN` |
| 预算判定 | seed `20260906` E2E在val_select由epoch 36的`61.4003 m`继续改善至epoch 60的`58.1384 m`；SG由epoch 20的`62.6949 m`微幅改善至epoch 60的`62.5712 m`。两轨best均位于60，按规则停止且不得宣称充分收敛 |
| 工程恢复 | 首次延长恢复在optimizer step前因CPU RNG状态随`map_location=device`被映射到GPU而停止。最小修复改为CPU加载并新增受限恢复入口；`repair_receipt.json`确认只有G3入口变化、科研合同未变、失败延长更新未复用，38 epoch证据保留 |
| 完整性与资源 | 训练后896张缓存、R2/D1证据身份再次通过；manifest/final/P2/repair SHA256=`9b6839c3…/3b207677…/19681cae…/9ac4351a…`。manifest至final约67.8分钟，低于90分钟硬线；P2评价计算`117.52 s`，不含此前一次性512条载入 |
| 统计审查 | 11/11扫描完成。两个新seed预注册且全部纳入；总体与K分层存在异质性，不能只报告总体均值；sample bootstrap不含training seed方差，仅两个确认seed不能生成可靠seed级区间；validation不能升级为test或论文最终性能 |
| 总判定 | `G3_FEEDBACK_STABLE_PASS / AUX_UNCERTAIN / TRAINING_BUDGET_UNRESOLVED`。短反馈路径的总体定位收益已跨seed复现，解锁下一步规模与逐步解冻方案设计；不等于模型充分收敛、所有K受益或频带风险消失 |
| 下一步边界 | 下一Gate预注册更充分预算、K分层和Hard频带监控；暂不专门诊断频带根因，不立即调loss、上梯度手术、增加空间计数头或读取test。任何扩大训练仍需另行审批 |
| 复核状态 | 两个seed四轨、38→60触发、512条冻结比较、4000次定位及4000次辅助bootstrap、finalize、源身份和`validation_report.md`均已回读 |

## E2E-RMSE-BACKFILL-20260905：G2-R2与G3冻结预测RMSE补评

| 字段 | 内容 |
|---|---|
| 实验 ID | `E2E-RMSE-BACKFILL-20260905` |
| 类型 | 重要只读诊断；基于冻结逐样本Hard最终预测补算matched RMSE，不重训、不重新推理 |
| 入口 | `统一模型代码/gates/g3/backfill_e2e_rmse.py` |
| 有效路径 | `outputs_e2e/unified/e2e_rmse_backfill/20260905_162701/rmse_backfill_report.json` |
| 输入 | G2-R2 `p3_report.json`、G3 `p2_report.json`及G1本地冻结`val_compare.mat`快照；报告登记三者和G2 manifest的完整SHA256 |
| 口径 | 对Hard最终预测位置集合与真实位置集合执行Hungarian最小欧氏距离匹配；报告matched RMSE/mean/median/P90/P95/max、匹配对数和真实源coverage。配对区间按整条样本、K分层2000次bootstrap，不把同一样本内多个源当作独立样本 |
| G2-R2 | SG/E2E RMSE=`568.7738/543.5034 m`，差`-25.2704 m`，95%区间`[-56.5230,3.3237] m`；median=`22.8242/14.7009 m`，P90=`1068.6702/1019.2227 m`，coverage=`95.7031/95.3125%` |
| G3 seed 20260905 | SG/E2E RMSE=`584.5354/542.7991 m`，差`-41.7363 m`，95%区间`[-68.3606,-18.0873] m`；median=`31.4235/15.4763 m`，P90=`1074.8100/1020.7191 m`，coverage均为`95.5729%` |
| G3 seed 20260906 | SG/E2E RMSE=`571.2086/548.8344 m`，差`-22.3742 m`，95%区间`[-45.6645,1.1033] m`；median=`18.1845/15.3563 m`，P90=`1072.0326/1045.7643 m`，coverage=`95.1823/94.7917%` |
| 结论 | 三组E2E RMSE点估计均优于SG，与GOSPA方向一致，但仅seed `20260905`的RMSE差区间排除0；G2-R2和seed `20260906`不能单靠RMSE宣称明确改善。RMSE远大于median且P90约1 km，说明少数极端错配主导平方误差；必须联合GOSPA、Recall和coverage解释 |
| 边界 | 不改变G2-R2或G3原判定；K=0没有RMSE。未读取test，未修改checkpoint、历史结果文件或数据 |
| 验证 | 512条样本的raw/local索引、真实K与冻结HDF5逐条一致；脚本通过`py_compile`和Ruff F/E9；报告无NaN/Inf且输入身份已登记 |

## E2E-G4-20260905：扩大数据与分组解冻选择门

| 字段 | 内容 |
|---|---|
| 实验 ID | `E2E-G4-20260905` |
| 类型 | 正式训练、双seed结构选择与完整validation比较；不是test或论文最终性能 |
| 入口与配置 | `统一模型代码/gates/g4/e2e_g4.py`、`统一模型代码/gates/g4/recovery/e2e_g4_eager_recovery.py`、`统一模型代码/configs/e2e_g4.json`；batch=4，端点/D8尾部/CH3尾部学习率=`1e-4/2e-5/1e-5`，每2 epoch评价 |
| 数据 | `train/val_select/val_compare=1024/256/512`，各K严格均衡；train为原256条的严格超集，两个validation集合均为此前未用于G2/G3选模或比较的样本；test=0 |
| 有效路径 | `outputs_e2e/unified/e2e_g4/20260905_164500`；唯一有效训练位于`training_v4/`，`training/`、`training_v2/`、`training_v3/`及`invalidated_*_reports/`均为保留的失效尝试 |
| P0与有效输入 | 三轨参数组、有限loss/梯度和约1.70–1.80 GB峰值显存通过。最终运行从原目录登记粗DPD逐样本双读，并从1792个SHA匹配的固定细DPD重建float32 RAM特征；训练期间不读取磁盘特征缓存 |
| P1筛选 | seed `20260907`、16 epoch的A0/A1/A2 GOSPA=`60.8131/60.5174/58.7538 m`，RMSE=`529.6194/541.7287/527.9049 m`，Recall@100m=`57.0313/55.9896/57.0313%`，coverage=`98.4375/98.4375/97.9167%`。A1相对A0的Recall下降`1.0417 pp`而未通过`-1 pp`边界；A2合格且改善GOSPA `2.0593 m`，超过`0.5 m`复杂度边界，故A2入选 |
| seed 20260907最终 | A0/A2成对延长到32 epoch，best=`30/32`。`val_compare` GOSPA=`59.4027/57.6310 m`；matched RMSE=`564.8142/539.2759 m`，median=`13.1149/11.3862 m`，P90=`1075.9459/1004.6537 m`，coverage=`94.5313/95.0521%`；Recall@10/30/50/100m分别为`43.6198/54.2969/55.4688/56.1198%`和`45.5729/56.7708/57.9427/58.5938%` |
| seed 20260907配对 | A2−A0 GOSPA=`-1.7717 m`，95%区间`[-3.9070,0.4324] m`；RMSE=`-25.5384 m`，区间`[-48.6009,-1.9221] m` |
| seed 20260908最终 | A0/A2成对延长到32 epoch，best=`14/24`。GOSPA=`60.7614/59.4786 m`；matched RMSE=`546.6638/538.8480 m`，median=`13.4556/12.1311 m`，P90=`1029.8494/976.2108 m`，coverage=`94.5313/95.0521%`；Recall@10/30/50/100m分别为`43.2292/54.1667/55.0781/55.8594%`和`45.4427/55.3385/56.3802/57.0313%` |
| seed 20260908配对 | A2−A0 GOSPA=`-1.2828 m`，95%区间`[-3.0925,0.6303] m`；RMSE=`-7.8158 m`，区间`[-31.3055,15.5015] m` |
| 章级输出 | seed `20260907` A0/A2 exact count=`87.8906/88.4766%`，band F1/IoU=`87.6411/83.1636%`与`88.1316/83.6381%`；seed `20260908` exact count=`86.9141/88.4766%`，band F1/IoU=`87.0997/82.5090%`与`88.0457/83.6576%`。A2未造成频带或计数实质退化 |
| GOSPA分量 | seed `20260907` A0/A2 localization/missed/false p-sum均值=`5922.07/410.16/224.61`与`5619.30/371.09/234.38`；seed `20260908`=`5948.00/410.16/273.44`与`5823.56/371.09/234.38` |
| 完整性异常与恢复 | 大型HDF5本地快照发生逻辑读取失败；分片特征的两次检查分别出现1个、17个不匹配，首个文件不在后一次异常名单内，不能表述为持续损坏扩展。所有依赖磁盘特征的训练作废。最终运行只在RAM中使用重建特征；两次RAM重建的三split摘要完全一致，模型、样本、loss、seed、学习率和精度未变。后续读取路径复核见`E2E-CACHE-READ-20260906` |
| 资源 | 固定DPD准备`4612.00 s`，最终RAM重建`25.43 s`；五条有效轨道及成对延长共11段训练调用合计`5442.63 s`。首seedA0/A2和第二seedA0/A2均为8192步，首seedA1为4096步；总训练墙钟低于4小时预算 |
| 身份 | manifest/P1/P2/final SHA256=`2d9f0d5e…/352228fa…/f6c120d0…/0d7d13ae…`；RAM重建报告/执行合同/终局审计=`b027126b…/41357bdc…/db937599…`。27个最终checkpoint与JSON各连续读取三次SHA稳定 |
| 总判定 | `G4_PARTIAL_UNFREEZE_SELECTED`。A2相对A0在两个seed的GOSPA与RMSE点估计均同向改善，并通过Recall、coverage与RMSE非劣护栏；后续采用A2的有限解冻范围 |
| 边界 | 两个seed的GOSPA区间仍跨0，不能称为各seed独立显著；首seed A2 best位于epoch 32边界，G5需预注册更充分预算。未读取test，不得把validation数值作为论文最终性能 |
| 复核状态 | RAM-only启动器在打印并落盘最终JSON后无堆栈返回码1，故未以进程码单独认定成功；独立终局审计`final_audit_report.json`返回0且为PASS，逐项确认预注册判定、报告齐全、数值有限、RAM摘要重复一致、27个证据文件三读稳定、失效证据隔离和test=0。入口通过`py_compile`与Ruff F/E9 |

### YYYY-MM-DD 实验或结果审查名称

| 字段 | 内容 |
|---|---|
| 实验 ID | 待填写，例如 `CH3-S2G1-YYYYMMDD` |
| 类型 | 正式训练 / 完整评估 / 重要诊断 / 仅重绘或导出 / 用户反馈待复核 |
| 入口 | 待填写 |
| 数据与配置 | 待填写 |
| 结果路径 | 待填写 |
| 代码版本或指纹 | 待填写 |
| 关键指标 | 待填写 |
| 完整性与公平性 | 待填写 |
| 证据来源 | 日志、模型权重、MAT、CSV、JSON、图像或用户反馈等 |
| 结论 | 待填写 |
| 适用边界 | 待填写 |
| 复核状态 | 待填写 |

## 历史结果索引

- `CH4-G3A-20260821`：第四章中等规模数据、pilot 完全确定性与资源预算，`PASS`，非论文性能证据。
- `CH4-G3B-20260821`：第四章两次60 epoch严格可重复训练与独立oracle-K评估，`PASS_WITH_MORE_EPOCHS_RECOMMENDED`，缩减数据证据。
- `CH3-S2G1-20260821-A`：第三章首次缩减数据生成资源通过，但1条train样本违反300 m源间距，`FAIL_DATA_INTEGRITY`，未训练。
- `CH3-S2G1-20260822-B`：第二次S2-G1运行因启动RAM不足停止，未启动数据生成或训练。
- `CH3-S2G1-20260822`：第三章缩减数据收敛、严格重复、冻结与独立评估通过，非论文正式配置。
- `CH4-S2G2-20260822`：第四章单次200 epoch趋势关闭并冻结新接口checkpoint，`PASS`，仅限缩减数据oracle-K。
- `SYS-S2G3-20260824`：跨章工程链通过；第三章跨分布模型门通过，19子带二值表示门失败，科学状态`REQUIRES_INTERFACE_REDESIGN`。
- `SYS-S2G4-20260824`：预检和pilot通过，完整数据在19子带正标签支持域缺口处停止；未训练D8，根因比较仍不确定。
- `SYS-S2G4-R1-20260824`：0.2支持域闭合并完成200 epoch Coarse-D8；仅恢复15.86% RMSE缺口，判为`REPRESENTATION_LIMIT_CANDIDATE_T020`。
- `SYS-S2G4-R2-20260825`：统一`BW_actual`后Hard-19-Actual达到oracle候选水平，Soft-19无额外收益，判为`SOFT19_INSUFFICIENT`。
- `SYS-S2G4-R3-20260825`：三seed配对确认Hard-19-Actual为稳定oracle候选；Soft仅1/3优于Hard，判为`HARD19_STABLE_CANDIDATE`。
- `SYS-S2G4-R4-D-20260826`：新共享IQ、嵌套manifest和Exact/Hard/Soft三表示DPD生成审计通过；训练尚未开始。
- `SYS-S2G4-R4-20260826`：1k/4k/8k三表示学习曲线完成；Hard-19接口充分性得到支持，Soft路线停止。
- `CH3-S2G5-R2-20260827`：CH3统一数据1k→4k→8k嵌套规模诊断通过；8k继续显著改善计数与频带预测，冻结为R3接口checkpoint。
- `SYS-S2G5-R3-20260828`：四轨级联诊断通过；频带预测无稳定定位损失，完整级联退化主要来自K错误，判定`COUNT_PATH_PRIORITY`。
- `CH3-S2G5-R4-PRETRAIN-20260828`：新增8k和严格嵌套16k通过；eager Dataset容量pilot触发系统RAM红线，长训练未启动，判定`STOPPED_AT_CAPACITY_GATE`。
- `CH3-S2G5-R4-LAZY-20260828`：懒加载抽样、增强和1k三轮配对严格等价；16k资源与吞吐门通过，判定`LAZY_READY_FOR_TRAINING`。
- `CH3-S2G5-R4-20260828`：16k训练、规模/置信度与更新四轨完成；K路径未随规模改善，候选K上限有恢复潜力，判定`ENTER_K_CANDIDATE_GATE`。
- `SYS-S2G5-R5A-20260828`：冻结模型的有序候选K与D8空间证据联合选择通过直接级联门；R5-B按停止规则不启动。
- `SYS-S2G5-R6A-20260829`：三seed联合选择均改善原始级联，但仅1/3通过严格直接门，判定`R6A_MIXED`并停止在R6-B与test前。
- `CH4-S2G5-R6B0-20260830`：三套冻结D8在论文4A/4B控制条件下得到`PAPER_CONDITION_MIXED`；中等SNR和K=2距离接近论文，低SNR与K=3距离长尾未复现。
- `E2E-G1-20260901`：Soft定位反馈相对同前向detach对照未改善GOSPA；完整性恢复后判定`G1_NO_GO`并锁定G2。
- `E2E-G1-D1-20260902`：硬槽位K将两条Soft轨恢复至Hard同量级，确认第一版Soft计数为绝对退化主因；定位反馈仍无收益，原G1判定不变。
- `E2E-G1-R1-20260902`：max-logit槽位存在、直接槽位BCE和Hard最终K恢复Soft绝对性能，但两轨均选择epoch 0，定位反馈无可选择收益，判定`G1_R1_NO_GO`。
- `E2E-G2-20260903`：固定全频输入、缓存速度和短梯度路径通过；第一版逐源拆分未通过32样本定位强过拟合门，判定`G2_NO_GO_REPRESENTATION`并不执行P3。
- `E2E-G2-R1-20260903`：修正训练与证据合同后，32条train样本达到Recall@30m 100%、无多源坍缩，但Recall@10m为79.17%，判定`G2_R1_SPLIT_PASS_FINE_FAIL`并按计划不执行条件P2。
- `E2E-G2-R2-20260904`：相同前向配对下定位反馈明确改善GOSPA和Recall@100m，但band F1/IoU轻微超过1个百分点退化护栏，判定`G2_R2_AUX_REGRESSION`并暂停进入G3。
- `E2E-G2-R2-D1-20260904`：排除联合匹配假象与简单阈值偏移，确认Hard频带下降集中于重叠多源，但其区间跨1个百分点容限；普遍梯度冲突未获支持。
- `E2E-G3-20260905`：两个新seed均复现E2E总体定位收益，判定`G3_FEEDBACK_STABLE_PASS`；Hard频带下降重复但实质边界不确定，且一条seed在60 epoch仍触顶。
- `E2E-G4-20260905`：1024条train上完成A0/A1/A2筛选与双seed确认；A2两seed同向改善并通过护栏，判定`G4_PARTIAL_UNFREEZE_SELECTED`。

## E2E-CACHE-READ-20260906：历史异常分片的三路径只读复核

| 字段 | 内容 |
|---|---|
| 性质与范围 | 用户批准的重要只读诊断；未训练、未评价模型、未读取test，未写原目录或历史缓存。取首次异常的`d8_d2_0240_0256.npy`、后次异常名单前两项`d8_e1_0352_0368.npy`和`d8_e1_0416_0432.npy`，以及`ch3_spatial_0000_0016.npy`对照，共4个train分片 |
| 方法 | 每文件先普通读取一次，再在`GENERIC_READ + FILE_SHARE_READ`句柄保持期间执行普通读取、只读mmap、`FILE_FLAG_NO_BUFFERING`各3次；第二轮逆序。绕缓存读取使用64 KiB对齐地址、8 MiB块，卷逻辑扇区实测512 B，末块按扇区取整请求后按实际文件长度截取 |
| 原始结果 | 4次初读及36次三路径读取全部匹配历史登记SHA；每文件三路径科研数组摘要完全一致、数值有限。文件ID、大小、mtime与链接数前后相同，4文件链接数均为1；4次排斥写入/删除共享的只读句柄均成功取得 |
| 判定 | `NOT_REPRODUCED`：三个历史异常分片当前均恢复到登记字节身份；未证明历史只是误报，也未定位Windows缓存、过滤驱动、写入者或其他底层根因。此次不扩展到全部异常文件或长期负载，不恢复使用旧失效训练证据，G4科学判定不变 |
| 进程证据边界 | 前后psutil打开文件快照均无匹配项，但分别有138/143个不可查询或已退出进程；Windows枚举可能遗漏句柄。无可用handle/Procmon命令，未执行写入事件跟踪，不能据此声称查明写入进程或排除历史写入。保持共享限制的比较窗口只用于限制普通并发写入/替换 |
| 异常保全 | 脚本在不匹配时将当次原始字节与首次读取保存在新目录，记录字节差异总数、前32个偏移和进程快照。本次无异常，未生成`.bin`证据；差异定位函数已用内存中的替换与长度变化用例核验 |
| 工具认识修正 | 历史检查器`open(..., buffering=0)`只关闭Python层缓冲；本次直接调用Windows绕系统缓存读取，不等于绕过设备缓存，也不能单靠重复读取证明持久化介质或系统软件无问题 |
| 资源与证据 | 总耗时44.263 s（含进程枚举）；入口`统一模型代码/common/diagnostics/cache_read_path_probe.py`；汇总及逐次证据位于`outputs_e2e/unified/cache_read_path_audit/20260906_probe01/report.json`及`file_*/`。输出全新，脚本SHA登记在汇总中 |
| 后续 | 继续使用已验证RAM路径推进主线；只有再次捕获不一致时，依据保全的异常字节和读取方式进一步定位。避免仅增加整文件SHA扫描次数 |

## E2E-CACHE-TRAIN-20260906：真实训练操作下的懒加载配对诊断

| 字段 | 内容 |
|---|---|
| 审批与性质 | 用户批准执行真实batch加载、前向/反向和checkpoint保存的两加载器比较。属于工程诊断，诊断权重不能作为Gate性能证据；未运行validation/test，不更改G4正式结果 |
| 样本与输入 | 复用G4 train中起始索引0、240、352、416的四组16条分片，共64条、12个特征文件，覆盖前次抽查的历史异常分片。全部输入运行前核对登记SHA与路径，权重核对G4登记身份；参考文件和历史缓存只读 |
| 配对合同 | G4 A2，seed=20260907，原G4学习率/loss/梯度裁剪，batch=4，64样本独立打乱8遍，每轨128 optimizer step。两轨同一初始化、同一样本顺序，先mmap再verified_lru；每16步保存并重载模型及optimizer，共16份诊断checkpoint。脚本限制15分钟，不模拟全量数据或数小时压力 |
| 加载方法 | mmap沿用G4只读映射与batch复制；verified_lru在分片缺页时读取独立bytes，对同一bytes核对SHA并经BytesIO解析，共享LRU容量256 MiB。两者都将实际返回的每样本特征与预检登记数组摘要比对，逐step记录三个输入摘要、loss和裁剪前梯度范数；正常观察期间不加排斥写入/替换的文件句柄 |
| 正式诊断结果 | `NOT_REPRODUCED_LOADERS_EQUIVALENT`。两轨128步输入/loss/梯度范数序列完全一致，初始和最终模型摘要相同，12份源特征运行后SHA与文件身份不变；本次未捕获异常数据 |
| 独立复核 | 从逐step JSONL重新比较128对记录，并从磁盘独立重载8对checkpoint；所有模型参数和optimizer状态逐值相同，运行脚本SHA与报告登记一致 |
| 资源 | 总88.212 s；mmap/verified_lru训练循环分别22.863/62.615 s（含输入审计及checkpoint保存重载），后者2.739倍。每步抽样进程RSS峰值2.361/2.074 GiB，CUDA allocated峰值约2.20 GiB；RSS不含全部系统文件缓存，也不是连续系统峰值采样 |
| LRU行为 | 实测容量峰值253.5 MiB；565次命中、971次缺页，累计应用层读取45.268 GiB，可能命中系统缓存，不能解释成同等物理磁盘I/O。随机样本访问与256 MiB容量造成大量分片重读；说明当前候选正确且内存有界，但吞吐尚需优化，不能将本轮速度比外推到全量训练 |
| 文件事件观察 | 采用ReadDirectoryChangesW观察源分片目录及新checkpoint目录：前者0条通知，后者34条通知，自检文件事件捕获成功，无通知溢出或线程错误。记录的是文件名/大小/最后写入相关目录通知，不是带PID的完整写入事件跟踪，不能排除历史写入或通知无法覆盖的情况 |
| ETW限制 | 已尝试WPR FileIO跟踪，Windows返回`0x80070005 Access is denied`；没有取得PID级内核文件I/O证据。未提权、未改变权限或结束其他跟踪会话。原始返回信息保存在报告的`wpr_start`字段 |
| 证据 | `outputs_e2e/unified/cache_training_path_audit/20260906_probe01/report.json`；合同、逐step日志、两轨报告、通知JSONL及诊断checkpoint均位于同目录。入口`统一模型代码/common/diagnostics/cache_training_path_probe.py` |
| 结论边界与后续 | 本次真实训练操作下仍未复现历史异常，不能排除长时负载/更大工作集或其他执行时序触发。分片校验LRU可作为后续有界内存加载候选；正式扩规模前需在保持样本顺序的条件下评估分片粒度、批内分组读取及缓存容量，不能直接采用此次高换入换出设置。历史失效训练继续作废，根因仍未定位 |
