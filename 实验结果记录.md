# 实验结果记录

> 本文件记录可追溯的正式实验事实、重要结果审查和具有独立研究意义的诊断结论。Gate 3A 与 Gate 3B 已录入为工程、训练与可重复性证据，不作为论文正式配置的性能结果。

## 记录原则

- 只记录实际运行实验、读取原始结果文件或明确来自用户反馈的事实，不编造指标、路径、权重或结论。
- 每条记录应标注运行类型、入口脚本、数据与结果路径、关键配置、指标、证据来源、可信度和复核需求。
- 用户口述结果标注“用户反馈/待复核”。
- 正式训练、完整评估、重要只读诊断、仅重绘或导出以及冒烟测试必须明确区分。
- 仅验证运行链路、结果字段或工程不变量的冒烟测试默认写入 `修改记录.md`，不作为性能证据。
- 单独执行 Python 语法编译、Ruff、MATLAB Code Analyzer 或代码结构检查，不作为实验结果。
- 声称结果可用于论文前，至少应能追溯实验设置、随机种子、数据、代码版本或指纹、原始输出和图表生成方式。
- 每项重要实验使用稳定实验 ID；其他文档通过实验 ID 或相对路径引用，不复制完整指标表。
- 运行完成时记录的文件数量、目录体积和哈希属于当时快照；后续若经审批清理无效数据或逐字节重复载荷，保留结构化报告、日志、manifest、唯一有效副本和清理记录，不把清理后的目录现状误写成原始运行状态。

## 当前结果状态

| 字段 | 内容 |
|---|---|
| 已核验正式实验 | 暂无论文正式配置实验；已完成第四章缩减数据60 epoch严格重复及单次200 epoch充分性诊断。 |
| 已核验重要验证 | 第四章 Gate 3A/3B和S2-G2已通过；第三章S2-G1经源间距门禁修复后完成缩减数据收敛、严格重复、冻结和独立评估。 |
| 已核验结果文件 | 第四章Gate 3A/3B及`outputs/s2g2_ch4/20260822_143749/s2g2_report.json`；第三章`outputs/s2g1_ch3/20260822_122055/s2g1_report.json`。 |
| 用户反馈结果 | 暂无。 |
| 当前边界 | 第三、第四章结果均来自缩减数据。第三章只做固定seed确定性重复；第四章S2-G2只做单次200 epoch训练且仍为oracle-K。二者均不能写成论文正式指标或端到端复现。 |

## CH4-G3A-20260821：第四章 Gate 3A 中等规模可重复性验证

| 字段 | 内容 |
|---|---|
| 实验 ID | `CH4-G3A-20260821` |
| 类型 | 重要可重复性验证与资源诊断；非正式性能实验。 |
| 入口 | `main31_loc_v3.m`、`gen_ch4_loc_data.py`、`train_yolo.py`、`gate3_ch4.py`、`gate3_stage_runner.py`。 |
| 数据与配置 | IQ/定位 train/val/test=`1024/256/256`；随机种子 MATLAB=`20260822`、Python=`42`；D8 `dualhead`、`dice_weight=0`、no-AMP、batch/val batch=`8/8`；pilot 训练 5 epoch，重复 2 次。 |
| 结果路径 | `outputs/gate3_ch4/20260821_152344`；总报告 `gate3a_gate_report.json`；完整定位数据与审计在 `05_full_loc_after_encoding_fix/`。 |
| 代码版本或指纹 | 当前未提交工作树；结构化 run config、完整 checkpoint、命令、日志和资源 CSV 均在运行目录。未建立 Git commit 指纹。 |
| 数据完整性 | N=2/3 各半；1536 个 IQ 样本哈希全部唯一且跨 split 无泄漏；定位任务共 1536 条、12 个分片、3,951,967,293 bytes；pilot/full 前缀全部张量和索引字段最大绝对差为 0。 |
| 可重复性结果 | 两次 5 epoch 运行的逐 epoch 指标、模型、优化器、调度器、随机状态及最终验证完全一致；三类 mismatch 数均为 0。 |
| 资源结果 | 完整 DPD train/val/test 总墙钟 839.130 s；纳入门禁的最低系统可用内存 13,033,127,936 bytes、峰值进程树 RSS 2,665,586,688 bytes、峰值系统总 GPU 已用显存 6957 MiB；无 warning/red flag。 |
| 预算结果 | pilot 稳态 epoch 中位数最大值 1.45815 s；按样本量 8 倍和 60 epoch 外推 699.912 s，即 11.665 min。该结果未实跑 60 epoch。 |
| 异常与证据保留 | 首次确定性比较 `CRASHED`，首次完整 DPD train 因日志编码/管道问题在 1500 s `TIMEOUT`；均经用户审批最小修复后在全新目录通过，失败目录未删除或覆盖。 |
| 结论 | Gate 3A=`PASS`：中等规模数据链、D8 pilot 完全确定性和本机资源可行性成立，可进入 Gate 3B 方案审批。 |
| 适用边界 | 不证明 60 epoch 实际耗时、收敛、泛化定位误差、论文图表、论文指标或端到端 predicted K；随机初始化/短训练 RMSE 不作性能解释。 |
| 复核状态 | 总门禁回读通过；25 个纳入判定的监控阶段均为 PASS，无 warning/red flag；formal 第四章目录不存在；交付前复核无遗留 Python/MATLAB 实验进程。 |
| 后续证据瘦身 | 2026-08-24 经审批删除 Gate 3A pilot 与完整定位数据逐字节相同的 `03_pilot_loc/data/train/loc_train_000.pt`，以及 `01_gate2_regression` 内与 Gate 2 成功运行逐字节相同的定位分片、pilot 分片和 D8 checkpoint；完整定位数据、Gate 2 成功副本、index、JSON/CSV、日志和总报告保留。上述原始体积与文件数仍表示运行完成时快照。 |

## CH4-G3B-20260821：第四章 Gate 3B 小规模完整训练与严格重复性验证

| 字段 | 内容 |
|---|---|
| 实验 ID | `CH4-G3B-20260821` |
| 类型 | 缩减数据上的完整训练、严格可重复性验证和独立 held-out 评估；非论文正式配置性能实验。 |
| 入口 | `train_yolo.py`、`eval_ch4_checkpoint.py`、`gate3_stage_runner.py`、`gate3b_ch4.py`。 |
| 数据与配置 | 复用 Gate 3A 已审计定位数据 train/val/test=`1024/256/256`、8/2/2 分片；D8 `dualhead_std`、FP32/no-AMP、batch/val batch=`8/8`、AdamW、初始学习率 `1e-3`、CosineAnnealingLR、weight decay `5e-3`、dropout `0.4`、60 epoch、patience 60、seed 42、deterministic。 |
| 结果路径 | `outputs/gate3b_ch4/20260821_173526`；总报告 `gate3b_gate_report.json`。Gate 3A 历史根 `outputs/gate3_ch4/20260821_152344` 保留原名和原位，仅被只读复用。 |
| 代码版本或指纹 | 当前未提交工作树；总报告记录 `train_yolo.py`、`eval_ch4_checkpoint.py`、`gate3_stage_runner.py` 和 `gate3b_ch4.py` 的路径、大小和 SHA256。 |
| 数据完整性 | 12 个定位分片和 3 个 index 共 3,951,967,293 bytes；训练前后15个文件的大小和 SHA256 完全一致。 |
| 训练结果 | A/B 均完成60 epoch且未提前停止；墙钟分别 681.222/683.018 s。best epoch 均为57，best validation oracle-K RMSE均为150.791921 m，最终学习率均为 `1e-6`。 |
| 学习判定 | 未训练模型 validation loss/RMSE 为 7.011946/1075.637084 m；冻结 best 为1.016160/150.791921 m，两项均严格改善，A/B均为 `LEARNING_CONFIRMED`。 |
| 严格重复性 | 排除耗时和资源字段后，60 epoch历史、初始/最终validation、summary关键字段、best/last checkpoint中的模型、optimizer、scheduler、scaler及RNG状态全部逐值一致，所有 mismatch 列表为空。 |
| 冻结 checkpoint | 只依据 validation 从训练A选取 epoch 57；`best_yolo_dualhead_std.pth` SHA256=`8cf7f9541b3269539b7b16580d7a13dd2b9a9770afa1b0032b3ec491844f0153`；test 未参与选择。 |
| validation oracle-K | 两次独立进程完全一致并与训练 best 完全一致：RMSE 150.791921 m，mean 35.004812 m，median 4.695417 m，within 10/30/50 m=`0.6859375/0.878125/0.93125`。 |
| test oracle-K | 两次独立进程完全一致：loss 0.999282，RMSE 110.565991 m，mean 26.120635 m，median 4.740854 m，within 10/30/50 m=`0.7171875/0.8953125/0.93125`。N=2/N=3 RMSE为81.613676/126.230752 m。 |
| 资源结果 | 14个阶段监控报告均PASS且无warning/red flag；最低系统可用RAM 14,255,198,208 bytes，峰值进程树RSS 3,764,129,792 bytes，峰值系统总GPU已用显存7388 MiB；报告前输出615,897,001 bytes。 |
| 总门禁 | `PASS_WITH_MORE_EPOCHS_RECOMMENDED`；工程、重复性、有效学习、独立评估、数据完整性和隔离均通过，但best epoch=57落在最后5个epoch，趋势为 `STILL_IMPROVING_AT_LIMIT`。 |
| 适用边界 | 全部定位指标使用真实源数决定Top-K，是oracle-K；数据规模和配置不等于论文正式实验，未验证predicted-K、论文图表、论文指标、全量泛化或当前创新性。原始test JSON中的 `metrics.val_loss` 表示当前test split loss，总报告已映射为 `test_loss`。 |
| 复核状态 | 总报告回读、代码语法/Ruff、数据前后SHA、formal目录隔离和残留实验进程检查均通过；未发生崩溃、超时、自动重试或失败目录复用。 |

## CH3-S2G1-20260821-A：第三章首次缩减数据生成与完整性失败

| 字段 | 内容 |
|---|---|
| 实验 ID | `CH3-S2G1-20260821-A` |
| 类型 | S2-G1缩减数据生成、资源诊断和数据完整性审计；未进入训练。 |
| 入口 | `main30.m`、`s2g1_ch3.py`、`gate3_stage_runner.py`。 |
| 数据与配置 | train/val/test=`2048/512/512`；0/1/2/3源=`20%/20%/30%/30%`；seed=`20260823`；19子带、81×81粗网格。 |
| 结果路径 | `outputs/s2g1_ch3/20260821_204124`。 |
| 代码身份 | Git HEAD=`a18e36a`，工作树含本次已批准的增量S2-G1代码和文档修改；预检保存完整状态，未创建提交。 |
| 预检 | `PASS`；31次采样，可用RAM中位数16,169,549,824 bytes、最低16,060,497,920 bytes，GPU总占用峰值4131 MiB，磁盘最低545,248,833,536 bytes；仅有低于16 GiB提示，未触及14 GiB启动线。 |
| 数据生成 | MATLAB退出码0，墙钟537.635 s，内部总耗时529.8 s；三文件共1,241,624,245 bytes；类别数量精确符合410/410/614/614和102/102/154/154。 |
| 资源 | 数据生成最低系统可用RAM13,268,566,016 bytes，峰值进程树RSS2,795,159,552 bytes，GPU总占用峰值5250 MiB，输出峰值1,241,637,071 bytes；无warning/red flag。 |
| 数据失败 | train样本索引1989为2源样本，最小源间距124.982101 m，小于声明约束300 m；val/test最小间距分别301.039581/300.300293 m。结构化 `data_audit.json`=`FAIL`。 |
| 原因 | 原始 `gen_multi_source_pos_v2.m` 在5万次约束放置失败后，以不检查源间距的随机位置补齐；不是第一阶段路径/设备修改造成。 |
| 总判定 | `FAIL_DATA_INTEGRITY`；按预先规则停在数据层，不自动修复、重生成或进入训练。 |
| 未执行 | batch 64容量检查、两次100 epoch训练、checkpoint冻结、validation和test均未执行。 |
| 适用边界 | 只证明当前受控数据生成资源可行并暴露原生成器边界；不能判断第三章收敛或性能。 |
| 后续证据保留 | 2026-08-24 经审批删除三个不合格 MAT 文件，共1,241,624,245 bytes；运行根、`data_audit.json`、`data_manifest_before.json`、阶段报告、资源CSV和日志保留。上方数据生成体积、类别数量和违规样本事实是清理前已完成审计的历史快照，原始 MAT 已不可直接回读。 |

## CH3-S2G1-20260822-B：源间距门禁修复后的资源预检停止

| 字段 | 内容 |
|---|---|
| 实验 ID | `CH3-S2G1-20260822-B` |
| 类型 | S2-G1全新目录重跑的启动资源预检；未执行数据生成或训练。 |
| 结果路径 | `outputs/s2g1_ch3/20260822_120601`。 |
| 代码状态 | S2受控路径已增加300 m后验检查、最多100次位置重试和重试元数据；共享 `gen_multi_source_pos_v2.m` 未修改。 |
| 预检结果 | `FAIL`；60秒采样的可用RAM中位数低于14 GiB启动线。失败JSON未保留精确中位数；预检结束后的只读快照为空闲RAM约13.861 GiB。 |
| 过程边界 | 未启动MATLAB，未生成train/val/test，未启动容量检查或训练；结束后无S2-G1残留实验进程。 |
| 总判定 | `BLOCKED_RESOURCE_PREFLIGHT`；属于当前系统资源不足，不是数据、算法或模型失败；不自动重试。 |

## CH3-S2G1-20260822：第三章缩减数据收敛与严格重复验证

| 字段 | 内容 |
|---|---|
| 实验 ID | `CH3-S2G1-20260822` |
| 类型 | 重要缩减数据收敛、确定性和独立重载验证；不是论文正式配置实验。 |
| 入口 | `main30.m`、`s2g1_train_ch3.py`、`s2g1_ch3.py`、`gate3_stage_runner.py`。 |
| 数据与配置 | train/val/test=`2048/512/512`，MATLAB seed=`20260823`；Transformer、M=10、FP32、batch/val batch=`64/64`、Python seed=`42`、100 epoch、warmup 5、patience 25、固定阈值0.5。 |
| 结果路径 | `outputs/s2g1_ch3/20260822_122055`；总报告 `s2g1_report.json`。 |
| 数据完整性 | 三文件共1,241,665,513 bytes、3072条样本无重复或跨split泄漏；train/val/test最小源间距为301.627/300.396/303.188 m，标签复算不一致均为0；train拒绝1个违规布局并重试1次。训练前后文件SHA256完全一致。 |
| 容量检查 | batch 64前向、loss、反向和optimizer step通过；PyTorch峰值allocated/reserved约3.42/5.23 GiB，监控GPU总占用峰值8343 MiB。 |
| 训练与重复性 | A/B均完成100 epoch，内部耗时885.050/820.310 s；100个epoch科研字段、best epoch及best/last的模型、optimizer、scheduler和RNG状态逐值一致，scientific mismatch=`0`。 |
| 冻结模型 | 训练A第96 epoch；SHA256=`22efb069102fb2e2c083d2897528610986e3031049222efd7875b7df70a190fb`，大小10,052,783 bytes；仅由warmup后最低validation loss选择，test未参与。 |
| Validation | 两次独立重载完全一致：loss=0.002593，count accuracy=0.90625，balanced=0.91546，count MAE=0.09961，0源虚警率=0.00980，活跃槽位band macro-F1=0.93296。 |
| Test | 仅运行一次：loss=0.003085，count accuracy=0.88867，balanced=0.89919，count MAE=0.12109，漏计/多计率=0.05078/0.06055，0源虚警率=0，活跃槽位band macro-F1=0.92720。 |
| 学习与收敛门禁 | validation loss相对初始化下降0.98525，balanced accuracy和band macro-F1分别提高0.91546/0.69146；末10轮相对此前最佳改善0.00431，小于1%，两项门禁均通过。 |
| 资源与时长 | 60秒预检RAM中位数/最低约19.45/19.16 GiB；全阶段最低可用RAM14,768,390,144 bytes，进程树RSS峰值3,085,451,264 bytes，GPU总占用峰值8840 MiB；目录1,283,101,317 bytes。14个监控阶段合计2326.277 s，另有62.341 s预检；无warning/red flag。 |
| 总判定 | `PASS`；第三章在当前缩减配置下能够稳定学习、严格重复并独立重载。 |
| 适用边界 | 单一缩减数据、单一固定seed和固定阈值；不证明跨seed统计稳定性、数据规模充分性、论文全量指标或当前创新性。 |
| 复核状态 | 总报告、数据前后SHA、checkpoint SHA、两次validation、单次test、代码指纹、14份监控报告和残留进程均已回读；无自动重试或目录复用。 |

## CH4-S2G2-20260822：第四章缩减数据充分训练诊断与接口checkpoint建立

| 字段 | 内容 |
|---|---|
| 实验 ID | `CH4-S2G2-20260822` |
| 类型 | 缩减数据单次200 epoch充分训练诊断、统一validation checkpoint比较和独立重载；非论文正式性能实验。 |
| 入口 | `train_yolo.py`、`eval_ch4_checkpoint.py`、`s2g2_ch4.py`、`gate3_stage_runner.py`。 |
| 数据与配置 | 只读复用Gate 3A定位数据train/val/test=`1024/256/256`；D8 `dualhead_std`、FP32/no-AMP、batch/val batch=`8/8`、AdamW、lr=`1e-3`、CosineAnnealingLR `T_max=200`、patience 200、seed 42、deterministic；未运行test。 |
| 结果路径 | `outputs/s2g2_ch4/20260822_143749`；总报告`s2g2_report.json`。 |
| 数据完整性 | 15个文件共3,951,967,293 bytes，训练前后大小和SHA256完全一致；正式data/output目录不存在。 |
| 训练结果 | 完成200/200 epoch、每轮128 step，墙钟2383.900 s；初始validation loss/RMSE=7.011946/1075.637084 m，best位于epoch 93，loss/RMSE=1.086389/130.726516 m；最后20轮无新增收益，`TREND_CLOSED`。 |
| 训练诊断 | epoch 93后train loss由0.873114继续降至0.703357，而最终val loss/RMSE升至1.346190/233.486805 m，记录为可能过拟合模式；仅作描述，不改变预设checkpoint选择规则。 |
| checkpoint比较 | 同一validation评估器下，Gate 3B epoch 57 RMSE=150.791921 m，新epoch 93 RMSE=130.726516 m，降低13.31%，故判定`NEW_200_SELECTED`。冻结SHA256=`5d5224d8d6478739af5042391ef1e718c08e2f81565882e133372c9fbd116dfd`。 |
| 长尾诊断 | Gate 3B→新权重：median=4.695417→5.421757 m，P90=37.247806→38.947443 m，P95=84.823923→89.758282 m，>500 m错误数=15→9，最大误差=1749.104492→1695.470215 m；640源中293改善、347变差，说明RMSE收益主要来自减少部分灾难性错误，并非普遍精度提升。 |
| 分层结果 | 新权重N=2/N=3 RMSE=73.015178/157.886750 m；overall mean=30.875801 m，within 10/30/50 m=`0.678125/0.8609375/0.91875`。 |
| 独立重载 | 冻结checkpoint第二次独立validation的聚合指标、P90/P95和640条逐源误差与首次评估完全一致，`checkpoint_reload=EXACT`。200 epoch训练本身未做第二次严格重复。 |
| 资源 | 训练峰值系统GPU已用6621 MiB，进程树RSS峰值3,764,576,256 bytes，最低系统可用RAM14,280,052,736 bytes；4份有效监控报告均PASS且无warning/red flag，报告前输出309,951,692 bytes。 |
| 工程异常 | 首次S2预检因Git输出使用Windows默认代码页而在训练前失败，修复为显式UTF-8后使用全新根目录；最终根中的首次Gate 3B评估又因手写历史时间戳错误立即失败，保留失败目录后按checkpoint SHA定位真实路径并在新目录通过。总门禁显式区分4份PASS证据与1份保留失败尝试。 |
| 总判定 | `PASS`；缩减配置的200 epoch趋势已关闭，并建立可用于S2-G3的接口checkpoint。 |
| 适用边界 | 仅限固定缩减数据、单seed、oracle-K和一次200 epoch训练；不证明跨seed稳定性、正式训练epoch、predicted band/K、论文指标或当前创新性。 |
| 复核状态 | 总报告、输入前后SHA、两候选统一validation、冻结SHA、第二次重载、4份PASS监控、失败证据、formal隔离和残留进程均已回读；S2-G2未生成test结果。 |

## SYS-S2G3-20260824：跨章可组合性、分布契约与频带表示上限审查

| 字段 | 内容 |
|---|---|
| 实验 ID | `SYS-S2G3-20260824` |
| 类型 | 重要跨章兼容性与表示上限诊断；不是论文正式端到端性能实验。 |
| 入口 | `s2g3_compute_coarse_dpd.m`、`s2g3_composability.py`、`gate3_stage_runner.py`；复用原第三章DPD/Transformer和第四章D8实现。 |
| 固定输入 | 只读使用Gate 3A的256条validation共享IQ和定位数据，N=2/N=3各128条；第三章epoch 96 checkpoint SHA256=`22efb069102fb2e2c083d2897528610986e3031049222efd7875b7df70a190fb`；第四章epoch 93 D8 checkpoint SHA256=`5d5224d8d6478739af5042391ef1e718c08e2f81565882e133372c9fbd116dfd`。 |
| 固定配置 | 第三章Transformer、M=10、阈值0.5，预测K保留0–10；第四章D8 `dualhead_std`、FP32/no-AMP、batch 8；GOSPA `p=2,c=100 m,alpha=2`；未调阈值。 |
| 结果路径 | `outputs/s2g3_composability/20260824_132001`；解释修正版总报告`s2g3_report_interpretation_fix.json`，SHA256=`ba55cc2e9b7ca44cc883db19687cd0d7f5e49b899e27bca9b823a85f61095610`；初版总报告保留为被替代证据。 |
| 代码身份 | 新增MATLAB入口SHA256=`fc453f9cb0bd3e828404658f2a830f0273f0d24502a6066a822718cf1533b8a3`；Python编排入口最终SHA256=`d29dd7d901a5eaa444505727a9603c4d7e34d710f8ff8d2c7f0ec75146a73cc5`；原训练和模型入口未修改。 |
| 数据身份 | 第四章raw validation IQ SHA256=`6d1749ae89c00c5ce863e8e38e8007fd692b63ea58ad8e8aecb34328d509e9bb`；粗DPD `coarse_val.mat` SHA256=`be1cd6b376d2e54f894d60a9fe7c8b8f142e324ae9b7efcbade2b036e11a5212`；样本索引严格为0–255且全部`group_idx=0`。 |
| 分布契约 | 阵列、载频、采样率、4096点观测和19子带等基础参数一致；第三章固定10 MHz symbol rate/13 MHz实际带宽，第四章实际带宽约2.665–25.935 MHz；两章还在频率重叠拓扑、位置半径/间距、功率范围和SNR定义上存在分布或定义差异。 |
| 第三章跨分布计数 | 两次独立推理的logits、概率、频带和K逐元素一致；accuracy/balanced accuracy=`0.691406/0.691406`，Wilson 95%下界=`0.632342`，N=2/N=3准确率=`0.492188/0.890625`，MAE=`0.3125`，K=0/K>3比例=`0.003906/0`。 |
| 第三章跨分布频带 | 活跃槽位precision/recall/F1=`0.889203/0.854427/0.871468`；物理合并IoU中位数=`0.753765`，相对全频带平凡基线的bootstrap 95%下界=`0.477717`；真实频带覆盖率中位数=`1.0`，空频带比例=`0.003906`。全部预设模型门通过。 |
| Exact-Oracle回归 | 新评估器最终精确回归S2-G2：640条匹配误差，RMSE=`130.72651561958395 m`，绝对差为0；`recall@100 m=0.95625`。原S2-G2入口独立复算也得到完全相同RMSE。 |
| Coarse-Oracle表示 | 按第三章真值19子带标签映射回FFT频带后，无空频带；细DPD SHA256=`a968afe4514bbbbfee20650112a197d7d7e86f73e2ff9e0e47f29308d886802e`。oracle-K匹配RMSE=`164.148382 m`，相对Exact比值=`1.255662`；`recall@100 m=0.95`，下降0.00625。 |
| 表示门 | Exact回归、样本完整、非空频带和100 m召回门通过；RMSE增幅超过10%门槛，因此`representation_gate_pass=false`。退化主要表现为长尾放大，不能仅凭接近的100 m召回判定接口充分。 |
| 条件四轨 | 因表示门失败，按预案未生成predicted-band细DPD，未运行`OB-PK/PB-OK/PB-PK`；`OB-OK`只作为Exact回归，`CO-OK`只作为表示控制。 |
| 资源 | 有效60秒预检RAM中位数/最低约14.81/14.74 GiB、GPU总占用峰值3747 MiB；15份PASS阶段监控合计317.463 s，最低系统可用RAM14,043,648,000 bytes，进程树RSS峰值1,686,446,080 bytes，GPU总占用峰值4549 MiB；运行根188,781,798 bytes，无warning/red flag。 |
| 工程修复与失败证据 | 保留2份未作为通过证据的监控失败：预检被外层包装器误识别为残留进程、完整粗DPD首次引用错误历史路径；另有一次监控采样间隔参数在子进程启动前被拒绝。Exact评估先后修正逐样本归一化归约顺序和float32距离口径，最终精确回归；旧评估输出均保留为被替代证据。 |
| 禁止范围核对 | 未生成新IQ，未训练或微调，未运行test，未调整阈值，未修改模型、DPD定义或原训练入口；结束后无S2-G3残留Python/MATLAB进程。 |
| 总判定 | 工程`PASS`；科学状态`REQUIRES_INTERFACE_REDESIGN`。第三章冻结模型通过跨分布门，但当前19子带映射与冻结D8的组合不满足预设定位长尾门槛；本实验不能单独区分接口信息损失和下游输入分布失配。 |
| 后续决策 | 在继续完整端到端四轨前，先单独审批连续频带边界、概率/更细频率表示或下游接口适配的oracle pilot；本结果不得表述为论文端到端性能、信息论上限或两章方法失效。 |

## SYS-S2G4-20260824：Coarse-Oracle D8适配的数据支持域停止审查

| 字段 | 内容 |
|---|---|
| 实验 ID | `SYS-S2G4-20260824` |
| 类型 | S2-G4身份/资源预检、工程pilot和完整元数据支持域诊断；在数据门停止，不是模型训练或性能实验。 |
| 入口 | `s2g4_resource_preflight.py`、`s2g4_coarse_d8.py`、增量S2-G4训练入口和`gate3_stage_runner.py`。 |
| 固定输入 | 只读复用Gate 3A train/validation=`1024/256`原始IQ和Exact定位数据；Exact-D8 epoch 93 SHA256=`5d5224d8d6478739af5042391ef1e718c08e2f81565882e133372c9fbd116dfd`；第三章10 MHz子带、0.3正标签覆盖阈值保持不变。 |
| 结果路径 | `outputs/s2g4_coarse_d8/20260824_164707`；停止报告`s2g4_stop_report.json`，频带支持明细`band_support_audit.json`。 |
| 预检与pilot | 轻量60秒RAM预检中位数21,531,000,832 bytes，身份预检通过；train/validation各8条Coarse数据构造通过，一次真实forward/loss/backward/optimizer step、validation、保存和重载通过，仅证明工程链。 |
| 数据门失败 | 完整train构造在样本205触发空Coarse-Oracle频带断言；该样本两个源的symbol rate为2.95/2.45 MHz，均无法达到正标签所需的3 MHz最小主瓣宽度。未回退Exact频带、未把ignore改为正带、未调整阈值。 |
| 完整支持域审计 | train共1024样本、2560有效源，其中154源无任何正子带，且与154个`symbolRate<3 MHz`源完全一致；有1个整样本为空。validation共256样本、640有效源，其中38源无正子带且同样与`symbolRate<3 MHz`完全一致；因同样本其他源提供正带，整样本空频带数为0。 |
| 结构结论 | `REPRESENTATION_SUPPORT_GAP_CONFIRMED`：当前第三章正标签接口不覆盖第四章完整2–20 MHz symbol-rate域；S2-G3只检查联合频带非空，曾掩盖validation中38个未被表达的窄带源。 |
| 资源 | 触发科学停止的完整构造尝试最低可用RAM18,036,236,288 bytes、进程树RSS峰值1,934,385,152 bytes、GPU总占用峰值6132 MiB、输出峰值328,851,679 bytes，无warning/red flag；失败不是资源超限。 |
| 未执行 | 完整Coarse train/validation、200 epoch从头训练、60 epoch微调、六项Exact/Coarse交叉评估、test、predicted-band/K及CH3修改/训练均未执行。 |
| 总判定 | `STOPPED_AT_DATA_GATE`；工程`PARTIAL_PASS`。原科学问题“D8分布失配还是19子带信息不足”为`INCONCLUSIVE_FOR_D8_VS_REPRESENTATION`，不得用本次结果宣称D8适配成败。 |
| 后续决策 | 另行审批小型oracle接口定义pilot，比较当前正标签、正标签加ignore、物理相交硬标签、软重叠权重和Exact连续边界；先闭合支持域，再恢复D8适配诊断。 |
| 复核状态 | 停止报告、全量元数据审计、pilot输出、监控报告和原始输入身份已回读；部分构造目录保留但不得复用为有效完整数据。 |

## SYS-S2G4-R1-20260824：0.2阈值Coarse-D8快速适配诊断

| 字段 | 内容 |
|---|---|
| 实验 ID | `SYS-S2G4-R1-20260824` |
| 类型 | 0.2 Coarse-Oracle支持域、完整缩减数据、单次200 epoch D8训练和EE/EC20/CC20三轨诊断；非论文正式性能实验。 |
| 入口 | `s2g4_resource_preflight.py`、`s2g4_coarse_d8.py`、`train_yolo.py --s2g4_scratch`和`gate3_stage_runner.py`。 |
| 固定输入 | 只读复用Gate 3A train/validation=`1024/256` IQ与Exact监督；S2-G2 epoch 93 Exact-D8 SHA256=`5d5224d8d6478739af5042391ef1e718c08e2f81565882e133372c9fbd116dfd`；19个10 MHz子带、5 MHz步长、覆盖阈值0.2。 |
| 标签边界 | 0.2标签由`fc_offset`、逐源`symbolRate`和子带边界独立重算，不读取旧0.3 `band_mask`，不把ignore转正，不使用Exact频带回退；原MATLAB默认阈值和CH3均未修改。 |
| 结果路径 | `outputs/s2g4r_threshold020/20260824_182523`；总报告`s2g4r_report.json`，SHA256=`96ce7f318934ddc37dc281f14a9c9ffb3b9a4e4bd1fbbfd0f783bf5a2800af1e`。 |
| 支持域与pilot | train的2560个源、validation的640个源全部至少有一个正子带，空联合频带均为0；train/validation各8条数据构造和一次forward/loss/backward/optimizer step通过。 |
| 完整数据 | 生成1024/256条0.2 Coarse细DPD，train/validation耗时596.201/147.536 s；分别8/2个紧凑分片，总运行根约1.21 GiB，无空频带或非有限值。 |
| 训练配置 | D8 `dualhead_std`、随机初始化、FP32/no-AMP、batch/val batch=`8/8`、AdamW、lr=`1e-3`、weight decay=`5e-3`、dropout=`0.4`、Focal+offset、dice=`0`、seed 42、deterministic、200 epoch、`T_max=200`。 |
| 训练结果 | 完成200 epoch，耗时2252.261 s；初始RMSE=1086.519373 m，best为epoch 93、RMSE=157.505013 m，最后20轮改善为0，`trend_closed=true`、`effective_learning=true`。best SHA256=`8bbeadc36439320a394ecb89a20fd78f1639641f4ab3eb30946b5066d92e2c10`。 |
| 三轨结果 | EE/EC20/CC20 RMSE=`130.726516/162.551301/157.505013 m`；mean GOSPA=`27.591597/29.530322/32.363494 m`；100 m recall=`0.95625/0.95/0.93125`。 |
| 长尾结果 | EE→EC20→CC20的median=`5.4218/5.1126/5.0440 m`，P90=`38.9474/44.9211/53.9678 m`，P95=`89.7583/101.5253/224.7599 m`；>100 m错误数=`29/33/44`，说明scratch的典型误差不差但长尾恶化。 |
| 因果判定 | RMSE恢复比例=`0.158565`，CC20/EE RMSE比=`1.204844`，mean GOSPA比=`1.172947`，100 m召回下降=`0.025`。RMSE和GOSPA等效门失败，召回门通过，科学状态=`REPRESENTATION_LIMIT_CANDIDATE_T020`。 |
| 资源 | 13个受监控阶段全部PASS且无warning/red flag；合计3098.437 s，最低系统可用RAM16,405,217,280 bytes，进程树RSS峰值3,298,168,832 bytes，GPU总占用峰值6318 MiB。 |
| 未执行 | 未运行test、predicted-band/K、CH3训练、Exact-D8微调或CE/FE/FC扩展轨道。 |
| 适用边界 | 只证明当前缩减数据、D8、0.2硬子带和单seed下，重新训练D8不足以恢复Exact性能；不是信息论证明，也不排除更大数据、其他模型、软子带或连续频带改善。 |
| 总判定 | 工程`PASS`；科学状态`REPRESENTATION_LIMIT_CANDIDATE_T020`。下一步优先验证更有表达力的oracle频带，不建议直接重训CH3采用0.2标签。 |
| 复核状态 | 支持审计、完整数据报告、三轨逐样本JSONL、200 epoch history、best SHA、总报告和13份监控报告均已回读；无失败重试和残留实验进程。 |

## 实验记录模板

### YYYY-MM-DD 实验或结果审查名称

| 字段 | 内容 |
|---|---|
| 实验 ID | 待填写，例如 `CH3-S2G1-YYYYMMDD` |
| 类型 | 正式训练 / 完整评估 / 重要诊断 / 仅重绘或导出 / 用户反馈待复核 |
| 入口 | 待填写 |
| 数据与配置 | 待填写 |
| 结果路径 | 待填写 |
| 代码版本或指纹 | 待填写 |
| 关键指标 | 待填写 |
| 完整性与公平性 | 待填写 |
| 证据来源 | 日志、模型权重、MAT、CSV、JSON、图像或用户反馈等 |
| 结论 | 待填写 |
| 适用边界 | 待填写 |
| 复核状态 | 待填写 |

## 历史结果索引

- `CH4-G3A-20260821`：第四章中等规模数据、pilot 完全确定性与资源预算，`PASS`，非论文性能证据。
- `CH4-G3B-20260821`：第四章两次60 epoch严格可重复训练与独立oracle-K评估，`PASS_WITH_MORE_EPOCHS_RECOMMENDED`，缩减数据证据。
- `CH3-S2G1-20260821-A`：第三章首次缩减数据生成资源通过，但1条train样本违反300 m源间距，`FAIL_DATA_INTEGRITY`，未训练。
- `CH3-S2G1-20260822-B`：第二次S2-G1运行因启动RAM不足停止，未启动数据生成或训练。
- `CH3-S2G1-20260822`：第三章缩减数据收敛、严格重复、冻结与独立评估通过，非论文正式配置。
- `CH4-S2G2-20260822`：第四章单次200 epoch趋势关闭并冻结新接口checkpoint，`PASS`，仅限缩减数据oracle-K。
- `SYS-S2G3-20260824`：跨章工程链通过；第三章跨分布模型门通过，19子带二值表示门失败，科学状态`REQUIRES_INTERFACE_REDESIGN`。
- `SYS-S2G4-20260824`：预检和pilot通过，完整数据在19子带正标签支持域缺口处停止；未训练D8，根因比较仍不确定。
- `SYS-S2G4-R1-20260824`：0.2支持域闭合并完成200 epoch Coarse-D8；仅恢复15.86% RMSE缺口，判为`REPRESENTATION_LIMIT_CANDIDATE_T020`。
