# 端到端多源定位框架：相关文献综述与结构设计建议

> **用途**：针对当前“CH3 检测计数/频带估计 → CH4 多源定位”统一端到端框架中，定位梯度虽可反传但未产生有效前向任务收益的问题，系统梳理相关文献，并给出下一阶段网络结构建议。
>
> **说明**：本文档整理自上一轮分析回答，并补齐参考文献、DOI、公开链接及来源类型。文中 `[n]` 均对应文末“参考文献与来源”。
> **文档分工**：本文档第一部分为文献综述，第二部分为概念/结构层面的推荐方案（将不断迭代更新）；推荐方案审批通过后的具体代码实现方案与实验执行记录见 [`统一端到端模型研究.md`](./统一端到端模型研究.md)。
> **公式兼容性**：行内公式使用 `$...$`；独立公式的 `$$` 定界符各占一行，公式主体统一保持在单个物理行内。这样可避免 Markdown 在公式内部把行首运算符误解析为项目符号；同时已统一采用 KaTeX/MathJax 更通用的数学命令。

---

## 核心结论

进一步检索 RF 多源定位、DOA/DPD、声学多源定位、source separation、set prediction、differentiable coordinate regression、model-based deep learning 和 multi-task optimization 等文献后，一个更明确的结论是：

> **下一版不应继续把“Soft 频带 → IQ/FFT 滤波 → 在线细 DPD → D8”作为首要端到端反馈接口，而应改为“逐源潜在表示 → 频率–空间联合拆分 → 粗细双分辨率特征融合 → 逐源定位图 → 可微坐标”的短反馈链。**

更具体地，推荐：

$$
\boxed{ \text{Source Query} +\text{Frequency-Spatial Latent Source Splitting} +\text{Dual-Resolution Fusion} +\text{Source-specific Heatmap/Offset} +\text{Set Matching} }
$$

Codex 提出的“逐源槽位 + Soft 频带门控 + 中期融合 + Hungarian + Soft 坐标”总体方向是合理的，但还缺一个关键维度：**不能只做频带门控，必须显式利用空间维度做逐源拆分。**

原因是本项目最重要的困难之一是**同频多源**。如果两个源具有几乎相同的 19 维频带概率，仅采用

$$
F_i= \frac{\sum_b b_{ib}F_b} {\epsilon+\sum_b b_{ib}}
$$

会使两个 source slots 得到近似相同的中间表示，无法利用第三章 DPD 空间谱中原本存在的空间可分辨信息。

---

# 第一部分：文献综述

## 1 文献检索覆盖范围

本轮文献检索不局限于“无线电多源定位”关键词，而按当前网络结构问题拆分为以下文献族：

| 文献族 | 主要研究问题 | 对本项目的价值 |
|---|---|---|
| RF 多发射源定位 | 多源、未知源数、radio map、RSS/DPD | 场景最直接 |
| 深度学习 DOA / DPD | 相干源、未知 K、经典空间谱与网络结合 | 物理模块设计 |
| 声学 SSL / SELD | 多源重叠、变数量、同类多源 | 逐源表示非常成熟 |
| Source separation / diarization | 混合观测内部拆分多个源 | 解决“逐源潜在特征” |
| DETR / set prediction | 无序、变数量目标输出 | 解决 slot/K/匹配 |
| Differentiable coordinates | Heatmap 到连续位置的可微映射 | 坐标 loss 稳定反传 |
| Model-based deep learning | 网络与 MUSIC/NLS/SRP 等物理求解器结合 | 保留物理解释并缩短反传链 |
| Multi-task optimization | 多损失/多梯度协同 | 后期训练稳定化 |

严格意义上无法保证“互联网所有相关论文绝无遗漏”，但下面覆盖的是与本项目结构问题高度相关的核心方法族。

---

## 2 RF / 无源多源定位文献

### 2.1 E2E-MSL：真正重要的是“定位反馈接口”

Zhang 等提出 E2E-MSL，将局部无线电地图构造与 Differentiable Localization Module（DLM）统一起来 [1]。其主要流程为：

$$
\text{Sparse RSS} \rightarrow \text{Local Radio Map} \rightarrow \text{DCS} \rightarrow \text{PIMS} \rightarrow \{\hat{\mathbf p}_i\}.
$$

论文的核心设计包括：

- 使用 sigmoid soft selection 替代训练阶段 hard threshold；
- DCS 保留错误空间区域的弱梯度，而不是直接删除；
- PIMS 在训练阶段保持可微；
- duplicate-mode merging 只在 inference 阶段执行；
- 多源预测与真值通过 Hungarian matching 配对；
- 先预训练 radio-map reconstruction，再进行 E2E fine-tuning；
- map loss 与 coordinate loss 联合训练，并使用 detached loss ratio 做量纲平衡 [1]。

对本项目最重要的启发并不是 PIMS 本身，而是：

$$
\boxed{ L_{\mathrm{loc}} \rightarrow \text{具有明确物理/空间语义的中间表示} }
$$

即最终定位误差应该直接告诉前级网络“哪个空间结构需要被修正”。

但 **PIMS 不宜直接照搬到 DPD 图**。E2E-MSL 的 local radio map 具有背景接近零、源区域局部连通、源/背景对比显著等特性，而本项目 DPD 图存在旁瓣、双曲线交叉、伪峰和多源干扰，因此不满足同样的拓扑假设 [1]。

---

### 2.2 DPV：variable-K 最值得复用的是 slot + confidence

Xia 等提出 DPV，用于可变数量发射机和接收机的无源定位 [2]。其网络将 signal data 经时频变换和 CNN 编码，other data 经 FCNN 编码，再进行特征融合；固定最大输出槽位，每个 emitter slot 同时预测：

$$
(\hat{\mathbf p}_i,\hat c_i).
$$

其中 confidence 用于判断该 slot 是否对应真实源，而不是先输出一个离散源数再决定后续定位网络 [2]。

这与本项目已经发现的 predicted-$K$ error propagation 高度一致，因此可直接借鉴：

$$
\hat K = \sum_i\mathbf 1(e_i>\tau),
$$

而不再采用

$$
\hat K \rightarrow \text{控制后级定位输出维度}.
$$

DPV 使用 Sort Invariant Training（SIT）解决多目标排列问题 [2]，但本项目最多 3 个源，Hungarian matching 的组合成本极低，而且频率排序/距离排序都可能在临界情况下发生不连续换序，因此**不建议直接照搬 SIT**。

---

### 2.3 DeepTxFinder → TL;DL → LRM-MSL → E2E-MSL

这几项工作构成了一条很清晰的 RF 多源定位演化路线。

DeepTxFinder 研究了稀疏 crowdsourced spectrum sensing 下同时估计多个发射机数量与位置的问题 [4]。TL;DL 则将输入测量映射到二维 transmitter-location image，通过 U-Net 类 image-to-image 模型直接生成空间位置图，强调在少量训练数据和低传感器密度下的实用性 [5]。

LRM-MSL 通过 local radio map、二值化和 connected component analysis 显式拆分多个源，再将 MSL 转化为多个单源定位问题 [6]。其主要问题是训练/推理之间包含不可微硬后处理，因此最终 localization supervision 无法返回 map construction 网络。E2E-MSL [1] 正是在这一点上完成了进一步端到端化。

这一系列工作给出的稳定结论是：

> **多源定位最好形成显式的 source/spatial representation，而不是把 K 作为定位网络之前的硬控制变量。**

---

### 2.4 MT-GCNN、DSLoc、SymNet：说明 Heatmap/粗细联合任务仍值得保留

MT-GCNN 将多发射机定位拆为 coarse grid classification 与 fine bias correction 两个任务，并用 joint loss 端到端优化 [7]：

$$
\text{coarse localization} + \text{fine correction}.
$$

这与本项目第四章的

$$
\text{Heatmap}+\text{Offset}
$$

在方法论上高度一致。

DSLoc 采用高分辨率网络和 heatmap regression，在稀疏 WSN 场景下保持空间定位精度 [8]。因此，**CH4 的 Heatmap+Offset 输出形式本身不应被推翻**。

本项目原硕士论文同样已经验证：基于 CSPDarknet/PAN-FPN/U-Net 风格 decoder 的同分辨率 Heatmap+Offset，比直接坐标回归和多尺度检测头具有更好的多源定位精度与鲁棒性 [3]。

SymNet 2026 采用共享表示，同时设置 radio-map reconstruction 与 transmitter localization 任务头，通过 joint learning 促进两个任务互补 [9]。这支持 shared/mid-level fusion，但也说明：**仅共享 backbone 并不等价于定位 loss 一定有效地修正频带选择**。如果希望定位监督明确作用于 band selection，就必须让 band probability/源特征进入定位分支的计算图。

---

## 3 逐源拆分与逐源潜在表示

### 3.1 Source Splitting：对当前结构问题最重要的外部方法之一

Subramanian 等提出 *Deep Learning Based Multi-Source Localization with Source Splitting* [10]。其核心思想不是在网络最终层才输出多个源，而是在**网络中间显式制造 source-specific hidden representations**。

其 source splitting / mask splitting 思路可以抽象为：

$$
z(t,q) \rightarrow w^n(t,q) \rightarrow \xi^n(q),
$$

其中共享潜在特征为 $z(t,q)$，第 $n$ 个源拥有 Soft mask $w^n(t,q)$，再构造逐源特征：

$$
\xi^n(q) = \frac{ \sum_t w^n(t,q)z(t,q) }{ \sum_t w^n(t,q) }.
$$

这篇工作的关键价值是：

> **masking 发生在 latent feature domain，而不是把网络先变成一个前级 hard estimator，再去控制传统信号处理模块。**

映射到本项目：

#### 当前 G1 路线

$$
\text{band probability} \rightarrow \text{FFT/IQ mask} \rightarrow \text{physical fine-DPD} \rightarrow \text{localization}.
$$

#### 更推荐的路线

$$
\text{shared DPD feature} \rightarrow \text{latent source mask} \rightarrow \text{source-specific representation} \rightarrow \text{localization}.
$$

这使定位梯度无需穿过长距离 FFT/特征值分解链路，而能直接回到 source-specific representation 和 band decision。

---

### 3.2 Attractor 系列：进一步支持“源应该是 latent entity”

Deep Attractor Network（DANet）为每个 source 在 embedding space 中构造 attractor，并依据每个时频 bin 与 attractor 的相似度生成 source masks [11]。该思想针对的两个基本困难正是：

- source permutation；
- unknown source number。

EEND-EDA 则从共享 frame embeddings 中逐个生成 speaker attractors，直到满足 stop condition，从而允许 source number 变化 [12]。

这些工作的共同思想不是一定要使用 attractor/LSTM，而是：

> **“源”应该在 latent space 中被表示成一个可被后续任务使用的实体，而不是仅在最终 output index 上才出现。**

本项目 CH3 已经拥有 source slots，因此完全可以直接升级为：

$$
\text{CH3 source slot} \Rightarrow \text{source query}.
$$

没有必要重新设计独立的 attractor generator。

---

### 3.3 2025–2026 Envelope / Blind Counting + Localization 工作值得重点跟进

2025 年工作 *Moving Sound Source Localization and Tracking based on Envelope Estimation for Unknown Number of Sources* 使用 EDA 首先估计 source number 并得到 source attractors，随后估计每个 source 的 envelope，再以逐源 envelope 作为 localization/tracking 线索 [13]。

其总体逻辑可以概括为：

$$
\text{shared mixture feature} \rightarrow \text{source attractors} \rightarrow \text{per-source representation} \rightarrow \text{localization}.
$$

同一研究方向在 ICASSP 2026 出现了：

> **An Envelope Separation Aided Multi-Task Learning Model for Blind Source Counting and Localization** [14]

从 ICASSP 官方会议信息可以确认该论文题名、作者和 session，但当前公开检索无法可靠取得全文。因此该文属于**建议通过 IEEE/Xidian 权限下载后重点阅读**的文献。

它可能是当前最接近“blind source counting + per-source separation + localization + multi-task”这一联合研究问题的外部工作。

---

## 4 集合预测与多尺度声源定位

### 4.1 DETR / Sound3DVDet / SoundLoc3D：把多源输出定义为集合

DETR 将目标检测建模为 direct set prediction，核心包括：

- fixed learned queries；
- bipartite/Hungarian matching；
- no-object 类；
- 无需 NMS [15]。

与本项目更接近的是 Sound3DVDet [16]。其将多个 3D sound sources 明确定义为一个预测集合：

$$
\hat Q = \{\hat q_1,\ldots,\hat q_N\},
$$

先由 microphone-array signal 生成潜在声源 queries，再融合 multiview visual feature，最后利用 Hungarian algorithm 与真实 source set 匹配。

SoundLoc3D 进一步沿用了这种 set representation / query refinement 思路 [17]。

对本项目而言，这意味着：

> **CH3 不应只向 CH4 传递“频带标签”，而应产生一个可被 fine spatial branch 进一步 refine 的 source hypothesis/query。**

---

### 4.2 Multi-ACCDOA：存在性与位置应该绑定在同一个 slot

Multi-ACCDOA 面向同一类别多个重叠声源，把 activity 与 Cartesian DOA 绑定在 multi-track output 中，并使用 ADPIT 解决重叠同类声源的排列问题 [18]。

对本项目可以抽象为：

$$
\boxed{ \text{每个 source query 同时输出} \{e_i,b_i,H_i,O_i\} }
$$

其中：

- $e_i$：存在概率；
- $b_i$：19 维频带概率；
- $H_i$：逐源 heatmap；
- $O_i$：逐源 offset。

这样源数、频带和位置属于**同一个 source entity 的不同属性**，而不是三个相互松散的全局任务输出。

---

### 4.3 FUN-SSL：粗细/全频窄频不应该是单向级联

FUN-SSL 使用 full-band processing 与多尺度 narrow-band U-Net 结合，并通过 U-Net 内部和 FUN blocks 之间的 skip connections 丰富全频/窄频信息交互，同时降低计算复杂度 [28]。

虽然任务是声源定位，但其 full-band/narrow-band 多尺度建模和本项目的：

$$
19\times81\times81 \quad+ 401\times401
$$

具有明显结构类比。

它进一步支持：

> **full-band 与 sub-band 更适合作为互补的多尺度 representation，而不是简单的“前级判断 → 后级服从”关系。**

---

## 5 可微坐标回归与多任务梯度优化

### 5.1 可微坐标回归：DSNT 与 Integral Human Pose Regression

DSNT [19] 和 Integral Human Pose Regression [20] 都说明，可以用 expectation / soft-argmax 将 heatmap 可微地映射成连续坐标，避免 hard argmax 造成的梯度阻断和网格量化问题。

### 5.2 多任务梯度优化：GradNorm / CAGrad / MTAN

GradNorm 用动态 loss weighting 调整多任务梯度 magnitude [21]；CAGrad 则明确针对不同任务间梯度冲突设计优化方向 [22]；MTAN 通过 task-specific soft attention 改善共享表示与任务专属表示之间的分离 [23]。

## 6 物理先验的取舍：Model-based DL 与 Gridless 方法

### 6.1 Model-based Deep Learning：SubspaceNet / DA-MUSIC / E2E-PrNet / Neural-SRP

SubspaceNet 并不是对完整稠密 MUSIC spectrum 的每一个中间环节做大规模反传，而是让神经网络学习一个 surrogate empirical autocorrelation，再把它送入可微 Root-MUSIC，通过最终 DOA supervision 训练 [24]。

DA-MUSIC 则在经典 MUSIC 流程中嵌入 neural components，形成 model-based/data-driven hybrid，在保持解释性的同时增强对 coherent source、模型失配和 source-number estimation 的鲁棒性 [25]。

E2E-PrNet 将 neural pseudorange correction 与 Differentiable Nonlinear Least Squares（DNLS）结合，最终位置 loss 穿过一个相对紧凑的优化器返回前级网络 [26]。

Neural-SRP 同样保留了 SRP-PHAT 的经典结构特征，再使用网络增强定位鲁棒性 [27]。

这些工作的共同启示是：

> 如果要保留可微物理模块，应让它足够紧凑、局部、具有明确输入输出语义，而不是把整幅全局高分辨率物理图生成过程作为第一版反传通道。

### 6.2 Gridless 前沿：Sensing for Free

*Sensing for Free* 使用 attention-only Transformer 直接处理 raw signal snapshots，进行：

- gridless DOA estimation；
- permutation-invariant multi-source prediction；
- variable snapshot count；
- end-to-end multi-source inference [29]。

## 7 2026 年仍建议下载全文的高相关论文

以下论文与当前问题高度相关，但公开检索目前不足以可靠读取完整正文，建议通过学校 IEEE/Elsevier/AES 权限下载后上传：

### 7.1 An Envelope Separation Aided Multi-Task Learning Model for Blind Source Counting and Localization

- Jiaqi Du, Donghang Wu, Xihong Wu, Tianshu Qu
- ICASSP 2026
- DOI：`10.1109/ICASSP55912.2026.11464467`
- ICASSP 官方会议信息可确认题目与作者 [14]

**优先级：最高。**

原因：题目已经明确同时覆盖：

- blind source counting；
- envelope/source separation；
- localization；
- multi-task learning。

与本项目“前级分源表示 + 后级定位反馈”高度重合。

---

### 7.2 TriAD: Tri-head with Auxiliary Duplicating Permutation Invariant Training for Multi-Task Sound Event Localization and Detection

- Bingnan Duan, Yinhuan Dong, Tughrul Arslan, John Thompson
- ICASSP 2026
- DOI：`10.1109/ICASSP55912.2026.11462845`
- ICASSP 官方页面可确认题名、作者和 session [30]

需要全文后才能负责任地判断其 tri-head 和 ADPIT 具体结构，不应仅凭题名推断实现细节。

---

### 7.3 Moving Sound Source Localization and Tracking based on Envelope Estimation for Unknown Number of Sources

- AES Europe 2025
- 摘要已确认使用 EDA → source attractor → per-source envelope → localization/tracking [13]
- 公开全文目前未可靠获取。

---

### 7.4 TCN-MUSIC

**TCN-MUSIC: Hybrid MUSIC-based deep network using temporal convolutions for DoA estimation**

- Digital Signal Processing, 2026
- DOI：`10.1016/j.dsp.2026.106054`

摘要可确认其属于 TCN + MUSIC 的 model-based/data-driven hybrid，并包含 source-number classification；建议补全文研究其 feature extraction 与 MUSIC/source count 的具体接口 [31]。

---

### 7.5 Joint source enumeration and multi-source DOA estimation via a deep cascaded network

- Digital Signal Processing, 2026
- DOI：`10.1016/j.dsp.2026.106192`

该方法属于明显的 count-first cascade，可作为本项目新的对照方向，但不建议作为新 E2E 主架构 [32]。

---

## 8 文献综合后的设计原则

| 设计问题 | 文献主要共识 | 本项目建议 |
|---|---|---|
| Variable K | slots/confidence、set prediction、attractor | **3 个 exchangeable source queries** |
| 多源重叠 | source-specific latent representation | **latent source splitting** |
| 同频多源 | frequency mask 本身不足 | **frequency × spatial mask** |
| 输出排序 | PIT/Hungarian | **Hungarian matching** |
| 定位表示 | spatial heatmap 优于直接坐标回归的情况很多 | **保留 Heatmap+Offset** |
| 坐标反传 | DSNT / integral / DLM | **Soft Heatmap+Offset expectation** |
| 粗/细信息 | feature-level multi-resolution fusion | **dual-resolution fusion** |
| 物理先验 | model-based neural hybrid | **保留 DPD 输入，暂不全局反传 EVD** |
| 多任务损失 | gradient balancing 可辅助 | **结构稳定后再引入** |
| Hard 操作 | 尽量留在 inference | **训练内部 Soft，最终解码 Hard** |

---

# 第二部分：推荐方案

## 9 问题定位：仅做 Band Gate 为什么仍然不够

Codex 提议：

$$
F_i^{c} = \frac{\sum_{b=1}^{19}b_{ib}F_b} {\epsilon+\sum_b b_{ib}}.
$$

这个设计比当前 Soft-IQ bridge 更合理，因为形成了明确的：

$$
L_{\mathrm{loc}} \rightarrow F_i^c \rightarrow b_{ib}.
$$

但它仍存在本项目特有的结构性问题：**同频多源无法仅靠 band probability 拆分。**

若两个源同频：

$$
b_{1b}\approx b_{2b},
$$

则

$$
F_1^c\approx F_2^c.
$$

两个 source slots 很可能看到几乎相同的 coarse representation。

而第三章能够处理同频多源，依赖的正是：

> 虽然频率不可分，但每个 81×81 DPD 中的空间结构仍提供不同信源的可分辨证据 [3]。

因此，在联合模型中必须保留 CH3 CNN **global pooling 之前**的 spatial feature map。

---

## 10 核心机制：频率 × 空间逐源潜在拆分

### 10.1 建议升级为“频率 × 空间”的逐源潜在拆分

设第 $b$ 个子带的粗 DPD 特征为：

$$
F_b^c \in \mathbb R^{C\times h\times w}, \qquad b=1,\ldots,19.
$$

第 $i$ 个 source query 为：

$$
q_i\in\mathbb R^D.
$$

#### 频率 gate

沿用 CH3 band logits：

$$
z_{ib},
$$

得到：

$$
g^{f}_{ib} = \sigma(z_{ib}).
$$

它表示“第 $i$ 个 source 使用哪些频带”。

---

#### 空间 gate

利用 source query 查询每个频带的 spatial feature：

$$
s_{ibxy} = \frac{ (W_q q_i)^T W_kF_b^c(x,y) }{\sqrt d}.
$$

得到：

$$
g^{s}_{ibxy} = \operatorname{softmax}_{x,y}(s_{ibxy}).
$$

它表示：

> 在第 $b$ 个频带中，哪些空间位置属于第 $i$ 个源。

---

#### 联合 source mask

定义：

$$
M_{ibxy} = g^{f}_{ib}\,g^{s}_{ibxy}.
$$

再构造 source-specific coarse feature：

$$
\tilde F_i^c(x,y) = \sum_{b=1}^{19} \bar g^{f}_{ib} g^{s}_{ibxy} V(F_b^c(x,y)),
$$

其中：

$$
\bar g^{f}_{ib} = \frac{g^{f}_{ib}} {\epsilon+\sum_{b'}g^{f}_{ib'}}.
$$

这可以视为 Source Splitting [10] 在本项目中的扩展版本：

$$
\boxed{ \text{source} \times \text{frequency} \times \text{space} }
$$

联合建模。

---

### 10.2 为什么 Frequency-Spatial Split 能处理同频多源

假设两个源完全同频：

$$
g^f_{1b}\approx g^f_{2b}.
$$

仅靠 frequency gate 无法区分它们。

但 source-specific spatial gate 可以满足：

$$
g^s_{1bxy} \neq g^s_{2bxy}.
$$

因此：

$$
\tilde F_1^c \neq \tilde F_2^c.
$$

即：

> **frequency gate 负责 frequency attribution；spatial gate 负责 co-frequency source disentanglement。**

这比简单 band-weighted average 更符合第三章的物理机制。

---

## 11 网络结构设计

### 11.1 推荐的完整网络结构

建议暂称为：

#### Frequency-Spatial Source-Splitting Dual-Resolution E2E

```text
19 × 81 × 81 coarse DPD
        │
        ▼
Existing CH3 shared CNN
        │
        ├── 保留每个 band 的空间特征 F_b^c
        │
        ▼
Existing Transformer
        │
        ▼
3 exchangeable Source Queries
 q1             q2             q3
 │              │              │
 ├── existence e_i
 ├── band logits z_i,1:19
 │
 ├──────── Frequency Gate g_i^f ────────┐
 │                                       │
 └──────── Spatial Source Attention ─────┤
                                         ▼
                   Frequency × Spatial Source Mask
                             M_i,b,x,y
                                  │
                                  ▼
                Source-specific Coarse Feature
                         F̃_i^c(x,y)
                                  │
                                  │
                                  ├───────────────┐
                                  │               │
401×401 Full-band DPD             │               │
(precomputed initially)           │               │
        │                         │               │
        ▼                         │               │
 Existing D8 encoder              │               │
        │                         │               │
   Fine feature F^f ──────────────┴── Query-conditioned Fusion
                                          │
                                     Shared decoder
                                          │
                         ┌────────────────┴───────────────┐
                         ▼                                ▼
                 source heatmap H_i                 offset O_i
                         │                                │
                         └──────────┬─────────────────────┘
                                    ▼
                      Soft integral coordinate
                              p̂_i
                                    │
                                    ▼
                         localization loss
```

第一版建议**保留现有 D8 encoder**，不要同时改成 HRNet；否则无法判断性能收益究竟来自 source splitting、fusion 还是 backbone 替换。

---

### 11.2 Source Query 的初始化方式

不建议完全照 DETR 使用三个与输入无关的静态 learned query。

因为本项目 CH3 已经形成经过验证的子带特征与 Transformer representation，更合理的是：

$$
q_i^0=q_i^{learned}
$$

作为 symmetry breaker，然后：

$$
q_i = \operatorname{CrossAttn} (q_i^0,T_{\mathrm{CH3}},T_{\mathrm{CH3}}).
$$

其中 $T_{\mathrm{CH3}}$ 包含来自 19 个频带的输入相关 tokens。

这样 source query 同时具备：

- exchangeable source hypothesis；
- sample-dependent coarse evidence；
- frequency/source association information。

---

### 11.3 Fine DPD 分支的融合方式

第一版建议不再在线执行 differentiable filtered 401×401 DPD。

先为每个样本预生成一张 full-band fine DPD：

$$
401\times401.
$$

D8 encoder 输出：

$$
F^f.
$$

随后让第 $i$ 个 source query 选择其对应的 fine spatial evidence。

最简方式可采用 FiLM：

$$
F_i^f = \gamma(q_i)\odot F^f+\beta(q_i).
$$

或者采用 cross-attention：

$$
q_i' = \operatorname{CrossAttn}(q_i,F^f,F^f).
$$

再将 coarse source map：

$$
\tilde F_i^c
$$

resize/project 到 compatible resolution，做：

$$
U_i = \operatorname{Fuse} \left( F_i^f, \operatorname{Up}(\tilde F_i^c) \right).
$$

共享 decoder 输出：

$$
U_i \rightarrow (H_i,O_i).
$$

---

### 11.4 CH3 输出如何保留

建议每个 source query 输出：

$$
\text{source query}_i \rightarrow \begin{cases} e_i, & \text{存在性}\\ b_i\in[0,1]^{19}, & \text{频带}\\ A_i^c(x,y), & \text{粗空间归属} \end{cases}
$$

源数：

$$
\hat K = \sum_{i=1}^{3}\mathbf1(e_i>\tau).
$$

因此无需额外增加 global spatial counting head。

第三章原本的任务仍完整保留：

- source existence / count；
- per-source frequency occupancy。

但 source identity 从固定的“频率排序 slot”升级为：

> **exchangeable source query + Hungarian assignment。**

---

## 12 定位读出、Set Matching 与联合 Loss

### 12.1 定位端：建议增加训练期可微坐标读出器

> **文献评述（DSNT / Integral Human Pose Regression）见第一部分 5.1 节。**

但本项目已经有经过验证的 Offset head，因此不建议简单替换成普通 DSNT，而建议定义：

$$
P_i(x,y) = \operatorname{softmax} \left( H_i(x,y)/T \right),
$$

然后：

$$
\boxed{ \hat{\mathbf p}_i = \sum_{x,y} P_i(x,y) \left[ \mathbf r_{xy}+O_i(x,y) \right] }
$$

其中 $\mathbf r_{xy}$ 是对应网格中心坐标。

训练时可以直接计算：

$$
L_{\mathrm{coord}} = \|\hat{\mathbf p}_i-\mathbf p_i^{GT}\|.
$$

于是 coordinate loss 同时进入：

$$
H_i \quad\text{和}\quad O_i.
$$

最终 inference 仍可使用原有：

$$
\arg\max H_i+\text{offset}.
$$

即贯彻：

> **Soft 用于训练和内部反向传播，Hard 只用于最终解码。**

需要强调：soft expectation 只能用于**source-specific heatmap**。若直接作用于一张含多个峰的 global heatmap，会把多个峰平均到中间位置。

---

### 12.2 联合 Hungarian Set Matching

新 E2E 分支中不建议继续用固定 frequency ordering 来定义 source identity。

每个真实源：

$$
y_j= \{b_j,\mathbf p_j\}.
$$

每个预测 query：

$$
\hat y_i= \{e_i,b_i,H_i,O_i,\hat{\mathbf p}_i\}.
$$

定义 matching cost：

$$
C_{ij} = \lambda_p \|\hat{\mathbf p}_i-\mathbf p_j\|_1 + \lambda_b L_{\mathrm{band}}(b_i,b_j) + \lambda_h L_{\mathrm{hm}}(H_i,G_j) - \lambda_e\log e_i.
$$

求：

$$
\pi^\star = \arg\min_\pi \sum_{(i,j)\in\pi}C_{ij}.
$$

这种联合匹配对本项目特别合适：

- **同频但位置不同**：position term 区分；
- **位置较近但频率不同**：band term 区分；
- **两者都接近**：由联合 evidence 决定。

这比依赖单独频率排序更自然，也与 DETR [15]、Sound3DVDet [16]、E2E-MSL [1] 中的 set matching 思想一致。

---

### 12.3 建议的 Loss

匹配完成后：

$$
L = \lambda_eL_{\mathrm{exist}} + \lambda_bL_{\mathrm{band}} + \lambda_hL_{\mathrm{hm}} + \lambda_oL_{\mathrm{offset}} + \lambda_pL_{\mathrm{coord}} + \lambda_aL_{\mathrm{align}}.
$$

其中建议增加：

#### Coarse Spatial Alignment Loss

由 source mask 得到粗空间 attention：

$$
A_i^c(x,y).
$$

将第 $j$ 个 GT source 转换为 81-grid Gaussian：

$$
G_j^c(x,y).
$$

匹配后：

$$
L_{\mathrm{align}} = \sum_{(i,j)\in\pi^\star} D(A_i^c,G_j^c).
$$

首版建议 $D$ 使用 BCE 或 focal-style map loss，暂时不需要复杂 KL 设计。

其意义是：

$$
\boxed{ \text{直接给 CH3 source representation 加入空间定位监督} }
$$

从而减少定位 loss 仅通过后级长链间接修正前级特征的不确定性。

---

## 13 梯度反馈路径与 Gate 验证

### 13.1 新梯度链与 G1-R1 的本质区别

#### 当前路线

$$
L_{\mathrm{loc}} \rightarrow D8 \rightarrow 401^2\text{ DPD eigenspectrum} \rightarrow FFT/IQ mask \rightarrow b_{ib}.
$$

该链数学上可微，但梯度必须穿过：

- IQ weighting；
- FFT；
- fine DPD 构造；
- 特征值相关运算；
- D8。

定位误差到达 band logits 时的语义实际上接近：

> “改变某个频点的输入权重会如何改变整张细 DPD 空间谱及最终位置误差？”

这与 CH3 的 source/band 语义并不直接对齐。

---

#### 新路线

$$
L_{\mathrm{loc}} \rightarrow \hat p_i \rightarrow H_i/O_i \rightarrow U_i \rightarrow \tilde F_i^c \rightarrow M_{ibxy} \rightarrow g^f_{ib} \rightarrow z_{ib}.
$$

同时：

$$
L_{\mathrm{loc}} \rightarrow M_{ibxy} \rightarrow q_i \rightarrow \text{CH3 encoder/Transformer}.
$$

这是一个：

$$
\boxed{ \text{source-specific} + \text{spatially aligned} + \text{short-path} }
$$

的反馈接口。

这更符合当前 G1-R1 暴露出的“梯度存在但任务语义弱/梯度收益不可选”的问题。

---

### 13.2 对 Codex 方案的四项关键修改

#### 修改 1：Band-only Gate → Frequency-Spatial Source Mask

这是最重要的一点。

否则同频时：

$$
b_1\approx b_2 \Rightarrow F_1\approx F_2.
$$

无法真正 source split。

---

#### 修改 2：不能只取 CH3 global token

必须从 CH3 CNN **global pooling 之前**保留：

$$
F_b^c(x,y).
$$

否则第三章赖以处理同频多源的 spatial DPD evidence 被提前消除。

---

#### 修改 3：DSNT/Integral 仅作为训练期 coordinate readout

不取代现有 Heatmap+Offset。

使用：

$$
\hat p_i = \sum P_i(x,y) (r_{xy}+O_i(x,y)).
$$

最终 inference 继续采用 hard peak + offset。

---

#### 修改 4：需要两个 stop-gradient 对照，而不是只做一个

需要区分两个科学问题：

1. localization loss 是否仅通过 shared features 带来收益？
2. localization loss 是否**通过 band/source gate**真正改变了前级 source/band decision？

因此 Gate 设计必须能把二者拆开。

---

### 13.3 建议的新一轮 Gate

建议命名：

#### G2-A：Latent Source-Splitting Fusion

至少设置：

| Track | CH3 coarse feature | band gate 进入 locator | loc gradient → band gate/CH3 |
|---|---:|---:|---:|
| Fine-only | × | × | × |
| Fusion-NoGate | ✓ | × | ✓（仅共享特征） |
| Band-SG | ✓ | ✓ forward | ×（gate detach） |
| Full-E2E | ✓ | ✓ | ✓ |

这样：

$$
\text{Full-E2E}-\text{Band-SG}
$$

可以直接回答：

$$
\boxed{ \text{定位监督是否真正通过 source/band selection 改善了前级？} }
$$

另外建议增加关键 ablation：

$$
\text{BandOnly} \quad\text{vs}\quad \text{FreqSpatial}.
$$

重点观察：

- co-frequency 2-source；
- co-frequency 3-source。

如果 Frequency-Spatial 在同频场景中明显优于 BandOnly，会形成非常有价值的机制性实验结果。

---

### 13.4 比 gradient cosine 更直接的反馈有效性指标

当前项目已经记录：

$$
\cos(g_{\mathrm{loc}},g_{\mathrm{CH3}}).
$$

建议继续保留，但增加一个更加直接的：

#### Localization-to-Band Transfer

只沿 localization gradient 做一个小步：

$$
\theta' = \theta-\eta g_{\mathrm{loc}}.
$$

计算：

$$
\Delta L_{\mathrm{band}} = L_{\mathrm{band}}(\theta') - L_{\mathrm{band}}(\theta).
$$

若：

$$
\Delta L_{\mathrm{band}}<0,
$$

说明仅沿 localization gradient 更新一次，就能改善 band objective。

如果长期：

$$
\Delta L_{\mathrm{band}}\approx0,
$$

即使 localization gradient norm 很大，也说明它对 CH3 的 task-relevant representation 没有实质帮助。

这个量比“梯度是否非零”更接近本项目真正的问题。

---

## 14 实验计划与诊断量

### 14.1 第一版实验规模建议

不建议一开始扩大 seed 和完整数据规模。

建议：

```text
现有 CH3 checkpoint
+
现有 D8 checkpoint
+
预生成 full-band 401×401 DPD
          ↓
冻结两个 encoder
          ↓
只训练
source queries
frequency-spatial splitter
fusion
source-conditioned decoder
```

先使用：

$$
16\sim32
$$

个样本进行强过拟合测试。

必须确认：

$$
\frac{\partial L_{\mathrm{coord}}} {\partial z_{ib}} \neq0,
$$

以及：

$$
\frac{\partial L_{\mathrm{coord}}} {\partial g^s_{ibxy}} \neq0.
$$

随后再进入 256 样本 causal Gate。

只有当：

$$
\text{Full-E2E} < \text{Band-SG}
$$

在 paired validation 上出现稳定收益，再逐步解冻：

- CH3 Transformer 最后 1–2 层；
- CH3 band head；
- D8 最后 encoder stage；
- decoder。

---

### 14.2 下一架构建议固定记录的诊断量

除 loss、GOSPA、count accuracy、Recall 外，建议固定记录：

#### Localization → band logits gradient

$$
\left\| \frac{\partial L_{\mathrm{loc}}} {\partial z_{ib}} \right\|.
$$

#### Source-query gradient norm

$$
\|g_{q_i}\|.
$$

#### Source attention entropy

$$
H(A_i) = -\sum_{xy}A_i(x,y)\log A_i(x,y).
$$

训练后 entropy 下降，意味着 source query 从弥散关注逐渐收缩到更明确的空间区域。

#### Query 间空间重叠

$$
R_{ij} = \frac{ \langle A_i,A_j\rangle }{ \|A_i\|_2\|A_j\|_2 }.
$$

尤其在同频多源子集上观察：

$$
R_{12}.
$$

如果训练后：

$$
g^f_1\approx g^f_2
$$

但：

$$
R_{12}\downarrow,
$$

即可直接说明模型正在利用**空间维度对同频 sources 做 latent disentanglement**。

---

## 15 路线取舍与备选方向

### 15.1 为什么暂时不应靠 PCGrad / GradNorm “修复”现有结构

> **文献评述（GradNorm / CAGrad / MTAN）见第一部分 5.2 节。**

但本项目当前 G1-R1 的：

$$
\cos(g_{\mathrm{loc}},g_{\mathrm{CH3}})\approx0
$$

更接近：

> localization gradient 与 CH3 task gradient 缺乏有效关联，

而不是：

> 两者稳定地朝相反方向发生强冲突。

因此：

$$
\boxed{ \text{gradient surgery 无法凭空制造缺失的 source semantics} }
$$

第一优先级应是先改反馈接口。等新结构建立后，如果出现持续负 cosine，再考虑：

- GradNorm；
- CAGrad；
- PCGrad；
- 其它多任务动态权重方法。

---

### 15.2 可微物理求解器的备选路线：G2-B

> **文献评述（SubspaceNet / DA-MUSIC / E2E-PrNet / Neural-SRP）见第一部分 6.1 节。**

因此本项目后续的第二候选路线更适合设计为：

1. query 先估计 coarse position / frequency support；
2. 只在 query 附近做小窗口 source-specific differentiable DPD refinement；
3. 最终位置 loss 通过局部 refinement 返回 source query。

而不是恢复全局：

$$
401\times401
$$

范围的完整 differentiable eigen-spectrum 反传。

建议把这一方向作为 **G2-B**，而不是 G2-A。

---

### 15.3 长期激进方案：彻底摆脱 DPD 网格

> **文献评述（Sensing for Free）见第一部分 6.2 节。**

它可以作为本项目长期 discussion / future work 中的“fully data-driven gridless”方向。

但目前不建议作为主线，因为这会同时推翻：

- CH3；
- CH4；
- DPD spatial prior；
- 现有数据生成与大量实验资产。

研究成本和归因难度过高。

---

## 16 最终推荐

下一阶段不应简单定义为“两个 encoder 做 feature fusion”，而应更准确地定义为：

$$
\boxed{\text{逐源频率–空间潜在特征拆分的双分辨率端到端多源定位网络}}
$$

完整创新链为：

$$
\boxed{\begin{aligned} \text{Coarse DPD} &\downarrow\\ \text{Source Queries} &\downarrow\\ \text{Frequency-Spatial Latent Source Splitting} &\downarrow\\ \text{Source-specific Coarse Spatial Representation} &\downarrow\\ \text{Fine DPD Feature Fusion} &\downarrow\\ \text{Source-specific Heatmap+Offset} &\downarrow\\ \text{Soft Coordinate Readout} &\downarrow\\ L_{\mathrm{loc}} &\uparrow\\ \text{Band Logits + Source Spatial Representation} \end{aligned}}
$$

它针对当前四个核心问题：

1. $K$ error propagation；
2. source permutation；
3. co-frequency source identity；
4. localization gradient semantic dilution。

同时保留现有已经验证的大量资产：

- 19×81×81 子带 DPD；
- CH3 shared CNN + Transformer；
- 401×401 fine DPD；
- D8 encoder（首版）；
- Heatmap+Offset；
- DPD 的物理可解释性。

真正从首要反馈路径中移除的是目前已经通过 G1-R1 证明“数学可微，但没有可选择 validation 收益”的：

$$
\boxed{ \text{Soft band} \rightarrow \text{FFT/IQ weighting} \rightarrow \text{online }401^2\text{ eigenspectrum} }
$$

作为主端到端接口。

下一步最值得做的不是直接编码完整新网络，而是先精确梳理：

- CH3 现有实现中每个 band 在 global pooling 之前的 feature tensor 位置与 shape；
- Transformer 的 source-slot/query tensor shape；
- D8 encoder / PAN 各尺度 feature tensor shape；
- 哪一层作为 coarse/fine fusion 接口最小侵入、梯度路径最短。

在此基础上再把上述概念结构落到真实 tensor-level 设计。

---

# 参考文献与来源

> **来源类型**：
> - **[用户提供全文]**：本对话中用户上传的 PDF，已读取全文/主要章节。
> - **[公开全文]**：可公开访问全文或作者版本。
> - **[摘要/出版信息]**：目前检索到摘要或会议信息，正文需学校/机构权限下载。

## 核心 RF / 当前项目来源

**[1]** Q. Zhang, H. Tang, Z. Song, W. Chen, and J. Wang, “A Lightweight End-to-End Multi-Source Localization Framework in Urban Environments via Local Radio Map Construction,” *IEEE Transactions on Cognitive Communications and Networking*, 2026. DOI: **10.1109/TCCN.2026.3727490**. **[用户提供全文]**  
用户提供 PDF：`A lightweight end-to-end multi-source localization framework in urban environments via local radio m-Zhang 等-2026.pdf`。

**[2]** R. Xia, J. Wang, T. Zhang, C. Xue, Y. Du, and B. Deng, “An End-to-End Deep Neural Network for Passive Positioning of Emitters and Receivers With Variable Numbers,” *IEEE Transactions on Cognitive Communications and Networking*, vol. 12, pp. 759–771, 2026. DOI: **10.1109/TCCN.2025.3564488**. **[用户提供全文]**  
用户提供 PDF：`An end-to-end deep neural network for passive positioning of emitters and receivers with variable nu-Xia 等-2026.pdf`。  
DBLP/出版信息：https://dblp.org/pid/406/3541.html

**[3]** 刘同振, 《基于深度学习的时频混叠多信号检测计数与被动定位方法研究》, 西安电子科技大学硕士学位论文, 2026. **[用户提供全文]**  
用户提供 PDF：`23011210426-刘同振-基于深度学习的时频混叠多信号检测计数与被动定位方法研究-信息与通信工程-李赞-答辩后意见修改.pdf`。

**[4]** A. Zubow, S. Bayhan, P. Gawłowicz, and F. Dressler, “DeepTxFinder: Multiple Transmitter Localization by Deep Learning in Crowdsourced Spectrum Sensing,” *ICCCN*, 2020. DOI: **10.1109/ICCCN49398.2020.9209727**. **[公开全文]**  
作者版本：https://ris.utwente.nl/ws/files/232513593/10.1109_icccn49398.2020.9209727.pdf  
出版信息：https://research.utwente.nl/en/publications/deeptxfinder-multiple-transmitter-localization-by-deep-learning-i/

**[5]** F. Mitchell, A. Baset, N. Patwari, S. K. Kasera, and A. Bhaskara, “Deep Learning-based Localization in Limited Data Regimes,” *WiseML@WiSec*, 2022, pp. 15–20. DOI: **10.1145/3522783.3529529**. **[公开全文]**  
PDF：https://span.engineering.wustl.edu/pub/mitchell2022deep.pdf  
DBLP：https://dblp.org/rec/conf/wisec/MitchellBPKB22.html

**[6]** Q. Zhang, H. Tang, W. Chen, Z. Song, and J. Wang, “Multiple Source Localization via Local Radio Map Construction in Urban Environments,” *IEEE Transactions on Cognitive Communications and Networking*, 2026. DOI: **10.1109/TCCN.2025.3635590**. **[公开预印本]**  
arXiv：https://arxiv.org/abs/2512.15724  
DBLP：https://dblp.org/rec/journals/tccn/ZhangTCSW26.html

**[7]** W. Wang, L. Zhu, Z. Huang, et al., “MT-GCNN: Multi-Task Learning with Gated Convolution for Multiple Transmitters Localization in Urban Scenarios,” *Sensors*, vol. 22, no. 22, 8674, 2022. DOI: **10.3390/s22228674**. **[公开全文]**  
https://pmc.ncbi.nlm.nih.gov/articles/PMC9694210/  
https://www.mdpi.com/1424-8220/22/22/8674

**[8]** R. Liu, Q. Zhang, Y. Zhang, R. Zhang, and T. Meng, “Deep Learning-Based Transmitter Localization in Sparse Wireless Sensor Networks,” *Sensors*, vol. 24, no. 16, 5335, 2024. DOI: **10.3390/s24165335**. **[公开全文]**  
https://pmc.ncbi.nlm.nih.gov/articles/PMC11359829/  
https://www.mdpi.com/1424-8220/24/16/5335

**[9]** L. Ye, T. D. Le, and Y. Huang, “SymNet: A Multi-Task Network for Joint Radio Map Reconstruction and Transmitter Localization,” *WACV*, 2026, pp. 150–159. **[公开全文]**  
CVF：https://openaccess.thecvf.com/content/WACV2026/html/Ye_SymNet_A_Multi-Task_Network_for_Joint_Radio_Map_Reconstruction_and_WACV_2026_paper.html  
PDF：https://openaccess.thecvf.com/content/WACV2026/papers/Ye_SymNet_A_Multi-Task_Network_for_Joint_Radio_Map_Reconstruction_and_WACV_2026_paper.pdf  
arXiv：https://arxiv.org/abs/2608.00087

## Source splitting / set prediction / multi-source representation

**[10]** A. S. Subramanian, C. Weng, S. Watanabe, M. Yu, and D. Yu, “Deep Learning Based Multi-Source Localization with Source Splitting and its Effectiveness in Multi-Talker Speech Recognition,” *Computer Speech & Language*, vol. 75, 101360, 2022. DOI: **10.1016/j.csl.2022.101360**. **[公开预印本/摘要]**  
arXiv：https://arxiv.org/abs/2102.07955  
ScienceDirect：https://www.sciencedirect.com/science/article/pii/S0885230822000079

**[11]** Z. Chen, Y. Luo, and N. Mesgarani, “Deep Attractor Network for Single-Microphone Speaker Separation,” *ICASSP*, 2017, pp. 246–250. DOI: **10.1109/ICASSP.2017.7952155**. **[公开全文]**  
arXiv：https://arxiv.org/abs/1611.08930  
PMC：https://pmc.ncbi.nlm.nih.gov/articles/PMC5805382/

**[12]** S. Horiguchi, Y. Fujita, S. Watanabe, Y. Xue, and P. Garcia, “Encoder-Decoder Based Attractors for End-to-End Neural Diarization,” *IEEE/ACM Transactions on Audio, Speech, and Language Processing*, vol. 30, pp. 1493–1507, 2022. DOI: **10.1109/TASLP.2022.3162080**. **[公开预印本]**  
arXiv：https://arxiv.org/abs/2106.10654

**[13]** D. Wu, J. Du, T. Qu, et al., “Moving Sound Source Localization and Tracking based on Envelope Estimation for Unknown Number of Sources,” *Audio Engineering Society European Convention*, Warsaw, 2025. **[摘要/出版信息]**  
ResearchGate 页面：https://www.researchgate.net/publication/392168938_Moving_Sound_Source_Localization_and_Tracking_based_on_Envelope_Estimation_for_Unknown_Number_of_Sources

**[14]** J. Du, D. Wu, X. Wu, and T. Qu, “An Envelope Separation Aided Multi-Task Learning Model for Blind Source Counting and Localization,” *ICASSP 2026*. DOI: **10.1109/ICASSP55912.2026.11464467**. **[摘要/会议信息，建议下载全文]**  
ICASSP 官方 session 页面：https://www.cmsworkshops.com/ICASSP2026/view_paper.php?PaperNum=6212&bare=1

**[15]** N. Carion, F. Massa, G. Synnaeve, N. Usunier, A. Kirillov, and S. Zagoruyko, “End-to-End Object Detection with Transformers,” *ECCV*, 2020. **[公开全文]**  
arXiv：https://arxiv.org/abs/2005.12872  
代码：https://github.com/facebookresearch/detr

**[16]** Y. He, S. Shin, A. Cherian, N. Trigoni, and A. Markham, “Sound3DVDet: 3D Sound Source Detection Using Multiview Microphone Array and RGB Images,” *WACV*, 2024, pp. 5496–5507. **[公开全文]**  
CVF：https://openaccess.thecvf.com/content/WACV2024/html/He_Sound3DVDet_3D_Sound_Source_Detection_Using_Multiview_Microphone_Array_and_WACV_2024_paper.html  
代码：https://github.com/yuhanghe01/Sound3DVDet

**[17]** Y. He, S. Shin, A. Cherian, N. Trigoni, and A. Markham, “SoundLoc3D: Invisible 3D Sound Source Localization and Classification Using a Multimodal RGB-D Acoustic Camera,” *WACV*, 2025, pp. 5408–5418. **[公开全文]**  
CVF：https://openaccess.thecvf.com/content/WACV2025/html/He_SoundLoc3D_Invisible_3D_Sound_Source_Localization_and_Classification_using_a_WACV_2025_paper.html  
arXiv：https://arxiv.org/abs/2412.16861

**[18]** K. Shimada, Y. Koyama, S. Takahashi, N. Takahashi, E. Tsunoo, and Y. Mitsufuji, “Multi-ACCDOA: Localizing and Detecting Overlapping Sounds from the Same Class with Auxiliary Duplicating Permutation Invariant Training,” 2021. **[公开预印本]**  
arXiv：https://arxiv.org/abs/2110.07124

## Differentiable coordinate regression

**[19]** A. Nibali, Z. He, S. Morgan, and L. Prendergast, “Numerical Coordinate Regression with Convolutional Neural Networks,” 2018. **[公开全文]**  
arXiv：https://arxiv.org/abs/1801.07372

**[20]** X. Sun, B. Xiao, F. Wei, S. Liang, and Y. Wei, “Integral Human Pose Regression,” *ECCV*, 2018, pp. 529–545. **[公开全文]**  
CVF：https://openaccess.thecvf.com/content_ECCV_2018/html/Xiao_Sun_Integral_Human_Pose_ECCV_2018_paper.html  
PDF：https://openaccess.thecvf.com/content_ECCV_2018/papers/Xiao_Sun_Integral_Human_Pose_ECCV_2018_paper.pdf

## Multi-task gradient / feature sharing

**[21]** Z. Chen, V. Badrinarayanan, C.-Y. Lee, and A. Rabinovich, “GradNorm: Gradient Normalization for Adaptive Loss Balancing in Deep Multitask Networks,” *ICML*, 2018, pp. 794–803. **[公开全文]**  
https://proceedings.mlr.press/v80/chen18a.html  
arXiv：https://arxiv.org/abs/1711.02257

**[22]** B. Liu, X. Liu, X. Jin, P. Stone, and Q. Liu, “Conflict-Averse Gradient Descent for Multi-Task Learning,” *NeurIPS*, 2021. **[公开全文]**  
https://papers.neurips.cc/paper_files/paper/2021/hash/9d27fdf2477ffbff837d73ef7ae23db9-Abstract.html  
arXiv：https://arxiv.org/abs/2110.14048

**[23]** S. Liu, E. Johns, and A. J. Davison, “End-To-End Multi-Task Learning With Attention,” *CVPR*, 2019, pp. 1871–1880. DOI: **10.1109/CVPR.2019.00197**. **[公开全文]**  
CVF：https://openaccess.thecvf.com/content_CVPR_2019/html/Liu_End-To-End_Multi-Task_Learning_With_Attention_CVPR_2019_paper.html  
arXiv：https://arxiv.org/abs/1803.10704

## Model-based / physics-guided deep localization

**[24]** D. H. Shmuel, J. P. Merkofer, G. Revach, R. J. G. van Sloun, and N. Shlezinger, “SubspaceNet: Deep Learning-Aided Subspace Methods for DoA Estimation,” 2023. **[公开预印本]**  
arXiv：https://arxiv.org/abs/2306.02271

**[25]** J. P. Merkofer, G. Revach, N. Shlezinger, T. Routtenberg, and R. J. G. van Sloun, “DA-MUSIC: Data-Driven DoA Estimation via Deep Augmented MUSIC Algorithm,” *IEEE Transactions on Vehicular Technology*, vol. 73, no. 2, pp. 2771–2785, 2024. DOI: **10.1109/TVT.2023.3320360**. **[公开预印本/机构全文]**  
arXiv：https://arxiv.org/abs/2109.10581  
TU/e：https://research.tue.nl/en/publications/da-music-data-driven-doa-estimation-via-deep-augmented-music-algo-2/

**[26]** X. Weng, K. V. Ling, H. Liu, and K. Cao, “Towards End-to-End GPS Localization with Neural Pseudorange Correction,” 2024. **[公开预印本]**  
arXiv：https://arxiv.org/abs/2401.10685

**[27]** E. Grinstein, T. van Waterschoot, M. Brookes, and P. A. Naylor, “The Neural-SRP Method for Positional Sound Source Localization,” 2024. **[公开预印本/代码]**  
arXiv：https://arxiv.org/abs/2403.09455  
代码：https://github.com/egrinstein/neural_srp

## 多尺度 / 最新相关方法

**[28]** Y. Choi, H. Kim, J. Jun, and J. W. Shin, “FUN-SSL: Full-band Layer Followed by U-Net with Narrow-band Layers for Multiple Moving Sound Source Localization,” 2025/ICASSP 2026. **[公开预印本]**  
arXiv：https://arxiv.org/abs/2509.17490  
ICASSP session：https://www.cmsworkshops.com/ICASSP2026/view_paper.php?PaperNum=6212&bare=1

**[29]** “Sensing for Free: Learn to Localize More Sources than Antennas without Pilots,” 2025. **[公开预印本]**  
arXiv：https://arxiv.org/abs/2506.23525

**[30]** B. Duan, Y. Dong, T. Arslan, and J. Thompson, “TriAD: Tri-head with Auxiliary Duplicating Permutation Invariant Training for Multi-Task Sound Event Localization and Detection,” *ICASSP 2026*. DOI: **10.1109/ICASSP55912.2026.11462845**. **[摘要/会议信息，建议下载全文]**  
ICASSP 官方 session 页面：https://www.cmsworkshops.com/ICASSP2026/view_paper.php?PaperNum=6212&bare=1

**[31]** “TCN-MUSIC: Hybrid MUSIC-based deep network using temporal convolutions for DoA estimation,” *Digital Signal Processing*, vol. 176, 106054, 2026. DOI: **10.1016/j.dsp.2026.106054**. **[摘要/出版信息，建议下载全文]**  
ScienceDirect：https://www.sciencedirect.com/science/article/pii/S1051200426001739

**[32]** “Joint source enumeration and multi-source DOA estimation via a deep cascaded network,” *Digital Signal Processing*, vol. 181, 106192, 2026. DOI: **10.1016/j.dsp.2026.106192**. **[摘要/出版信息，建议下载全文]**  
ScienceDirect：https://www.sciencedirect.com/science/article/pii/S1051200426003118

---

## 参考来源使用说明

1. 本文档中对 [1]–[3] 的方法细节优先依据用户提供 PDF 内容整理，而不是仅依据网页摘要。
2. [14]、[30]、[31]、[32] 当前主要依据官方会议/出版页面与摘要判断，尚未在本轮获得可可靠阅读全文的公开版本，因此本文档没有将其未公开的具体网络细节作为确定事实。
3. 其它公开文献均优先列出 arXiv、CVF、PMC、PMLR、机构作者版等可公开获取来源。
4. 文中提出的 “Frequency-Spatial Source Splitting”“Localization-to-Band Transfer”等属于**针对本项目的综合设计建议/推导**，不是上述任一单篇文献的原始方法名称。

