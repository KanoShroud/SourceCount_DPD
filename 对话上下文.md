# 对话上下文

> 当前状态摘要更新至 2026-08-21。
> 当前唯一主线任务及执行状态见 `当前任务.md`；阶段性研究认识见 `研究总结.md`。

## 1. 当前状态

| 字段 | 内容 |
|---|---|
| 项目主题 | 在四站分布式被动接收场景中，以多子带 DPD 空间谱为物理先验，先检测频带并统计源数，再对目标混叠频带的细 DPD 谱进行多源定位。 |
| 主要材料 | 一份答辩后意见修改版学位论文、`第三章代码/` 和 `第四章代码/`。 |
| 创新重点 | 第三章：共享残差 CNN、跨子带 Transformer 与频率有序槽位联合完成频带占用和计数。第四章：CSPDarknet/PAN-FPN/U-Net 式空间谱重建，以热力图和 offset 实现多源亚网格定位。 |
| 当前主线 | 论文内容解读与第三、第四章代码一致性审查。 |
| 当前阶段 | 第一阶段静态审查、Gate 0、第三章 Gate 1、第四章 Gate 2、Gate 3A 与 Gate 3B 均已完成。Gate 3B 总报告 `outputs/gate3b_ch4/20260821_173526/gate3b_gate_report.json` 为 `PASS_WITH_MORE_EPOCHS_RECOMMENDED`。 |
| 已核验事实 | Gate 3B 只读复用 Gate 3A 的 `[1024,256,256]` 定位数据；训练前后15个分片/index的大小和SHA完全一致。两次seed 42、D8 `dualhead_std`、no-AMP、batch 8的60 epoch训练在科研历史、初始/最终validation及best/last完整状态上逐值一致，单次墙钟681.222/683.018 s。冻结epoch 57 checkpoint后，两次独立validation和两次独立test oracle-K评估各自完全一致；validation/test RMSE为150.792/110.566 m。14个阶段监控报告均无warning/red flag。 |
| 尚待核验 | Gate 3B best epoch=57，需决定是否延长训练并确认更充分的收敛；第三章小规模收敛；两章端到端 predicted K；论文正式配置和指标；4A–4D图表追溯；当前文献中的创新性。 |
| 当前修改状态 | Gate 3B 已增加冻结配置校验、初始validation及RNG恢复、checkpoint SHA/有限性/参数检查、长周期严格比较、数据前后SHA和总门禁。既有 `outputs/gate3_ch4` 保留为Gate 3A历史证据根，不改名为 `gate3a_ch4`；Gate 3B使用独立 `outputs/gate3b_ch4`。未改变模型、生产损失、数据物理模型、optimizer或formal配置。 |

## 2. 当前材料与作用

| 路径 | 类型 | 当前作用 | 证据状态 |
|---|---|---|---|
| `23011210426-刘同振-基于深度学习的时频混叠多信号检测计数与被动定位方法研究-信息与通信工程-李赞-答辩后意见修改.docx` | Word 学位论文 | 项目研究内容、方法、公式、实验和创新表述的主要依据 | 已完整读取并逐页检查 |
| `第三章代码/` | Python 与 MATLAB 源码 | 第三章方法、数据生成、训练、评估和可视化的实现 | 已完成静态调用与数据流映射 |
| `第四章代码/` | Python 与 MATLAB 源码 | 第四章方法、数据生成、训练、定位评估和绘图的实现 | 已完成静态调用与数据流映射 |
| `AGENTS.md` | 协作规则 | 规定环境、审批、验证、Git 和长期文档更新方式 | 已初始化并补充文档规则 |

## 3. 当前研究认识

1. 第三章将四站 IQ 划为 19 个重叠子带，在 81×81 粗网格上生成 DPD 最大特征值谱；共享 CNN 与 Transformer 输出 10×19 槽位占用矩阵，非空槽位数即源数。
2. 第四章对目标频带生成 401×401 细 DPD 谱，用 CSPDarknet、PAN-FPN 和 U-Net 式解码器预测高斯热力图及 offset，再按信号数取 Top-K 峰得到位置。原作者已确认主模型为 D8，即 `dualhead`、`dice_weight=0`，训练损失为 Focal+offset。
3. 核心实现基本齐全，但第四章现有评估从实验标签读取真实 `n_src`，未调用第三章预测，因此只证明 oracle-count 条件下的定位链，不是端到端系统。
4. 第四章 4A–4D 折线图脚本硬编码论文数值，未消费 `eval_exp.py` 的结果文件，当前图表证据链不闭合。
5. 原始配置的第三章数据约 25.1 GB；第四章训练分片约 128.6 GB，控制实验处理文件约 741.1 GB。整库载入也会逼近或超过 32 GB RAM，因此不能直接正式运行。

## 4. 当前审查范围

1. 从论文提取项目总体研究目标、第三章和第四章的章节结构、公式、算法步骤、实验设置和创新声明。
2. 对两章代码按入口脚本、核心模块、数据生成、训练或优化、评估、绘图和外部依赖分类。
3. 追踪输入数据、MATLAB/Python 中间文件、模型权重、预测结果和绘图数据的流向。
4. 将论文中的每个关键方法、对比实验和图表映射到源码文件、函数和输出；标记缺失、冗余或无法确认的部分。
5. 区分静态完整性、轻量可运行性和正式复现能力，避免把语法通过等同于实验复现。

## 5. 主要风险与边界

- Gate 0 已新增统一运行说明和最小依赖清单；完整数据 schema、逐入口命令和结果 manifest 仍缺失，仓库也没有数据和模型权重。
- `h5py`、`ultralytics` 已安装；Pillow 固定到 11.3.0 后已与 Matplotlib/torchvision 组合导入通过；源码中的 `/mnt/data` 和 `cuda:2` 已清除。
- 第四章默认 Dice Loss、batch 192 与已确认的 D8 及论文主描述不同；Focal Loss 正样本分母和解码器 Dropout 层数也存在实现/文字差异。论文写启用 AMP，但原作者确认实际训练应关闭；模型分支已经定案，其余原始命令和日志仍缺失。
- D8 的原作者确认、论文 Focal+offset 公式和 `plot_cdf.py` 的 `Proposed Net` 映射相互一致。D5 与 D3d 不再作为主模型；Gate 3B 已证明当前缩减数据上60 epoch训练严格可重复且发生有效学习，但best epoch=57，尚不能宣称充分收敛或论文性能复现。
- 第三章论文写 Adam，代码为 AdamW；M=100 消融没有完整现成评估入口。
- 未经用户批准，不因审查发现的问题直接修改代码或论文。

## 6. 当前下一步

1. Gate 3B 已完成；下一步单独审批第三章小规模收敛和 oracle-K/predicted-K 双轨端到端评估。
2. 另行设计第四章延长 epoch 的最小验证，优先回答epoch 57后是否还能稳定改善，不盲目扩大数据或网络。
3. 修复结果文件到4A–4D绘图的追溯链，保持test不参与模型选择。
4. 正式数据规模、论文指标复现和当前文献创新性核验继续作为独立阶段，不由缩减数据上的Gate 3B自动放行。
