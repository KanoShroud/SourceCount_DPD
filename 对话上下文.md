# 对话上下文

> 当前状态摘要更新至 2026-08-28。当前唯一任务见 `当前任务.md`，稳定研究认识见 `研究总结.md`，精确实验事实见 `实验结果记录.md`。

## 1. 当前阶段

- 第一阶段“论文与第三、第四章代码接管审查”已完成并冻结。
- 第二阶段 `S2-G0` 已采用最小逻辑封存完成：代码引用 `f6a802a`，数据和权重分别引用 `CH4-G3A-20260821`、`CH4-G3B-20260821`。
- `S2-G1`源间距后验门禁只作用于S2受控路径，共享位置生成器未修改；完整运行中拒绝并重采样1个违规train布局，3072条样本最终全部满足300 m约束。
- `S2-G1`已通过：Transformer、M=10、FP32、batch 64在`2048/512/512`数据上完成两次100 epoch严格重复训练；训练A第96 epoch的validation-best已冻结，两次validation重载一致，test只运行一次。精确事实见 `CH3-S2G1-20260822`。
- `S2-G2`已通过：D8在Gate 3A缩减数据上完整运行200 epoch，训练趋势CLOSED；按统一validation oracle-K RMSE选择新epoch 93 checkpoint，独立重载完全一致，未运行test。精确事实见`CH4-S2G2-20260822`。
- `S2-G3`工程执行已通过：第三章冻结模型在第四章256条validation IQ上通过跨分布模型门，但19子带二值真值接口使第四章匹配RMSE相对Exact-Oracle增加25.57%，超过10%表示门槛；科学状态为`REQUIRES_INTERFACE_REDESIGN`，完整四轨按预案未运行。精确事实见`SYS-S2G3-20260824`。
- `S2-G4`已按方案执行至完整数据构造门：预检、8/8数据pilot和单步训练pilot通过，但train样本205在冻结的19子带正标签规则下为空频带，按预设停止。全量元数据审计确认train/validation分别有154/38个`symbolRate<3 MHz`源无任何正子带；D8训练和因果比较未执行。精确事实见`SYS-S2G4-20260824`。
- `S2-G4-R1`已完成：0.2阈值使train/validation全部有效源获得正子带，但200 epoch新D8只恢复15.86%的Exact—Coarse RMSE缺口；CC20相对EE的RMSE/GOSPA比值为1.20484/1.17295，判为`REPRESENTATION_LIMIT_CANDIDATE_T020`。精确事实见`SYS-S2G4-R1-20260824`。
- `S2-G4-R2`已完成：统一以`BW_actual`定义物理频带后，Hard-19-Actual适配D8的RMSE为112.06 m，Soft-19适配为141.23 m，Exact基线为130.73 m；Soft未优于Hard且GOSPA等效门失败，判为`SOFT19_INSUFFICIENT`。精确事实见`SYS-S2G4-R2-20260825`。
- `S2-G4-R3`已完成：Exact/Hard/Soft在seed 42/1042/2042上配对比较。Hard的RMSE在三个seed均低于Exact，2/3通过联合等效门，判为`HARD19_STABLE_CANDIDATE`；Soft仅1/3优于Hard，未通过重新考虑门。精确事实见`SYS-S2G4-R3-20260825`。
- `S2-G4-R4`已全部完成：8192/2048/2048共享IQ、嵌套manifest、三表示DPD、9次训练、9次冻结`val_compare`评估和最终分析均PASS，test只生成原始IQ并锁定。精确事实见`SYS-S2G4-R4-20260826`。
- R4显示Hard在1k时明显劣于Exact，到4k/8k后差距消失到配对不确定性范围；Soft在4k点估较好、8k反而劣于Exact，与Hard也无稳定差异。当前因此固定Hard-19-Actual为CH3接口目标，停止Soft路线。
- `S2-G5-R1`已通过：统一入口覆盖K=0/1/2/3，Hard-19-Actual标签独立复算一致，CH3原Dataset/Transformer/loss可直接消费；K=1只保留未实现接口。精确事实见`SYS-S2G5-R1-20260827`。
- `S2-G5-R2`及8k条件扩展已通过：统一数据上1k/4k/8k Transformer均有效学习并早停；4k相对1k、8k相对4k的计数和频带主指标均有明确配对改善。8k相对4k分别提高5.47和3.06个百分点，预注册判定为`FREEZE_8K_FOR_R3`。精确事实见`CH3-S2G5-R2-20260827`。
- `S2-G5-R3`已通过：在固定512条K=2/3 validation样本上完成OB-OK、OB-PK、PB-OK、PB-PK四轨。频带路径GOSPA差异区间跨0，K路径稳定增加5.63 m；完整级联相对基准GOSPA增加25.14%，未通过等效门，判定`COUNT_PATH_PRIORITY`。精确事实见`SYS-S2G5-R3-20260828`。
- `S2-G5-R4`懒加载门已通过：新增8k和严格嵌套16k保持不变；512条频谱、Dataset/增强及1k三轮Eager/Lazy训练均严格一致。完整16k epoch的最低可用RAM约15.14 GiB、RSS约7.89 GiB、GPU约7.33 GiB，最终状态`LAZY_READY_FOR_TRAINING`；正式训练未启动。精确事实见`CH3-S2G5-R4-LAZY-20260828`。

## 2. 已确定的关键决定

- 第三章使用19幅81×81粗 DPD 谱，经共享 CNN、Transformer 和频率有序槽位联合完成频带检测与计数。
- 第三章当前缩减数据checkpoint已确认稳定学习和严格重复；它只用于第二阶段接口与规模诊断，不替代论文全量复现。
- 第四章接管主模型为 D8 `dualhead_std`，采用 Focal+offset、FP32/no-AMP；Gate 3B已确认60 epoch严格重复，S2-G2进一步建立了完整200 epoch调度下的接口checkpoint。
- 当前第四章评估使用真实 `n_src` 决定 Top-K，属于 oracle-K，不是端到端 predicted-K。
- S2-G2冻结权重为epoch 93、SHA256=`5d5224d8d6478739af5042391ef1e718c08e2f81565882e133372c9fbd116dfd`；只服务S2-G3接口工程，不是论文正式权重。
- Gate 3A 历史目录保持原名原位；Gate 3B 使用独立目录，后续不得移动或覆盖这些证据。
- 两章现成模型仍不能直接宣称联合系统成立；但R2已表明，统一为`BW_actual`并重新训练D8后，Hard-19本身可以达到当前oracle候选水平，主要待解决问题转为CH3如何学习并预测一致的Actual频带标签。
- 当前第三章正标签规则使用10 MHz子带和0.3覆盖阈值，对`symbolRate<3 MHz`的第四章源无法分配正子带；validation中“整样本不为空”曾掩盖部分源未被接口表达的问题。
- R1的0.2主瓣Hard-19结果受到`symbolRate`主瓣与`BW_actual`实际频带定义不一致的混杂；不能再据此单独认定19窗口分辨率是主要限制。
- Hard-19-Actual已通过三seed稳定性筛查，成为下一步CH3 Actual标签与预测接口的固定oracle候选；Soft-19不再优先。
- R4完成后，Hard-19-Actual已作为CH3接口候选冻结；后续不再优先Soft、Hard-39或更细频率网格，而是建立CH3 Actual标签和predicted-band/K链路。
- 统一系统按源数分流：K=0直接结束，K=1定位暂空，K=2/3进入Hard-19-Actual D8。K=2/3与G4-R4保持同一条件分布但使用独立样本，因此R1本身不触发D8重训。
- R2的1k/4k/8k best checkpoint都只由同一`val_select`选择，`val_compare`只用于规模决策。8k所有K准确率均高于4k，冻结为R3工程接口checkpoint；这仍是单training seed和固定validation证据，不代表论文最终规模。
- R3确认当前8k模型的级联退化来自K漏计/多计，但R2规模曲线尚未饱和；R4因此先扩展16k原CH3基线，再决定是否研究K置信度、候选K或稳健解码。当前16k数据和等价懒加载入口已就绪，等待用户选择正式训练运行主体。
- R3中真值K=2/3但预测K=1的样本只使用D8 Top-1作反事实误差隔离，不代表真实K=1定位接口已实现。

## 3. 未决风险

- 第三章已完成统一数据1k→4k→8k学习曲线并冻结8k接口checkpoint，但仍只有单training seed，尚未验证跨seed波动或论文正式指标。
- 第四章缩减配置的200 epoch趋势已关闭，但后段存在可能过拟合模式；新权重整体RMSE更低主要来自减少极端失配，median、P90/P95未同步改善，长尾机制仍待解释。
- 新接口的predicted band/K四轨已完成，但完整级联未通过GOSPA等效门；下一风险集中在K判决改进是否真正降低集合基数误差。
- S2-G4的0.3版本因表示支持域不闭合而停止；该历史问题已由获批的S2-G4-R1以0.2阈值继续验证，不再是当前待办。
- S2-G4-R3仍只有三个训练seed、同一缩减validation和oracle-K；Hard稳定候选不能外推为理论上优于Exact或论文test性能。
- R4只使用一个training seed和冻结validation；sample bootstrap不包含训练seed方差，且未运行test。不得将Hard接口充分性表述成理论优于Exact或论文最终性能。
- Soft在两个seed劣于Hard、一个seed优于Hard，且各seed样本bootstrap区间均跨0；不能声称Soft必然有害，只能判定其收益不稳定且不值得当前接口改造成本。
- 4A–4D绘图仍缺少结构化结果追溯链，待最终定位接口与模型路线明确后排期。
- 正式数据规模、论文指标和面向当前投稿的创新边界尚未核验。
- R3只使用单个CH3和D8训练seed；四轨sample bootstrap不包含训练随机性，后续正式结论仍需多seed和冻结test。
- K=1定位仍为空接口，因此当前系统只完成K=0分流和K=2/3诊断级联，不能宣称覆盖K=0–3的完整可部署系统。
- R2只有单training seed和validation证据；8k虽继续改善，最终论文配置仍需补跨seed与独立test，不能把样本bootstrap当作训练随机性证据。
- 16k eager Dataset的RAM风险已由R4专用懒加载解决；当前剩余风险是正式16k训练尚未运行，单epoch线性时间估算不包含validation、checkpoint和早停开销。

## 4. 恢复工作入口

- 当前任务和审批状态：`当前任务.md`
- 第一阶段详细解读与历史 Gate：`第一阶段_论文与代码接管审查.md`
- 第二阶段目标、路线和退出条件：`第二阶段_缩减规模复现与端到端闭环.md`
- 稳定研究结论：`研究总结.md`
- 精确实验事实：`实验结果记录.md`
- 修改历史：`修改记录.md`
- 环境与安全命令：`运行基础说明.md`
