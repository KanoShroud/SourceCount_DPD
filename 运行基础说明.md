# 运行环境与安全执行说明

> 本文件只维护稳定环境、路径、安全模式和可复用命令。某次运行的结果、资源、异常和历史目录分别见 `实验结果记录.md` 与 `修改记录.md`。

## Python

项目解释器：

```powershell
D:\Software\anaconda3\envs\PyTorch\python.exe
```

统一配置位于 `runtime_config.py`。Python 与 MATLAB 均默认采用 smoke。当前E2E工作树的路径约定：

- 正式数据：`data/chapter3`、`data/chapter4`
- 正式输出：`outputs_e2e/formal/<chapter>/<entry>`
- smoke 数据与输出：`outputs_e2e/smoke/<chapter>/...`
- 原项目冻结参考：由`SOURCECOUNT_REFERENCE_OUTPUT_ROOT`显式指向`F:\SourceCount_DPD\outputs`，只读
- GPU：`cuda:0`

PyCharm中的E2E运行配置应包含：

```powershell
$env:SOURCECOUNT_REFERENCE_OUTPUT_ROOT = 'F:\SourceCount_DPD\outputs'
$env:SOURCECOUNT_OUTPUT_ROOT = 'F:\SourceCount_DPD_E2E\outputs_e2e'
$env:SOURCECOUNT_DATA_ROOT = 'F:\SourceCount_DPD_E2E\data'
$env:SOURCECOUNT_RUN_MODE = 'formal'
$env:PYTHONUTF8 = '1'
$env:PYTHONIOENCODING = 'utf-8:replace'
```

统一模型入口必须通过`统一模型代码/preflight_reference.py`核对参考产物；参考根与输出根相同或互相嵌套时拒绝运行。历史Gate命令保留其原项目路径语义，不在本工作树批量改写。

大型参考输入不直接用于训练。先生成冻结manifest，再用Windows无缓冲方式固化并验证本地快照：

```powershell
& 'D:\Software\anaconda3\envs\PyTorch\python.exe' `
  'F:\SourceCount_DPD_E2E\统一模型代码\materialize_reference_snapshot.py' `
  --manifest '<运行目录>\manifest.json' `
  --snapshot-root '<运行目录>\reference_snapshot'
```

训练和评价必须改读`snapshot_manifest.json`登记的本地路径；每个阶段开始和结束后执行同一命令并增加`--verify-only`。任一大小、SHA或HDF5读取异常均停止，不回写原目录，也不继续使用异常缓存页形成科研结论。

正常门禁只复核本地快照SHA，不反复扫描原目录。只有SHA异常时才运行分级诊断：

```powershell
& 'D:\Software\anaconda3\envs\PyTorch\python.exe' `
  'F:\SourceCount_DPD_E2E\统一模型代码\classify_data_integrity.py' `
  --candidate '<待核验MAT>' --baseline '<冻结快照MAT>' `
  --logical on-mismatch --output '<全新诊断报告.json>'
```

工具先重复计算候选文件SHA；字节完全匹配时立即结束。只有稳定的文件SHA不同才解码全部科研dataset，区分容器重打包与数据值变化，避免把正常路径变成长期全量HDF5扫描。

PowerShell 中显式启用 smoke：

```powershell
$env:SOURCECOUNT_RUN_MODE = 'smoke'
```

启用正式模式：

```powershell
$env:SOURCECOUNT_RUN_MODE = 'formal'
```

恢复安全默认：

```powershell
$env:SOURCECOUNT_RUN_MODE = 'smoke'
```

可选覆盖：`SOURCECOUNT_DATA_ROOT`、`SOURCECOUNT_OUTPUT_ROOT`、`SOURCECOUNT_DEVICE`。读取原项目历史产物时必须另设`SOURCECOUNT_REFERENCE_OUTPUT_ROOT`。

## MATLAB

MATLAB 数据入口默认采用 smoke，正式运行必须显式设置：

```matlab
setenv('SOURCECOUNT_RUN_MODE', 'formal')
```

恢复安全默认：

```matlab
setenv('SOURCECOUNT_RUN_MODE', 'smoke')
```

smoke 与正式输出由根目录 `gate0_runtime.m` 隔离。关闭 MATLAB 后环境变量通常不会保留；每次正式运行前都应重新确认控制台打印的 `[Gate0] mode=formal`。

## 边界

- Gate 0 只修路径、设备、依赖、入口保护和输出隔离。
- smoke 只验证运行链路，不产生论文性能证据。
- `trim_exp_pt.py` 默认把精简副本写入独立输出目录，不再原位覆盖输入数据。
- 正式数据生成、训练和完整评估仍按项目规则在 MATLAB/PyCharm 中执行。

## Gate 1 第三章最小闭环

每次运行必须建立新的根目录，不能复用已有 smoke：

```powershell
$gate1RunId = Get-Date -Format 'yyyyMMdd_HHmmss'
$gate1Root = "F:\SourceCount_DPD\outputs\gate1_ch3\$gate1RunId"
$env:SOURCECOUNT_RUN_MODE = 'smoke'
$env:SOURCECOUNT_OUTPUT_ROOT = $gate1Root
$env:SOURCECOUNT_DEVICE = 'cuda:0'

& 'D:\Software\MATLAB\R2025b\bin\matlab.exe' `
  -batch "run('F:/SourceCount_DPD/第三章代码/main30.m')"

& 'D:\Software\anaconda3\envs\PyTorch\python.exe' -X utf8 `
  'F:\SourceCount_DPD\第三章代码\gate1_smoke_ch3.py' `
  --data_dir "$gate1Root\smoke\chapter3\data" `
  --output_dir "$gate1Root\smoke\chapter3\gate1_smoke_ch3" `
  --device cuda:0 --batch_size 2 --seed 20260820
```

上述命令复现核心数据与模型检查，但不自动生成外部资源监控 CSV；受控门禁仍须遵循 `第一阶段_论文与代码接管审查.md` 第 8.2 节的资源基线、采样和硬超时规则。具体历史结果通过实验 ID 或修改事件查询，不在本文件维护。

## S2-G1 第三章受控缩减训练

S2-G1使用独立时间戳根目录。MATLAB规模和seed覆盖必须成对设置，缺一项即拒绝运行：

```powershell
$s2g1RunId = Get-Date -Format 'yyyyMMdd_HHmmss'
$s2g1Root = "F:\SourceCount_DPD\outputs\s2g1_ch3\$s2g1RunId"
$env:SOURCECOUNT_RUN_MODE = 'formal'
$env:SOURCECOUNT_DATA_ROOT = "$s2g1Root\data"
$env:SOURCECOUNT_OUTPUT_ROOT = $s2g1Root
$env:SOURCECOUNT_CH3_TRIALS_LIST = '2048,512,512'
$env:SOURCECOUNT_CH3_RANDOM_SEED = '20260823'
```

- `main30.m`只在上述受控覆盖启用时拒绝低于300 m的源布局并有限重试；普通smoke/formal调用保持原生成行为。
- `s2g1_train_ch3.py`提供`capacity`、`train`、`eval`三个独立入口，复用原`train_v26.py`的数据集、模型和loss实现。
- `s2g1_ch3.py`提供数据manifest/审计、训练比较、checkpoint冻结、评估比较和总报告入口。
- 完整顺序、固定参数和停止条件以 `第二阶段_缩减规模复现与端到端闭环.md` 第8章为准；精确历史结果只记录在 `实验结果记录.md`。

## Gate 2 第四章最小闭环

Gate 2 类第四章 smoke 每次也必须使用全新根目录，并遵循接管审查文档第 8.3 节的基线、资源采样、硬超时和失败停止规则。可复用接口包括：

- `main31_loc_v3.m`：smoke 固定 `[4,2,2]` 与 seed 20260821，且不启动未使用的并行池；formal 仍保留 `[40000,5000,5000]`。
- `gen_ch4_loc_data.py`：支持 `--max_samples`、`--chunk_size` 和 `--report_path`，可先做单任务 pilot，再生成三个 split。
- `train_yolo.py`：支持独立 `--val_batch_size`；未传入时保留历史默认行为。
- `gate2_smoke_ch4.py`：提供 `mat-audit`、`audit` 和 `model` 三种模式，D8 映射固定为 dualhead、no-AMP、Focal+offset。

这些入口只用于受控链路和容量检查，不证明收敛、定位性能或论文结果。失败不得在原目录续写为通过证据；修复后必须使用全新目录。历史运行与修复见 `第一阶段_论文与代码接管审查.md` 第 8.3 节、`修改记录.md` 和 `实验结果记录.md`。

## S2-G4-R4 PyCharm点击训练入口（历史备用）

R4长训练不要求用户在命令行输入参数。打开`第四章代码/s2g4r4_pycharm_train.py`，将PyCharm解释器设为：

```text
D:\Software\anaconda3\envs\PyTorch\python.exe
```

该入口曾用于交付`Exact/1024/seed42`的PyCharm点击运行方式，并绑定了R4数据根、manifest、D8、FP32/no-AMP、batch 8、10秒资源监控和无墙钟超时配置。R4后续已由Codex按用户授权执行完毕；本入口仅作历史备用，不得用于覆盖现有R4证据目录。
