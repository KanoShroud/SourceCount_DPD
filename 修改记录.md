# 修改记录

> 本文件只记录具有项目意义的修改事件：为什么修改、改了哪些文件、是否改变算法以及如何验证。精确实验配置、指标、资源、哈希和运行目录见 `实验结果记录.md` 或对应阶段文档。

## 2026-08-20 建立长期项目文档体系

| 字段 | 内容 |
|---|---|
| 修改原因 | 建立可持续维护的项目状态、研究认识、任务、修改和实验记录体系。 |
| 主要修改 | 新建长期文档；精简从其他项目迁入的 `AGENTS.md`，保留环境、审批、验证、Git 和文档规则。 |
| 验证 | 中文编码、文件职责和根目录链接检查通过。 |

## 2026-08-20 完成论文与第三、第四章代码静态接管审查

| 字段 | 内容 |
|---|---|
| 修改原因 | 建立论文方法、源码入口、调用关系、数据流和完整性之间的可追溯映射。 |
| 主要修改 | 新建 `第一阶段_论文与代码接管审查.md`，形成项目总图、两章讲义、论文—代码对应表、风险和资源估算。 |
| 验证 | Word逐页检查、源码静态映射、参数量和依赖核验完成；不包含正式实验。 |

## 2026-08-20 完成 Gate 0 运行基础改造

| 字段 | 内容 |
|---|---|
| 修改原因 | 解决硬编码路径/设备、依赖冲突、MATLAB误启正式规模和输出覆盖风险。 |
| 主要修改 | 增加统一 Python/MATLAB 配置、smoke/formal 隔离、`cuda:0` 约定、入口保护和最小依赖说明。 |
| 算法边界 | 未修改信号模型、DPD、网络、损失或正式参数。 |
| 验证 | Python入口、核心MATLAB函数、组合导入、静态检查和输出隔离通过。 |

## 2026-08-20 补全论文自学讲义并形成 Gate 1 方案

| 字段 | 内容 |
|---|---|
| 修改原因 | 将论文公式、张量和代码关系整理为可独立学习的材料，并设计第三章最小闭环。 |
| 主要修改 | 扩充第一阶段文档第2–4章和Gate 1方案。 |
| 验证 | 公式、形状、入口和交叉引用检查通过；当时未运行Gate 1。 |

## 2026-08-21 实施并完成 Gate 1

| 字段 | 内容 |
|---|---|
| 修改原因 | 验证第三章 MATLAB→MAT/HDF5→Dataset→Transformer→loss/checkpoint 工程闭环。 |
| 主要修改 | 新增 `gate1_smoke_ch3.py`；smoke模式跳过未使用MATLAB并行池；监控日志统一UTF-8安全转发。 |
| 失败与修复 | 首次运行因无用并行池导致内存越线；按审批最小修复后使用全新目录重跑。 |
| 算法边界 | 未改变数据物理模型、标签、网络或生产损失。 |
| 验证 | 标签独立复算、ignore-loss、单步训练、保存重载、资源和隔离门禁通过；详细历史见第一阶段第8.2节。 |

## 2026-08-21 实施并完成 Gate 2

| 字段 | 内容 |
|---|---|
| 修改原因 | 验证第四章 IQ→细DPD→标签/坐标→D8单步链，并确定主模型、AMP和batch接口。 |
| 主要修改 | 增加smoke数量/种子、分片审计、独立验证batch和 `gate2_smoke_ch4.py`；固定D8、Focal+offset、no-AMP。 |
| 失败与修复 | 首次坐标oracle因测试与生产float32运算顺序不同而停止；按审批只修测试oracle并在全新目录重跑。 |
| 算法边界 | 未改变DPD、模型、标签、损失阈值或正式配置。 |
| 验证 | 数据、坐标、oracle、D8单步、checkpoint和容量门禁通过；详细历史见第一阶段第8.3节。 |

## 2026-08-21 实施并完成 Gate 3A

| 字段 | 内容 |
|---|---|
| 修改原因 | 验证第四章中等规模数据、完整DPD、短训练确定性和资源可行性。 |
| 主要修改 | 增加smoke-only规模/种子覆盖、输出/任务保护、确定性训练、完整checkpoint和结构化日志；新增Gate 3监控、审计和评估入口。 |
| 失败与修复 | 先后修正比较器成功路径控制流和Windows日志编码/管道问题；失败目录保留，修复后从受影响阶段新建目录运行。 |
| 算法边界 | 未改变DPD、D8、生产损失或数据物理模型。 |
| 验证 | 数据语义、前缀一致性、双重复pilot、资源和隔离门禁通过；精确事实见 `CH4-G3A-20260821`。 |

## 2026-08-21 实施并完成 Gate 3B

| 字段 | 内容 |
|---|---|
| 修改原因 | 完成缩减数据上的长周期严格重复训练、冻结权重和独立oracle-K评估。 |
| 主要修改 | 增加Gate 3B冻结参数、初始validation及RNG恢复、checkpoint配置/SHA检查、完整训练比较和总门禁。 |
| 目录决策 | Gate 3A历史目录保持原名原位；Gate 3B使用独立根目录。 |
| 算法边界 | 未改变DPD、D8、Focal+offset、数据物理模型、optimizer或formal参数。 |
| 验证 | 语法、Ruff、两次完整训练、数据前后SHA和独立评估通过；精确事实见 `CH4-G3B-20260821`。 |

## 2026-08-21 回填 Gate 0–3 证据并重构第一阶段决策

| 字段 | 内容 |
|---|---|
| 修改原因 | 第一阶段第8章已记录Gate过程，但其他章节仍有过时状态，第10章缺少阶段级决策。 |
| 主要修改 | 回填第三、第四章实证状态，更新完整性、验证、资源、学习路线和证据边界；将第10章改为简明阶段路线。 |
| 涉及文件 | `第一阶段_论文与代码接管审查.md`、`修改记录.md`。 |
| 验证 | 标题、路径、指标引用、过时措辞和Markdown差异检查通过。 |

## 2026-08-21 实施长期文档去重并切换第二阶段

| 字段 | 内容 |
|---|---|
| 修改原因 | 多份长期文档重复维护Gate指标、当前状态、运行目录和下一步，存在漂移风险；第一阶段接管目标已经完成。 |
| 主要修改 | `AGENTS.md`增加单一事实源和阶段冻结规则；按职责精简状态、总结、任务、实验和运行说明；第一阶段标记完成并冻结；新建第二阶段框架并把当前任务切换到S2-G0方案待审批。 |
| 涉及文件 | `AGENTS.md`、六份长期状态/记录文档、`运行基础说明.md`、第一阶段文档及新建第二阶段文档。 |
| 算法边界 | 仅文档治理，不修改代码、数据、权重或实验结论。 |
| 验证 | 全局职责、实验ID、链接、标题、重复项和Markdown差异检查。 |

## 2026-08-21 以最小方案完成 S2-G0 基线引用

| 字段 | 内容 |
|---|---|
| 修改原因 | 当前基线仅用于承接第二阶段，后续不会作为正式复现基线；完整快照工具、重复哈希、额外manifest和Git标签超出实际需要。 |
| 主要修改 | 复用代码提交 `f6a802a`、数据证据 `CH4-G3A-20260821` 和权重/评估证据 `CH4-G3B-20260821` 完成逻辑封存；第二阶段转入S2-G1方案待审批。 |
| 涉及文件 | `第二阶段_缩减规模复现与端到端闭环.md`、`当前任务.md`、`对话上下文.md`、`修改记录.md`。 |
| 算法边界 | 未修改代码、数据、权重或实验结论；未创建快照、manifest、标签或新实验目录。 |
| 验证 | 代码提交可解析；Gate 3A/3B报告和冻结checkpoint存在；报告状态与长期实验记录一致；Markdown差异检查通过。 |

## 2026-08-21 增量建立 S2-G1 第三章受控训练与审计入口

| 字段 | 内容 |
|---|---|
| 修改原因 | 在不破坏师弟原训练入口的前提下执行第三章缩减数据收敛、确定性和独立评估门禁。 |
| 主要修改 | `main30.m`增加成对的可选规模/seed覆盖及运行元数据；新增 `s2g1_train_ch3.py` 和 `s2g1_ch3.py`，复用原Dataset、模型和loss，提供容量、训练、评估、数据/重复性审计和资源预检。 |
| 兼容策略 | 未修改 `train_v26.py`；未设置S2环境变量时，`main30.m`保留原smoke/formal规模、并行池与入口行为。原模型、DPD、标签和loss未修改。 |
| 第一阶段兼容审计 | 从初始化提交到第一阶段的原文件改动主要为Windows路径、`cuda:0`、smoke/formal隔离和可选审计参数；所有Python文件编译通过，第三/第四章关键模块导入与CLI帮助通过，9个被改MATLAB入口的Code Analyzer无错误。bare命令默认改为安全smoke和隔离路径，formal仍可显式进入；`trim_exp_pt.py`改为隔离副本而非原位覆盖，是有意的安全语义变化。 |
| 工程验证 | 首次容量smoke因当前PyTorch不接受带`torch.device`的峰值统计API而在训练前失败；最小修正后在全新 `outputs/s2g1_engineering_smoke/20260821_203908` 完成容量、两次6 epoch训练、checkpoint状态逐值比较和两次validation重载，全部一致；不作为性能结果。 |
| 实际Gate状态 | S2-G1首次数据运行在数据审计发现原生成器无约束回退导致1条源间距违规，已停止；精确事实见 `CH3-S2G1-20260821-A`。 |

## 2026-08-22 为 S2-G1 受控数据增加源间距后验门禁

| 字段 | 内容 |
|---|---|
| 修改原因 | 首次S2-G1数据审计发现原位置生成器在约束放置失败后会无约束补齐，产生低于300 m最小源间距的样本。 |
| 主要修改 | 仅在 `main30.m` 的S2受控路径拒绝违规布局并有限重试；保存逐样本重试次数、split拒绝总数、300 m阈值和100次上限；`s2g1_ch3.py`同步核验元数据、重试计数和零间距违规。 |
| 增量兼容边界 | 未修改共享 `gen_multi_source_pos_v2.m`、`train_v26.py`、DPD、标签、模型或loss；未设置S2环境变量时，位置生成结果不因后验检查而重采样。 |
| 静态验证 | 两个S2 Python入口通过 `py_compile` 和Ruff F/E9；`main30.m` Code Analyzer无错误，仅保留既有GVMIS/AGROW/LOGSUM提示；共享生成器Git差异为空。 |
| 运行验证 | `outputs/s2g1_ch3/20260822_120601`因启动RAM不足停止；释放资源后在全新 `20260822_122055` 完整重跑，train中1个违规布局被拒绝并一次重试，最终数据零违规，S2-G1全部门禁通过，见 `CH3-S2G1-20260822`。 |

## 2026-08-22 实施 S2-G2 并固化低级工程错误防线

| 字段 | 内容 |
|---|---|
| 修改原因 | 在不改变师弟原入口的前提下完成第四章200 epoch充分训练诊断，同时把本项目多次出现的编码、日志、API、路径和报告汇总类低级错误转化为稳定防错规则。 |
| 主要代码 | `train_yolo.py`新增默认关闭的`--s2g2_d8`严格入口；`eval_ch4_checkpoint.py`新增可选逐源长尾诊断；新增`s2g2_ch4.py`完成预检、manifest、训练审计、候选比较、冻结、重载比较和总门禁；`gate3_stage_runner.py`统一Python子进程UTF-8替换策略。 |
| 兼容边界 | 原普通、Gate 3A和Gate 3B入口均保持；未修改D8结构、DPD、数据、loss、优化器或评价指标。S2-G2只在显式开关下生效，输入数据只读，未运行test。 |
| 低级错误复盘 | 历史上出现过MATLAB中文监控日志乱码、Windows日志转发线程因编码异常退出并引发假超时、比较器成功路径控制流遗漏、PyTorch显存统计API参数版本不兼容；本次又出现Git子进程默认代码页解码失败及`stdout=None`二次异常、Python子进程未统一UTF-8、手写历史时间戳路径错误、总门禁不能容纳保留失败证据。共同根因是长任务前缺少小型接口自检、依赖系统默认编码/版本行为、输入身份未先按文件和SHA解析，以及报告器把工程历史与通过证据混为一谈。 |
| 规则固化 | `AGENTS.md`新增Windows子进程显式编码、非ASCII自检、日志线程异常回传、可空输出防护、输入路径与SHA预检、失败/PASS证据分离，以及高置信低风险工程错误可自动最小修复并继续的条件和边界。 |
| 失败与修复 | 预检失败根`20260822_142153`和代码后续变化前的`20260822_143549`均未作为通过证据；有效根为`outputs/s2g2_ch4/20260822_143749`。其中错误Gate 3B路径评估目录`03_eval_gate3b`保留，按SHA定位后在`03_eval_gate3b_after_path_fix`通过；总报告显式记录该失败尝试。 |
| 运行结果 | 200 epoch完整通过并冻结epoch 93新权重，趋势`TREND_CLOSED`，总门禁`PASS`；精确指标、资源和边界见`CH4-S2G2-20260822`。 |
| 验证 | 训练前后15个输入文件SHA256一致；两候选统一validation和冻结checkpoint第二次重载通过；4份有效监控无warning/red flag；Python语法、Ruff F/E9、总报告回读、formal隔离和残留进程检查通过。 |

## 2026-08-24 清理无效数据、重复载荷和工程缓存

| 字段 | 内容 |
|---|---|
| 修改原因 | 降低 `outputs` 占用，同时保留当前研究主线、有效冻结权重和可审计的历史结论。 |
| 删除内容 | 删除 `CH3-S2G1-20260821-A` 的三个不合格 MAT 文件；删除 outputs 中大于等于1 MiB且SHA256逐字节重复的非主副本，保留 Gate 3A 完整定位数据或 Gate 2 成功运行副本；删除 `outputs/s2g1_engineering_smoke`、Python `__pycache__` 和 Ruff 缓存。删除后为空的目标目录一并移除。 |
| 保留证据 | 失败运行根、审计JSON、manifest、资源CSV、日志和总报告均保留；Gate 3A完整定位数据、Gate 2成功副本、S2-G1/S2-G2冻结best checkpoint及其他非重复checkpoint未删除。工程 smoke 仅为轻量链路验证，已被完整S2-G1通过证据替代。 |
| 空间结果 | `outputs` 从8,625,192,655 bytes降至6,916,248,740 bytes；连同源码目录缓存共删除1,708,943,915 bytes，约1.592 GiB。其中失败MAT为1,241,624,245 bytes、逐字节重复大文件为426,531,288 bytes、工程smoke与缓存为40,788,382 bytes。 |
| 算法边界 | 未修改代码、数据生成逻辑、模型、loss、配置、指标或实验结论；未删除任何当前有效数据、冻结best权重或唯一结果副本。 |
| 验证 | 删除前重新计算全部大文件SHA256并明确唯一保留副本；删除后复核关键数据、checkpoint和总报告存在，复扫无剩余大文件重复组，并检查空路径与Git差异。 |

## 2026-08-24 增量实施 S2-G3 跨章可组合性审查

| 字段 | 内容 |
|---|---|
| 修改原因 | 在不硬串异分布模型得出性能结论的前提下，审查两章数据契约、第三章跨分布能力和19子带二值接口的信息上限。 |
| 主要代码 | 新增`s2g3_compute_coarse_dpd.m`，只读第四章IQ并复用第三章DPD实现生成19×81×81输入；新增`s2g3_composability.py`，提供契约审计、双推理比较、细DPD生成、D8集合评估、表示门和最终决策。 |
| 增量兼容 | 未修改`main30.m`、`main31_loc_v3.m`、`train_v26.py`、`train_yolo.py`、原模型或原评估默认入口；新增脚本显式调用且输出拒绝覆盖。未生成新IQ、训练、微调、调阈值或运行test。 |
| 高置信工程修复 | 修正监控器参数名/采样间隔、错误历史IQ路径和MATLAB日志编码；Exact回归阶段将新评估器改为与`LocDataset`一致的逐样本归一化归约顺序，并保持第四章原生float32距离口径。原S2-G2入口复算证明修复后RMSE精确一致。 |
| 失败证据 | 外层包装器导致的预检假残留和首次完整粗DPD错误路径均保留独立监控目录；被替代的两份Exact评估也保留，不混入最终表示门。 |
| 文档同步 | 更新`第二阶段_缩减规模复现与端到端闭环.md`、`当前任务.md`、`对话上下文.md`、`研究总结.md`、`实验结果记录.md`和本记录；精确实验事实唯一记录为`SYS-S2G3-20260824`。 |
| 科研表述修正 | 初版总报告把Coarse-Oracle退化直接归因为表示信息上限；最终复核将其收紧为“当前映射与冻结D8不能直接复用，尚不能区分信息损失与下游分布失配”，保留初版并生成解释修正版报告。 |
| 验证 | Python `py_compile`与Ruff F/E9、MATLAB Code Analyzer、CLI/import、Git差异和17份监控报告回读通过；最终15份有效阶段PASS且无warning/red flag，2份工程失败单列；总报告工程`PASS`、科学状态`REQUIRES_INTERFACE_REDESIGN`。 |

## 2026-08-24 增量实施 S2-G4 并在频带支持域门停止

| 字段 | 内容 |
|---|---|
| 修改原因 | 按批准方案比较Coarse-D8从头训练与Exact-D8微调，以区分下游输入分布失配和19子带表示不足；同时保持师弟原入口默认行为。 |
| 主要代码 | `train_yolo.py`新增默认关闭、互斥且配置严格校验的S2-G4 scratch/finetune入口，微调只加载校验过SHA的D8权重而不恢复优化器、调度器或RNG；新增`s2g4_coarse_d8.py`完成数据构造、支持域审计、统一评估和停止/最终报告；新增`s2g4_resource_preflight.py`避免PyTorch导入污染启动RAM基线。 |
| 增量兼容 | 普通、Gate 3A/3B和S2-G2入口默认行为不变；未修改D8结构、DPD算法、原数据、loss、第三章代码或阈值。所有S2-G4输出使用唯一目录并拒绝覆盖。 |
| 高置信工程修复 | 最初资源预检在导入PyTorch后采样，错误把预检进程自身占用计入系统启动基线，改为独立轻量脚本后重测；首次完整构造因外层工具过早关闭输出管道而中断，保留失败目录并以正确等待方式在新目录重跑。两项均未改变资源门槛或科研语义。 |
| 科学停止 | 新构造在train样本205发现空Coarse-Oracle频带。追加只读全量元数据审计，确认train/validation分别有154/38个`symbolRate<3 MHz`源无法获得任何正子带；按预案未作Exact回退、ignore转正或阈值调整，也未启动完整数据、D8训练和交叉评估。 |
| 文档同步 | 将批准方案和实际停止结论写入第二阶段文档；同步更新`当前任务.md`、`对话上下文.md`、`研究总结.md`、`实验结果记录.md`及本记录。精确事实唯一记录为`SYS-S2G4-20260824`。 |
| 验证 | 身份/资源预检、8/8数据pilot和单步训练pilot通过；停止报告结构化保存。Python语法、Ruff F/E9、CLI、JSON、Git差异和残留进程在交付前复核。 |

## 2026-08-24 实施 S2-G4-R1 0.2阈值快速D8适配诊断

| 字段 | 内容 |
|---|---|
| 修改原因 | 用户选择不做阈值搜索，固定0.2快速闭合第四章2 MHz下限的逐源支持域，并以最短EE/EC20/CC20三轨验证D8重训能否恢复性能。 |
| 主要代码 | `s2g4_coarse_d8.py`新增显式`--s2g4r_t020`标签重算、0.2支持审计、EC20/CC20评估标识和`finalize-r1`三轨判定；复用既有`train_yolo.py --s2g4_scratch`，未新增训练算法。 |
| 增量兼容 | 0.2分支只在显式开关下启用；原0.3 MAT标签、MATLAB入口、CH3、普通训练及历史S2-G4目录不变。新数据和报告均记录阈值与物理元数据重算身份。 |
| 执行结果 | 支持域、8/8 pilot、完整1024/256数据、EE/EC20、200 epoch scratch、CC20和总判定全部通过工程门；科学状态为`REPRESENTATION_LIMIT_CANDIDATE_T020`，精确事实见`SYS-S2G4-R1-20260824`。 |
| 范围控制 | 按快速方案取消微调和CE/FE/FC，不运行test、predicted-band/K或CH3训练；未因结果调整epoch、阈值或模型。 |
| 文档同步 | 更新第二阶段方案与执行结论、`当前任务.md`、`对话上下文.md`、`研究总结.md`、`实验结果记录.md`和本记录。 |
| 验证 | Python `py_compile`、Ruff F/E9、CLI与Git差异检查；13份监控报告均PASS且无warning/red flag；逐样本三轨、best SHA和总报告回读一致。 |

## 2026-08-25 系统重构第二阶段文档并补充候选频带表示

| 字段 | 内容 |
|---|---|
| 修改原因 | S2-G4-R1后第二阶段总览未完整反映当前决策，Gate方案以长段落为主，执行结果重复精确实验流水；同时需要说明后续候选频带接口。 |
| 主要修改 | 更新阶段目标、基线、Gate路线、完成条件和停止规则；将S2-G1至S2-G4-R1统一改为“问题—方案卡—执行/判定—结论”结构，只有不少于5个连续步骤时使用箭头；结果改为结论先行和关键证据表，精确路径、资源、哈希及完整指标回归`实验结果记录.md`。 |
| 方法补充 | 新增Soft-19、连续频带边界和Hard-39/79的表示、FFT映射、细DPD影响、CH3/CH4改造范围及判读方式；补充统一物理频带定义和多源频带求并集两个混杂因素。 |
| 证据边界 | 明确现有EE已给出连续真实边界oracle上限；候选方法尚未批准或运行新实验，既有Gate状态、实验ID和科研结论不变。 |
| 算法边界 | 本次只修改文档，未修改代码、数据、模型、权重、配置或实验结论。 |
| 验证 | 对照`实验结果记录.md`核验五个Gate实验ID和关键数字；检查标题唯一性、表格列数、公式/代码围栏、过时状态、重复流水、UTF-8及Git差异。 |

## 2026-08-25 实施 S2-G4-R2 Soft-19公平适配诊断

| 字段 | 内容 |
|---|---|
| 修改原因 | 在统一`BW_actual`定义下公平比较Hard-19与Soft-19，判断硬二值化是否是R1性能缺口的主要来源。 |
| 主要代码 | `dpd_calculator_torch.py`新增默认关闭、与旧`freq_mask`互斥的`freq_weights`接口；新增`s2g4r2_soft19.py`完成Actual覆盖率、Hard/Soft数据构造、权重语义测试、五轨评估和预设判定。 |
| 增量兼容 | 旧位置参数和布尔mask路径保持不变；所有R2功能仅由新入口显式启用。未修改CH3、原数据、D8结构、loss、普通训练默认值或历史输出。 |
| 执行结果 | 资源/身份、权重自测、pilot、完整1024/256双表示数据、两次200 epoch训练和五轨评估完成；工程`PASS`，科学状态`SOFT19_INSUFFICIENT`，精确事实见`SYS-S2G4-R2-20260825`。 |
| 工程事件 | 首次Hard train完整数据在产物和PASS报告落盘后，外层120秒等待超时导致孤立进程停在结果回传；精确终止PID并保留监控失败，随后用全量Hard/Soft逐字段比较回读全部1024条Hard train数据。后续长任务改用覆盖阶段硬时限的外层超时。 |
| 科研修正 | R2显示Hard-19-Actual达到当前oracle候选水平，Soft不优于Hard；R1的主瓣/实际带宽定义差异被确认为重要混杂因素，原“19窗口本身更可能是主要限制”的表述已收紧。 |
| 文档同步 | 更新第二阶段方案与结论、`当前任务.md`、`对话上下文.md`、`研究总结.md`、`实验结果记录.md`和本记录。 |
| 验证 | Python语法与Ruff F/E9、CLI、权重三项语义回归、1280条监督一致性/DPD差异、训练趋势、五轨指标、checkpoint SHA、监控资源、禁止范围及Git差异均复核。 |

## 2026-08-25 回填R2解释并增强通用监控器

| 字段 | 内容 |
|---|---|
| 修改原因 | R2原结论未充分解释Soft/Hard的训练轮次、三源长尾和有效带宽；阶段监控器传入5秒采样时会在启动前报错，完整日志转发也不适合后续长训练。 |
| 文档修改 | 在第二阶段第14.7节补充200 epoch事实、双源/三源分层、有效频率孔径、逐样本差异、bootstrap边界和Soft潜在不利机制；`当前任务.md`转为S2-G4-R3方案待审批。 |
| 代码修改 | `gate3_stage_runner.py`将低于10秒的采样请求自动提升为10秒；硬超时可用0显式禁用；新增默认关闭的`summary`控制台模式，完整子进程输出仍写入`stage.log`。旧调用默认完整日志，正数硬超时语义保持兼容。 |
| 实验边界 | 未启动新训练或评估；R3实验设计和新资源门限仍待用户审批。 |
| 验证 | 对监控器执行Python语法、Ruff F/E9、CLI及5秒自动提升/无硬超时/摘要日志自测；核对第14.7节数值与R2结构化报告。 |

## 2026-08-25 实施 S2-G4-R3 三seed配对确认

| 字段 | 内容 |
|---|---|
| 修改原因 | 验证R2的Hard候选是否依赖seed 42，并区分Soft/Hard表示差异与训练初始化方差。 |
| 主要代码 | `train_yolo.py`新增默认关闭、仅允许`42/1042/2042`的`s2g4r3_scratch`严格入口及初始模型SHA；新增`s2g4r3_multiseed.py`完成数据/历史证据审计、初始化pilot、评估、manifest、配对bootstrap和三seed预设判定。 |
| 监控改进 | `gate3_stage_runner.py`支持独立配置RAM/RSS/GPU/磁盘门限；R3使用10秒采样、摘要控制台和`timeout=0`。`s2g4_resource_preflight.py`新增可配置启动RAM阈值，旧默认14 GiB保持不变。 |
| 增量兼容 | 原S2-G2、S2-G4和R2的seed 42门禁不变；未修改D8结构、数据、DPD表示、loss、CH3、普通训练默认值或历史结果。 |
| 执行结果 | 身份/初始化pilot、seed 1042/2042共六次200 epoch训练、六次独立评估和三seed汇总全部通过；科学状态=`HARD19_STABLE_CANDIDATE`，精确事实见`SYS-S2G4-R3-20260825`。 |
| 科研结论 | Hard在三个seed的RMSE均低于Exact，2/3通过联合门；Soft只在1/3优于Hard，收益不稳定。R2的Soft三源长尾机制不能上升为必然规律。 |
| 文档同步 | 更新第二阶段第15.6节、`当前任务.md`、`对话上下文.md`、`研究总结.md`、`实验结果记录.md`和本记录。 |
| 验证 | Python语法、Ruff F/E9、CLI、1280条监督一致性、初始化SHA、训练趋势、逐seed指标/bootstrap、17份监控报告、资源/禁止范围、Markdown及Git差异均复核。 |

## 2026-08-26 实施 S2-G4-R4-A至R4-D并交付PyCharm训练入口

| 字段 | 内容 |
|---|---|
| 修改原因 | 扩大共享数据并冻结嵌套比较集，以判断R3中的Hard/Soft排序是否受小数据和场景覆盖影响；长训练交由用户在PyCharm点击运行。 |
| MATLAB增量 | `main31_loc_v3.m`新增默认关闭的`s2g4r4` profile，只在显式环境变量下允许并强校验`8192/2048/2048`与seed 20260826；普通smoke/formal仍保持原规模和行为。 |
| DPD增量 | `gen_ch4_loc_data.py`新增默认关闭的`--d8_minimal`，只省略D8不读取的`hyp_mask/gauss_multi`；`s2g4r2_soft19.py`新增显式样本数门禁，旧R2默认1024/256不变。 |
| 清单与审计 | 新增`s2g4r4_scale_study.py`，建立1k⊂4k⊂8k及两份validation清单，并逐分片流式审计三表示监督、索引、有限性和表示差异，避免三套大数据同时载入RAM。 |
| 训练兼容 | `train_yolo.py`新增可选manifest筛选和默认关闭的`s2g4r4_scratch`严格门禁；manifest缺省时原数据加载行为不变。新增`s2g4r4_pycharm_train.py`，当前固定首项Exact/1k/seed42，用户在PyCharm直接点击Run，无需命令行参数。 |
| 执行结果 | R4-A旧场景诊断、8192/2048/2048 IQ、五份manifest及三种8192/2048 DPD均完成并通过审计；精确事实见`SYS-S2G4-R4-D-20260826`。 |
| 工程修复 | 保留短外层超时、错误历史IQ路径和两次过严审计的失败证据；分别改为无墙钟等待、文件系统解析正确路径、流式审计及记录统一权重数学等价样本，均未改变科研语义。 |
| 文档同步 | 更新第二阶段第16章、`当前任务.md`、`对话上下文.md`、`运行基础说明.md`、`实验结果记录.md`和本记录；未重复改写稳定研究总结。 |
| 验证 | MATLAB Code Analyzer仅见既有性能建议；Python `py_compile`、Ruff F/E9、CLI help、1k/val manifest真实加载、六阶段监控、完整DPD审计及Markdown/Git差异检查通过。另以全新目录复跑默认4/2/2 MATLAB smoke和默认完整字段Exact单样本，证明旧入口仍可运行。R4-E正式训练尚未执行。 |

## 2026-08-26 完成 S2-G4-R4-E/R4-F 训练与统一分析

| 字段 | 内容 |
|---|---|
| 修改原因 | 用户改为授权Codex执行R4全部长训练和分析，需要保存原始样本索引、支持冻结清单评估，并建立可重算的配对统计报告。 |
| 代码增量 | `train_yolo.py`保留manifest选中的原样本索引；`s2g4_coarse_d8.py`扩展可选manifest和动态样本数；`s2g4r4_scale_study.py`新增冻结评估、checkpoint哈希复核、配对bootstrap、场景分层、资源汇总和11类谬误扫描。未修改D8、DPD表示、loss、数据划分或训练配置。 |
| 执行结果 | 1k/4k/8k×Exact/Hard/Soft共9次训练和9次冻结评估全部PASS，总报告为`outputs/s2g4r4_scale/20260826_132829/11_analysis/final_report.json`；精确指标见`SYS-S2G4-R4-20260826`。 |
| 工程修复 | 统计脚本首次运行时把checkpoint结构化字段误当路径；已改为解析`checkpoint.path`并复核SHA256。完成审计又发现评估加载顺序与manifest排列不同，初版场景元数据与样本错配；总体指标和配对bootstrap未受影响。无效报告已保留为`final_report_invalid_manifest_order.json`，改为以评估`sample_index`对齐场景后在全新`final_report.json`重算通过。 |
| 文档同步 | 更新第二阶段第16.6节、`实验结果记录.md`、`当前任务.md`、`对话上下文.md`、`研究总结.md`和`运行基础说明.md`，将精确指标仅保留在实验记录。 |
| 验证 | 涉及Python文件已通过`py_compile`和Ruff F/E9；18份监控报告、9份训练summary、9份评估JSON/JSONL、checkpoint SHA256、禁止范围和统计总报告已统一复核。 |

## 2026-08-27 清理 S2-G4 停止载荷与未选 last checkpoint

| 字段 | 内容 |
|---|---|
| 修改原因 | 第一批存储清理仅处理已明确失去复用价值的 S2-G4 停止运行载荷，以及全项目未被选择的 `last` checkpoint；不删代码，不处理收益很小或证据状态不明确的内容。 |
| 删除内容 | 删除 `outputs/s2g4_coarse_d8/20260824_164707` 下的 `pilot_data`、`pilot_train_run`、`coarse_data`、`coarse_data_after_wrapper_fix`；删除全项目共 29 个 `last_*.pth` / `last_*.pt`。 |
| 保留证据 | 保留所有被选择的 `best` / frozen checkpoint、S2-G4 停止报告、频带支持审计、监控 JSON/CSV、日志、manifest 与结构化分析报告；历史 `best` / `last` 比较结论继续作为清理前已回读快照。 |
| 空间结果 | `outputs` 从 37,230,387,472 B 降至 32,880,235,156 B，共删除 4,350,152,316 B，约 4.051 GiB。 |
| 算法边界 | 未修改代码、配置、数据语义、模型选择结果或科研结论。 |
| 验证 | 清理后重新扫描 `last_*.pth` / `last_*.pt` 为 0；四个目标目录均不存在；关键 `best` checkpoint 与 S2-G4 停止证据仍存在，并执行 `git diff --check`。 |

## 2026-08-27 建立S2-G5总体路线并回填阶段文档

| 字段 | 内容 |
|---|---|
| 修改原因 | R4已完成并固定Hard-19-Actual为接口目标，第二阶段文档仍有若干停留在R2/R3时点的“下一步”表述，且尚未给后续统一数据与端到端接口路线编号。 |
| 文档修改 | 在`第二阶段_缩减规模复现与端到端闭环.md`新增S2-G5总体路线，将统一契约、4k CH3训练、四轨隔离、不确定性适配和16k/test划为R1至R5候选子阶段；同步更新顶部状态、Gate总表及R2/R3历史前瞻表述。 |
| 当前任务 | `当前任务.md`转为S2-G5-R1详细方案待审批，明确未审批前不改代码、不生成数据或训练模型。 |
| 科研边界 | 本次仅更新路线与带宽定义判断；未修改算法、标签、数据、模型或结果，未运行实验。`BW_actual`暂解释为生成器设计/裁剪带宽，是否作为最终论文带宽口径由R1审计后冻结。 |
| 验证 | 复读MATLAB波形生成、RRC与Hard-19实现；检查Markdown标题、表格、代码围栏、过时状态、UTF-8和Git差异。 |

## 2026-08-27 实施S2-G5-R1统一数据与接口契约

| 字段 | 内容 |
|---|---|
| 修改原因 | CH3需要在与CH4一致的物理分布上学习Hard-19-Actual，同时必须明确K=0/1/2/3的下游分流。 |
| 代码修改 | `main31_loc_v3.m`增加显式`s2g5r1` profile和版本化契约字段；新增`build_hard19_actual_labels.m`及`s2g5_r1_contract.py`。默认入口和`s2g4r4`受控分支保持原语义。 |
| 接口边界 | K=0直接结束；K=1只保留未实现接口；K=2/3沿用G4-R4条件分布并进入Hard-D8。未改波形、粗/细DPD算法、模型、loss或训练配置。 |
| 文档修改 | 第二阶段第17章补充R1方案和结论；当前任务转入R2方案待制定；同步更新实验事实、上下文和稳定研究认识。 |
| 验证 | MATLAB Code Analyzer新增函数0项告警；Python compile/Ruff通过；三split标签独立复算、CH3 forward/loss及默认smoke回归均PASS。精确事实见`SYS-S2G5-R1-20260827`。 |

## 2026-08-27 实施S2-G5-R2嵌套规模训练并重构G5章节

| 字段 | 内容 |
|---|---|
| 修改原因 | 需要在统一Actual标签上判断CH3是否可学习，并依据1k→4k的配对结果决定是否值得扩展8k。 |
| 代码修改 | `main31_loc_v3.m`增加显式`s2g5r2` profile和ASCII错误标识；`s2g5_r1_contract.py`支持指定profile；新增`s2g5_r2_materialize_subset.m`和`s2g5_r2_ch3.py`，负责嵌套划分、审计、初始化身份、配对分析和总门禁。 |
| 原版兼容 | 所有新行为只由显式R2入口启用；未修改师弟默认数据规模、默认标签、CH3模型、loss或原训练入口默认行为。 |
| 文档修改 | `第二阶段_缩减规模复现与端到端闭环.md`将G5-R1和G5-R2提升为与S2-G4各R同级的独立章节，并同步方案、结论和`ENTER_8K`边界；其他长期文档按主责去重更新。 |
| 自动修复 | 拒错pilot改用ASCII错误标识规避控制台乱码；子集审计改为核对manifest非连续索引；独立重载命令由误写的`evaluate`改为已有`eval`。均未改变科研配置，失败证据保留。 |
| 验证 | Python compile/Ruff、MATLAB Code Analyzer、数据/标签/manifest门禁、batch 64容量、1k/4k训练、独立重载、2000次配对bootstrap和最终证据审计均通过。精确结果见`CH3-S2G5-R2-20260827`。 |

## 2026-08-27 完成S2-G5-R2的8k条件扩展

| 字段 | 内容 |
|---|---|
| 修改原因 | 1k→4k预注册判定为`ENTER_8K`，需要在不破坏旧4k和固定validation的前提下确认规模收益是否继续。 |
| 代码修改 | `main31_loc_v3.m`新增显式`s2g5r2_8k_add`增量profile；`s2g5_r2_ch3.py`增加粗DPD分块合并、严格前缀审计、4k/8k配对bootstrap、预注册决策和最终门禁。所有行为仅由8k专用子命令启用。 |
| 数据与输出 | 在原R2根目录`outputs/s2g5r2_ch3/20260827_191207`内新增4k IQ/粗DPD、严格嵌套8k、`train_8k`、`analysis/8k`和后续监控目录；未覆盖旧4k、validation、checkpoint或原分析报告。 |
| 自动修复 | profile拒错pilot补充ASCII identifier输出；最终审计将浮点逐位相等改为绝对容差`1e-12`，对应的实际差异仅`2.22e-16`。失败目录均保留，未改变数据、模型、训练或指标。 |
| 文档修改 | 第二阶段第19.6节记录8k方案，第19.7节统一汇总1k/4k/8k结论；实验结果、当前任务、上下文和研究总结同步转入R3待审批状态。 |
| 验证 | 新增4k IQ/标签/粗DPD、旧4k严格前缀、batch 64、8k训练、独立重载、2000次配对bootstrap和最终SHA门禁均通过；默认smoke仍为`[4,2,2]`、seed 20260821、`mainlobe_t030`和阈值0.3。判定`FREEZE_8K_FOR_R3`，精确结果见`CH3-S2G5-R2-20260827`。 |

## 2026-08-28 实施S2-G5-R3四轨级联误差隔离

| 字段 | 内容 |
|---|---|
| 修改原因 | 需要在统一共享IQ上分别隔离频带预测和源数预测对定位的影响，判断现有CH3与Hard-D8能否直接级联，以及后续应优先改进哪一条接口。 |
| 代码修改 | 新增`第四章代码/s2g5r3_cascade.py`，提供输入冻结、pilot、CH3推理、oracle/predicted细DPD构建、四轨评估、配对bootstrap和最终证据门禁。复用既有CH3、细DPD、D8、解码和GOSPA实现；未修改师弟原入口或算法。 |
| 执行与证据 | 在`outputs/s2g5r3_cascade/20260828_131043`完成512条K=2/3定位样本的两套细DPD与四轨评估，并同时审计1024条K=0/1/2/3分流。工程闭环通过，但直接级联尚不满足预设等价标准；误差隔离判定为`COUNT_PATH_PRIORITY`。精确指标见`SYS-S2G5-R3-20260828`。 |
| 自动修复 | 预检将float32阈值的逐位比较改为`np.isclose`；评估各轨后显式释放模型和CUDA缓存，并在全新目录生成正式结果；最终汇总器只接纳预注册的权威PASS阶段，同时登记两次已保留的失败尝试。外层短等待结束时，确认子进程仍连续运行后继续观察，未重启计算。上述修复均未改变数据、模型、阈值或指标。 |
| 科研边界 | 未训练或微调模型，未使用test，未改变频带阈值、源数判决、K=1占位接口或R4/16k数据。匹配样本RMSE下降被明确识别为漏检困难源后的选择效应，不作为性能改善证据。 |
| 文档修改 | 第二阶段第20章补充方案与执行结论；同步更新`实验结果记录.md`、`当前任务.md`、`对话上下文.md`、`研究总结.md`和本记录。 |
| 验证 | Python `py_compile`、Ruff F/E9和CLI help通过；冻结输入哈希、CH3逐样本复算、两套DPD索引、四轨独立重载、2000次分层配对bootstrap、资源报告、输出哈希和最终总门禁均通过。 |

## 2026-08-28 实施S2-G5-R4长训练前数据与容量门

| 字段 | 内容 |
|---|---|
| 修改原因 | R3的K瓶颈可能随训练规模自然缩小，需要先建立16k原CH3基线，避免提前修改K算法。 |
| 代码修改 | `main31_loc_v3.m`新增默认关闭的`s2g5r4_16k_add` profile；新增`s2g5_r4_ch3_scale.py`和固定参数的`s2g5r4_pycharm_train.py`。未修改CH3/D8模型、loss、阈值或旧R2/R3入口。 |
| 数据结果 | 新增8k统一IQ、Hard-19-Actual标签和粗DPD通过；与冻结旧8k合并为严格嵌套16k，每个K各4096条，旧8k的9个科研字段逐块一致。精确事实见`CH3-S2G5-R4-PRETRAIN-20260828`。 |
| 容量结果 | batch 64的forward/loss/backward和validation可完成，但eager Dataset使系统可用RAM跌破6 GiB红线；训练入口会据此拒绝启动，正式训练未执行。 |
| 工程事件 | IQ启动的外层等待误设过短，但监控器和MATLAB保持连续运行并最终PASS，未重启；控制台乱码未用于源码或数据判断。 |
| 文档同步 | 第二阶段新增第21章及21.7进度；同步更新实验结果、当前任务、对话上下文和本记录。稳定研究总结未因单纯资源结论修改。 |
| 验证 | Python compile/Ruff、MATLAB Code Analyzer、R4错误规模拒绝、默认smoke、冻结SHA、IQ/标签/距离、粗DPD有限性、16k严格前缀、硬链接身份和容量计算均完成；test和长训练未运行。 |

## 2026-08-28 实施S2-G5-R4专用懒加载与等价门

| 字段 | 内容 |
|---|---|
| 修改原因 | 原eager Dataset使16k频谱全部常驻RAM并触发资源红线，需要只改变存储方式、保持训练语义不变。 |
| 代码修改 | 新增`s2g5_r4_lazy_dataset.py`和`s2g5_r4_train_ch3.py`，以预标准化只读NumPy memmap提供R4专用Dataset；`s2g1_train_ch3.py`只增加默认关闭的Dataset工厂注入；固定PyCharm入口改为读取lazy总门禁。 |
| 原版兼容 | 未修改`train_v26.SourceDetectionDataset`、模型、loss、增强、seed或原CLI默认行为；旧1k eager容量回归通过，lazy门禁形成前PyCharm入口会拒绝启动。 |
| 自动修复 | 删除3个静态检查发现的未使用导入；最终资源汇总同时采用容量与完整epoch监控的保守极值，避免4秒容量阶段短于10秒采样周期而漏记峰值。均未改变科研配置。 |
| 文档同步 | 批准方案和简明结论归入第二阶段第21.8–21.9节；实验结果、当前任务、上下文和本记录同步更新。 |
| 验证 | Python compile、Ruff F/E9、CLI帮助、非ASCII输出、原入口回归、512条频谱、Dataset/增强、1k三轮训练、16k容量与单epoch吞吐全部通过；最终状态`LAZY_READY_FOR_TRAINING`，正式训练与test未运行。精确事实见`CH3-S2G5-R4-LAZY-20260828`。 |

## 近期重要修改索引

| 日期 | 事件 | 结果 |
|---|---|---|
| 2026-08-20 | 长期文档体系与静态接管审查 | 文档体系和第一阶段详细审查建立 |
| 2026-08-20 | Gate 0 | 运行基础改造通过 |
| 2026-08-21 | Gate 1 | 第三章最小工程闭环通过 |
| 2026-08-21 | Gate 2 | 第四章最小工程闭环通过 |
| 2026-08-21 | Gate 3A | 中等规模数据与短训练可重复性通过，见 `CH4-G3A-20260821` |
| 2026-08-21 | Gate 3B | 缩减数据完整训练通过并建议延长，见 `CH4-G3B-20260821` |
| 2026-08-21 | 文档治理与阶段切换 | 第一阶段冻结，第二阶段S2-G0方案待审批 |
| 2026-08-21 | S2-G0最小逻辑封存 | 复用现有代码提交和Gate 3A/3B实验ID，第二阶段转入S2-G1方案待审批 |
| 2026-08-21 | S2-G1增量入口与首次数据审计 | 原入口兼容；首次数据因1条源间距违规在训练前停止，见 `CH3-S2G1-20260821-A` |
| 2026-08-22 | S2-G1源间距后验门禁与完整重跑 | 受控路径最小修复完成，第三章缩减数据收敛和严格重复通过，见 `CH3-S2G1-20260822` |
| 2026-08-22 | S2-G2充分训练诊断与工程防错规则 | 200 epoch趋势关闭，新接口checkpoint冻结，总门禁通过，见`CH4-S2G2-20260822` |
| 2026-08-24 | 运行证据瘦身 | 删除无效MAT、逐字节重复载荷和工程缓存，保留报告、日志、唯一有效副本与冻结best权重 |
| 2026-08-24 | S2-G3跨章可组合性审查 | 工程链通过；第三章模型门通过、19子带二值表示门失败，见`SYS-S2G3-20260824` |
| 2026-08-24 | S2-G4 Coarse-D8适配数据门 | 预检/pilot通过；19子带正标签不支持全部窄带源，按计划停止，见`SYS-S2G4-20260824` |
| 2026-08-24 | S2-G4-R1 0.2阈值快速适配 | 逐源支持闭合但D8重训仅恢复15.86% RMSE缺口，见`SYS-S2G4-R1-20260824` |
| 2026-08-25 | 第二阶段文档系统重构 | 同步当前路线，统一Gate方案/结论写法，并明确Soft-19、连续边界、Hard-39/79候选接口 |
| 2026-08-25 | S2-G4-R2 Soft-19公平诊断 | Hard-19-Actual达到候选水平，Soft-19无额外收益；R1物理频带定义混杂得到纠正 |
| 2026-08-25 | R2解释与监控器增强 | 补充Soft/Hard机制边界；5秒采样自动提升为10秒，并支持无硬超时和摘要控制台模式 |
| 2026-08-25 | S2-G4-R3三seed配对确认 | Hard成为稳定oracle候选；Soft收益不稳定，不再优先 |
| 2026-08-26 | S2-G4-R4-A至R4-D | 新共享IQ、嵌套manifest和三表示DPD完成；PyCharm首项训练入口待用户点击 |
| 2026-08-26 | S2-G4-R4-E至R4-F | 九组学习曲线与统一分析完成；Hard-19接口充分性得到支持，Soft路线停止 |
| 2026-08-27 | S2-G4停止载荷与last checkpoint清理 | 删除四个无效或一次性目录及29个未选`last` checkpoint，保留`best`与结构化证据 |
| 2026-08-27 | S2-G5-R1统一数据与接口契约 | K=0/1/2/3统一IQ、Hard-19-Actual标签、分流和CH3消费检查通过 |
| 2026-08-27 | S2-G5-R2嵌套规模诊断 | 1k/4k/8k训练与配对分析通过，8k仍显著受益，判定`FREEZE_8K_FOR_R3` |
| 2026-08-28 | S2-G5-R3四轨级联误差隔离 | 工程闭环通过；直接级联尚不满足等价标准，后续判定`COUNT_PATH_PRIORITY` |
| 2026-08-28 | S2-G5-R4长训练前数据与容量门 | 新增8k和严格嵌套16k通过；eager Dataset触发RAM红线，长训练未启动 |
| 2026-08-28 | S2-G5-R4懒加载等价与资源门 | 抽样、增强和1k短训严格等价；16k容量/吞吐通过，正式训练入口已解锁 |
