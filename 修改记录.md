# 修改记录

## 2026-09-22 G5-R5实现、执行与结论回填

- 新增`统一模型代码/gates/g5/r5/`内`__init__.py`、`g5_r5.py`、`test_g5_r5.py`，实现固定位置排列、粗DPD局部采样、共享阈值校准与逐样本比较；复用已登记缓存和同字节校验加载，不修改历史源码或结果。
- 项目解释器执行`python -m py_compile`检查两份Python文件、`python -m ruff check 统一模型代码/gates/g5/r5 --select F,E9`及`python -m unittest 统一模型代码.gates.g5.r5.test_g5_r5 -v`全部通过。7项测试覆盖预处理、坐标采样、中心化配对、退化回退、固定划分、阈值及三源排列。
- 正式运行与独立回读审计完成，事实见实验结果记录`E2E-G5-R5-20260922`；同步实现文档17.16.7、当前任务、上下文、研究总结及脚本索引。无后续追加实验。

## 2026-09-22 G5-R5保守身份纠错方案同步

- 按用户要求新增实现文档17.16，登记原绑定、R4显式匹配器和粗DPD直接证据三组对照，以及退化向量回退、共享阈值校准、固定检查部分和结果分流；同步文档首尾入口及当前任务。
- 按researchwrite局部修订流程保留历史结论，不扩展模型或实验范围。完成文档差异、章节与链接检查；仅修改三份文档，未编写脚本或运行实验，实施与运行待审批。

## 2026-09-22 G5-R4固定位置身份配对实现与完成

- 新增`统一模型代码/gates/g5/r4/g5_r4.py`和`test_g5_r4.py`；复用R3投影前特征与R1固定位置，构建全交叉配对及最多6种排列，提供同任务拼接MLP/64维归一化显式匹配对照；train/val同步评价、原排列优先、每2轮保存checkpoint，运行日志仅阶段汇报。
- 补齐R3现存epoch0/20训练排名回读，未重训历史实验。4项合成测试、编译、Ruff与diff检查通过；正式四轨和输入/输出审计完成，事实见实验结果记录`E2E-G5-R4-20260922`。同步实现文档17.15、任务、上下文、研究总结及脚本索引。

## 2026-09-22 G5-R3组合监督与冻结评分器实验实现

- 新增`统一模型代码/gates/g5/r3/g5_r3.py`及`test_g5_r3.py`：固定基础模型、按样本字节范围校验缓存投影前特征、旧监督/组合监督同初始化同顺序对照、20轮选模和输入阶段后审计。复用原评分器、候选、解码和指标；未修改R2合同源码与历史产物。
- 三项合成测试通过（组合标签/真源置换、梯度方向、零初始化/可训练投影、无源处理），语法编译、Ruff F/E9与diff检查通过。方案及结论写入实现文档17.14，同步任务、上下文、研究总结及索引；正式实验事实见`E2E-G5-R3-20260922`。


## 2026-09-22 G5-R2固定checkpoint评分开关诊断

- 新增`统一模型代码/gates/g5/r2/g5_r2_diagnose.py`，复用冻结合同、按样本校验加载和既有模型/解码/指标；单次前向分别评分开/关，保存候选两类分数与逐样本证据，不修改训练代码或历史结果。更新脚本索引及研究状态文档。
- `py_compile`、Ruff F/E9通过；合成测试覆盖候选合格标注、分数翻转、全ignore频带和K1开关不变。首个尝试因候选标注的空频带边界需对齐而主动中止，保留目录与说明；修正后从新目录完整执行。新旧证据及正式诊断事实见实验结果记录`E2E-G5-R2-SCORE-TOGGLE-20260922`。

## 2026-09-21 G5-R2增加基础轮数并批准12小时预算

- 按用户要求将基础训练由12轮提高到20轮，保留条件24轮上限，总预算由8小时调整为12小时。延长判据同步移至best在18/20轮、16/18/20轮联合Recall净改善；同seed三组一起延长。模型、数据、loss、学习率、batch、精度与seed不变。
- 更新runtime配置、训练调度、轮数相关报告名、吞吐外推和单元测试，登记新合同并保留旧合同；同步实现文档17.13、当前任务、入口说明和脚本索引。按研究计划修订技能限定为本次轮数/预算调整。
- 编译、Ruff F/E9与13项单元测试通过；重新完成三组短时CUDA测试，初始预测、梯度与恢复等价及504个消费范围阶段后校验通过。有效准备目录为`outputs_e2e/unified/e2e_g5_r2/20260921_approved/engineering/20260921_214259_1789998179598759500/`，约34.83秒；根目录preparation_report.json指向本次结果。
- 最新保守外推：20轮约8.74小时，全24轮约10.40小时，包含25%吞吐余量及20分钟收尾预留；预算检查通过。实际一键入口`--check-only`通过，当前为READY_FOR_USER_RUN；未启动正式训练、完整比较或test，运行入口和结果回读文件不变。

## 2026-09-21 G5-R2实现与手动运行交付

- 按批准方案新增`统一模型代码/gates/g5/r2/`专用实现及`tests/`，入口和说明集中于`运行入口/E2E/G5_R2/`；不改旧G5/R1源码和结果。实现中心采样、最近真实源归属的多正例关联loss、三组同SG起点、两seed、12/条件24轮、相同解码与开发集配对比较；保持A2、batch4及FP32。
- 沿用同字节校验的有界缓存与单batch预取。训练去除不参与loss的联合枚举，候选与R1逐项一致；每20秒最多更新一次控制台进度，磁盘日志仅保留阶段/epoch摘要。模型、优化器、随机数与排列生成器按完整epoch保存，提交标记在回读校验后发布；失败保存不推进恢复点，重启不重置运行预算。
- 验证命令：项目Python执行`-m compileall -q 统一模型代码/gates/g5/r2 运行入口/E2E/G5_R2`、`-m ruff check 统一模型代码/gates/g5/r2 运行入口/E2E/G5_R2 --select F,E9`及`-m unittest discover -s 统一模型代码/gates/g5/r2/tests -v`。13项单元测试通过，覆盖坐标、标签冲突、解码等价、梯度、低频中文日志、互斥锁、保存故障、配对延长和合成报告。
- 有效工程证据：`outputs_e2e/unified/e2e_g5_r2/20260921_approved/engineering/20260921_213251_1789997571300550000/preparation_report.json`，当前准备报告另存根目录同名文件。三组各32 batch测速；初始预测一致，SG定位/关联反馈为零，E2E为有限非零；三组保存后重放的loss、模型和优化器精确一致，消费的504个样本范围阶段后校验通过。测试约34.95秒，采样RAM峰值61.5%。这些均为工程证据，不是科研性能结论。
- 当次短测外推基础12轮约5.44小时、全24轮约10.46小时，含25%吞吐余量及20分钟收尾预留。超过原8小时预算，当时入口保持预算阻止并申请12小时；后续轮数和预算修订见本文件上方记录。未启动六轨正式训练、val_compare比较或test，审批和入口以当前任务为准。
- 准备期间最小修复：Windows互斥锁二次打开时避免读取已锁字节；源码登记排除第三方虚拟相对路径；train与validation的raw_index按不同母文件解释，互斥检查针对同母文件的val_select/val_compare。原G5已有相同身份约定，未修改数据或划分。旧准备目录及被替代合同保留，后续验证均使用新目录。
- 实现文档17.13、当前任务、脚本分类索引与入口说明已同步；按matplotlib技能加入可追溯案例图导出，正式图片在用户运行比较阶段后生成。结果回读为运行目录的训练报告，以及evaluation下的摘要、比较报告和最终审计。

## 2026-09-21 G5-R2方案局部完善

- 按用户批准的审查意见修订实现文档17.13：将3×3平均改为候选中心双线性采样，正例增加最近真实源归属，并明确并列候选处理及训练/评价定义的区别；必要检查并入已有准备阶段，不新增P0。
- 明确30 m为继承的去重参数，100 m为关联正例范围；频带和计数下降1 pp仅作监控提示。三组对照、两seed、训练预算和主要判定保持不变，同步当前任务的修订状态。
- 按研究计划修订技能限定修改范围；文档差异及空白检查通过。仅修改三份文档，未修改代码、运行实验或复核历史产物；实施仍待审批。

## 2026-09-21 G5-R2原因分析与待审批方案

- 将上一轮固定位置/频带的身份排列分析登记到实验结果记录；实现文档新增17.13，明确关联评分器、正负候选监督、共同SG checkpoint起点的三组对照、两seed与预算/延长规则，当前入口由曾建议的G6改为用户指定G5-R2。
- 同步当前任务为方案待审批。使用研究计划写作技能将范围限定为一个关联模块，区分模块收益、继续训练收益和反馈收益；精确诊断事实只在实验结果记录集中登记。
- 仅修改文档，不新增代码、运行数据或训练，不因目录整理重验历史文件；后续实现遵循`统一模型代码/gates/g5/r2/`、`运行入口/E2E/`与独立`outputs_e2e`结果目录。文档差异/标题检查通过，正式验证未执行。

## 2026-09-21 按Gate整理源码与运行入口

- 按用户批准的结构迁移67个Python脚本及1份运行说明：公共实现归入`统一模型代码/common`，Gate实现归入`统一模型代码/gates/g0`至`g5`，一键入口归入`运行入口/E2E`；诊断、恢复、测试及G5-R1分别归类。完整入口、职责和历史执行边界见[脚本分类索引](脚本分类索引.md)。第三章、第四章及其MATLAB重复实现保持原位。
- 修改限于导入、项目根定位、脚本路径和对应说明；保留文件名形式的重复进程检测。按授权刷新81份JSON中的源码路径、SHA及关联元数据身份，不改变数据、权重、实验配置、指标或研究结论。历史源码快照保持原样；迁移前原文件备份、旧新映射及检查报告见`outputs_e2e/maintenance/code_layout_20260921/`，备份本身不代表独立运行环境。
- 验证：67个迁移脚本编译及受控导入通过，Ruff F/E9通过；G5-R1的5项单元测试、训练合同和IO冒烟通过；相同合成输入下G5两种梯度模式的5项前向输出逐位一致。G5与G5-R1一键入口`--check-only`通过。迁移前后计算AST及JSON字段差异检查通过，现行合同身份匹配；未执行正式训练、完整评价或test。

## 2026-09-21 获批缓存清理（部分完成）

- 用户批准删除G4原始`feature_cache`的9个NPY、初版G5槽位诊断4个重复NPZ、加载器优化`shards4`及源码区Python/Ruff缓存。本轮仅9个G4 NPY删除成功，共16,994,927,744 bytes（约15.83 GiB）；其余删除命令在进程创建前被工具策略拒绝，尚未删除，不记为完成。
- 删除前确认全部目标在当前工作树内且无重解析点；96个保留的G4源分片匹配登记SHA，4对诊断NPZ逐字节哈希一致。删除后140个受保护文件SHA全部不变，包括源分片、最终版NPZ、3个`targets.pt`及相关报告/manifest。
- G4原始`feature_cache`现保留各split的`targets.pt`，原9个NPY路径已不存在；旧整块特征读取和现场复查不再可用。RAM恢复所需目标文件、`feature_cache_v2`、固定DPD、正式权重及G5输入保持原位。历史manifest不改写，科研结论不变。
- 清单、保留身份及部分完成报告见`outputs_e2e/maintenance/cleanup_20260921_approved/precheck.json`和`final_report.json`。未移动源码或产物，未改算法及配置，未训练或评价；无需编译或正式实验。
- 同日继续：用户再次批准后，4个初版诊断NPZ删除成功（51,668,602 bytes，约49.28 MiB）；累计删除13个文件、17,046,596,346 bytes。`shards4`与8处源码缓存的独立目录删除命令仍被策略拒绝，继续保留。140个受保护文件SHA再次全部一致；最新状态见同目录`followup_report.json`，原部分完成报告保留。初版NPZ旧路径不再可读，对应最终版文件保持在`20260920_slot_analysis_v2`，历史manifest不改写。
- 最新状态：用户反馈已手动删除`outputs_e2e/unified/loader_optimization/20260919_01/shards4`，本轮核对该路径不存在；源码区缓存按用户要求保留，不再清理。

## 2026-09-20 G5-R1一键入口与批准方案登记

- 用户完整运行后，回读六轨产物并将结论写入实验结果记录及实现文档17.12.7，同步任务、上下文和研究总结。修正“反馈普遍有效”的当前解释为“原独立解码下有效，同用联合解码后额外收益未确立”；冻结运行报告保持原样。验证25个产物SHA、现行61项及历史111项身份、逐样本候选选择/联合匹配及四组配对区间通过；未再运行网络或训练。
- 新增`运行入口/E2E/G5_R1/G5_R1一键运行.py`、`统一模型代码/gates/g5/r1/e2e_g5_r1.py`、`g5_r1_decode.py`、`g5_r1_report.py`及`audits/test_g5_r1.py`。固定模型进行原/联合解码配对，支持实时UTF-8日志、预算和RAM停止、同字节校验读取、逐样本候选、联合关联指标、跨seed场景配对bootstrap及案例图；不修改原G5模型、加载器或历史证据。
- 批准方案同步到实现文档17.12，当前任务设为待用户一键运行。独立合同及源码快照登记于`outputs_e2e/unified/e2e_g5_r1/20260920_approved/`；已有运行不自动覆盖或重新计时。
- 验证：项目解释器py_compile、Ruff F/E9及5组单元测试通过；中文子进程输出捕获和一键入口`--check-only`通过。旧G5 v4登记身份核验通过；四个既有典型样本的E2E轻量前向回放、logits精确复现、位置/GOSPA核对及实际消费范围阶段后校验通过，见该目录`smoke_20260920_225643/smoke_report.json`；从回放结果生成案例图并检查显示。确认正式evaluation目录及launch_receipt尚不存在。这仅是工程验证，未运行32条正式pilot、六轨全开发评价或test。

## 2026-09-20 G5执行结论回填与重复定位可视化诊断

- 新增独立诊断脚本`统一模型代码/gates/g5/diagnostics/g5_slot_diagnosis.py`、`g5_slot_probes.py`：回读已有开发预测，按登记身份回放四个典型样本，保存中间张量；另做同全频原D8及候选联合解码探针，生成12组PNG/SVG。原训练、评价、模型代码和G5证据均未修改。
- 将综合结论、典型图解、按证据排序的原因及最小解决建议写入实现文档17.10—17.12；修正本章、首页及执行入口仍称“首个SG暂停/待分析”的过时状态。精确事实登记`实验结果记录.md`的`E2E-G5-ANALYSIS-20260920`，其他状态文档仅保留职责内摘要；未修改AGENTS.md。
- 按用户反馈重写17.11节：采用“现象—样本对照—诊断—改进”的连贯叙述，将重复限制说明集中到节末，简化原因表和图注；保留原数据、图片链接及结论，未新增实验。
- 验证：项目解释器py_compile、Ruff F/E9通过；正式v4合同111项身份、实际读取输入前后复核通过；四例两轨logits一致、位置仅舍入差异。最终目录为`outputs_e2e/unified/e2e_g5_diagnosis/20260920_slot_analysis_v2/`；初版诊断目录保留。初版脚本的闭包缓存删除触发Ruff静态告警，最小改为clear后复检并在新目录重新生成最终证据，未改科研语义。
- 本轮未重训、未重新选择checkpoint、未运行全开发集新解码评价或test；四例候选解码不作为正式修复，后续实施须审批。

## 2026-09-20 G5历史参考坐标精度核对与只收尾恢复

- 修复`e2e_g5_evaluate.py`：分别以历史真值复现历史指标、以G5米制真值统一参考指标；增加已登记历史真值读取、FP32舍入边界检查和差异记录，不简单放宽GOSPA阈值，不改变模型预测。
- 修改`运行入口/E2E/G5/G5一键继续.py`及合同选择器：登记独立v4合同、冻结已有训练与比较证据，只运行失败的Hard参考和最终审计，完整PASS后拒绝重复工作；旧失败目录保留，新目录保存修复结果。
- 编译/Ruff通过；1024条历史GOSPA精确复现、1024条修复参考逐条独立重算一致、真实收尾及最终审计PASS、完成态预检通过。长期文档同步为G5完整结束待分析，未重训、未重跑六轨比较；精确事实见实验结果记录E2E-G5-HARD-RECOVERY-20260920。

## 2026-09-19 PyCharm实时日志与启动耗时估算

- 新增`统一模型代码/common/g5_live_log.py`，修改`运行入口/E2E/G5/G5一键继续.py`和G5合同选择器：子进程仍直接写文件，入口增量读取并实时转发中文输出、显示批次/阶段进度；显式处理UTF-8边界与控制台编码，异常上报并停止子进程，无新增日志线程或stdout管道。
- 耗时外推只作启动提示，不改变原保守预算门禁；工程v3合同独立登记并保留v2，数值训练路径不变。静态检查、真实短子进程流式输出/文件一致性/失败退出、模拟资源停止和进度计算、真实入口预检通过。精确估算及验证边界见实验结果记录E2E-G5-LIVELOG-20260919；正式训练未运行。

## 2026-09-19 G5推荐加载配置正式接入与一键待运行交付

- 新增`统一模型代码/common/g5_runtime_v2.py`及一次性`统一模型代码/gates/g5/diagnostics/G5工程接入验证.py`；修改G5 model/train/contract模块，将训练接入逐样本读取与单batch预取，评价使用逐样本读取，保持科学配置和epoch恢复语义。后台线程独占LRU，结束/异常时清理队列且不吞掉待处理异常；RAM85%边界仅应用新工程运行。
- 更新`运行入口/E2E/G5/G5一键继续.py`：要求新工程合同与恢复PASS证据，验证旧SG checkpoint身份，加入Windows系统锁，保留原预算和已有评价拒绝规则。新训练报告登记所用工程合同。原合同和源码快照不覆盖，新合同另存。
- 更新手动运行说明、当前任务与实现文档；真实短窗口恢复、抽样特征一致性、静态检查及模拟恢复/锁/失败分流通过，真实`--check-only`通过。交付检查曾把历史空目录当成新训练，已改查实际产物并保留失败检查记录。精确事实见实验结果记录`E2E-G5-INTEGRATION-20260919`；完整训练/评价未执行，AGENTS与Git历史未修改。

## 2026-09-19 G5有界预取与batch吞吐筛选

- 新增根目录`统一模型代码/gates/g5/diagnostics/g5_prefetch_probe.py`，复用逐样本SHA加载器；单生产者只提前准备一个batch，工作线程独占LRU，后台异常传回主线程，不改冻结训练源码。按相同样本数比较SG/E2E、预取开关及不同batch，反转顺序重复；学习率、FP32和模型保持不变。
- 用户批准整机RAM占用85%预警，本次探针达到预警即停止继续分配；正式历史合同不追改。允许后续因新配置更优而重跑SG，不能因此将不同batch旧新结果混作严格配对。
- 首次尝试因batch=16触及显存保留上限停止；修复筛选流程，将单候选资源超限记录为淘汰而非终止整个矩阵，并跳过同轨更大候选。失败目录保留，新目录重新执行。语法编译、Ruff F/E9、预取顺序及后台异常传播检查通过；精确证据与最终状态见实验结果记录。

> 本文件只记录具有项目意义的修改事件：为什么修改、改了哪些文件、是否改变算法以及如何验证。精确实验配置、指标、资源、哈希和运行目录见 `实验结果记录.md` 或对应阶段文档。

## 2026-09-19 G5按样本范围读取候选及等价验证

- 新增根目录`统一模型代码/common/g5_sample_range.py`、`统一模型代码/gates/g5/diagnostics/G5样本读取验证.py`，从SHA匹配的C序NPY分片登记样本范围，使用相同已校验字节构造不可写数组和共享有界LRU。拒绝Fortran/对象/结构化dtype，不复制全量特征、不加预取；原冻结源码不变。
- 修复验证入口psutil路径兼容小错（Path转str），旧失败目录`20260919_223408`保留，新目录`20260919_223427`。py_compile、Ruff F/E9及合成读取/重试测试通过；SG/E2E各256步等价和速度验证完成，事实见E2E-G5-RANGE-20260919。
- 更新实现文档第17.9节、任务及研究状态。未修改正式G5恢复入口，未启动剩余轨道，接入需新工程合同。

## 2026-09-19 G5首个SG暂停与手动执行交付

- 按用户最新范围，在首个SG48完整报告落盘后停止原进程，保留监督程序主动停止日志；没有修改冻结训练代码或启动其他轨道。
- 新增根目录`运行入口/E2E/G5/G5一键继续.py`、`统一模型代码/gates/g5/recovery/G5停止回读.py`、`运行入口/E2E/G5/G5资源采样.py`及`运行入口/E2E/G5/G5手动运行说明.md`；同步当前任务、实现文档第17章与实验事实。入口复用既有数据和checkpoint，拒绝重复进程、预算不足及覆盖评价，不重置原预算。
- 项目解释器py_compile与Ruff F/E9通过；真实重复进程拒绝、模拟预算/覆盖拒绝和停止后真实只检查预检通过。停止回读审计与输入身份复核通过。完整恢复流水线、候选加载加速和剩余正式轨道未执行；研究配置不变。

## 2026-09-19 G5批准后的执行入口与工程合同检查（已完成准备与首个SG，后续暂停）

- 48小时恢复后的首次训练入口在参数更新前因CUDA未初始化即重置显存统计而失败。独立进程复现原错误，显式`torch.cuda.init()`后统计重置通过；训练代码仅增加此一行，恢复入口逐字比对其余旧源码合同不变，原失败日志及空训练目录保留于`execution_logs/training.*`、`cuda_init_failure_evidence`，新冻结目录`training_source_snapshot_cuda_init`，新训练日志`training_cuda_init.log`。语法编译、Ruff F/E9通过，恢复前再执行全输入复核，不改变随机种子、算法或训练配置。
- 用户批准48小时预算后新增`e2e_g5_resume_budget.py`：仅修订运行manifest中的墙钟限制，断言其余字段及原始开始时间完全不变；归档原manifest、pilot、冻结合同和停止状态至`budget36_evidence`，复用实测量重判预算并重新冻结、复核输入后串行接续。原配置模板仍保留36小时，当前运行修订由`budget_amendment.json`明确登记。语法编译、Ruff F/E9通过；无模型/数据/loss/seed修改。
- 准备完成后的归档工程修复：PyTorch动态模块`torch.ops/classes`的虚拟相对`__file__`被误识别为仓库源码，首次freeze失败，未进入训练。排除无来源的动态相对文件名，真实源码仍逐文件登记；新增一次性`e2e_g5_recover_freeze.py`接续入口，保留原`execution_logs/freeze.*`及未完成归档，新证据使用`freeze_recovery.*`和`training_source_snapshot_recovery`。语法编译、Ruff F/E9及70个源码路径存在性检查通过；不改变科研配置，不重做已完成特征。
- 按实现文档第17章实施G5数据准备、A2配对训练、一次开发集评价入口；新增`e2e_g5_*.py`、配置及对应`audits/test_g5_*.py`，不修改历史Gate、模型方案或test边界。
- `g5_verified_io.py`执行首次异常留证、最多重读一次的授权规则；正式训练代码身份登记与阶段输入复核由`e2e_g5_contract.py`承担，新增磁盘检查每分钟执行，避免每batch遍历输出目录。冻结合同只约束冻结后的训练，不追认生成特征启动时所有依赖已冻结。
- 评价边界修正：无有效匹配时RMSE未定义，不能记为零；缺失有效RMSE比较时不据此宣布反馈全面通过。移除未使用导入，不改变科研指标定义、训练参数或数据。
- 轻量验证：语法编译、Ruff F/E9通过；延长规则四种情形、配对差方向、无匹配RMSE、嵌套checkpoint与中文字段往返均通过，证据`outputs_e2e/g5_training_contract_h0_dx8h6/report.json`。该检查不是正式训练性能证据；128步真实训练恢复试跑仍须特征准备完成后执行。
- 当前仅数据准备进程在运行；六轨训练、一次开发集比较及最终审计尚未完成，后续事实以当前任务和原始运行报告为准。
- 后续接入：新增`e2e_g5_execute.py`串行执行入口与`audits/verify_e2e_g5_final.py`完成性审计，等待已核对命令行和创建时间的准备进程，失败不自动重启。候选最佳checkpoint按epoch分别保存，避免中断发生在候选保存与last保存之间时覆盖旧最佳；正式best按已完成history发布。新增K=0虚警率显式字段，不改变已有指标。子进程UTF-8日志测试通过（`outputs_e2e/g5_runner_smoke__xuk2njq`）；训练合同烟测复跑通过（`outputs_e2e/g5_training_contract_uq_va2at`）。顺序入口已启动等待准备，最终审计脚本仅静态通过，尚无正式审计结论。

## 2026-09-19 有界特征加载优化及统一模型方案整理

- 原因：避免全量RAM恢复路径限制规模，同时补上另行校验与实际消费字节不同的证据缺口；修复G3/G4误嵌G2、结构公式和当前状态过期。
- 代码：新增`统一模型代码/common/verified_feature_loader.py`及`统一模型代码/common/diagnostics/optimize_feature_loader.py`，支持同bytes校验解析、只读数组、批内分片合并、有界共享LRU及异常字节保存。未修改模型、loss、历史Gate或AGENTS.md。
- 文档：实现文档G2/G3/G4改为并列章节，新增G5待审批方案，修正query锚点、频带归一化、空间残差、FiLM及匹配说明；同步当前任务、上下文与实验事实，G5/test未执行。
- 验证：项目Python语法编译、Ruff F/E9通过；轻量验证覆盖顺序、重复索引、批内合并、淘汰、不可写视图、Fortran数组、错误SHA捕获及非ASCII子进程日志。烟测证据`outputs_e2e/loader_smoke_ws5gjp2p`。512样本/256步验证见`E2E-LOADER-OPT-20260919`，正确性通过但速度目标未完全达到，不能记作全项PASS。
- 执行审查：未写原outputs、未恢复旧失效训练。G5仍须审批，正式规模加载与训练稳定性尚未验证。

## 2026-08-20 建立长期项目文档体系

| 字段 | 内容 |
|---|---|
| 修改原因 | 建立可持续维护的项目状态、研究认识、任务、修改和实验记录体系。 |
| 主要修改 | 新建长期文档；精简从其他项目迁入的 `AGENTS.md`，保留环境、审批、验证、Git 和文档规则。 |
| 验证 | 中文编码、文件职责和根目录链接检查通过。 |

## 2026-08-20 完成论文与第三、第四章代码静态接管审查

| 字段 | 内容 |
|---|---|
| 修改原因 | 建立论文方法、源码入口、调用关系、数据流和完整性之间的可追溯映射。 |
| 主要修改 | 新建 `第一阶段_论文与代码接管审查.md`，形成项目总图、两章讲义、论文—代码对应表、风险和资源估算。 |
| 验证 | Word逐页检查、源码静态映射、参数量和依赖核验完成；不包含正式实验。 |

## 2026-08-20 完成 Gate 0 运行基础改造

| 字段 | 内容 |
|---|---|
| 修改原因 | 解决硬编码路径/设备、依赖冲突、MATLAB误启正式规模和输出覆盖风险。 |
| 主要修改 | 增加统一 Python/MATLAB 配置、smoke/formal 隔离、`cuda:0` 约定、入口保护和最小依赖说明。 |
| 算法边界 | 未修改信号模型、DPD、网络、损失或正式参数。 |
| 验证 | Python入口、核心MATLAB函数、组合导入、静态检查和输出隔离通过。 |

## 2026-08-20 补全论文自学讲义并形成 Gate 1 方案

| 字段 | 内容 |
|---|---|
| 修改原因 | 将论文公式、张量和代码关系整理为可独立学习的材料，并设计第三章最小闭环。 |
| 主要修改 | 扩充第一阶段文档第2–4章和Gate 1方案。 |
| 验证 | 公式、形状、入口和交叉引用检查通过；当时未运行Gate 1。 |

## 2026-08-21 实施并完成 Gate 1

| 字段 | 内容 |
|---|---|
| 修改原因 | 验证第三章 MATLAB→MAT/HDF5→Dataset→Transformer→loss/checkpoint 工程闭环。 |
| 主要修改 | 新增 `gate1_smoke_ch3.py`；smoke模式跳过未使用MATLAB并行池；监控日志统一UTF-8安全转发。 |
| 失败与修复 | 首次运行因无用并行池导致内存越线；按审批最小修复后使用全新目录重跑。 |
| 算法边界 | 未改变数据物理模型、标签、网络或生产损失。 |
| 验证 | 标签独立复算、ignore-loss、单步训练、保存重载、资源和隔离门禁通过；详细历史见第一阶段第8.2节。 |

## 2026-08-21 实施并完成 Gate 2

| 字段 | 内容 |
|---|---|
| 修改原因 | 验证第四章 IQ→细DPD→标签/坐标→D8单步链，并确定主模型、AMP和batch接口。 |
| 主要修改 | 增加smoke数量/种子、分片审计、独立验证batch和 `gate2_smoke_ch4.py`；固定D8、Focal+offset、no-AMP。 |
| 失败与修复 | 首次坐标oracle因测试与生产float32运算顺序不同而停止；按审批只修测试oracle并在全新目录重跑。 |
| 算法边界 | 未改变DPD、模型、标签、损失阈值或正式配置。 |
| 验证 | 数据、坐标、oracle、D8单步、checkpoint和容量门禁通过；详细历史见第一阶段第8.3节。 |

## 2026-08-21 实施并完成 Gate 3A

| 字段 | 内容 |
|---|---|
| 修改原因 | 验证第四章中等规模数据、完整DPD、短训练确定性和资源可行性。 |
| 主要修改 | 增加smoke-only规模/种子覆盖、输出/任务保护、确定性训练、完整checkpoint和结构化日志；新增Gate 3监控、审计和评估入口。 |
| 失败与修复 | 先后修正比较器成功路径控制流和Windows日志编码/管道问题；失败目录保留，修复后从受影响阶段新建目录运行。 |
| 算法边界 | 未改变DPD、D8、生产损失或数据物理模型。 |
| 验证 | 数据语义、前缀一致性、双重复pilot、资源和隔离门禁通过；精确事实见 `CH4-G3A-20260821`。 |

## 2026-08-21 实施并完成 Gate 3B

| 字段 | 内容 |
|---|---|
| 修改原因 | 完成缩减数据上的长周期严格重复训练、冻结权重和独立oracle-K评估。 |
| 主要修改 | 增加Gate 3B冻结参数、初始validation及RNG恢复、checkpoint配置/SHA检查、完整训练比较和总门禁。 |
| 目录决策 | Gate 3A历史目录保持原名原位；Gate 3B使用独立根目录。 |
| 算法边界 | 未改变DPD、D8、Focal+offset、数据物理模型、optimizer或formal参数。 |
| 验证 | 语法、Ruff、两次完整训练、数据前后SHA和独立评估通过；精确事实见 `CH4-G3B-20260821`。 |

## 2026-08-21 回填 Gate 0–3 证据并重构第一阶段决策

| 字段 | 内容 |
|---|---|
| 修改原因 | 第一阶段第8章已记录Gate过程，但其他章节仍有过时状态，第10章缺少阶段级决策。 |
| 主要修改 | 回填第三、第四章实证状态，更新完整性、验证、资源、学习路线和证据边界；将第10章改为简明阶段路线。 |
| 涉及文件 | `第一阶段_论文与代码接管审查.md`、`修改记录.md`。 |
| 验证 | 标题、路径、指标引用、过时措辞和Markdown差异检查通过。 |

## 2026-08-21 实施长期文档去重并切换第二阶段

| 字段 | 内容 |
|---|---|
| 修改原因 | 多份长期文档重复维护Gate指标、当前状态、运行目录和下一步，存在漂移风险；第一阶段接管目标已经完成。 |
| 主要修改 | `AGENTS.md`增加单一事实源和阶段冻结规则；按职责精简状态、总结、任务、实验和运行说明；第一阶段标记完成并冻结；新建第二阶段框架并把当前任务切换到S2-G0方案待审批。 |
| 涉及文件 | `AGENTS.md`、六份长期状态/记录文档、`运行基础说明.md`、第一阶段文档及新建第二阶段文档。 |
| 算法边界 | 仅文档治理，不修改代码、数据、权重或实验结论。 |
| 验证 | 全局职责、实验ID、链接、标题、重复项和Markdown差异检查。 |

## 2026-08-21 以最小方案完成 S2-G0 基线引用

| 字段 | 内容 |
|---|---|
| 修改原因 | 当前基线仅用于承接第二阶段，后续不会作为正式复现基线；完整快照工具、重复哈希、额外manifest和Git标签超出实际需要。 |
| 主要修改 | 复用代码提交 `f6a802a`、数据证据 `CH4-G3A-20260821` 和权重/评估证据 `CH4-G3B-20260821` 完成逻辑封存；第二阶段转入S2-G1方案待审批。 |
| 涉及文件 | `第二阶段_缩减规模复现与端到端闭环.md`、`当前任务.md`、`对话上下文.md`、`修改记录.md`。 |
| 算法边界 | 未修改代码、数据、权重或实验结论；未创建快照、manifest、标签或新实验目录。 |
| 验证 | 代码提交可解析；Gate 3A/3B报告和冻结checkpoint存在；报告状态与长期实验记录一致；Markdown差异检查通过。 |

## 2026-08-21 增量建立 S2-G1 第三章受控训练与审计入口

| 字段 | 内容 |
|---|---|
| 修改原因 | 在不破坏师弟原训练入口的前提下执行第三章缩减数据收敛、确定性和独立评估门禁。 |
| 主要修改 | `main30.m`增加成对的可选规模/seed覆盖及运行元数据；新增 `s2g1_train_ch3.py` 和 `s2g1_ch3.py`，复用原Dataset、模型和loss，提供容量、训练、评估、数据/重复性审计和资源预检。 |
| 兼容策略 | 未修改 `train_v26.py`；未设置S2环境变量时，`main30.m`保留原smoke/formal规模、并行池与入口行为。原模型、DPD、标签和loss未修改。 |
| 第一阶段兼容审计 | 从初始化提交到第一阶段的原文件改动主要为Windows路径、`cuda:0`、smoke/formal隔离和可选审计参数；所有Python文件编译通过，第三/第四章关键模块导入与CLI帮助通过，9个被改MATLAB入口的Code Analyzer无错误。bare命令默认改为安全smoke和隔离路径，formal仍可显式进入；`trim_exp_pt.py`改为隔离副本而非原位覆盖，是有意的安全语义变化。 |
| 工程验证 | 首次容量smoke因当前PyTorch不接受带`torch.device`的峰值统计API而在训练前失败；最小修正后在全新 `outputs/s2g1_engineering_smoke/20260821_203908` 完成容量、两次6 epoch训练、checkpoint状态逐值比较和两次validation重载，全部一致；不作为性能结果。 |
| 实际Gate状态 | S2-G1首次数据运行在数据审计发现原生成器无约束回退导致1条源间距违规，已停止；精确事实见 `CH3-S2G1-20260821-A`。 |

## 2026-08-22 为 S2-G1 受控数据增加源间距后验门禁

| 字段 | 内容 |
|---|---|
| 修改原因 | 首次S2-G1数据审计发现原位置生成器在约束放置失败后会无约束补齐，产生低于300 m最小源间距的样本。 |
| 主要修改 | 仅在 `main30.m` 的S2受控路径拒绝违规布局并有限重试；保存逐样本重试次数、split拒绝总数、300 m阈值和100次上限；`s2g1_ch3.py`同步核验元数据、重试计数和零间距违规。 |
| 增量兼容边界 | 未修改共享 `gen_multi_source_pos_v2.m`、`train_v26.py`、DPD、标签、模型或loss；未设置S2环境变量时，位置生成结果不因后验检查而重采样。 |
| 静态验证 | 两个S2 Python入口通过 `py_compile` 和Ruff F/E9；`main30.m` Code Analyzer无错误，仅保留既有GVMIS/AGROW/LOGSUM提示；共享生成器Git差异为空。 |
| 运行验证 | `outputs/s2g1_ch3/20260822_120601`因启动RAM不足停止；释放资源后在全新 `20260822_122055` 完整重跑，train中1个违规布局被拒绝并一次重试，最终数据零违规，S2-G1全部门禁通过，见 `CH3-S2G1-20260822`。 |

## 2026-08-22 实施 S2-G2 并固化低级工程错误防线

| 字段 | 内容 |
|---|---|
| 修改原因 | 在不改变师弟原入口的前提下完成第四章200 epoch充分训练诊断，同时把本项目多次出现的编码、日志、API、路径和报告汇总类低级错误转化为稳定防错规则。 |
| 主要代码 | `train_yolo.py`新增默认关闭的`--s2g2_d8`严格入口；`eval_ch4_checkpoint.py`新增可选逐源长尾诊断；新增`s2g2_ch4.py`完成预检、manifest、训练审计、候选比较、冻结、重载比较和总门禁；`gate3_stage_runner.py`统一Python子进程UTF-8替换策略。 |
| 兼容边界 | 原普通、Gate 3A和Gate 3B入口均保持；未修改D8结构、DPD、数据、loss、优化器或评价指标。S2-G2只在显式开关下生效，输入数据只读，未运行test。 |
| 低级错误复盘 | 历史上出现过MATLAB中文监控日志乱码、Windows日志转发线程因编码异常退出并引发假超时、比较器成功路径控制流遗漏、PyTorch显存统计API参数版本不兼容；本次又出现Git子进程默认代码页解码失败及`stdout=None`二次异常、Python子进程未统一UTF-8、手写历史时间戳路径错误、总门禁不能容纳保留失败证据。共同根因是长任务前缺少小型接口自检、依赖系统默认编码/版本行为、输入身份未先按文件和SHA解析，以及报告器把工程历史与通过证据混为一谈。 |
| 规则固化 | `AGENTS.md`新增Windows子进程显式编码、非ASCII自检、日志线程异常回传、可空输出防护、输入路径与SHA预检、失败/PASS证据分离，以及高置信低风险工程错误可自动最小修复并继续的条件和边界。 |
| 失败与修复 | 预检失败根`20260822_142153`和代码后续变化前的`20260822_143549`均未作为通过证据；有效根为`outputs/s2g2_ch4/20260822_143749`。其中错误Gate 3B路径评估目录`03_eval_gate3b`保留，按SHA定位后在`03_eval_gate3b_after_path_fix`通过；总报告显式记录该失败尝试。 |
| 运行结果 | 200 epoch完整通过并冻结epoch 93新权重，趋势`TREND_CLOSED`，总门禁`PASS`；精确指标、资源和边界见`CH4-S2G2-20260822`。 |
| 验证 | 训练前后15个输入文件SHA256一致；两候选统一validation和冻结checkpoint第二次重载通过；4份有效监控无warning/red flag；Python语法、Ruff F/E9、总报告回读、formal隔离和残留进程检查通过。 |

## 2026-08-24 清理无效数据、重复载荷和工程缓存

| 字段 | 内容 |
|---|---|
| 修改原因 | 降低 `outputs` 占用，同时保留当前研究主线、有效冻结权重和可审计的历史结论。 |
| 删除内容 | 删除 `CH3-S2G1-20260821-A` 的三个不合格 MAT 文件；删除 outputs 中大于等于1 MiB且SHA256逐字节重复的非主副本，保留 Gate 3A 完整定位数据或 Gate 2 成功运行副本；删除 `outputs/s2g1_engineering_smoke`、Python `__pycache__` 和 Ruff 缓存。删除后为空的目标目录一并移除。 |
| 保留证据 | 失败运行根、审计JSON、manifest、资源CSV、日志和总报告均保留；Gate 3A完整定位数据、Gate 2成功副本、S2-G1/S2-G2冻结best checkpoint及其他非重复checkpoint未删除。工程 smoke 仅为轻量链路验证，已被完整S2-G1通过证据替代。 |
| 空间结果 | `outputs` 从8,625,192,655 bytes降至6,916,248,740 bytes；连同源码目录缓存共删除1,708,943,915 bytes，约1.592 GiB。其中失败MAT为1,241,624,245 bytes、逐字节重复大文件为426,531,288 bytes、工程smoke与缓存为40,788,382 bytes。 |
| 算法边界 | 未修改代码、数据生成逻辑、模型、loss、配置、指标或实验结论；未删除任何当前有效数据、冻结best权重或唯一结果副本。 |
| 验证 | 删除前重新计算全部大文件SHA256并明确唯一保留副本；删除后复核关键数据、checkpoint和总报告存在，复扫无剩余大文件重复组，并检查空路径与Git差异。 |

## 2026-08-24 增量实施 S2-G3 跨章可组合性审查

| 字段 | 内容 |
|---|---|
| 修改原因 | 在不硬串异分布模型得出性能结论的前提下，审查两章数据契约、第三章跨分布能力和19子带二值接口的信息上限。 |
| 主要代码 | 新增`s2g3_compute_coarse_dpd.m`，只读第四章IQ并复用第三章DPD实现生成19×81×81输入；新增`s2g3_composability.py`，提供契约审计、双推理比较、细DPD生成、D8集合评估、表示门和最终决策。 |
| 增量兼容 | 未修改`main30.m`、`main31_loc_v3.m`、`train_v26.py`、`train_yolo.py`、原模型或原评估默认入口；新增脚本显式调用且输出拒绝覆盖。未生成新IQ、训练、微调、调阈值或运行test。 |
| 高置信工程修复 | 修正监控器参数名/采样间隔、错误历史IQ路径和MATLAB日志编码；Exact回归阶段将新评估器改为与`LocDataset`一致的逐样本归一化归约顺序，并保持第四章原生float32距离口径。原S2-G2入口复算证明修复后RMSE精确一致。 |
| 失败证据 | 外层包装器导致的预检假残留和首次完整粗DPD错误路径均保留独立监控目录；被替代的两份Exact评估也保留，不混入最终表示门。 |
| 文档同步 | 更新`第二阶段_缩减规模复现与端到端闭环.md`、`当前任务.md`、`对话上下文.md`、`研究总结.md`、`实验结果记录.md`和本记录；精确实验事实唯一记录为`SYS-S2G3-20260824`。 |
| 科研表述修正 | 初版总报告把Coarse-Oracle退化直接归因为表示信息上限；最终复核将其收紧为“当前映射与冻结D8不能直接复用，尚不能区分信息损失与下游分布失配”，保留初版并生成解释修正版报告。 |
| 验证 | Python `py_compile`与Ruff F/E9、MATLAB Code Analyzer、CLI/import、Git差异和17份监控报告回读通过；最终15份有效阶段PASS且无warning/red flag，2份工程失败单列；总报告工程`PASS`、科学状态`REQUIRES_INTERFACE_REDESIGN`。 |

## 2026-08-24 增量实施 S2-G4 并在频带支持域门停止

| 字段 | 内容 |
|---|---|
| 修改原因 | 按批准方案比较Coarse-D8从头训练与Exact-D8微调，以区分下游输入分布失配和19子带表示不足；同时保持师弟原入口默认行为。 |
| 主要代码 | `train_yolo.py`新增默认关闭、互斥且配置严格校验的S2-G4 scratch/finetune入口，微调只加载校验过SHA的D8权重而不恢复优化器、调度器或RNG；新增`s2g4_coarse_d8.py`完成数据构造、支持域审计、统一评估和停止/最终报告；新增`s2g4_resource_preflight.py`避免PyTorch导入污染启动RAM基线。 |
| 增量兼容 | 普通、Gate 3A/3B和S2-G2入口默认行为不变；未修改D8结构、DPD算法、原数据、loss、第三章代码或阈值。所有S2-G4输出使用唯一目录并拒绝覆盖。 |
| 高置信工程修复 | 最初资源预检在导入PyTorch后采样，错误把预检进程自身占用计入系统启动基线，改为独立轻量脚本后重测；首次完整构造因外层工具过早关闭输出管道而中断，保留失败目录并以正确等待方式在新目录重跑。两项均未改变资源门槛或科研语义。 |
| 科学停止 | 新构造在train样本205发现空Coarse-Oracle频带。追加只读全量元数据审计，确认train/validation分别有154/38个`symbolRate<3 MHz`源无法获得任何正子带；按预案未作Exact回退、ignore转正或阈值调整，也未启动完整数据、D8训练和交叉评估。 |
| 文档同步 | 将批准方案和实际停止结论写入第二阶段文档；同步更新`当前任务.md`、`对话上下文.md`、`研究总结.md`、`实验结果记录.md`及本记录。精确事实唯一记录为`SYS-S2G4-20260824`。 |
| 验证 | 身份/资源预检、8/8数据pilot和单步训练pilot通过；停止报告结构化保存。Python语法、Ruff F/E9、CLI、JSON、Git差异和残留进程在交付前复核。 |

## 2026-08-24 实施 S2-G4-R1 0.2阈值快速D8适配诊断

| 字段 | 内容 |
|---|---|
| 修改原因 | 用户选择不做阈值搜索，固定0.2快速闭合第四章2 MHz下限的逐源支持域，并以最短EE/EC20/CC20三轨验证D8重训能否恢复性能。 |
| 主要代码 | `s2g4_coarse_d8.py`新增显式`--s2g4r_t020`标签重算、0.2支持审计、EC20/CC20评估标识和`finalize-r1`三轨判定；复用既有`train_yolo.py --s2g4_scratch`，未新增训练算法。 |
| 增量兼容 | 0.2分支只在显式开关下启用；原0.3 MAT标签、MATLAB入口、CH3、普通训练及历史S2-G4目录不变。新数据和报告均记录阈值与物理元数据重算身份。 |
| 执行结果 | 支持域、8/8 pilot、完整1024/256数据、EE/EC20、200 epoch scratch、CC20和总判定全部通过工程门；科学状态为`REPRESENTATION_LIMIT_CANDIDATE_T020`，精确事实见`SYS-S2G4-R1-20260824`。 |
| 范围控制 | 按快速方案取消微调和CE/FE/FC，不运行test、predicted-band/K或CH3训练；未因结果调整epoch、阈值或模型。 |
| 文档同步 | 更新第二阶段方案与执行结论、`当前任务.md`、`对话上下文.md`、`研究总结.md`、`实验结果记录.md`和本记录。 |
| 验证 | Python `py_compile`、Ruff F/E9、CLI与Git差异检查；13份监控报告均PASS且无warning/red flag；逐样本三轨、best SHA和总报告回读一致。 |

## 2026-08-25 系统重构第二阶段文档并补充候选频带表示

| 字段 | 内容 |
|---|---|
| 修改原因 | S2-G4-R1后第二阶段总览未完整反映当前决策，Gate方案以长段落为主，执行结果重复精确实验流水；同时需要说明后续候选频带接口。 |
| 主要修改 | 更新阶段目标、基线、Gate路线、完成条件和停止规则；将S2-G1至S2-G4-R1统一改为“问题—方案卡—执行/判定—结论”结构，只有不少于5个连续步骤时使用箭头；结果改为结论先行和关键证据表，精确路径、资源、哈希及完整指标回归`实验结果记录.md`。 |
| 方法补充 | 新增Soft-19、连续频带边界和Hard-39/79的表示、FFT映射、细DPD影响、CH3/CH4改造范围及判读方式；补充统一物理频带定义和多源频带求并集两个混杂因素。 |
| 证据边界 | 明确现有EE已给出连续真实边界oracle上限；候选方法尚未批准或运行新实验，既有Gate状态、实验ID和科研结论不变。 |
| 算法边界 | 本次只修改文档，未修改代码、数据、模型、权重、配置或实验结论。 |
| 验证 | 对照`实验结果记录.md`核验五个Gate实验ID和关键数字；检查标题唯一性、表格列数、公式/代码围栏、过时状态、重复流水、UTF-8及Git差异。 |

## 2026-08-25 实施 S2-G4-R2 Soft-19公平适配诊断

| 字段 | 内容 |
|---|---|
| 修改原因 | 在统一`BW_actual`定义下公平比较Hard-19与Soft-19，判断硬二值化是否是R1性能缺口的主要来源。 |
| 主要代码 | `dpd_calculator_torch.py`新增默认关闭、与旧`freq_mask`互斥的`freq_weights`接口；新增`s2g4r2_soft19.py`完成Actual覆盖率、Hard/Soft数据构造、权重语义测试、五轨评估和预设判定。 |
| 增量兼容 | 旧位置参数和布尔mask路径保持不变；所有R2功能仅由新入口显式启用。未修改CH3、原数据、D8结构、loss、普通训练默认值或历史输出。 |
| 执行结果 | 资源/身份、权重自测、pilot、完整1024/256双表示数据、两次200 epoch训练和五轨评估完成；工程`PASS`，科学状态`SOFT19_INSUFFICIENT`，精确事实见`SYS-S2G4-R2-20260825`。 |
| 工程事件 | 首次Hard train完整数据在产物和PASS报告落盘后，外层120秒等待超时导致孤立进程停在结果回传；精确终止PID并保留监控失败，随后用全量Hard/Soft逐字段比较回读全部1024条Hard train数据。后续长任务改用覆盖阶段硬时限的外层超时。 |
| 科研修正 | R2显示Hard-19-Actual达到当前oracle候选水平，Soft不优于Hard；R1的主瓣/实际带宽定义差异被确认为重要混杂因素，原“19窗口本身更可能是主要限制”的表述已收紧。 |
| 文档同步 | 更新第二阶段方案与结论、`当前任务.md`、`对话上下文.md`、`研究总结.md`、`实验结果记录.md`和本记录。 |
| 验证 | Python语法与Ruff F/E9、CLI、权重三项语义回归、1280条监督一致性/DPD差异、训练趋势、五轨指标、checkpoint SHA、监控资源、禁止范围及Git差异均复核。 |

## 2026-08-25 回填R2解释并增强通用监控器

| 字段 | 内容 |
|---|---|
| 修改原因 | R2原结论未充分解释Soft/Hard的训练轮次、三源长尾和有效带宽；阶段监控器传入5秒采样时会在启动前报错，完整日志转发也不适合后续长训练。 |
| 文档修改 | 在第二阶段第14.7节补充200 epoch事实、双源/三源分层、有效频率孔径、逐样本差异、bootstrap边界和Soft潜在不利机制；`当前任务.md`转为S2-G4-R3方案待审批。 |
| 代码修改 | `gate3_stage_runner.py`将低于10秒的采样请求自动提升为10秒；硬超时可用0显式禁用；新增默认关闭的`summary`控制台模式，完整子进程输出仍写入`stage.log`。旧调用默认完整日志，正数硬超时语义保持兼容。 |
| 实验边界 | 未启动新训练或评估；R3实验设计和新资源门限仍待用户审批。 |
| 验证 | 对监控器执行Python语法、Ruff F/E9、CLI及5秒自动提升/无硬超时/摘要日志自测；核对第14.7节数值与R2结构化报告。 |

## 2026-08-25 实施 S2-G4-R3 三seed配对确认

| 字段 | 内容 |
|---|---|
| 修改原因 | 验证R2的Hard候选是否依赖seed 42，并区分Soft/Hard表示差异与训练初始化方差。 |
| 主要代码 | `train_yolo.py`新增默认关闭、仅允许`42/1042/2042`的`s2g4r3_scratch`严格入口及初始模型SHA；新增`s2g4r3_multiseed.py`完成数据/历史证据审计、初始化pilot、评估、manifest、配对bootstrap和三seed预设判定。 |
| 监控改进 | `gate3_stage_runner.py`支持独立配置RAM/RSS/GPU/磁盘门限；R3使用10秒采样、摘要控制台和`timeout=0`。`s2g4_resource_preflight.py`新增可配置启动RAM阈值，旧默认14 GiB保持不变。 |
| 增量兼容 | 原S2-G2、S2-G4和R2的seed 42门禁不变；未修改D8结构、数据、DPD表示、loss、CH3、普通训练默认值或历史结果。 |
| 执行结果 | 身份/初始化pilot、seed 1042/2042共六次200 epoch训练、六次独立评估和三seed汇总全部通过；科学状态=`HARD19_STABLE_CANDIDATE`，精确事实见`SYS-S2G4-R3-20260825`。 |
| 科研结论 | Hard在三个seed的RMSE均低于Exact，2/3通过联合门；Soft只在1/3优于Hard，收益不稳定。R2的Soft三源长尾机制不能上升为必然规律。 |
| 文档同步 | 更新第二阶段第15.6节、`当前任务.md`、`对话上下文.md`、`研究总结.md`、`实验结果记录.md`和本记录。 |
| 验证 | Python语法、Ruff F/E9、CLI、1280条监督一致性、初始化SHA、训练趋势、逐seed指标/bootstrap、17份监控报告、资源/禁止范围、Markdown及Git差异均复核。 |

## 2026-08-26 实施 S2-G4-R4-A至R4-D并交付PyCharm训练入口

| 字段 | 内容 |
|---|---|
| 修改原因 | 扩大共享数据并冻结嵌套比较集，以判断R3中的Hard/Soft排序是否受小数据和场景覆盖影响；长训练交由用户在PyCharm点击运行。 |
| MATLAB增量 | `main31_loc_v3.m`新增默认关闭的`s2g4r4` profile，只在显式环境变量下允许并强校验`8192/2048/2048`与seed 20260826；普通smoke/formal仍保持原规模和行为。 |
| DPD增量 | `gen_ch4_loc_data.py`新增默认关闭的`--d8_minimal`，只省略D8不读取的`hyp_mask/gauss_multi`；`s2g4r2_soft19.py`新增显式样本数门禁，旧R2默认1024/256不变。 |
| 清单与审计 | 新增`s2g4r4_scale_study.py`，建立1k⊂4k⊂8k及两份validation清单，并逐分片流式审计三表示监督、索引、有限性和表示差异，避免三套大数据同时载入RAM。 |
| 训练兼容 | `train_yolo.py`新增可选manifest筛选和默认关闭的`s2g4r4_scratch`严格门禁；manifest缺省时原数据加载行为不变。新增`s2g4r4_pycharm_train.py`，当前固定首项Exact/1k/seed42，用户在PyCharm直接点击Run，无需命令行参数。 |
| 执行结果 | R4-A旧场景诊断、8192/2048/2048 IQ、五份manifest及三种8192/2048 DPD均完成并通过审计；精确事实见`SYS-S2G4-R4-D-20260826`。 |
| 工程修复 | 保留短外层超时、错误历史IQ路径和两次过严审计的失败证据；分别改为无墙钟等待、文件系统解析正确路径、流式审计及记录统一权重数学等价样本，均未改变科研语义。 |
| 文档同步 | 更新第二阶段第16章、`当前任务.md`、`对话上下文.md`、`运行基础说明.md`、`实验结果记录.md`和本记录；未重复改写稳定研究总结。 |
| 验证 | MATLAB Code Analyzer仅见既有性能建议；Python `py_compile`、Ruff F/E9、CLI help、1k/val manifest真实加载、六阶段监控、完整DPD审计及Markdown/Git差异检查通过。另以全新目录复跑默认4/2/2 MATLAB smoke和默认完整字段Exact单样本，证明旧入口仍可运行。R4-E正式训练尚未执行。 |

## 2026-08-26 完成 S2-G4-R4-E/R4-F 训练与统一分析

| 字段 | 内容 |
|---|---|
| 修改原因 | 用户改为授权Codex执行R4全部长训练和分析，需要保存原始样本索引、支持冻结清单评估，并建立可重算的配对统计报告。 |
| 代码增量 | `train_yolo.py`保留manifest选中的原样本索引；`s2g4_coarse_d8.py`扩展可选manifest和动态样本数；`s2g4r4_scale_study.py`新增冻结评估、checkpoint哈希复核、配对bootstrap、场景分层、资源汇总和11类谬误扫描。未修改D8、DPD表示、loss、数据划分或训练配置。 |
| 执行结果 | 1k/4k/8k×Exact/Hard/Soft共9次训练和9次冻结评估全部PASS，总报告为`outputs/s2g4r4_scale/20260826_132829/11_analysis/final_report.json`；精确指标见`SYS-S2G4-R4-20260826`。 |
| 工程修复 | 统计脚本首次运行时把checkpoint结构化字段误当路径；已改为解析`checkpoint.path`并复核SHA256。完成审计又发现评估加载顺序与manifest排列不同，初版场景元数据与样本错配；总体指标和配对bootstrap未受影响。无效报告已保留为`final_report_invalid_manifest_order.json`，改为以评估`sample_index`对齐场景后在全新`final_report.json`重算通过。 |
| 文档同步 | 更新第二阶段第16.6节、`实验结果记录.md`、`当前任务.md`、`对话上下文.md`、`研究总结.md`和`运行基础说明.md`，将精确指标仅保留在实验记录。 |
| 验证 | 涉及Python文件已通过`py_compile`和Ruff F/E9；18份监控报告、9份训练summary、9份评估JSON/JSONL、checkpoint SHA256、禁止范围和统计总报告已统一复核。 |

## 2026-08-27 清理 S2-G4 停止载荷与未选 last checkpoint

| 字段 | 内容 |
|---|---|
| 修改原因 | 第一批存储清理仅处理已明确失去复用价值的 S2-G4 停止运行载荷，以及全项目未被选择的 `last` checkpoint；不删代码，不处理收益很小或证据状态不明确的内容。 |
| 删除内容 | 删除 `outputs/s2g4_coarse_d8/20260824_164707` 下的 `pilot_data`、`pilot_train_run`、`coarse_data`、`coarse_data_after_wrapper_fix`；删除全项目共 29 个 `last_*.pth` / `last_*.pt`。 |
| 保留证据 | 保留所有被选择的 `best` / frozen checkpoint、S2-G4 停止报告、频带支持审计、监控 JSON/CSV、日志、manifest 与结构化分析报告；历史 `best` / `last` 比较结论继续作为清理前已回读快照。 |
| 空间结果 | `outputs` 从 37,230,387,472 B 降至 32,880,235,156 B，共删除 4,350,152,316 B，约 4.051 GiB。 |
| 算法边界 | 未修改代码、配置、数据语义、模型选择结果或科研结论。 |
| 验证 | 清理后重新扫描 `last_*.pth` / `last_*.pt` 为 0；四个目标目录均不存在；关键 `best` checkpoint 与 S2-G4 停止证据仍存在，并执行 `git diff --check`。 |

## 2026-08-27 建立S2-G5总体路线并回填阶段文档

| 字段 | 内容 |
|---|---|
| 修改原因 | R4已完成并固定Hard-19-Actual为接口目标，第二阶段文档仍有若干停留在R2/R3时点的“下一步”表述，且尚未给后续统一数据与端到端接口路线编号。 |
| 文档修改 | 在`第二阶段_缩减规模复现与端到端闭环.md`新增S2-G5总体路线，将统一契约、4k CH3训练、四轨隔离、不确定性适配和16k/test划为R1至R5候选子阶段；同步更新顶部状态、Gate总表及R2/R3历史前瞻表述。 |
| 当前任务 | `当前任务.md`转为S2-G5-R1详细方案待审批，明确未审批前不改代码、不生成数据或训练模型。 |
| 科研边界 | 本次仅更新路线与带宽定义判断；未修改算法、标签、数据、模型或结果，未运行实验。`BW_actual`暂解释为生成器设计/裁剪带宽，是否作为最终论文带宽口径由R1审计后冻结。 |
| 验证 | 复读MATLAB波形生成、RRC与Hard-19实现；检查Markdown标题、表格、代码围栏、过时状态、UTF-8和Git差异。 |

## 2026-08-27 实施S2-G5-R1统一数据与接口契约

| 字段 | 内容 |
|---|---|
| 修改原因 | CH3需要在与CH4一致的物理分布上学习Hard-19-Actual，同时必须明确K=0/1/2/3的下游分流。 |
| 代码修改 | `main31_loc_v3.m`增加显式`s2g5r1` profile和版本化契约字段；新增`build_hard19_actual_labels.m`及`s2g5_r1_contract.py`。默认入口和`s2g4r4`受控分支保持原语义。 |
| 接口边界 | K=0直接结束；K=1只保留未实现接口；K=2/3沿用G4-R4条件分布并进入Hard-D8。未改波形、粗/细DPD算法、模型、loss或训练配置。 |
| 文档修改 | 第二阶段第17章补充R1方案和结论；当前任务转入R2方案待制定；同步更新实验事实、上下文和稳定研究认识。 |
| 验证 | MATLAB Code Analyzer新增函数0项告警；Python compile/Ruff通过；三split标签独立复算、CH3 forward/loss及默认smoke回归均PASS。精确事实见`SYS-S2G5-R1-20260827`。 |

## 2026-08-27 实施S2-G5-R2嵌套规模训练并重构G5章节

| 字段 | 内容 |
|---|---|
| 修改原因 | 需要在统一Actual标签上判断CH3是否可学习，并依据1k→4k的配对结果决定是否值得扩展8k。 |
| 代码修改 | `main31_loc_v3.m`增加显式`s2g5r2` profile和ASCII错误标识；`s2g5_r1_contract.py`支持指定profile；新增`s2g5_r2_materialize_subset.m`和`s2g5_r2_ch3.py`，负责嵌套划分、审计、初始化身份、配对分析和总门禁。 |
| 原版兼容 | 所有新行为只由显式R2入口启用；未修改师弟默认数据规模、默认标签、CH3模型、loss或原训练入口默认行为。 |
| 文档修改 | `第二阶段_缩减规模复现与端到端闭环.md`将G5-R1和G5-R2提升为与S2-G4各R同级的独立章节，并同步方案、结论和`ENTER_8K`边界；其他长期文档按主责去重更新。 |
| 自动修复 | 拒错pilot改用ASCII错误标识规避控制台乱码；子集审计改为核对manifest非连续索引；独立重载命令由误写的`evaluate`改为已有`eval`。均未改变科研配置，失败证据保留。 |
| 验证 | Python compile/Ruff、MATLAB Code Analyzer、数据/标签/manifest门禁、batch 64容量、1k/4k训练、独立重载、2000次配对bootstrap和最终证据审计均通过。精确结果见`CH3-S2G5-R2-20260827`。 |

## 2026-08-27 完成S2-G5-R2的8k条件扩展

| 字段 | 内容 |
|---|---|
| 修改原因 | 1k→4k预注册判定为`ENTER_8K`，需要在不破坏旧4k和固定validation的前提下确认规模收益是否继续。 |
| 代码修改 | `main31_loc_v3.m`新增显式`s2g5r2_8k_add`增量profile；`s2g5_r2_ch3.py`增加粗DPD分块合并、严格前缀审计、4k/8k配对bootstrap、预注册决策和最终门禁。所有行为仅由8k专用子命令启用。 |
| 数据与输出 | 在原R2根目录`outputs/s2g5r2_ch3/20260827_191207`内新增4k IQ/粗DPD、严格嵌套8k、`train_8k`、`analysis/8k`和后续监控目录；未覆盖旧4k、validation、checkpoint或原分析报告。 |
| 自动修复 | profile拒错pilot补充ASCII identifier输出；最终审计将浮点逐位相等改为绝对容差`1e-12`，对应的实际差异仅`2.22e-16`。失败目录均保留，未改变数据、模型、训练或指标。 |
| 文档修改 | 第二阶段第19.6节记录8k方案，第19.7节统一汇总1k/4k/8k结论；实验结果、当前任务、上下文和研究总结同步转入R3待审批状态。 |
| 验证 | 新增4k IQ/标签/粗DPD、旧4k严格前缀、batch 64、8k训练、独立重载、2000次配对bootstrap和最终SHA门禁均通过；默认smoke仍为`[4,2,2]`、seed 20260821、`mainlobe_t030`和阈值0.3。判定`FREEZE_8K_FOR_R3`，精确结果见`CH3-S2G5-R2-20260827`。 |

## 2026-08-28 实施S2-G5-R3四轨级联误差隔离

| 字段 | 内容 |
|---|---|
| 修改原因 | 需要在统一共享IQ上分别隔离频带预测和源数预测对定位的影响，判断现有CH3与Hard-D8能否直接级联，以及后续应优先改进哪一条接口。 |
| 代码修改 | 新增`第四章代码/s2g5r3_cascade.py`，提供输入冻结、pilot、CH3推理、oracle/predicted细DPD构建、四轨评估、配对bootstrap和最终证据门禁。复用既有CH3、细DPD、D8、解码和GOSPA实现；未修改师弟原入口或算法。 |
| 执行与证据 | 在`outputs/s2g5r3_cascade/20260828_131043`完成512条K=2/3定位样本的两套细DPD与四轨评估，并同时审计1024条K=0/1/2/3分流。工程闭环通过，但直接级联尚不满足预设等价标准；误差隔离判定为`COUNT_PATH_PRIORITY`。精确指标见`SYS-S2G5-R3-20260828`。 |
| 自动修复 | 预检将float32阈值的逐位比较改为`np.isclose`；评估各轨后显式释放模型和CUDA缓存，并在全新目录生成正式结果；最终汇总器只接纳预注册的权威PASS阶段，同时登记两次已保留的失败尝试。外层短等待结束时，确认子进程仍连续运行后继续观察，未重启计算。上述修复均未改变数据、模型、阈值或指标。 |
| 科研边界 | 未训练或微调模型，未使用test，未改变频带阈值、源数判决、K=1占位接口或R4/16k数据。匹配样本RMSE下降被明确识别为漏检困难源后的选择效应，不作为性能改善证据。 |
| 文档修改 | 第二阶段第20章补充方案与执行结论；同步更新`实验结果记录.md`、`当前任务.md`、`对话上下文.md`、`研究总结.md`和本记录。 |
| 验证 | Python `py_compile`、Ruff F/E9和CLI help通过；冻结输入哈希、CH3逐样本复算、两套DPD索引、四轨独立重载、2000次分层配对bootstrap、资源报告、输出哈希和最终总门禁均通过。 |

## 2026-08-28 实施S2-G5-R4长训练前数据与容量门

| 字段 | 内容 |
|---|---|
| 修改原因 | R3的K瓶颈可能随训练规模自然缩小，需要先建立16k原CH3基线，避免提前修改K算法。 |
| 代码修改 | `main31_loc_v3.m`新增默认关闭的`s2g5r4_16k_add` profile；新增`s2g5_r4_ch3_scale.py`和固定参数的`s2g5r4_pycharm_train.py`。未修改CH3/D8模型、loss、阈值或旧R2/R3入口。 |
| 数据结果 | 新增8k统一IQ、Hard-19-Actual标签和粗DPD通过；与冻结旧8k合并为严格嵌套16k，每个K各4096条，旧8k的9个科研字段逐块一致。精确事实见`CH3-S2G5-R4-PRETRAIN-20260828`。 |
| 容量结果 | batch 64的forward/loss/backward和validation可完成，但eager Dataset使系统可用RAM跌破6 GiB红线；训练入口会据此拒绝启动，正式训练未执行。 |
| 工程事件 | IQ启动的外层等待误设过短，但监控器和MATLAB保持连续运行并最终PASS，未重启；控制台乱码未用于源码或数据判断。 |
| 文档同步 | 第二阶段新增第21章及21.7进度；同步更新实验结果、当前任务、对话上下文和本记录。稳定研究总结未因单纯资源结论修改。 |
| 验证 | Python compile/Ruff、MATLAB Code Analyzer、R4错误规模拒绝、默认smoke、冻结SHA、IQ/标签/距离、粗DPD有限性、16k严格前缀、硬链接身份和容量计算均完成；test和长训练未运行。 |

## 2026-08-28 实施S2-G5-R4专用懒加载与等价门

| 字段 | 内容 |
|---|---|
| 修改原因 | 原eager Dataset使16k频谱全部常驻RAM并触发资源红线，需要只改变存储方式、保持训练语义不变。 |
| 代码修改 | 新增`s2g5_r4_lazy_dataset.py`和`s2g5_r4_train_ch3.py`，以预标准化只读NumPy memmap提供R4专用Dataset；`s2g1_train_ch3.py`只增加默认关闭的Dataset工厂注入；固定PyCharm入口改为读取lazy总门禁。 |
| 原版兼容 | 未修改`train_v26.SourceDetectionDataset`、模型、loss、增强、seed或原CLI默认行为；旧1k eager容量回归通过，lazy门禁形成前PyCharm入口会拒绝启动。 |
| 自动修复 | 删除3个静态检查发现的未使用导入；最终资源汇总同时采用容量与完整epoch监控的保守极值，避免4秒容量阶段短于10秒采样周期而漏记峰值。均未改变科研配置。 |
| 文档同步 | 批准方案和简明结论归入第二阶段第21.8–21.9节；实验结果、当前任务、上下文和本记录同步更新。 |
| 验证 | Python compile、Ruff F/E9、CLI帮助、非ASCII输出、原入口回归、512条频谱、Dataset/增强、1k三轮训练、16k容量与单epoch吞吐全部通过；最终状态`LAZY_READY_FOR_TRAINING`，正式训练与test未运行。精确事实见`CH3-S2G5-R4-LAZY-20260828`。 |

## 2026-08-28 完成S2-G5-R4正式训练与K路径复核

| 字段 | 内容 |
|---|---|
| 修改原因 | 用户授权完成16k正式训练和R4全部后续分析，需要验证规模是否自然消除K瓶颈，并落实Top-2候选上限判据。 |
| 代码修改 | `s2g5_r4_ch3_scale.py`增加checkpoint独立重载一致性和R4整体总门禁；新增`s2g5r4_cascade.py`，复用R3 Oracle细DPD和冻结D8，生成16k Predicted细DPD、更新四轨，并增加候选K oracle上限轨。 |
| 算法边界 | 未修改CH3/D8模型、loss、阈值、数据或训练配置；候选K上限用真值择优，只用于判断后续研究价值，不作为部署方法。 |
| 自动修复 | 首次D8 batch 64评估触发GPU红线后，保留失败目录，仅将推理batch降为8并在新权威目录重跑；总门禁明确区分失败尝试和有效PASS证据。 |
| 文档同步 | 第二阶段第21.10节、实验结果、当前任务、上下文、研究总结和本记录同步为R4完成状态。 |
| 验证 | 16k训练、checkpoint双重加载、2000次规模bootstrap、Predicted细DPD、D8双重重载、四轨/候选轨、2000次级联bootstrap、冻结输入SHA和整体总门禁均通过；test未运行。精确事实见`CH3-S2G5-R4-20260828`。 |

## 2026-08-28 实施S2-G5-R5冻结模型候选K联合判决

| 字段 | 内容 |
|---|---|
| 修改原因 | R4证明相邻K候选具有较高恢复上限，需要在不看真值、不先训练新模型的条件下建立可部署选择规则。 |
| 代码修改 | 新增`第四章代码/s2g5r5_candidate_k.py`，实现16k懒加载冻结logits、有序条件probe、相邻K候选、`val_select`预测细DPD、D8 Top-4峰缓存、固定六特征选择器、配对bootstrap和总门禁。 |
| 原版兼容 | 未修改师弟原CH3/D8入口、模型、loss、阈值、数据或checkpoint；R5只新增独立入口并只读复用R4证据。 |
| 路线结果 | R5-A通过`DIRECT_CASCADE_SUPPORTED`门，因此按预注册停止规则不实现或训练R5-B有序计数头。精确结果见`SYS-S2G5-R5A-20260828`。 |
| 文档修改 | 第二阶段更新过时R4状态、新增第22章方案与简明结论；当前任务、上下文、研究总结、实验结果和本记录同步到R5完成状态。 |
| 验证 | Python compile、Ruff F/E9、CLI help、冻结SHA、训练/校准/比较集隔离、概率和相邻候选契约、D8/选择器独立重载、2000次分层bootstrap、资源监控和总门禁均通过；test未运行。 |

## 2026-08-29 清理暂停R6载荷与新增工程缓存

| 字段 | 内容 |
|---|---|
| 修改原因 | S2-G5-R6已明确暂停且重启必须使用全新运行目录；本次中止过程产生多轮失败准备数据、重复MAT/NumPy缓存、持久化诊断副本和不完整训练checkpoint，不构成R6-A性能证据。 |
| 删除内容 | 删除R6中≥1 MiB的数据、缓存与checkpoint载荷及纯诊断副本；删除当前7个未选`last_*.pth` / `last_*.pt`；删除R4 eager/lazy短训、compat smoke、`s2g4r4_compat`、全局空smoke、`__pycache__`和`.ruff_cache`；连带删除86个空目录。 |
| 保留证据 | R6保留81个JSON/CSV/日志等小型证据文件、15份阶段监控报告，共224,937 B。当前R6仍需的R4 16k CH3数据与lazy cache、Hard-19 D8、manifest、冻结best、R5总报告和锁定test均保持原位。 |
| 空间结果 | 共删除92个文件、57,809,658,089 B，约53.8394 GiB；其中`outputs`由134,884,042,756 B降至77,074,390,111 B。 |
| 算法边界 | 未修改代码、配置、数据语义、冻结模型选择、评价结果或科研结论；中止R6仍不得作为性能证据。 |
| 验证 | 清理后`last_*.pth` / `last_*.pt`为0，R6内≥1 MiB文件为0；逐项确认R4 CH3 16k输入/cache/best、R4 Hard-D8数据/manifest/best、R5总报告和锁定test仍存在，并执行`git diff --check`。 |

## 2026-08-29 完成S2-G5-R6-A多seed稳定性确认

| 字段 | 内容 |
|---|---|
| 修改原因 | 需要严格复用R4冻结输入，对当前16k CH3、8k D8和R5候选K机制进行三seed稳定性确认，同时避免旧R6入口引入私有副本、缓存重建或test读取。 |
| 代码修改 | 增量完善`第三章代码/s2g5_r6_train_ch3.py`和`第四章代码/s2g5r6_multiseed.py`，加入冻结输入/manifest/每K数量门禁、初始化哈希、真实CUDA pilot及训练前后SHA复核；新增`第四章代码/s2g5r6_evaluate.py`，只在validation上完成三条主系统和3×3交叉组合，并在精简摘要中显式汇总匹配RMSE与匹配覆盖率。`train_yolo.py`仅补齐R6严格入口的初始validation行为。 |
| 自动修复 | 静态检查发现新评价入口有一个未使用的`math`导入，直接删除后重新检查通过；未修改模型、loss、数据、seed、训练配置或评价规则。 |
| 执行与证据 | 在全新目录`outputs/s2g5r6/20260829_170441`顺序完成四次训练和全部R6-A validation评价；判定`R6A_MIXED`并按方案停止，精确事实见`SYS-S2G5-R6A-20260829`。 |
| 科研边界 | 未启动MATLAB、数据/DPD生成、缓存重建、R6-B至R6-E或test；历史失败目录未作为通过证据。 |
| 文档修改 | 第二阶段第23.8节补充GOSPA、匹配RMSE及覆盖率结论；同步更新`实验结果记录.md`、`当前任务.md`和本记录，不写工程排错流水。 |
| 验证 | 相关Python文件`py_compile`和Ruff F/E9通过；冻结SHA、样本分层、三seed初始化、CH3/D8 CUDA pilot、配置同一性、资源监控、checkpoint、主对角及3×3结果门禁全部通过。 |

## 2026-08-30 实施S2-G5-R6-B0论文控制条件诊断

| 字段 | 内容 |
|---|---|
| 修改原因 | R6-A为`MIXED`，需要在不重训模型、不读取test的前提下，先判断当前三套8k D8与论文第四章4A/4B控制结果的实际差距。 |
| 代码修改 | 新增`第四章代码/s2g5r6b0_generate_control.m`生成12个论文控制条件的独立IQ；新增`第四章代码/s2g5r6b0_paper_conditions.py`完成冻结权重/训练支持域预检、IQ审计、Exact/Hard细DPD、三seed oracle-K评价、样本bootstrap和11类统计谬误检查。扩展分支只增量支持基础与512–999分片按样本索引合并。 |
| 自动修复 | 首次MATLAB pilot因新函数目录未加入路径而停止；只修正调用命令的`addpath`和日志解码，在全新pilot目录重跑。扩展评价后只修正“已经达到1000条上限仍建议继续扩展”的报告字段语义；均未改变模型、数据条件、DPD、指标或判定门。 |
| 执行与证据 | 在`outputs/s2g5r6/20260830_132057`生成12,000条IQ和15,216个Exact/Hard细DPD任务，完成三套D8正式评价及三个歧义锚点扩展；最终判定`PAPER_CONDITION_MIXED`，精确事实见`CH4-S2G5-R6B0-20260830`。 |
| 科研边界 | 未训练模型、未运行CH3、未构建或读取冻结test、未选择checkpoint，也未把12个锚点解释为完整4A–4D复现。 |
| 文档修改 | 第二阶段第23章新增R6-B0方案与结论；同步更新`实验结果记录.md`、`当前任务.md`、`对话上下文.md`、`研究总结.md`和本记录。 |
| 验证 | Python `py_compile`、Ruff F/E9、MATLAB Code Analyzer、CUDA pilot、12,000条IQ审计、索引连续性、两次扩展重载科学字段一致性及全部正式监控门通过；有效阶段0 warning/0 red flag。 |

## 2026-08-31 建立E2E工作树只读参考与独立输出

| 字段 | 内容 |
|---|---|
| 修改原因 | 新工作树不包含被Git忽略的原`outputs`，同时必须避免统一模型实验回写原研究证据。 |
| 代码修改 | E2E默认输出改为`outputs_e2e`；Python/MATLAB运行层新增显式只读参考根和根目录分离门禁；新增统一模型路径、8个冻结模型/选择器注册及只读预检入口。历史Gate脚本未批量修改。 |
| 规则与文档 | `AGENTS.md`简要增加三条路径隔离规则；运行说明、当前任务和上下文同步为`main-e2e`研究线。 |
| 算法边界 | 未修改CH3/D8结构、loss、数据、checkpoint、评价规则或科研结论；未加载模型、训练或读取冻结test。 |
| 验证 | Python `py_compile`、Ruff F/E9、MATLAB Code Analyzer通过；Python预检核对三套CH3、三套D8和R5两个选择器的大小/SHA；Python与MATLAB均拒绝同根配置，smoke只写入`outputs_e2e`。预检报告位于`outputs_e2e/smoke/unified/reference_preflight/20260831_path_isolation.json`。 |

## 2026-08-31 初始化统一端到端模型研究路线

| 字段 | 内容 |
|---|---|
| 修改原因 | E2E工作树需要先明确“真正端到端”的判据、现有障碍、候选架构和逐级验证路线，再决定是否实施模型改造。 |
| 文档修改 | 新增`统一端到端模型研究.md`，参照前两阶段文档组织目标、非目标、证据基线、研究问题、文献地图、候选架构、`E2E-G0...G6`方案卡、指标和停止规则；同步当前任务与恢复入口。 |
| 资料范围 | 只读复核项目文档和关键源码，提取师弟论文65条参考文献，检索Zotero本地库并核对最相近端到端被动定位论文全文，再以公开原始论文补齐集合输出、可微选择和多任务优化。 |
| 研究边界 | 临时Gate编号不代表原主线阶段切换；软频带桥、集合解码和loss方案均为待审批候选，未运行实验或形成性能结论。 |
| 验证 | Markdown结构、参考文献编号与链接、长期文档去重、UTF-8读取和Git差异检查；未执行正式训练或评估。 |

## 2026-08-31 确认统一模型路线优先级并细化执行方案

| 字段 | 内容 |
|---|---|
| 修改原因 | 用户确认统一模型同时输出CH3与CH4结果，并批准以原级联语义下的联合训练为首要研究路线。 |
| 路线决定 | 首要路线为训练软连接、推理硬级联；双编码器＋逐源中期融合作为次要路线；`19×401×401`频率分辨细DPD因资源代价暂时搁置。 |
| 方案细化 | 补充训练/推理双图、连续基数、K=0/1/2/3契约、`detach`因果对照、计划文件边界、G3硬推理闭环和路线转换停止条件。 |
| 消融边界 | CH4空间计数头不进入基础模型和G0—G4，仅在定位反馈成立但K仍稳定受限时作为G5条件消融。 |
| 验证边界 | 只修改研究与长期状态文档；未修改模型代码、loss、数据或训练入口，未运行实验。 |

## 2026-08-31 将首要路线修订为Soft内部同图并压缩Gate

| 字段 | 内容 |
|---|---|
| 修改原因 | 用户确认推理内部无需恢复Hard级联，只需最终输出为Hard，并要求缩短首要路线的可行性验证周期。 |
| 路线修订 | 训练与推理统一使用连续频带桥、在线细DPD和D8；硬频带、整数K与位置集合只在最终解码生成。历史Hard/Hard级联保留为基线，不再作为目标部署图。 |
| Gate重排 | 将原G0—G6压缩为G0连续桥/冻结前向/资源预检、G1单seed因果训练、G2多seed长尾与消融、G3冻结test；补齐G0样本、容差、资源停止线、报告与失败状态。 |
| 边界 | G0不训练、不读取test、不增加CH4空间计数头；双编码器仍为次要路线，19通道细DPD继续搁置。 |
| 验证 | 仅修改研究与长期状态文档并核对当前设备基线；未修改模型代码、loss、数据或训练入口，未运行DPD、训练或评价。 |

## 2026-08-31 实施E2E-G0连续桥与完整梯度审计

| 字段 | 内容 |
|---|---|
| 修改原因 | 用户批准验证Soft内部训练/推理、Hard最终解码首要路线在401×401在线细DPD下的数值、梯度、解码和资源可行性。 |
| 代码修改 | 新增固定子带—FFT连续桥、固定支持可微细DPD、Hard最终解码器、梯度审计和G0编排入口；历史第三/四章实现及原`outputs`未修改。 |
| 工程修复 | 首次manifest的无定义距离由非JSON `inf`改为`null`；二值等价夹具经用户批准改为DPD相对误差＋Hard峰值/坐标判据，避免把固定支持与动态支持的浮点求和顺序误判为算法差异。失败目录全部保留。 |
| 执行结果 | 正式目录`outputs_e2e/smoke/unified/e2e_g0/20260831_235442`通过二值、稳定性、算子梯度、解码、资源和确定性门，但完整系统有限差分与autograd异号，判定`STOP_GRADIENT`；精确事实见`E2E-G0-20260831`。 |
| 文档修改 | `统一端到端模型研究.md`整体清理G0待审批/未验证表述并冻结停止结论；同步当前任务、上下文、研究总结、实验结果和本记录。 |
| 验证边界 | 未训练、未读取test；16样本冻结前向因预注册停止顺序未执行。G1未解锁，未自动平滑特征值、替换loss或切换次要路线。 |

## 2026-09-01 实施E2E-G0D梯度根因诊断

| 字段 | 内容 |
|---|---|
| 修改原因 | 用户批准在G1前插入G0D，尽快判断原G0完整系统梯度异号是否否决首要路线。 |
| 代码修改 | 可微DPD增加默认兼容的checkpoint模式和float32/float64诊断接口；新增方向导数阶梯及G0D的FP32、FP64、混合精度和full-double诊断入口。默认G0算法行为、D8结构和loss定义不变。 |
| 执行结果 | checkpoint一致；FP64物理链15/15通过；混合阶梯首次不可判定层为D8 Heatmap logits；full-double完整D8/loss链7/7通过。唯一判定为`IMPLEMENTATION_FIX`，精确事实见`E2E-G0D-20260901`。 |
| 文档同步 | G0D方案位于`统一端到端模型研究.md`的G0与G1之间，并已回填结论；同步当前任务、上下文、研究总结、实验记录和本记录。 |
| 验证边界 | 未训练、未读取test、未更换最大特征值、D8或loss；G1仍锁定，精度/审计修复及重跑G0尚待审批。 |

## 2026-09-01 实施E2E-G0-R1分段混合精度重验

| 字段 | 内容 |
|---|---|
| 修改原因 | 用户批准按“CH3/D8 FP32＋在线物理链FP64/complex128”第一版合同重验首要路线，并将方案和结果回填G0D章节。 |
| 代码修改 | G0入口增加默认不变的显式精度合同；新合同在频带概率后转FP64，DPD/log1p/z-score保持FP64，进入D8前转回FP32；新增FP64物理链多方向与full-double整链抽查，替代已证实失真的FP32整链有限差分。 |
| 工程修复 | 首次正式尝试在冻结前向暴露19维硬子带掩码直接传入4096维FFT接口；使用既有冻结子带—FFT覆盖矩阵展开支持域，未改变算法、数据、loss或Gate，并从全新目录完整重跑。 |
| 执行结果 | 有效运行七项门禁全部通过，判定`G0_PASS`并解锁G1方案审批；精确事实见`E2E-G0-R1-20260901`。失败目录保留且不计入PASS。 |
| 文档同步 | G0D第12.7节回填方案和结论；当前任务、上下文、研究总结、实验记录和本记录同步至G1方案待审批。 |
| 验证边界 | 未训练、未读取test、未增加空间计数头；通过只证明数值、梯度和资源可行，不证明端到端收益。 |

## 2026-09-01 完成E2E-G1并增加参考输入完整性防护

| 字段 | 内容 |
|---|---|
| 修改原因 | G1训练和评价已完成，但结束SHA门禁遇到普通读取漂移及HDF5 gzip解码失败；用户批准不重训恢复、根因排查和长期防护。 |
| 代码修改 | 新增无缓冲参考快照入口及阶段前后`--verify-only`复核；恢复入口只在冻结输入及既有训练/评价证据完整时生成final report。原参考目录不写入。 |
| 根因边界 | 无缓冲副本匹配冻结SHA且随后普通读取恢复，只能确认读取路径曾不一致；具体软件环节尚未唯一定位，不直接称为文件损坏或优先归因硬件。 |
| 文档同步 | 简明更新`AGENTS.md`和`运行基础说明.md`的快照与分级门禁；研究方案、当前任务、上下文、总结和实验记录同步至`G1_NO_GO`及无需重训恢复边界。 |
| 验证 | 4个G1/恢复Python入口通过`py_compile`与Ruff F/E9；配置JSON可解析；9项快照首次验证和`--verify-only`复核均PASS；`git diff --check`通过。未重跑训练、未读取test。 |

## 2026-09-02 增加文件字节与HDF5逻辑身份分级诊断

| 字段 | 内容 |
|---|---|
| 修改原因 | 单次整文件SHA漂移不能直接区分文件永久变化、读取流不稳定与HDF5容器重打包，需要避免再次误判为数据文件损坏。 |
| 代码修改 | 新增`classify_data_integrity.py`：候选文件先重复计算SHA；精确匹配立即结束；只有稳定字节漂移才按需解码科研dataset并计算逻辑身份。报告只允许写入E2E输出根。 |
| 开销边界 | 正常9项G1快照复核实测约6.02 s；499 MB真实MAT精确匹配约0.93 s且不做逻辑扫描。强制完整逻辑比较约16.54 s，仅用于异常诊断。 |
| 工程修复 | 首次真实MAT强制逻辑测试把“文件和逻辑均相同”误标为容器漂移；已最小修正为`EXACT_FILE_AND_LOGICAL_MATCH`，初版报告保留为失败smoke证据，v2报告通过。 |
| 文档修改 | 根因统一改为`READ_PATH_INCONSISTENCY`且具体软件环节待定位；补记原项目相似事件为用户反馈待复核，并明确当前恢复的是完整原定G1而非缩水补救。 |
| 验证 | Python语法、Ruff F/E9、CLI help、精确匹配、容器漂移但逻辑相同、科研数值变化及真实MAT强制逻辑比较通过；未修改G1冻结结果或恢复脚本身份。 |

## 2026-09-02 完成G1原CH3硬计数重解码诊断

| 字段 | 内容 |
|---|---|
| 修改原因 | 用户批准不重训地检验第一版Poisson-binomial Soft计数是否造成G1绝对性能退化。 |
| 代码修改 | 新增`diagnose_g1_hard_count_decode.py`，复用G1保存的稳定有序Top-K位置并按`hard_count`截断；读取本地快照真值，登记源checkpoint/评价身份，执行阶段前后快照复核和配对bootstrap。 |
| 工程修复 | 前两次启动分别因项目根未加入`sys.path`和未显式设置冻结参考根而在导入阶段退出，均未创建输出或读取数据；最小修复入口后使用全新run-id完成有效运行。 |
| 执行结果 | 有效目录`outputs_e2e/unified/e2e_g1_hard_count_decode/20260902_143158`；两轨GOSPA恢复至`18.343600/18.373396 m`，确认第一版Soft计数为绝对退化主因，但E2E仍不优于SG，原`G1_NO_GO`不变。精确事实见`E2E-G1-D1-20260902`。 |
| 文档同步 | 在`统一端到端模型研究.md` G1章节增加G1-D1方案、结果和边界；同步当前任务、上下文、研究总结、实验记录和本记录。 |
| 验证 | 诊断入口通过`py_compile`和Ruff F/E9；512/512样本前缀合同、独立均值复算、2000次bootstrap、9项快照前后身份及11/11统计谬误审查通过；未重训、未做新模型推理、未读取test。 |

## 2026-09-02 完成G1-R1语义正确槽位存在修正版Gate

| 字段 | 内容 |
|---|---|
| 修改原因 | 用户批准以max-logit槽位存在、直接槽位BCE和Hard最终K正式修复第一版Soft计数语义，并重新检验定位反馈收益。 |
| 代码修改 | `band_bridge.py`增加可选`max_logit`存在模式；`e2e_g1.py`参数化Gate、快照清单、槽位BCE和Hard最终解码；新增`e2e_g1_r1.py`与冻结配置。旧Noisy-OR路径保留，原G1证据目录未覆盖。 |
| 工程修复 | 首次prepare因包装入口未把项目根加入`sys.path`而在导入阶段退出，未创建输出；最小修复后使用新run-id执行。另一次轻量导入因终端未显式设置只读参考根退出，补齐环境变量后从头验证，均未产生实验结果。 |
| 执行结果 | 有效目录`outputs_e2e/unified/e2e_g1_r1/20260902_153159`；P0/P1通过，两轨完整训练和三轨评价完成。Soft-SG恢复Hard同量级，但SG/E2E均选择epoch 0且正式差值为0，判定`G1_R1_NO_GO`并停止当时预写的多seed扩展。精确事实见`E2E-G1-R1-20260902`。 |
| 文档同步 | G1章节回填方案、结果和停止边界；同步当前任务、上下文、研究总结、实验记录和本记录。 |
| 验证 | 三个Python文件通过`py_compile`与Ruff F/E9；7项合成语义合同、16样本两轨全链路、512步梯度、三轨512条评价、两条oracle、2000次bootstrap和输入身份复核完成；未读取test。 |

## 2026-09-03 更新E2E-G2逐源潜在融合方案

| 字段 | 内容 |
|---|---|
| 修改原因 | G1-R1已否定旧在线物理反馈接口，但不否定其他端到端结构；原实现文档仍把双编码器逐源融合写为次要备选，并占用G2/G3记录未执行的旧后续计划。 |
| 文档修改 | 保留E2E-G0/G1及其历史实验身份；当前E2E-G2更新为“逐源query＋频率—空间潜在拆分＋固定细DPD单次D8编码＋逐源Heatmap/offset”；原预写G2/G3合并到一个一级标题并标记为已作废历史计划。 |
| Gate方案 | E2E-G2采用单Gate内P0张量等价、P1固定输入兼容/测速、P2 32样本强过拟合、P3 256样本SG/E2E因果比较；任一步失败即停止。 |
| 状态边界 | 同步实现文档、当前任务、对话上下文和研究总结；未修改模型代码、生成数据、训练或读取test。 |
| 验证 | `git diff --check`通过；标题、路线状态、输入输出契约、Gate编号、历史计划归档和禁止范围完成全文检索。 |

## 2026-09-03 实施E2E-G2并停止于P2表示能力门

| 字段 | 内容 |
|---|---|
| 修改原因 | 用户批准执行E2E-G2，需要实现张量接口、固定全频DPD缓存、逐源潜在融合和顺序Gate审计。 |
| 代码修改 | 新增`models/e2e_latent_fusion.py`、`audits/e2e_g2_preflight.py`、`e2e_g2_latent_fusion.py`及配置；提取CH3/D8中间张量，实现source query、Soft频带空间拆分、FiLM融合、逐源Heatmap/offset、集合匹配、四项loss和SG/E2E梯度审计。原CH3/D8源码与冻结checkpoint未修改。 |
| 工程恢复 | 首次prepare因5秒工具超时退出；随后两次因参考根导入顺序/嵌套安全检查在创建目录前退出。修正为导入阶段显式只读原`outputs`、运行阶段切换本地快照后完成。`20260903_153008`因P0/P1审计字段不全被保留为先导证据，补齐K覆盖、batch和缓存身份后在新目录正式重跑。 |
| 执行结果 | 有效目录`outputs_e2e/unified/e2e_g2_latent_fusion/20260903_153441`。P0/P1通过；P2计数/频带与梯度路径通过，但逐源Recall@10m最高约64.6%，判定`G2_NO_GO_REPRESENTATION`，P3按计划未执行。精确事实见`E2E-G2-20260903`。 |
| 文档同步 | 更新实现文档第14章、当前任务、对话上下文、研究总结、实验结果记录和本记录；未修改建议文档或AGENTS.md。 |
| 验证 | 三个新增Python文件通过`py_compile`与Ruff F/E9；P0覆盖四种K和batch=4且旧新旁路误差为0；P1完成32条样本的两类输入比较与固定全频缓存身份审计；P2完成32张缓存、120 epoch/960步、梯度阻断审计及冻结D8同输入诊断；未读取test。 |

## 2026-09-03 实施E2E-G2-R1混杂消解与能力恢复门

| 字段 | 内容 |
|---|---|
| 修改原因 | 原G2-P2失败同时受到Heatmap loss、正源梯度审计、优化时长和best记录问题影响，不能据此直接重构source splitting。 |
| 代码修改 | 新增`统一模型代码/gates/g2/e2e_g2_r1.py`及`统一模型代码/configs/e2e_g2_r1.json`，复用原G2模型和缓存，实现同样本输入对照、正源分项梯度、原D8 Focal loss、分段学习率、真实best保存及顺序停止。 |
| 工程修复 | Ruff发现一处未使用局部变量，按低风险工程规则最小删除后再执行；未改变模型、loss、数据、seed或门禁。 |
| 执行结果 | 有效目录`outputs_e2e/unified/e2e_g2_r1/20260903_183506`。P0通过；P1完成300 epoch并判定`G2_R1_SPLIT_PASS_FINE_FAIL`。由于30 m粗分离已通过，条件P2按方案不执行。精确事实见`E2E-G2-R1-20260903`。 |
| 文档同步 | 更新实现文档第14.8节、当前任务、对话上下文、研究总结、实验结果记录和本记录；未修改建议文档或`AGENTS.md`。 |
| 验证 | 新入口通过`py_compile`与Ruff F/E9；P0/P1/final结构化报告及best checkpoint已回读，finalize确认参考G2输入身份未变化；完成11/11统计谬误扫描，未读取test。 |

## 2026-09-04 制定E2E-G2-R2定位反馈配对方案

| 字段 | 内容 |
|---|---|
| 修改原因 | 复核确认95% Recall@10m是未按既有D8能力校准的强记忆门；当前R1已达到同样本D8同量级，不应先修Heatmap/offset而跳过核心反馈问题。 |
| 文档修改 | `统一端到端模型研究.md`新增第14.9节，冻结`FS-SG/FS-E2E`唯一变量、256/128/512数据职责、约2432步预算、选模/配对bootstrap、判定和资源线；补充分阶段解冻条件。`当前任务.md`转为R2方案待审批。 |
| 算法边界 | 本次只形成方案；未修改代码、模型、loss、数据、缓存或实验结果，未读取test。 |
| 验证 | 检索并纠正“先修10 m精定位”的过时状态；检查R2编号、样本互斥、前向等价、冻结/解冻条件、判定分支和禁止范围一致。 |

## 2026-09-04 实施E2E-G2-R2定位反馈配对门

| 字段 | 内容 |
|---|---|
| 修改原因 | 直接检验相同前向结构中，定位loss进入CH3/query是否比stop-gradient带来独立validation收益。 |
| 代码修改 | 新增`统一模型代码/gates/g2/e2e_g2_r2.py`和`configs/e2e_g2_r2.json`，实现固定缓存、前向/梯度等价审计、SG/E2E同初始化训练、独立选模、512条配对bootstrap及finalize。 |
| 工程修复 | 首次P1在缓存完成后因既有梯度函数调用少传两个参数停止；最小修复后创建新运行目录，登记并复制已核验缓存，逐张复核后继续，未重算DPD或复用失败训练。另固定第三/第四章同名`chapter_runtime`的导入身份。 |
| 算法边界 | 未修改G2-R1网络、四项loss、样本划分、seed、冻结范围或判定阈值；未读取test。 |
| 执行结果 | 定位GOSPA和Recall@100m明确改善，但band F1/IoU超过1个百分点退化护栏，判定`G2_R2_AUX_REGRESSION`；精确事实见`E2E-G2-R2-20260904`。 |
| 文档同步 | 更新实现文档第14.9节、当前任务、对话上下文、研究总结、实验结果记录和本记录；未修改建议文档或`AGENTS.md`。 |
| 验证 | `py_compile`与Ruff F/E9通过；P0/P1、两轨2432步训练、512条冻结比较、2000次bootstrap、finalize和11/11统计谬误扫描完成；源身份未变化。 |

## 2026-09-04 实施E2E-G2-R2-D1辅助退化诊断

| 字段 | 内容 |
|---|---|
| 修改原因 | 判断R2频带护栏轻微越界来自联合匹配、阈值校准、真实Hard判决变化还是普遍梯度冲突。 |
| 代码修改 | 新增`统一模型代码/gates/g2/e2e_g2_r2_d1.py`及对应配置，复用R2证据实现三种匹配、配对bootstrap、`val_select`阈值冻结应用和64条条件梯度审计。 |
| 算法边界 | 无optimizer、无权重更新，不修改R2模型、loss、阈值或原判定；不读取test。 |
| 执行结果 | 排除匹配假象与简单阈值偏移；Hard频带下降集中于重叠K=2/3，但BCE改善且实质损害相对1个百分点容限不确定。普遍梯度冲突未获支持。精确事实见`E2E-G2-R2-D1-20260904`。 |
| 文档同步 | 更新实现文档第14.10节、当前任务、对话上下文、研究总结、实验结果记录和本记录；未修改建议文档或`AGENTS.md`。 |
| 验证 | 新入口通过`py_compile`、Ruff F/E9；128/512条诊断、2000次配对bootstrap和64条梯度审计完成；结构化证据、源SHA、零更新及test冻结复核通过。 |

## 2026-09-05 实施E2E-G3跨seed稳定性门

| 字段 | 内容 |
|---|---|
| 修改原因 | 验证R2定位反馈收益和Hard频带轻微下降能否跨独立training seed复现，并避免38 epoch预算偏紧造成误判。 |
| 代码修改 | 新增`统一模型代码/gates/g3/e2e_g3.py`及配置；只读复用R2固定细DPD，支持两个预注册seed的配对训练、38→60成对延长、完整续训状态、band-only统计和三类辅助判定。 |
| 算法边界 | 不改模型、冻结范围、loss、数据角色、解码或R2历史判定；不读取test。38/60规则、seed和分流在运行前写入实现文档。 |
| 工程恢复 | 延长首次加载把CPU RNG状态映射到GPU，在optimizer step前停止；最小修复为CPU加载，并新增受限恢复入口和修复身份收据，未复用失败延长更新。 |
| 执行结果 | 两个新seed均复现总体定位改善，主判定`G3_FEEDBACK_STABLE_PASS`；Hard频带点估计重复下降但实质边界不确定，判定`AUX_UNCERTAIN`；一条seed两轨best均在60，保留`TRAINING_BUDGET_UNRESOLVED`。精确事实见`E2E-G3-20260905`。 |
| 文档同步 | 更新实现文档第14.11节、当前任务、对话上下文、研究总结、实验结果记录和本记录；未修改建议文档或`AGENTS.md`。 |
| 验证 | 新入口通过`py_compile`与Ruff F/E9；P0缓存/公平检查、四轨训练、一组成对延长、512条冻结比较、配对bootstrap、finalize和11/11统计谬误扫描完成；源身份未变化。 |

## 2026-09-05 补充E2E历史RMSE评价

| 字段 | 内容 |
|---|---|
| 修改原因 | G2-R2与G3正式汇报缺少RMSE和匹配覆盖率，无法判断GOSPA收益是否伴随已匹配位置精度改善。 |
| 代码修改 | 新增`统一模型代码/gates/g3/backfill_e2e_rmse.py`，从冻结逐样本预测和本地validation快照补算matched RMSE、分位数、coverage、K/重叠分层及整样本配对bootstrap。 |
| 算法边界 | 不重训、不重新推理、不覆盖历史报告，不读取test；RMSE不替代GOSPA主判定。 |
| 执行结果 | 三组E2E RMSE点估计均低于SG，但仅G3 seed `20260905`区间排除0；长尾极重。精确事实见`E2E-RMSE-BACKFILL-20260905`。 |
| 验证 | `py_compile`、Ruff F/E9、逐样本身份核对及2000次分层配对bootstrap通过。 |

## 2026-09-05至09-06 实施E2E-G4扩大数据与分组解冻选择门

| 字段 | 内容 |
|---|---|
| 修改原因 | 在不拆分过多Gate的前提下，验证扩大数据后的A0基准，并选择是否解冻D8尾部或进一步解冻CH3语义层。 |
| 代码修改 | 新增`统一模型代码/gates/g4/e2e_g4.py`及配置，实现冻结母集抽样、固定细DPD、A0/A1/A2筛选、双seed确认、16→24→32预算规则、完整定位指标与配对bootstrap；新增RAM-only恢复入口及G4缓存/终局审计工具。 |
| 完整性修复 | 大型旧快照逻辑读取失败，单文件及16条分片特征缓存均出现SHA漂移；保留并隔离全部磁盘缓存训练，最终从登记粗DPD和1792个SHA匹配的逐样本细DPD重建RAM特征，在单进程`training_v4`全新训练。另修正P0冷启动投影和finalize读取旧P0的报告错误；科研合同未变。 |
| 执行结果 | A2通过首seed筛选，并在两个seed上相对A0同向改善GOSPA/RMSE且通过Recall、coverage与RMSE护栏，正式判定`G4_PARTIAL_UNFREEZE_SELECTED`。精确事实见`E2E-G4-20260905`。 |
| 文档同步 | 更新实现文档第14.12节、当前任务、对话上下文、研究总结、实验结果记录和本记录；未修改建议文档或`AGENTS.md`。 |
| 验证 | 两次RAM重建摘要一致；11段有效训练报告、两个seed的512条评价和2000次bootstrap完成；27个最终小型证据文件连续三次SHA稳定，终局审计PASS；入口通过`py_compile`和Ruff F/E9，test未读取。 |

## 近期重要修改索引

| 日期 | 事件 | 结果 |
|---|---|---|
| 2026-08-20 | 长期文档体系与静态接管审查 | 文档体系和第一阶段详细审查建立 |
| 2026-08-20 | Gate 0 | 运行基础改造通过 |
| 2026-08-21 | Gate 1 | 第三章最小工程闭环通过 |
| 2026-08-21 | Gate 2 | 第四章最小工程闭环通过 |
| 2026-08-21 | Gate 3A | 中等规模数据与短训练可重复性通过，见 `CH4-G3A-20260821` |
| 2026-08-21 | Gate 3B | 缩减数据完整训练通过并建议延长，见 `CH4-G3B-20260821` |
| 2026-08-21 | 文档治理与阶段切换 | 第一阶段冻结，第二阶段S2-G0方案待审批 |
| 2026-08-21 | S2-G0最小逻辑封存 | 复用现有代码提交和Gate 3A/3B实验ID，第二阶段转入S2-G1方案待审批 |
| 2026-08-21 | S2-G1增量入口与首次数据审计 | 原入口兼容；首次数据因1条源间距违规在训练前停止，见 `CH3-S2G1-20260821-A` |
| 2026-08-22 | S2-G1源间距后验门禁与完整重跑 | 受控路径最小修复完成，第三章缩减数据收敛和严格重复通过，见 `CH3-S2G1-20260822` |
| 2026-08-22 | S2-G2充分训练诊断与工程防错规则 | 200 epoch趋势关闭，新接口checkpoint冻结，总门禁通过，见`CH4-S2G2-20260822` |
| 2026-08-24 | 运行证据瘦身 | 删除无效MAT、逐字节重复载荷和工程缓存，保留报告、日志、唯一有效副本与冻结best权重 |
| 2026-08-24 | S2-G3跨章可组合性审查 | 工程链通过；第三章模型门通过、19子带二值表示门失败，见`SYS-S2G3-20260824` |
| 2026-08-24 | S2-G4 Coarse-D8适配数据门 | 预检/pilot通过；19子带正标签不支持全部窄带源，按计划停止，见`SYS-S2G4-20260824` |
| 2026-08-24 | S2-G4-R1 0.2阈值快速适配 | 逐源支持闭合但D8重训仅恢复15.86% RMSE缺口，见`SYS-S2G4-R1-20260824` |
| 2026-08-25 | 第二阶段文档系统重构 | 同步当前路线，统一Gate方案/结论写法，并明确Soft-19、连续边界、Hard-39/79候选接口 |
| 2026-08-25 | S2-G4-R2 Soft-19公平诊断 | Hard-19-Actual达到候选水平，Soft-19无额外收益；R1物理频带定义混杂得到纠正 |
| 2026-08-25 | R2解释与监控器增强 | 补充Soft/Hard机制边界；5秒采样自动提升为10秒，并支持无硬超时和摘要控制台模式 |
| 2026-08-25 | S2-G4-R3三seed配对确认 | Hard成为稳定oracle候选；Soft收益不稳定，不再优先 |
| 2026-08-26 | S2-G4-R4-A至R4-D | 新共享IQ、嵌套manifest和三表示DPD完成；PyCharm首项训练入口待用户点击 |
| 2026-08-26 | S2-G4-R4-E至R4-F | 九组学习曲线与统一分析完成；Hard-19接口充分性得到支持，Soft路线停止 |
| 2026-08-27 | S2-G4停止载荷与last checkpoint清理 | 删除四个无效或一次性目录及29个未选`last` checkpoint，保留`best`与结构化证据 |
| 2026-08-27 | S2-G5-R1统一数据与接口契约 | K=0/1/2/3统一IQ、Hard-19-Actual标签、分流和CH3消费检查通过 |
| 2026-08-27 | S2-G5-R2嵌套规模诊断 | 1k/4k/8k训练与配对分析通过，8k仍显著受益，判定`FREEZE_8K_FOR_R3` |
| 2026-08-28 | S2-G5-R3四轨级联误差隔离 | 工程闭环通过；直接级联尚不满足等价标准，后续判定`COUNT_PATH_PRIORITY` |
| 2026-08-28 | S2-G5-R4长训练前数据与容量门 | 新增8k和严格嵌套16k通过；eager Dataset触发RAM红线，长训练未启动 |
| 2026-08-28 | S2-G5-R4懒加载等价与资源门 | 抽样、增强和1k短训严格等价；16k容量/吞吐通过，正式训练入口已解锁 |
| 2026-08-28 | S2-G5-R4正式训练与K路径复核 | 16k未消除K瓶颈；候选K上限恢复约66%，判定进入候选K Gate |
| 2026-08-28 | S2-G5-R5候选K联合判决 | 冻结模型后处理通过直接级联门；按停止规则取消R5-B长训练 |
| 2026-08-29 | 暂停R6载荷与新增工程缓存清理 | 删除中止半成品、7个未选last和工程smoke/cache，释放约53.84 GiB并保留小型故障证据 |
| 2026-08-29 | S2-G5-R6-A三seed稳定性确认 | 3/3主seed改善原始级联、仅1/3通过严格直接门，判定`R6A_MIXED`并停止在R6-B与test前 |
| 2026-08-30 | S2-G5-R6-B0论文条件诊断 | Exact在8/12锚点兼容论文，中等SNR与K=2距离对齐，低SNR及K=3距离长尾未对齐，判定`PAPER_CONDITION_MIXED` |
| 2026-08-31 | E2E工作树路径隔离 | 原`outputs`只读参考、`outputs_e2e`独立写入及冻结产物预检通过 |
| 2026-08-31 | 统一端到端模型研究初始化 | 明确端到端定义、第一候选与E2E-G0至E2E-G6临时路线，全部待审批 |
| 2026-08-31 | 统一模型路线优先级确认 | 训练软连接/推理硬级联为首要路线，双编码器为次要路线，19通道细DPD暂缓 |
| 2026-08-31 | Soft内部同图与Gate压缩 | 首要路线改为Soft内部训练/推理、Hard最终解码；当前仅G0实施待审批 |
| 2026-08-31 | E2E-G0连续桥与梯度审计 | 工程与资源门通过，完整系统梯度方向不一致，判定`STOP_GRADIENT`并锁定G1 |
| 2026-09-01 | E2E-G0D梯度根因诊断 | 排除checkpoint与物理DPD梯度阻塞，定位FP32整链有限差分分辨率问题，判定`IMPLEMENTATION_FIX` |
| 2026-09-01 | E2E-G0-R1分段混合精度重验 | 七项G0门禁全部通过，判定`G0_PASS`并解锁G1准确方案审批 |
| 2026-09-01 | E2E-G1执行、无缓冲恢复与长期防护 | 完成配对训练和恢复性finalize，新增参考快照/复核入口与简明数据完整性规则；判定`G1_NO_GO`并停止当时预写的多seedG2，未重跑训练或读取test |
| 2026-09-02 | 双层完整性分级诊断 | 正常快速核对文件字节，只有异常才比较HDF5科研逻辑身份；纠正读取路径异常与G1完整恢复边界 |
| 2026-09-02 | G1硬计数重解码诊断 | 第一版Soft计数故障确认；恢复Hard同量级但定位反馈仍无收益，G1判定不变 |
| 2026-09-02 | G1-R1语义修正版配对Gate | 计数语义修复恢复Hard同量级，但两轨均选择epoch 0，判定`G1_R1_NO_GO`并停止旧在线物理接口 |
| 2026-09-03 | E2E-G2逐源潜在融合方案 | 保留G0/G1历史编号；G2改为当前逐源频率—空间潜在融合，原预写G2/G3合并作废 |
| 2026-09-03 | E2E-G2执行 | P0/P1通过，P2逐源表示强过拟合失败，判定`G2_NO_GO_REPRESENTATION`并不执行P3 |
| 2026-09-03 | E2E-G2-R1执行 | 训练合同修正后30 m粗分离通过、10 m精定位未通过，判定`G2_R1_SPLIT_PASS_FINE_FAIL`并停止条件P2/P3 |
| 2026-09-04 | E2E-G2-R2方案 | 以基线相对能力替代未校准的95%@10m准入门，制定`FS-SG/FS-E2E`定位反馈配对因果方案待审批 |
| 2026-09-04 | E2E-G2-R2执行 | 定位反馈明确改善GOSPA/Recall，但频带辅助护栏轻微失败，判定`G2_R2_AUX_REGRESSION` |
| 2026-09-04 | E2E-G2-R2-D1诊断 | Hard频带下降真实但实质程度不确定；匹配/单阈值与普遍梯度冲突均非主因 |
| 2026-09-05 | E2E-G3跨seed稳定性门 | 两个新seed均复现总体定位收益；频带风险和60 epoch预算限定继续保留 |
| 2026-09-06 | E2E-G4扩大数据与分组解冻 | 1024条train与双seed比较完成；A2有限解冻两seed同向改善并通过护栏 |

## 2026-09-06 缓存读取路径诊断与异常保全

- 新增`统一模型代码/common/diagnostics/cache_read_path_probe.py`，在少量指定缓存上比较普通读取、只读映射和Windows绕系统缓存读取；记录文件身份、有限的进程快照，异常时保全原始字节并定位差异，不改模型或训练数据。
- 同步实验记录、实现文档、当前任务和上下文，纠正“异常数量增长即持续损坏”的表述；精确诊断事实见`实验结果记录.md`的`E2E-CACHE-READ-20260906`。
- Python语法编译、Ruff `F/E9`、中文子进程输出捕获和差异偏移内存用例通过；一次命令行引号导致的捕获自检语法错误已改用PowerShell单引号here-string解决，发生在正式诊断前，未影响正式输出。未执行训练或模型评价。

## 2026-09-06 训练操作下的有界懒加载诊断

- 新增`统一模型代码/common/diagnostics/cache_training_path_probe.py`，复用G4 A2前向/loss/optimizer合同，在隔离目录比较只读映射与256 MiB分片校验LRU；记录实际消费的数据摘要，执行诊断checkpoint保存重载，增加目录事件观察及WPR可用性尝试。
- 仅新增诊断入口，未替换G4正式加载器或改变既有性能证据；方案与认识同步到实现文档、任务和上下文。事实和事件采集权限限制见`实验结果记录.md`的`E2E-CACHE-TRAIN-20260906`。
- 语法编译和Ruff `F/E9`通过，逐步日志及配对模型/optimizer checkpoint独立复核通过；执行的是工程训练操作诊断，非正式性能Gate，未评价模型或读取test。

## 2026-09-07 清理重复与先导缓存

- 用户批准删除`outputs_e2e/unified/e2e_g2_r2/20260904_154608/fixed_fullband_cache`、`outputs_e2e/unified/e2e_g2_latent_fusion/20260903_153008/cache`及工作树内9处`__pycache__`、`.ruff_cache`；共删除11个目标目录、1003个文件，逻辑大小613,113,409 bytes（约584.71 MiB）。
- 删除前确认目标位于当前工作树内且无重解析点，并重新核对失败目录与成功目录的896对DPD缓存SHA256全部一致。删除后确认目标不存在，保留的7个报告/manifest文件及成功目录896个缓存文件共903个文件SHA256前后一致。
- 两个历史目录现仅保留报告与来源记录，已删除的缓存载荷不可再按历史路径直接使用；G2-R2有效缓存继续位于`20260904_163951/fixed_fullband_cache`。历史运行和科学判定不变。
- 未修改源码、模型或实验配置，未触及G4缓存、参考快照及原项目冻结`outputs`；未运行训练或评价。本次仅执行文件清理与完整性核验，无需语法编译或正式实验。

## 2026-09-19 优化协作规则与 Skills 路由

- 按用户批准的规则审查方案修改 `AGENTS.md`：明确已有授权的复用、只读要求优先级与低风险诊断边界；合并重复检查和记录规则，限定预检与文档读取的适用范围。
- 同步优化用户级 scientific-toolkit、ARS、researchwrite、nature-reader、nature-academic-search、research-writing、office-academic、PDF/XLSX 及 OpenAI Docs Skills；收窄触发、支持局部交付、移除固定提问/主张数量要求。ARS 资源清单迁至 `codex/resource-index.md` 按需加载；当前安装的 documents 插件副本改为按编辑批次检查，保留最终逐页验收。用户级和插件文件位于仓库外，插件更新可能覆盖其本地修改。
- 保留冻结数据与输出隔离、阶段身份复核、正式训练授权、科研语义变更审批、环境安装及敏感操作授权、失败证据保留和必要静态/行为验证；未改变代码、算法、数据、模型或实验结论。
- 验证：11 个受影响 Skill 的 `quick_validate.py` 检查通过；迁移清单中的39个具体资源路径及新增路由目标存在。另核对修改前后文本、UTF-8与原换行格式，并检查规则冲突及敏感边界。仅执行文档与规则静态验证，未运行训练、评价或完整 Skill 行为实验。
