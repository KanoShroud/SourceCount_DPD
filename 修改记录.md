# 修改记录

> 本文件记录已经完成且具有项目意义的代码或文档修改。
> 历史状态以当时记录为准；判断当前状态时以 `当前任务.md` 和 `对话上下文.md` 为准。更新本文件本身不触发新的修改记录。

## 2026-08-20 建立长期项目文档体系

| 字段 | 内容 |
|---|---|
| 目标 | 参考既有研究项目的长期文档职责与更新触发方式，为本项目建立可持续维护的任务、上下文、研究总结、修改和实验结果记录体系。 |
| 新增文件 | `当前任务.md`、`对话上下文.md`、`研究总结.md`、`修改记录.md`、`实验结果记录.md` |
| 修改文件 | `AGENTS.md` |
| 核心变化 | 在不迁移旧项目研究结论的前提下，建立五类长期文档；在 `AGENTS.md` 中补充各文档职责、更新触发条件、按需读取方式和实验结果证据规范。 |
| 初始化内容 | 记录现有论文、第三章代码和第四章代码材料；将“读取论文并审查两章代码结构、数据流、完整性及论文一致性”设为当前主线任务。 |
| 证据边界 | 未读取论文正文、未完成代码结构审查、未运行 Python 或 MATLAB 实验；所有方法、指标、完整性与一致性结论保持待核验。 |
| 验证 | 对新增及修改的 Markdown 文件执行 UTF-8 回读、标题结构、相互引用、旧项目专属词和 Git 差异检查。 |
| 实验影响 | 本轮仅修改项目治理文档，不修改研究代码、配置、论文、数据或实验结果，不产生性能证据。 |

## 2026-08-20 完成论文与第三、第四章代码静态接管审查

| 字段 | 内容 |
|---|---|
| 目标 | 完整理解论文研究主线，建立第三、第四章论文—代码—数据—实验映射，并预判本机运行风险。 |
| 新增文件 | `第一阶段_论文与代码接管审查.md` |
| 更新文件 | `当前任务.md`、`对话上下文.md`、`研究总结.md`、`修改记录.md` |
| 核心产物 | 项目研究总图、两章方法逐步解读、入口和调用关系图、论文—代码一致性表、P0/P1/P2 问题清单、磁盘/RAM/VRAM 预算、最小 smoke 门控方案和六讲学习路线。 |
| 关键发现 | 两章核心实现基本齐全；第四章评估使用真实源数而非第三章预测，4A–4D 绘图数据硬编码；现有整库加载和中间数据格式不适配本机资源。 |
| 静态验证 | Word 逐页检查；全部 Python 文件 AST 解析；全部 MATLAB 文件 Code Analyzer；MATLAB 工具箱与关键函数定位；Python 包导入探测；按 shape/dtype 计算资源量。 |
| 验证边界 | 未修改研究源码，未安装依赖，未生成数据，未训练或评估模型；静态结论不构成论文指标复现证据。 |

## 2026-08-20 完成 Gate 0 运行基础改造

| 字段 | 内容 |
|---|---|
| 目标 | 不改变算法，只解决项目路径、GPU 设备、基础依赖、大样本误启动风险和 smoke 输出覆盖风险。 |
| 新增文件 | `runtime_config.py`、`gate0_runtime.m`、第三/第四章 `chapter_runtime.py`、`requirements-gate0.txt`、`运行基础说明.md` |
| 修改范围 | 第三、第四章的 Python 数据、训练、评估和绘图入口；9 个 MATLAB 数据生成入口；`.gitignore`；长期状态文档。 |
| 核心变化 | 默认运行模式改为 `smoke`；正式模式必须显式设置；默认路径统一到项目内 `data/` 与 `outputs/`；GPU 默认且严格校验为 `cuda:0`；所有运行产物按模式、章节和入口隔离；MATLAB 主数据量从 formal 的 40k/5k/5k 降为 smoke 的极小样本；`trim_exp_pt.py` 改为输出精简副本而不原位覆盖。 |
| 环境变化 | 安装 `h5py 3.14.0`、`ultralytics 8.3.191` 及其缺失依赖；因 Pillow 12.1.1 与 torchvision 本地扩展发生 `_imaging` DLL 冲突，将 Pillow 固定为 11.3.0。未重装现有 `torch 2.8.0+cu129`、`torchvision 0.23.0+cu129`。 |
| 验证 | 29/29 Python 文件语法编译通过，29/29 入口完成 `--help` 或导入；Pillow/Matplotlib/torchvision/h5py/ultralytics 组合导入与 `pip check` 通过；检测到 1 张 RTX 4070 Ti SUPER，统一配置解析为 `cuda:0`；MATLAB 13 个核心入口/函数可见，集中配置和 smoke 路径断言通过。 |
| 静态告警边界 | Ruff 的剩余 F 类告警及 MATLAB Code Analyzer 的 21 条提示来自原有未使用变量、星号导入、无占位符 f-string、全局变量和循环扩容等历史代码；Gate 0 不为清理告警而改动算法结构。 |
| 实验影响 | 未生成研究数据、未训练或评估模型、未复现论文指标；本次验收只证明运行基础和入口保护成立。 |

## 2026-08-20 补全论文自学讲义并形成 Gate 1 审批方案

| 字段 | 内容 |
|---|---|
| 目标 | 将原有六讲提纲扩展为可脱离对话独立阅读的论文—公式—代码讲义，并依据 Gate 0 实际结果更新接管报告。 |
| 修改文件 | `第一阶段_论文与代码接管审查.md`、`当前任务.md`、`对话上下文.md`、`研究总结.md`、`修改记录.md` |
| 讲义增量 | 补充四站接收信号模型、路径损耗和频域时延；DPD 相关矩阵推导与代码维度；第三章标签手算、槽位、张量和损失；第四章频率分组、细 DPD、热力图/offset、网络形状、损失、Top-K 与 Hungarian 评价口径。 |
| 状态更新 | 将依赖缺失、Pillow 导入失败、Linux 路径、`cuda:2` 和 Gate 0 待执行等旧状态更新为已关闭；保留缺少数据/权重、整库加载、端到端 K 和图表溯源等未关闭问题。 |
| 新增静态证据 | Gate 0 后在 CPU 上实例化第四章网络：bbox 11.135 M、单热力图 12.777 M、双头 12.786 M 参数。 |
| Gate 1 方案 | 明确审批后的小范围修改、`[8,4,4]` 数据规模、唯一输出根、数据/标签审计、batch 2 单步闭环、资源采样、硬停止条件、输出证据和失败分支。 |
| 验证边界 | 本轮只修改文档并进行模型静态实例化；未执行 MATLAB 数据生成、PyTorch forward/backward、训练或论文指标评估。 |

## 2026-08-21 实施 Gate 1 检查入口并完成首次受控运行

| 字段 | 内容 |
|---|---|
| 目标 | 按已批准方案执行第三章 `[8,4,4]` 极小数据生成、独立标签审计和 batch 2 单步闭环，并严格执行资源门禁。 |
| 代码修改 | `第三章代码/main30.m`：smoke 样本改为 `[8,4,4]`，仅 smoke 固定随机种子 `20260820`，新增实际带宽、滚降系数和种子元数据；新增 `第三章代码/gate1_smoke_ch3.py`，用于 MAT/标签独立审计及 Transformer M=10 单步闭环。 |
| 运行目录 | `outputs/gate1_ch3/20260821_115546`；包含启动前基线、MATLAB日志、资源 CSV、阶段/总门禁 JSON、formal 前后 manifest 和三个 smoke MAT。 |
| 首次结果 | 60 秒基线通过；MATLAB 退出码 0，在 25.922 s 内生成 16 个样本；实验进程树峰值 10.174 GB，超过 8 GB 红线，Gate 1 判为 `FAIL` 并停在 G1.1。系统可用 RAM 最低 9.143 GB，GPU 已用显存最高 4.512 GB。 |
| 隔离验证 | 三个 MAT 均位于本次 smoke 目录，formal manifest 前后无变化；运行结束后无遗留 MATLAB/Python 进程。 |
| 停止边界 | 遵守失败不自动重试规则；未执行 MAT 独立审计、Python forward/backward/step/validation、checkpoint 保存与重载，也未进入第四章。 |
| 原因定位 | 资源时间线显示进程数由 4 增至 12 时工作集跨越红线；`main30.m` 无 `parfor` 却无条件启动默认并行池，因此未使用 worker 是首要内存来源候选，需用户批准最小修复后才能重跑。 |
| 证据边界 | 本次是工程 smoke 失败记录，只证明数据生成入口可完成且当前并行池配置不满足资源门禁，不构成论文性能复现证据。 |

## 2026-08-21 修复 Gate 1 资源问题并重跑通过

| 字段 | 内容 |
|---|---|
| 批准修复 | `第三章代码/main30.m` 仅在 smoke 下跳过主循环未使用的默认并行池，formal 行为保持不变；MATLAB 监控输出按 GBK 936 解码后以 UTF-8 保存。 |
| 新运行目录 | `outputs/gate1_ch3/20260821_120635`；与首次失败目录完全隔离。 |
| MATLAB 结果 | 60 秒基线通过；16 个样本生成墙钟 10.843 s；实验进程树峰值由 10.174 GB 降至 1.643 GB；可用 RAM 最低 18.027 GB；GPU 已用显存最高 4.582 GB；无预警或红线。 |
| 数据审计 | train/val/test 逻辑 shape 分别为 `(8,19,81,81)`、`(4,19,81,81)`、`(4,19,81,81)`；float32、有限性、非负性、类别分布、标签互斥、空槽位、频率排序全部通过；band/ignore 独立复算均为 0 项不一致。 |
| 修复等价性 | 新旧运行三个 split 的全部数值数据集逐元素完全相等，确认跳过未使用并行池未改变本次 smoke 数据。 |
| Python 闭环 | Transformer M=10、batch 2、`cuda:0` 完成 forward/loss/backward/AdamW step/validation/save/reload；ignore-loss 差值 0，梯度非零且有限，参数发生有限变化，checkpoint 重载输出差值 0。 |
| Python 资源 | 外部阶段墙钟 12.426 s；实验进程树峰值 1.360 GB；可用 RAM 最低 17.368 GB；GPU 已用显存最高 3.762 GB；无预警或红线。 |
| 隔离与证据 | formal manifest 前后完全一致；总门禁报告生成前的 20 个证据文件共 9904938 bytes；JSON、checkpoint、日志和资源 CSV 可回读；结束后无遗留 MATLAB/Python 进程。 |
| 门禁结论 | Gate 1 为 `PASS`，只证明第三章最小工程闭环和数据契约成立，不证明模型收敛、论文指标或创新性。下一步只能进入 Gate 2 方案审批。 |

## 2026-08-21 编制、实施并完成 Gate 2

| 字段 | 内容 |
|---|---|
| 目标 | 为第四章极小 IQ、401×401 细 DPD、定位分片、双头网络单步和资源容量探测建立可审批的分层门禁。 |
| 更新文件 | `第四章代码/main31_loc_v3.m`、`第四章代码/gen_ch4_loc_data.py`、`第四章代码/train_yolo.py`、`第四章代码/gate2_smoke_ch4.py`、`运行基础说明.md`、`第一阶段_论文与代码接管审查.md`、`当前任务.md`、`对话上下文.md`、`研究总结.md`、`修改记录.md` |
| 方案修正 | 将 smoke 从覆盖不足的 `[2,1,1]` 规划为 `[4,2,2]`；保留现有完整分片 schema；先做单任务 DPD pilot；原作者最终确认 D8（Focal+offset）为主模型，D5 与 D3d 退出 Gate 2；D8 关闭 AMP 并完成 batch 1；predicted-K 性能推迟到具备收敛第三章权重和共享样本映射之后。 |
| 资源方案 | 总墙钟预警/硬停止保留 70/100 min，模型审计阶段回落为 10/15 min；单任务 DPD 为 10/20 min；可用 RAM 10/8 GB、实验进程工作集 6/8 GB、GPU 总显存 12/14 GB、输出 250/500 MB。 |
| 容量方案 | D8 的 train/val batch 1 为必选工程闭环；增加独立 `val_batch_size` 以避免小训练 batch 首次验证仍用 32；batch 2/4/8 在 D8 下使用独立进程逐级探测，首次不安全即停止。 |
| 实施内容 | MATLAB smoke 改为 `[4,2,2]`、固定随机种子并跳过未使用并行池；DPD 生成器新增样本上限、chunk 与 JSON 报告；训练入口新增独立 `val_batch_size`；新增 MAT/分片/坐标/D8/容量独立门禁脚本。未改变正式 `[40000,5000,5000]`、模型结构、生产损失或数据物理模型。 |
| 静态验证 | 本轮 Python 文件均通过 `py_compile`；新增门禁脚本通过 Ruff `F,E9`。MATLAB Code Analyzer 仅报告既有的未使用变量、数组增长和日志相关警告；正式目录前后 manifest 一致。 |
| 首次运行 | 目录 `outputs/gate2_ch4/20260821_135058`。60 秒基线、`[4,2,2]` MAT 及独立审计、单任务 DPD pilot、八任务完整 DPD、分片 schema 和 pilot/full 一致性均通过；总输出约 23.122 MiB，无资源红线或遗留进程。 |
| 停止原因 | G2.4 的 oracle offset loss 为 `1.22487545e-5`，未满足 `<=1e-6`，因此 Gate 2 结论为 `FAIL`，未运行 D8 batch 1 与 batch 2/4/8。按方案未自动修复、重试、切换 AMP/chunk/模型。 |
| 只读诊断 | 失败 oracle 使用 Python float64 中间运算，而生产损失使用 `torch.float32`；按生产顺序复算 loss 为 0，最大活跃 offset 差约 `2.43187e-5`。该诊断只用于提出修复，不覆盖本次失败结论。 |
| 批准修复 | 用户批准后，仅把测试 oracle 的像素与 offset 构造改为和 `compute_offset_loss` 相同的 `torch.float32` 乘、加、除及 round 顺序；`1e-6` 阈值、生产数据、模型、损失和正式配置均未改变。 |
| 修复后重跑 | 全新目录 `outputs/gate2_ch4/20260821_141215` 从 60 秒基线开始完整执行，未复用失败目录。MATLAB `[4,2,2]`、MAT/分片/高斯、pilot/full、坐标/Top-K/K=2/3 均通过；oracle offset loss 为 0，交换 dx/dy 后为 0.020497。 |
| D8 闭环 | D8 `dualhead`、`dice_weight=0`、no-AMP、train/val batch 1 完成 forward、Focal+offset、backward、AdamW step、validation、checkpoint 保存和重载，重载 heatmap/offset 最大差均为 0。batch 2/4/8 各在独立进程完成单步，最高已测安全档为 8。 |
| 资源与隔离 | 总门禁报告生成约 601.46 s；53 个资源采样点无预警或红线，可用 RAM 最低 16,434,364,416 bytes、系统总 GPU 已用显存最高 6833 MiB、证据约 72.103 MiB；formal manifest 前后相同，无遗留进程。 |
| 门禁结论 | Gate 2 为 `PASS`，只证明第四章极小数据契约和 D8 单步工程闭环；不证明收敛、定位性能、论文结果或 batch 8 的长期训练适用性。下一步仅进入 Gate 3 方案审批。 |

## 2026-08-21 实施并完成 Gate 3A 中等规模可重复性门禁

| 字段 | 内容 |
|---|---|
| 目标 | 在不启动正式收敛训练的前提下，验证第四章中等规模 IQ/定位数据、D8 no-AMP pilot 训练的确定性、完整 DPD 资源占用和 60 epoch 时间预算。 |
| 代码修改 | `main31_loc_v3.m` 新增 smoke-only 数量/种子覆盖；`gen_ch4_loc_data.py` 新增期望任务数、空输出和分片保护；`train_yolo.py` 新增确定性、非有限值失败、逐 epoch 完整 checkpoint 与结构化日志；新增 `gate3_stage_runner.py`、`gate3_ch4.py`、`eval_ch4_checkpoint.py`。 |
| 运行目录 | `outputs/gate3_ch4/20260821_152344`；总报告为 `gate3a_gate_report.json`，完整定位数据位于 `05_full_loc_after_encoding_fix/data`。 |
| 数据与审计 | MATLAB 生成 `[1024,256,256]` IQ；N=2/3 各半，1536 个样本哈希唯一且跨 split 无泄漏。完整定位数据为 1024/256/256 条、8/2/2 个分片、共 3,951,967,293 bytes；pilot/full 前缀的 DPD、标签、位置和索引字段最大差均为 0。 |
| 确定性验证 | 两次 seed 42、D8 `dualhead`、no-AMP、batch 8、5 epoch pilot 的逐 epoch 指标、模型/优化器/调度器/随机状态和最终验证完全一致；比较覆盖 5 epoch，全部 mismatch 列表为空。 |
| 完整 DPD 资源 | train/val/test 分别用时 545.535/145.270/148.325 s，总计 839.130 s，低于 2700 s 门槛；纳入总判定的最低系统可用内存 13,033,127,936 bytes、峰值进程树 RSS 2,665,586,688 bytes、峰值系统总 GPU 已用显存 6957 MiB。 |
| 时间预算 | 两次 pilot 排除首 epoch 后的稳态中位数为 1.4582/1.4487 s；按 8 倍样本量和 60 epoch 线性外推约 11.665 min，低于 150/180 min 预警/硬门槛。该外推只用于资源调度，不是收敛或性能证据。 |
| 首次失败与修复 | 首次确定性比较因成功路径提前访问空 mismatch 列表而 `CRASHED`；最小控制流修复后在全新 v3 目录 PASS。首次完整 DPD train 因 Python 输出编码与日志转发解码不一致导致读取线程退出并在 1500 s `TIMEOUT`；用户批准后，包装器显式设置 UTF-8 并安全转发不可编码字符，全新目录三 split 均 PASS。失败证据均保留。 |
| 总门禁与边界 | 25 个纳入判定的监控阶段全部 PASS，无 warning/red flag；Gate 3A 总输出在报告生成前为 5,017,944,906 bytes，低于 8 GiB 红线；formal 第四章数据和输出目录均不存在。Gate 3A=`PASS`，不构成模型收敛、定位性能或论文指标复现。 |

## 2026-08-21 实施并完成 Gate 3B 小规模完整训练门禁

| 字段 | 内容 |
|---|---|
| 目标 | 在 Gate 3A 已审计的1024/256/256定位数据上，完成两次D8 no-AMP 60 epoch训练、严格重复性比较、冻结checkpoint和独立validation/test oracle-K评估。 |
| 目录决策 | 不把既有 `outputs/gate3_ch4/20260821_152344` 重命名为 `gate3a_ch4`，避免破坏Gate 3A报告、数据、哈希和文档引用；Gate 3B使用全新 `outputs/gate3b_ch4/20260821_173526`。 |
| 代码修改 | `train_yolo.py` 增加Gate 3B冻结参数、未训练初始validation及RNG保存/恢复、best epoch和最终学习率记录；`eval_ch4_checkpoint.py` 增加输出防覆盖、checkpoint SHA/配置/有限性检查和oracle-K标记；`gate3_stage_runner.py` 增加通用Gate标签及可配置输出门槛；新增 `gate3b_ch4.py` 负责数据SHA、训练审计、严格比较、冻结、独立评估比较和总门禁。 |
| 算法边界 | 未改变DPD公式、D8结构、Focal+offset生产损失、数据物理模型、AdamW、余弦调度器或formal参数；test原始 `val_loss` 键只在总报告中明确映射为 `test_loss`，数值未重算。 |
| 静态验证 | 相关Python入口通过 `py_compile`、Ruff `F,E9` 和 `--help`；最终 `gate3b_ch4.py` 再次通过语法与Ruff检查。 |
| 执行结果 | 两次60 epoch均通过且无warning/red flag；60 epoch科研字段、初始/最终validation及best/last训练状态逐值一致；训练前后15个数据文件SHA一致；validation/test各两次独立评估一致。 |
| 总门禁 | `gate3b_gate_report.json` 为 `PASS_WITH_MORE_EPOCHS_RECOMMENDED`；best epoch=57，故建议另行审批延长训练。详细指标写入 `实验结果记录.md`。 |
| 文档更新 | 更新 `第一阶段_论文与代码接管审查.md`、`当前任务.md`、`对话上下文.md`、`研究总结.md`、`实验结果记录.md` 和本记录，明确oracle-K及非论文复现边界。 |

## 近期重要修改索引

| 日期 | 事件 | 结果 |
|---|---|---|
| 2026-08-20 | 建立长期项目文档体系 | 五份长期文档已初始化，`AGENTS.md` 已补充更新规则 |
| 2026-08-20 | 论文与两章代码静态接管审查 | 详细报告与长期状态文档已完成；等待批准 smoke 前置改造 |
| 2026-08-20 | Gate 0 运行基础改造 | 路径、设备、依赖、MATLAB smoke 保护和输出隔离已完成静态验收 |
| 2026-08-20 | 论文自学讲义与 Gate 1 方案 | 接管报告已可独立学习；Gate 1 等待用户审批，尚未执行 |
| 2026-08-21 | Gate 1 首次受控运行 | G1.1 数据生成完成但实验进程树超过 RAM 红线；未审计、未进入 Python、未自动重试 |
| 2026-08-21 | Gate 1 修复后重跑 | smoke 跳过未使用并行池后完整通过数据、模型、资源和隔离门禁 |
| 2026-08-21 | 编制、实施并首次运行 Gate 2 | IQ、DPD 与分片链通过；坐标 oracle 严格断言失败后停止，D8 未运行，等待最小测试修复与全新重跑审批 |
| 2026-08-21 | Gate 2 最小修复后重跑 | oracle float32 顺序修复后，全新目录完整通过坐标、D8 batch 1 及 batch 2/4/8 容量门禁，Gate 2=`PASS` |
| 2026-08-21 | Gate 3A 中等规模可重复性门禁 | `[1024,256,256]` IQ/定位数据、两次 5 epoch 完全确定性、完整 DPD 与时间预算通过；两次失败证据保留，Gate 3A=`PASS` |
| 2026-08-21 | Gate 3B 小规模完整训练门禁 | 两次D8 60 epoch训练严格可重复，冻结checkpoint的validation/test oracle-K重复评估通过；best epoch=57，状态=`PASS_WITH_MORE_EPOCHS_RECOMMENDED` |
