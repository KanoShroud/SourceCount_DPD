# G6-P1一键运行

2026-09-23状态：正式六轨及后续冻结checkpoint诊断已完成，无需重跑。结论见根目录`统一端到端模型研究.md`25.8—25.9及`实验结果记录.md`；以下保留原运行和恢复说明。

入口：本目录`G6_P1一键运行.py`，PyCharm解释器选择`D:\Software\anaconda3\envs\PyTorch\python.exe`，无需参数。入口在同一进程执行，不另开窗口。

**当前优化、工程复核和真实启动预检均通过，可直接点击运行。** 仍保留12小时预算，最新耗时预测见`outputs_e2e/unified/e2e_g6_p1/20260922_approved/preparation_report.json`；精确短测事实见根目录`实验结果记录.md`的`E2E-G6-P1-SPEED-20260922`。

启动条件满足后，一键顺序执行：补齐物理缓存 → C0/C1/C2各两个seed → val_compare统一比较 → 中文摘要及审计。短测只生成所需缓存，全量缓存约66 GiB；沿用按样本验证加载，不全部放入RAM。

已接入同batch物理计算、256网格分块及单batch后台预取。batch4、FP64物理分支和原训练计划不变，不需要在入口另行修改参数。

控制台每约20秒更新一行进度条，每轮汇报训练/验证耗时与最佳轮次；磁盘日志保留阶段和epoch摘要。若控制台不支持覆盖进度行，进度可能显示为多行，但不会影响训练。

中断后重新点击可从最后一个完整epoch继续，未完成epoch重算；已完成缓存按登记身份复用。总预算从第一次正式启动计时，中断不会重置截止时间。日志及失败尝试保留，未核验失败不当成通过。

正式结果根：`outputs_e2e/unified/e2e_g6_p1/20260922_approved/run/`。

完成后请回读：

- `evaluation/运行摘要.md`
- `evaluation/comparison_report.json`
- `evaluation/final_audit_report.json`
- `training_report.json`

如停止，请回读`logs/`内最近一次的`failure.json`与`run.log`。没有最终审计PASS时，不把已有部分轨道当成六轨完成。

只检查入口、不训练：命令行附加`--check-only`，当前已实测通过。代码目录`统一模型代码/gates/g6/`，全部新产物写入`outputs_e2e/`，test与原outputs不动。
