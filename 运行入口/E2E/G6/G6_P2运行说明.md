# G6-P2一键运行

2026-09-24状态：正式六轨及结果回读已完成，最终审计PASS，无需重跑。结论见实现文档25.12和实验结果记录G6-P2条目；以下保留原操作及恢复说明。

入口：[G6_P2一键运行.py](G6_P2一键运行.py)。PyCharm选择解释器`D:\Software\anaconda3\envs\PyTorch\python.exe`，不填参数直接运行。

执行顺序：核对冻结输入 → A/B/C各两个seed → 开发集配对比较 → 中文摘要与审计。复用P1物理缓存，不重复生成大数据，不读取test。每个seed的三组均从对应P1-C1最佳模型起步；新实验输出独立保存在`outputs_e2e/unified/e2e_g6_p2/20260923_approved/`。

控制台约20秒更新进度条，每轮输出训练/验证耗时和最佳轮次。中断后重新点击，从最后完整epoch继续；未完成epoch重算，12小时截止时间不重置。正式权重不继承工程短测更新。

启动会检查工程报告、合同、资源及最长24轮预算；若被拦截，请回读错误，不直接改预算或跳过检查。最新预测位于本结果根的`preparation_report.json`，完整方案见根目录实现文档25.10。

完成后请回读结果根下：

- `run/evaluation/运行摘要.md`
- `run/evaluation/comparison_report.json`
- `run/evaluation/final_audit_report.json`
- `run/training_report.json`

如中途停止，回读`run/logs/`最新目录的`failure.json`和`run.log`。只检查而不训练时，给入口加`--check-only`参数。
