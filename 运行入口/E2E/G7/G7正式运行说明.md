# G7正式小区域训练

## 当前入口

本次扩容中断后，在PyCharm中使用`D:\Software\anaconda3\envs\PyTorch\python.exe`运行同目录`G7预算续跑.py`，参数留空。只检查时添加`--check`，不会训练或改动预算。不要用原40小时的`G7正式训练一键运行.py`恢复本次任务。

当前没有训练进程，入口已验证、待用户启动。点击后核对原合同和checkpoint，按批准的中断记录一次性补记耗时，再从第二seed的S轨第18轮开头恢复。累计60小时及全部科研配置不变；原合同与旧预算追加保留，当前登记见输出目录`budget_extension_60h_recovery_pathfix.json`。Windows磁盘检查的Path类型错误已修复，--check现覆盖同一检查。精确事实见根目录`实验结果记录.md`的G7-FORMAL-RECOVERY-20260926。

进度条、每轮耗时和报错直接显示在PyCharm控制台；状态见`budget_extension_status.json`及`progress.json`。旧后台等待已退出，旧`预算续跑.stdout.log`和`预算续跑.stderr.log`只是历史日志。本次点击后状态变为`RESUMING`；`COMPLETED`表示完成。若为`NEEDS_ATTENTION`，回读错误再处理，不盲目重复启动；其他异常不自动重试。

新输出：`F:\SourceCount_DPD_E2E\outputs_e2e\unified\e2e_g7_compact\formal_v1_40h`。旧`formal_v1`预检合同和`v2_full_adaptation`保留；仅复用核验后的数据与物理输入，不加载旧权重。

## 自动执行顺序

1. 两个seed从随机初始化训练CH3与D8全部参数，各完成原生任务。
2. 每seed用新基础权重训练全新共同候选生成器。
3. 根据新候选生成Top5局部输入，复核Top5/8覆盖，运行同起点F/S/E。
4. 冻结best，在val_compare比较候选、F/S/E和原生硬级联，报告RMSE、联合指标及真实在线耗时。

训练进度以阶段、单行进度条及每轮摘要显示。正常关闭或异常终止后保留完整epoch；同一入口恢复最近已提交epoch及优化器、学习率、随机顺序。强制杀进程留下未关闭预算时先回读状态，不自行清空预算。资源限制不会因重新点击而重置。

## 运行后回读

均在新输出目录中：

- `evaluation/运行摘要.md`
- `evaluation/comparison_report.json`
- `evaluation/final_audit_report.json`

如果报错，提供`execution_status.json`、`progress.json`和PyCharm末尾日志。准备阶段最新报告由上一级`formal_preparation_report.json`指向。正式运行前不得随意修改已登记源码或配置，避免混用训练合同。

## 数据与测试边界

正式训练只读现有train；val_select用于选模，val_compare用于冻结后比较。二者都已是开发数据。当前不使用历史模型，不新增空间计数头，不改变FP64物理链。

独立全K测试集尚未登记；`G7正式测试一键运行.py`目前只给出封存提示，不能运行测试。待开发阶段结束、模型与配置冻结后，再准备并审批测试。现有仅K=2/3的历史2048条数据不冒充全K正式test。
