# 研究总结

> 本文件只维护稳定研究认识、方法关系和创新边界。详细方法见阶段文档，当前任务见 `当前任务.md`，精确实验事实见 `实验结果记录.md`。

## 1. 研究定位

- 学位论文题目为“基于深度学习的时频混叠多信号检测计数与被动定位方法研究”。
- 项目面向四站分布式被动接收系统中的时频混叠多信号，以 DPD 空间谱连接检测计数与多源定位。
- 第三章在粗网格多子带空间谱上联合输出频带占用与源数；第四章在目标频带的细网格空间谱上输出多源连续位置。

## 2. 方法关系

```text
四站 IQ
→ 19 个重叠子带与粗 DPD
→ 第三章预测频带和源数 K
→ 对目标频带生成细 DPD
→ 第四章按 K 解码多源位置
```

两章在理论上组成检测—计数—定位链。S2-G3证明现成模型不能直接硬串；S2-G4确认原0.3主瓣标签不覆盖全部窄带源。R2/R3在小数据多seed下将Hard-19-Actual确立为oracle候选，R4又证明它在4k/8k数据上可逼近Exact；Soft-19则始终没有稳定收益。S2-G5统一数据和标签后，四轨诊断将主要瓶颈收缩为源数K及其基数错误传播；R5进一步表明，相邻K候选结合D8峰值和空间证据，可以在不重训CH3/D8的条件下使固定validation上的K=2/3级联进入预设等效范围。

## 3. 第三章创新与边界

- 以多子带粗 DPD 谱保留跨站几何相干信息，同时控制空间搜索成本。
- 使用共享残差 CNN 提取各子带谱形态，以 Transformer 建模跨子带关系。
- 用频率有序槽位统一表达频带占用和源数，配合 ignore 区与 Focal BCE 处理边缘模糊和类别不平衡。
- 核心实现与论文主体高度一致；Adam/AdamW 表述及 M=100 消融评估链仍有差异。
- 当前已获得缩减数据上的可重复收敛 checkpoint；跨seed、数据规模学习曲线和论文指标仍未验证。
- 该checkpoint在第四章256条validation IQ上通过预设跨分布模型门，但2源准确率明显低于3源，且该诊断不等于统一分布上的正式泛化验证。

## 4. 第四章创新与边界

- 以401×401细 DPD 将阵列定位物理约束转化为二维可学习场。
- 使用 CSPDarknet、PAN-FPN 和 U-Net 式解码器同时建模全局多峰结构与局部峰形。
- 以集合热力图避免固定源顺序，以 offset 回归突破10 m网格量化。
- 接管主模型固定为 D8 `dualhead_std`，采用 Focal+offset、FP32/no-AMP；当前缩减配置已经验证能够稳定学习，60 epoch严格重复成立，200 epoch诊断趋势已关闭。
- 200 epoch诊断选择的新接口权重降低了oracle-K总体RMSE，但改进主要来自减少少量灾难性错误，median和P90/P95未同步改善；predicted band/K、论文全量配置、4A–4D追溯和论文指标仍未验证。

## 5. 稳定研究结论

1. 两章的算法主体、数据生成、训练和主要评估入口齐全，具备继承基础。
2. 代码与论文总体一致，但“实现存在”和“小规模可运行”不能替代论文级复现。
3. 第四章 oracle-K 是模型定位上限的一种条件评估，不能写成端到端系统性能。
4. 当前硬件适合分片、精简数据和较小 batch；现有整库加载与全套控制实验格式不适合直接扩展。
5. 第三章原位置生成器在约束放置达到5万次仍失败时会无约束补齐，可能生成低于300 m最小源间距的样本；正式数据必须增加生成后不变量检查，不能只依赖控制台类别统计。
6. 第三章Transformer在2048/512/512缩减数据上已验证稳定学习、两次确定性训练逐值一致和独立重载；该结论不外推为跨seed稳定性或论文正式指标。
7. 第四章D8在1024/256/256缩减数据上完成200 epoch充分性诊断，最佳点位于epoch 93且后续趋势关闭；冻结权重适合作为接口checkpoint，但未做200 epoch双重复，也不代表正式epoch规律。
8. 第四章oracle-K误差呈明显长尾，单独报告RMSE或median都会遗漏重要行为；后续需同时报告分位数、极端错误和带基数惩罚的端到端集合指标。
9. 现成19子带映射配合冻结D8会放大定位长尾，但R2证明该现象不能简单归因于19窗口分辨率：统一使用`BW_actual`并重训D8后，Hard-19-Actual达到当前oracle候选水平。
10. 在CH3能够输出与Hard-19-Actual一致的频带语义前，不应继续完整predicted-band四轨或宣称端到端联合性能。
11. 论文图表必须由冻结配置和结构化结果重新生成，硬编码数组不能作为证据链。
12. 面向新投稿的创新性必须重新检索，学位论文不能替代当前文献核验。
13. 第三章10 MHz子带、0.3覆盖阈值的正标签语义要求至少3 MHz主瓣宽度；第四章数据中的窄带源可能完全没有正子带。接口审计必须按“每个源是否被表达”检查，不能只检查多源联合频带是否非空。
14. S2-G4的0.3版本在支持域缺口处按预案停止；该阶段本身没有获得Coarse-D8结果，随后由S2-G4-R1以0.2阈值完成下游适配诊断。
15. S2-G4-R1证明0.2覆盖阈值可使全部源获得正子带，但其主瓣Hard-19结果不能再单独证明19窗口是主要限制；R2表明`symbolRate`主瓣与`BW_actual`实际频带定义不一致是重要混杂因素。
16. 0.2 Coarse-D8的median略优于Exact，但P90/P95和极端错误明显更差，说明问题主要仍在长尾而非典型样本；不能用median或100 m召回单独宣称接口已恢复。
17. S2-G4-R2中Hard-19-Actual与Soft-19的训练趋势均关闭；HHA/SSA/EE RMSE为112.06/141.23/130.73 m，Soft未优于Hard且GOSPA等效门失败，故Soft-19不是当前优先接口。
18. S2-G4-R3中Hard/Exact RMSE比在三个seed均低于1，中位数为0.9590；2/3通过RMSE、GOSPA和recall联合等效门，Hard-19-Actual因此成为稳定oracle候选，但仍不能解释为离散频带理论上优于连续Exact。
19. Soft仅在1/3 seed优于Hard，三seed中位RMSE改善为负，未通过重新考虑门；其三源长尾在一个seed反转，说明Soft的不利影响具有训练seed依赖，不能表述为必然损失信息。
20. S2-G4-R4的数据规模学习曲线显示，Hard在1k明显劣于Exact，但4k/8k时已进入配对不确定性范围；这支持19子带Hard表示的信息充分性，不支持其理论优于Exact。Soft在4k/8k也未稳定优于Hard，因此停止Soft路线。
21. R4的sample bootstrap只衡量固定training seed下的样本不确定性；单seed、validation和oracle-K边界仍然存在，不能将R4当作论文test性能或最终数据规模。
22. S2-G5-R1已建立统一分流契约：K=0直接结束，K=1定位暂空，K=2/3进入Hard-19-Actual D8；CH3使用全部K=0/1/2/3样本，CH4仍只消费K=2/3。
23. K=2/3统一数据与G4-R4保持相同条件分布和Hard-19-Actual语义，但采用独立样本；因此R1无需重训D8，也避免复用训练样本造成评价泄漏。
24. S2-G5-R2证明CH3能够在统一物理分布上学习Hard-19-Actual与K；计数和频带两个主指标随1k→4k→8k连续获得明确配对改善。8k相对4k时所有K准确率均提高，说明4k并非当前配置的平台；8k因此冻结为R3工程接口checkpoint，但单seed validation证据仍不能把它认定为论文最终规模。
25. R2仍是单seed validation诊断；配对bootstrap只衡量固定模型下的样本差异，不能替代训练seed重复，也不能作为论文test性能。
26. S2-G5-R3四轨中，Predicted-Band/Oracle-K与Oracle-Band/Oracle-K的GOSPA差异区间跨0，而Oracle-Band/Predicted-K稳定增加GOSPA；因此当前接口不需要优先提高频率表示精度，R4应先处理K不确定性和稳健解码。
27. R3完整级联的100 m召回只下降约2.42个百分点，但GOSPA相对增加约25.14%；匹配RMSE反而降低是漏掉难目标后的选择效应。端到端评价必须继续以带基数惩罚的集合指标为主，不能用条件RMSE替代。
28. S2-G5-R4中16k相对8k显著提高Band macro-F1，但balanced count accuracy增益区间跨0，严格规模门未通过；数据翻倍没有稳定消除K判决瓶颈，因此当前不应直接扩32k。
29. 16k四轨仍显示K路径增加约5.63 m GOSPA，完整级联相对GOSPA增加约20.02%并未通过等效门；Predicted-Band/Oracle-K反而略优于Oracle-Band/Oracle-K，进一步确认频带精度不是当前优先问题。
30. 16k错误样本的相邻Top-2候选覆盖约91%，oracle候选选择可恢复约66%的K路径损失，支持进入候选K与稳健解码研究；该结果只证明可恢复上限，不证明已有可部署选择规则。
31. S2-G5-R5中，冻结有序probe的Top-1没有优于原0.5计数规则；性能提升来自相邻候选与D8空间证据的联合判决，而不是单纯换一个K分类器。
32. R5联合选择将固定`val_compare`上K=2/3计数准确率由87.30%提高到95.51%，完整级联的GOSPA和100 m召回同时通过预设门，说明定位热力图可作为源数判决的互补物理证据。
33. R5-A仍是单training seed、固定validation和K=2/3证据；其选择器经过独立的`val_select`拟合但尚未经过冻结test或跨seed验证。K=1定位仍为空，因此不能宣称K=0–3完整系统或论文最终性能。

## 6. 阶段路线

- 第一阶段：论文与代码接管审查——已完成并冻结。
- 第二阶段：缩减规模复现与端到端闭环——当前阶段。
- 第三阶段：创新核验与正式实验设计。
- 第四阶段：正式实验与证据冻结。
- 第五阶段：英文论文与投稿。

## 7. 证据索引

- 第一阶段详细审查：`第一阶段_论文与代码接管审查.md`
- 第二阶段路线：`第二阶段_缩减规模复现与端到端闭环.md`
- 当前状态：`对话上下文.md`
- 当前任务：`当前任务.md`
- 实验事实：`实验结果记录.md`
- 修改历史：`修改记录.md`
- 稳定规则：`AGENTS.md`
