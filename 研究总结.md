# 研究总结

> 本文件更新至 2026-08-21。详细证据、调用图、问题清单和资源预算见 `第一阶段_论文与代码接管审查.md`。

## 1. 研究定位

- 学位论文题目为“基于深度学习的时频混叠多信号检测计数与被动定位方法研究”。
- 项目面向四站分布式被动接收系统中的时频混叠多信号，使用 DPD 空间谱连接检测计数与多源定位。
- 第三章在粗网格多子带空间谱上联合输出频带占用与源数；第四章在检测频带的细网格空间谱上输出多源连续位置。

## 2. 现有研究材料

| 材料 | 内容 | 当前状态 |
|---|---|---|
| 学位论文 Word 版 | 项目背景、理论模型、第三章和第四章方法、实验设计及结论 | 已完整读取并逐页检查 |
| `第三章代码/` | MATLAB 数据生成，Python 训练、评估和绘图 | 已完成静态入口、调用、数据流和一致性审查 |
| `第四章代码/` | MATLAB IQ 生成，Python 细 DPD、训练、评估和绘图 | 已完成静态入口、调用、数据流和一致性审查 |

## 3. 第三章与第四章创新映射

| 章节 | 初步主题 | 需要核验的内容 | 当前结论 |
|---|---|---|---|
| 第三章 | 多子带 DPD 粗空间谱 + 共享残差 CNN + 跨子带 Transformer + 频率有序槽位 | 以 `(M,19)` 占用矩阵统一表达频带检测和计数，使用 ignore 区及 Focal BCE 处理边缘与不平衡 | 核心实现与论文高度一致；Adam/AdamW 和 M=100 评估链有差异；尚未复现指标 |
| 第四章 | 细 DPD 空间谱 + CSPDarknet/PAN-FPN/U-Net 解码 + 热力图/offset 双头 | 保留物理空间谱结构，以多峰热力图解决多源集合输出，以 offset 消除 10 m 网格量化 | 原作者已确认主模型为 D8（Focal+offset）且关闭 AMP；这与论文 loss 公式和 `plot_cdf.py` 的主模型映射一致，但与论文 AMP 文字及代码默认 D5/batch 192 不一致。Gate 3B 已证明 `[1024,256,256]` 缩减数据上的两次60 epoch训练严格可重复并发生有效学习；best epoch=57，充分收敛、端到端 predicted K、原始论文配置及图表溯源仍待实证 |

## 4. 当前阶段结论

1. 两章的算法主体、数据生成、训练和主要评估脚本都存在，研究工作具有可继承基础，不属于只有论文没有实现的情况。
2. 静态一致性总体较高，但“能读懂和能对应”尚不等于“可复现”：Gate 0 已解决路径、设备、基础依赖和误启动大样本风险；整库加载、超大中间数据和结果溯源仍需后续整改与实测。
3. 第四章当前评估使用真实源数作为 Top-K，必须把论文定位性能解释为 oracle-count 条件结果；在接入第三章预测前不能声称端到端性能。
4. 第四章 4A–4D 脚本中的数值是硬编码数组，必须由原始结果文件重新生成后，才能形成论文级可追溯证据。
5. 当前硬件适合做精简数据、懒加载和较小 batch 的复现；不适合按现有格式一次性生成全部第四章控制实验数据。
6. 第四章正式接管配置固定为 D8：`method=dualhead`、`dice_weight=0`、关闭 AMP。D5 与 D3d 不再作为主模型候选；Gate 3B 已确认当前缩减数据上有效学习，但不能据此声称充分收敛或论文指标已复现。
7. Gate 2 首次受控运行因测试 oracle 的浮点运算顺序与生产损失不一致而在 G2.4 停止；该 FAIL 目录继续保留。最小修复没有改变阈值、数据、模型或损失，全新重跑的 oracle loss 为 0，验证了诊断。
8. Gate 3A 的两次 D8 no-AMP、batch 8、5 epoch pilot 在逐 epoch 指标、checkpoint 全状态和最终验证上完全一致，说明固定种子、数据、代码与环境下的工程可重复性成立；短训练指标仍没有论文性能含义。
9. Gate 3A 的 1536 个 IQ 样本哈希全部唯一且跨 split 无泄漏；完整 1536 条定位任务相对 pilot 前缀的所有张量与索引逐值一致，证明分批扩大生成没有引入前缀语义漂移。
10. Gate 3B 两次60 epoch实耗分别约11.35/11.38 min，与Gate 3A的11.67 min外推吻合；最低系统可用RAM约13.28 GiB、峰值进程树RSS约3.51 GiB、峰值系统总显存约7.21 GiB，证明当前硬件能安全承担这一缩减配置。
11. Gate 3B 的60 epoch科研历史、best/last训练状态及独立评估均严格可重复；冻结epoch 57 checkpoint的validation/test oracle-K RMSE为150.792/110.566 m。best落在最后5个epoch，故状态为 `PASS_WITH_MORE_EPOCHS_RECOMMENDED`，不能表述为充分收敛。
12. 50k 任务的全量 DPD 预处理仍预计需要数小时，且原始完整第四章控制实验数据远大于本次 3.95 GB；因此正式复现继续采用分阶段缩减规模和分片策略，不因 Gate 3B 通过而恢复论文全量。

## 5. 当前下一步

1. Gate 0 可移植性改造已完成，算法与方法结论未变。
2. Gate 3B 已完成并建议延长训练；下一步先审批第三章小规模收敛及 oracle K/predicted K 双轨评估，不把oracle-K单章结果写成端到端性能。
3. 单独设计第四章延长epoch的最小验证，检查epoch 57后的改善是否稳定；若找到可验证的原始命令、日志或权重，则优先恢复其余原始超参数。
4. 修复结果文件到4A–4D绘图的追溯链，确保所有曲线都能回溯到冻结配置和原始结果。
5. 再依据小规模收敛、predicted-K和资源实测确定正式数据规模及不超过约3小时的正式训练方案。
6. 正式投稿前另做当前文献检索，核验创新边界；学位论文不能代替最新文献审查。

## 6. 证据索引

- 当前任务与执行边界：`当前任务.md`
- 当前项目状态与风险：`对话上下文.md`
- 已完成修改：`修改记录.md`
- 正式实验与结果证据：`实验结果记录.md`
- 稳定协作及文档规则：`AGENTS.md`
- 第一阶段详细接管审查：`第一阶段_论文与代码接管审查.md`
